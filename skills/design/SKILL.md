---
name: pmai-design
description: |
  功能模块设计与重做的唯一入口。分三阶段：还原真实问题、按需求类型梳理信息、产出规格文档。新建与重做均经此流程（重做为原地升版），仅修改文档、不涉及代码。纯样式调整、文案订正及不涉及信息结构的缺陷请直接修改。
  触发词：设计 / 重做 / 改某张卡或某个页面、模块 / 写规格 / 把信息理清 / 起新功能。
---

# /pmai-design · 设计驾驶舱（三段式）

> 本 skill 是设计的"驾驶舱"。三段式：**①探索 → ②设计 → ③交给 prd-writing 成文**——先懂问题再设计（探索 ⊥ 信息设计是两个正交阶段，上一轮 reshape 把 ① 整段砍没了、这次补回）。方法论全文不内嵌在这——开工前 `@读`：
> - `skills/_shared/module-questioning.md`（**①探索**：对话诊断还原真问题、escape-hatch 默认档、闻味触发清单）
> - `skills/_shared/info-design.md`（**②设计 + ③成文**：真通用内核 3 条、按需求类型选方法七类、规格通用骨架、磨文字纪律、规格 4 问治法）
> - `skills/_shared/consistency-scan.md`（收口：改原型 / 规格 → 对账 → flag）
>
> 给 PM 的话里**不出现** 通用内核 / 4 环 / 三身份 / drift 这类内部术语，全程大白话。

## When To Use

- 起一个**新功能模块**（PM 一句话需求："做一张能力匹配卡" / "加一个产品库管理页"）。
- **重做 / 改一个已有模块**的信息结构（一行的单元变了、字段增删、卡面 / 抽屉重分层、状态机变化、判定规则改）。
- PM 说某个东西"不对 / 不合理 / 没明白"——回信息模型重新想（必要时回探索重想要解决什么），不打补丁。

**不适用**：纯样式微调、文案错字、不动信息结构的实现 bug —— 直接改完 `/pmai-close`，别走这套。

## 这个 skill 干什么 · 不干什么

- **干**：判断新开还是重做哪个模块 → 加载跨需求记忆（分档读）→ **①探索还原真问题** → **②按需求类型选方法理清信息**（一步一停、必要时调 `/mockup` 确认设计）→ **③调用 `/pmai-prd-writing` 把已拍板结论写成 `spec.md`** → 一致性扫描收口。
- **不干**：开 worktree（本 skill **全程在 main 上只动文档**）、写代码建原型（那是 `/pmai-build`）、merge 定稿（那是 `/pmai-close`）。

> **它是唯一的设计入口**：探索 + 理结构 + PM 拍板在这一步完成；最终成文交给 `/pmai-prd-writing` 的「模块规格模式」，统一控制 `spec.md` 的结构和语言风格。原探索 / 打磨职责已并入本流程，成文规则集中在 prd-writing。

## PM 视图规则（必读）

主产物是模块三件套（`discussion.md` / `decisions.md` / `spec.md`），PM 拍板。须遵守 `skills/_shared/PM-VIEW-RULES.md`（主索引），具体读：
- `_shared/pm-view/writing-rules.md`（写作规则：明确指代 / 正向描述 / 禁工程词 / 禁像素颜色）
- `_shared/pm-view/askuser-rules.md`（PM 答题四条硬规则：空答 STOP / 没拿到答案禁止落盘 / runtime 退化保留 wait / 多决策拆开顺序问）
- `_shared/pm-view/banner-rules.md`（入口 banner + 收尾 ▶ Next Up）
- `_shared/pm-view/attachments-upload.md`（PM 给材料时，归档到 `docs/inputs/attachments/`，用 `_lib.attachments.copy_attachment` 登记）
- 写规格时由 `/pmai-prd-writing` 执行成文与语言把关；本 skill 只负责把内容讨论清楚、把决策拍定。语言纪律全在 `_shared/info-design.md` 第四块 + **禁用清单单一真相源 `skills/prd-writing/references/writing-rules.md`（L1–L6 + 8 类禁用）**——本 skill 不重抄。

## 铁律 · 先懂问题再设计、先讨论清楚再改

- **先懂问题再设计**：①探索（要解决什么真问题）必须在 ②设计（怎么把信息摆清楚）之前做完。单 PM 没有真实用户 / 同事 review，探索段是唯一外部对抗源——跳过 = 在回音壁里裸奔，把根本不该做的功能做精致。
- **先把信息模型讨论清、拍进决策记录，再动原型**。本 skill 默认产出是**设计结论 + 规格**，不是代码。
- **一步一停**：每步和 PM 确认了再进下一步，不一口气把全部分析做完丢给 PM。
- **根因优先（本 skill 自带，不依赖任何全局 / 外部规则）**：PM 说"不对 / 不合理 / 不易懂" → **第一次就停**，回信息模型（必要时回探索）重新想，不在表面加提示文字、不出"再改一版"。**打补丁自检信号**：当你发现自己在出"修订版 / 又改一版"、或在原方案上加补字段 / 加提示 / 加输出块——这就是在打补丁；停下，明说"我可能在打补丁了"，回信息模型（必要时回探索）重想，而不是继续修。

## Preamble

```bash
source "$HOME/.pmai/scripts/skill-preamble.sh"
echo "SKILL: design"

python3 "$PMAI_HOME/scripts/status-view.py" --banner-only --skill DESIGN || true
```

## Workflow（三段式：探索 → 设计 → 成文）

> 三段正交：**①探索**=要解决什么真问题、**②设计**=怎么把信息摆清楚、**③成文**=调用 `/pmai-prd-writing` 把已拍板内容固化成 `spec.md`。物理对齐三件套自然顺序（discussion → decisions → spec）。

### 准备 A · 判断新开还是重做哪个模块

从 PM 一句话需求判断这次是：

- **新开模块** —— 产品里还没有这个东西 → 段③在 `docs/modules/<新模块>/` 立一套新三件套。
- **重做 / 改已有模块** —— 已经有 `docs/modules/<模块>/`，这次改它 → 段③**就地升版**那套三件套（版本 +1、变更日志、supersede，不另开文件夹）。

判断依据：扫一眼 `docs/modules/` 有没有对得上的模块。拿不准是"改老的"还是"开新的"（模块身份是否还是同一个），用一句话问 PM，别擅自决定。

> 模块文件夹命名英文 kebab；三件套文件名固定英文 `discussion.md` / `decisions.md` / `spec.md`；正文仍中文。

### 准备 B · 加载跨需求记忆（分档读，别全量淹没；进场侧闭环）

抛任何探索 / 设计问题之前，先把历史读进来——AI 知道哪些拍过、用哪些既有词，PM 不重复说。**分档读（开放4·已拍 A）**，别无脑全量（PRODUCT-RULES 越长全量越贵、越淹没重点）：

| 改动范围 | 读什么 |
|---|---|
| **小改 / 单模块** | 本模块 `spec.md`（当前真相源）+ `decisions.md`（拍过 / 否过什么）+ `PRODUCT-RULES.md` 里 **scope=全局** 的规则（全局铁律，始终读）+ `PRODUCT.md` 业务术语表 |
| **跨模块 / 新模块** | 上面全部 + **全量** `PRODUCT-RULES.md` + 相关 `docs/decisions/` 理路冻结档（查"为什么"按需读）+ `PRODUCT-STATE.md` 现状 |
| **拿不准是否触及跨模块约束** | 升档按"跨模块"全量读（两道兜防漏读全局规则：铁律节始终读 + 拿不准升档） |

**进场侧闭环（治"回写做实了进场没接住"）**：开场顺手读 `docs/TODO.md`——本需求若命中某条遗留（上一轮 close 推下来的），浮出来提醒 PM。理路（护城河 / 机制咬合 / 演进故事）在 `docs/decisions/` 冻结档，**查"为什么这么定"时按需读**，不每次全载。

> **落点口径（⑤理路拆家后）**：`PRODUCT-RULES.md` 只装**跨模块规则**（活、scope）；**理路**在 `docs/decisions/` 冻结档（按需读）；单模块决策在模块 `decisions.md`。别再把 PRODUCT-RULES 当"规则 + 理路混装"读。

顺手查索引漂移（advisory、不阻塞）：

```bash
python3 "$PMAI_HOME/scripts/check-state-index-drift.py" "$REPO_ROOT" || true
```

有漏挂的文档一句话提示 PM，点头再补，不强制。基线缺失（新项目还没沉淀）→ 当白纸起步，直接进段①，不报错。

### 段 ① 探索 · 还原真问题（@读 `module-questioning.md`）

PM 拿"想要的方案"来，先还原成"要解决的真问题"——这是上一轮 reshape 砍没、现在补回的一整段（探索 ⊥ 信息设计）。

- **escape-hatch 默认放行（开放6·已拍 A）**：每个需求过一遍"闻味判断"，**默认一句话放行**直接进段②；只在闻到味才升档 push。**宁可偶尔漏 push，不可默认每次审**（单 PM 一天起五六个小需求，默认审一遍必弃用）。PM 随时可手动"帮我深想这个"反向升档。
- **闻味触发清单**（嗅到任一 → push）：方案先行措辞（"加个 X 按钮 / 做个 Y 弹窗"）/ 伪需求信号（"感觉应该有"）/ 跨模块措辞 / 推翻已有决策 / 根本目标模糊。详见 `module-questioning.md`。
- **push 时**按 module-questioning 三档深度做对话诊断：反拍马屁（不附和、直指不适）/ 逼到具体 / push once then again / 现状是真对手（"现在没这功能你怎么凑合的"）/ 给方向 + 代价让 PM 选。**砍掉外部 demand 三问**（有人掏钱吗 / 谁被炒鱿鱼 / 坐用户后面看过吗——单 PM 内部产品问不出真答案、强问逼 PM 编）。
- **减法性质需求**（这需求其实是砍 / 简化某现有功能）→ @读 `anti-cut-check.md` 走对抗三问。
- **换高度工具（升维 ↑ / 第一性原理 ↓）**：push 深想时，若问题触及"这功能到底改变谁的判断 / 影响哪张账"（定位 · 优先级）或"大家都这么做、这前提真成立吗"（要不要做、为什么这么做）——调 `/pmai-elevate`：往下用第一性原理拆到地基事实（分清真约束 vs 只是惯例的假设），往上用升维找它服务的最高判断。**仍走 escape-hatch**：小需求默认放行不调，闻到"定位 / 账 / 旧范式"味、或 PM 说"帮我深想 / 拔高 / 这话太功能层"才调。调完**必须降回**段②理结构，不停在高空。

**产物 = `discussion.md` 第一节（探索段的第一产物，不是写规格时才补的过程笔记）**，按需含：真正要解决的问题 + **当前产品现状整理（切片化：只装本需求相关现状，明写"全局现状看 PRODUCT-STATE"）** + 值得商榷之处 + 推荐方向 + 代价 + 岔路口 + 未决业务问题（逐条列、引导 PM 编号答，禁"有 N 个问题"摘要带过）。

**收尾必做**：AI 说一句 **"这是 X 类需求，所以打算用 Y 套设计问题"**、PM 拍——**定类型是段②的前提，必须在动手理结构之前完成**。

### 段 ② 设计 · 按需求类型选方法把信息理清（@读 `info-design.md` 一、二 · 一步一停）

按①定的需求类型选方法理清结构。**不是"一套主干 5 问套所有需求"，而是"很小的真通用内核（3 条）+ 每类一套特化问题集"**（已删原"5 问对任何需求成立"）。

- **真通用内核 3 条**（任何需求都过）：① 真问题 / 谁能做什么决策 + 下游倒逼（①已答、这里确认）② 派生信息 4 环（来源 → 生成[可落 prompt] → 变更 → 重算影响）③ 状态别只画正常态（空 / 计算中 / 待重算 / 冲突待澄清 / 人工已锁定）。
- **页面定位掰不清时**（这页是给人看任务、还是做判断？信息该堆哪些）→ 可调 `/pmai-elevate` 的 UI 5 层问法（信息 / 任务 / 判断 / 管理 / 经营）：先定"这页服务谁的哪个判断"，再反推卡面 / 主操作 / 默认筛选 / 不该出现的东西。轻量、可选，思路已清就别调。
- **按类型加特化问题集（两面：逻辑面 + 呈现面，详见 `info-design.md` 二）**：先判**逻辑面**（后端规则 / 纯算法 / Agent · 引擎 / 流程 · 状态机），再判**有没有呈现面**——**只要结果给人看，就必须同时按呈现类型（信息密集 UI 卡 / 多对象管理页 / 数据产物）把那张脸设计出来**（落点 / 卡面 / 抽屉 / 交互[看依据 · 否决 · 过目] / 待办落点）、和 PM 确认、视觉调 /mockup。**别把'后端引擎'判定只当逻辑面就收工**（ROI 反面教训）。**三身份只在"信息密集 UI 卡"**。判出某面"不属任何已列类型" → 逐类说理由 + PM 拍 + 事后沉淀回 info-design。
- **深度自适应、AI 提议 PM 拍**：判这次走全套还是轻过，说一句给 PM（"这个信息挺密、我想把字段从哪来、上卡面还是收抽屉都过一遍，行吗？"），PM 同意再走。**不机械化成"每需求必跑全部问题"**——思路清的简单需求轻过，别拖。
- **一步一停**：每问（或每两问）和 PM 对一次。讨论过程续写进 `discussion.md`；拍定的点**上移**进 `decisions.md`（含"这是 X 类需求"判定）/ `spec.md`，底稿不再当真相。
- **呈现面收尾自检（进段③成文前必过）**：这东西有没有用户看得见的输出？有 → 我跟 PM 确认过它**显示在哪、长什么样、怎么交互**了吗？没确认，不许进成文。

成文前跑未决问题检查；如还有未回答项，逐条让 PM 答完再落 `spec.md`：

```bash
python3 "$PMAI_HOME/scripts/check-open-questions.py" "$MODULE_DIR/discussion.md"
```

**（可选）调 /mockup 确认设计**（降回可选第三档，不是信息密集类默认动作）：视觉 / 布局靠文字掰不清时才调 `/mockup` 借 `/design-shotgun` 出多变体让 PM 挑。纯后端 / 无界面 / 小改、或文字已讲清的，**不调**。mock 是探索草稿建在 `mocks/`、和 `prototype/` 分开；规格不嵌 ASCII、只引用确认的 mock 路径。PM 选定后记住路径，段③引用。

> **护栏（防机械化）**：什么时候多问、什么时候轻过、什么时候调 mock——AI 临场判断，不得写成"每个需求必跑全部特化问题"的死流程。框架只管两头（记忆进场 + 三件套落盘），中间收敛交给判断。

### 段 ③ 成文 · 调 `/pmai-prd-writing` 演进 spec + 收口 + 分流（@读 `info-design.md` 三、四、五）

把①②的结论交给 `/pmai-prd-writing` 的「模块规格模式」落成 `spec.md`，并与前面已写的 `discussion.md` / `decisions.md` 组成模块三件套。三件**维度不同、内容零重叠**：

| 文档 | 装什么 | 铁律 |
|---|---|---|
| `discussion.md` | **第一节 = ①探索结论**（真问题 + 内部现状切片 + 岔路）；其后续写②的讨论过程 | 探索结论是真产物；讨论过程里某点一拍定就**上移**进 decisions / spec，底稿不再当真相 |
| `decisions.md` | 拍定的**本模块**决策（结论 + 为什么 + 否过什么 + 被推翻标 supersede）+ "这是 X 类需求"判定 | 只记决策，**不复制规格口径**、链回 spec |
| `spec.md` | **唯一真相源**：信息模型 + 判定规则 + 字段口径 + 状态机 + 文案 | 评审 / 开发只看这份；别处不许再写一份口径；正文 = 纯最终事实、不留迭代痕迹 |

**spec 怎么写**由 `/pmai-prd-writing` 负责执行（详见 `info-design.md` 三）：顶部钉权威声明（与原型不一致以规格为准、没说清先确认）；章节是菜单不是死模板（§六信息模型按需出现、§八核心动作**按七类自适应**、其余有就写没有省）；不嵌原型 ASCII（引用确认的 mock 路径）；术语 grep 收口（用准备 B 加载的业务术语表既有词）。

**重做模块 = 就地升版同一文件夹**：`spec.md` 版本 +1 + 变更日志加一行；被推翻的旧结论在 `decisions.md` 标 supersede（不删假装没发生）；只有"模块身份本身变了"才另起 + 归档旧的（例外，PM 拍）；合并旧内容先 **reconcile**（矛盾旧结论标 supersede 或删，不照搬进新真相源）。

**收口三道**（详见 `info-design.md` 四 + `consistency-scan.md`）：

1. **成文把关**：由 `/pmai-prd-writing` 对照 9 条逐条扫（只写最终事实 / 不造词 / 去 AI 腔 / 多步流程用叙述 / 术语全文一致……），改完 grep 自查残留。**只动文字、不动结构**——发现要改的是结构，停下回段②（甚至段①）。禁用清单单一真相源 = `skills/prd-writing/references/writing-rules.md`。
2. **派通用 subagent 冷读自检**（白纸读 spec，**不是 `analysis-reviewer`**——那个只评审探索段 discussion 第一节、不审 spec）：拿 spec 当尺子扫两类残留——① 机制 / 落点没说清（状态 / 动作只给名字没说怎么发生、落在哪、记什么）② 内部简称读者要猜。拿回清单逐条改。silent 自检，不升格强制门。
3. **一致性扫描**：这次改动碰的字段 / 规则 / 概念，grep 规格 ↔ 原型 ↔ DESIGN ↔ 术语表对账；对不上的 flag 给 PM 拍（哪个对 / 有意删还是漏实现 / 术语要不要统一），不静默吞、不自动改。

**收尾分流**（两条都不在本 skill 开 worktree）：

| 情况 | 去哪 |
|---|---|
| **小改 / 纯文档**（信息结构定了、原型改动小或没有） | 直接 `/pmai-close`：定稿、决策 / 术语回写基线、文档归位 |
| **大需求**（要建一坨原型 / 真实现） | 转 `/pmai-build`：PM 选工具 + 选要不要 worktree → 建 → review loop，完了再 `/pmai-close` |

输出一句给 PM 说清在哪、下一步：

```
模块三件套已写好 / 升版（docs/modules/<模块>/）：真问题、信息模型、规格、决策都齐了，成文已按 prd-writing 规则磨过，设计也用 mock 确认过（如涉及界面）。

▶ Next Up：
  · 改动不大 → 直接 /pmai-close 收尾（定稿 + 回写基线 + 归位）
  · 要建原型 / 真实现 → /pmai-build（你选工具、选要不要开独立工作区）
```

## Rules

- **三段式：①探索 → ②设计 → ③调用 prd-writing 成文**，先懂问题再设计。探索段是单 PM 唯一外部对抗源，不许跳（但 escape-hatch 默认放行、闻味才 push，不是每次审一遍）。
- **全程在 main 上只动文档、不开 worktree、不建原型**。开 worktree 是 `/pmai-build` 的事、merge 定稿是 `/pmai-close` 的事。
- **唯一组织单位 = 功能模块**；一模块一文件夹 `docs/modules/<模块>/`，三件套 `discussion.md` / `decisions.md` / `spec.md`，文件名英文、正文中文。
- **重做模块 = 就地升版同一文件夹**（版本 + 变更日志 + supersede + git 留痕），不 per-版本另开文件夹；身份变了才另起 + 归档旧的（例外，PM 拍）。
- **开场分档加载跨需求记忆（开放4）**：小改读本模块 spec+decisions + PRODUCT-RULES 全局 scope + 术语表；跨模块 / 新模块全量读 PRODUCT-RULES + 相关 docs/decisions 理路 + PRODUCT-STATE；拿不准升档。顺手读 TODO（遗留浮出）。别让 PM 重复说历史决策和术语。
- **落点口径（⑤理路拆家）**：PRODUCT-RULES 只装跨模块规则、理路在 docs/decisions 冻结档（按需读）、单模块决策在模块 decisions.md。
- **按需求类型选方法（开放3）**：真通用内核 3 条任何需求都过；类型分**两面**——逻辑面（后端规则 / 算法 / 引擎 / 流程）+ 呈现面（信息密集 UI 卡 / 管理页 / 数据产物），各配特化问题集。**先判逻辑面、再判有没有呈现面；结果给人看 → 呈现面必做（落点 / 卡面 / 抽屉 / 交互）+ PM 确认 + 视觉调 /mockup，别把引擎判定只当后端就收工**。三身份只在信息密集 UI 卡。判新型逐类说理由 + PM 拍 + 沉淀回 info-design。**不再"5 问对任何需求成立"**。
- **深度自适应、AI 提议 PM 拍**：信息密集走全套讨论、后端规则 / 流程 / 小改轻过。不机械化成"每需求必跑全部问题"。
- **设计确认靠 mock（可选档）**：视觉 / 布局文字掰不清时才调 `/mockup`；纯后端 / 无界面 / 小改、或文字已讲清可省。规格不嵌 ASCII，只引用确认的 mock。
- **spec 章节是菜单不是死模板**：§六信息模型按需、§八核心动作按七类自适应、其余有就写没有省；唯一判据 = 讲清楚。**但格式骨架统一、不灵活**——标题带序号（大标题中文数字、子标题 N.M）、名词解释 / 版本信息 / 变更日志用表格（见 `info-design` 第三块「格式骨架」）；灵活的只是"哪几节、功能清单用什么形式"，不是连标题序号、术语表格都随意。
- **三件套维度零重叠**：spec 唯一真相源、decisions 只记决策（不复制 spec 口径）、discussion 第一节=探索结论 + 后附讨论过程。
- **规格 4 问是验收标准**（详见 `info-design.md` 五）：分不清原型规格（不嵌原型 + 顶部声明 + mock 确认）/ 术语不统一（名词锁定 + 术语表加载 + grep 收口）/ 多余原型解释（为什么甩 decisions、原型样子甩 mock）/ review 删规格（一致性扫描 close 对账）。
- **收口三道**：磨文字（只动文字不动结构）+ subagent 冷读自检 + 一致性扫描。都是 silent 自检 / 顺手 flag，**不升格成强制 review 门**（守 I-RV1）。
- 给 PM 的话翻译掉 通用内核 / 4 环 / 三身份 / drift 等内部术语，用大白话。
