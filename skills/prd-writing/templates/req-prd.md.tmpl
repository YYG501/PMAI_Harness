# {{REQ_ID}} — {{REQ_NAME}} PRD

> **本 req 的功能规格（WHAT）** —— 由 `/pmai-prd-writing` 在 **stage 3** 产出，**stage 3 定稿后冻结**，
> 整个执行期 task 按它做。
> **生命周期**：stage 3 定稿冻结 → 执行期 task 驱动 → `/pmai-close-req` 收尾反向对齐成 as-built。
> 执行期对 PRD 的偏离不直接改本文件，记入 req 事件流（`req-events.jsonl` 的 `adjustment` 事件），
> 由 close-req 一次性反向对齐回填。
> （v5 已砍项目主 PRD `docs/prd.md`；req 级 PRD = 本 req 的功能规格，前置驱动 task）

## 摘要

<!-- 1-3 句话：本 req 解决什么问题、覆盖什么范围、属于哪个迭代 -->

## 一、文档版本信息

| 字段 | 值 |
| --- | --- |
| 文档版本号 | V |
| 创建日期 | |
| 编写人 | |
| 产品版本号 | V |
| 对应原型版本号 | V（如无则留空） |

## 二、变更日志

| 时间 | 版本号 | 对应原型版本 | 变更人 | 主要变更内容 |
| --- | --- | --- | --- | --- |

## 三、名词解释

| 术语 / 缩略词 | 说明 |
| --- | --- |

## 四、需求分析

### 4.1 业务诉求

<!--
开头 1-2 句引子，说明本模块在业务上面临一组什么样的真实诉求；
然后每条以「**加粗短句。**」开头 + 一段说明该诉求的具体场景与影响。
来源于 analysis.md，需翻译为用户视角，不要直接搬"PM 讲不清"等团队内部诱因。
-->

### 4.2 设计原则

<!--
首句承接：§4.1 的业务诉求推导出本模块的 N 条设计原则。第六章的功能定义直接承接这些原则。
表格输出，列：编号 / 原则 / 内容 / 对应业务诉求（可选）。
原则名 4-8 字短句；内容写产品规则一句话；不写 "本 PRD 是规格交付层" 等元描述。
-->

| 编号 | 原则 | 内容 | 对应业务诉求 |
| --- | --- | --- | --- |

### 4.3 方案价值（Impact）

<!-- 3-5 条 bullet，格式：**加粗短句。** 具体结果 -->


## 五、用户与场景

### 5.1 用户角色

<!--
默认 3 列扁平表（如下）。
多平台 / 多层场景（租户 / 主运营 / 下级运营三层等）改用 4 列分组表：
角色归属 / 角色 / 描述 / 与本模块的关系。一律用管道表格（| ... |），禁用 HTML <table>
（飞书发布只认管道表格，HTML 表会塌成纯文本）。「角色归属」列同一归属下第一行写名称、
其余行该格留空，发布到飞书自动合并成跨行单元格——示例见 prd-writing skill few-shots §5.1。
-->

| 角色名 | 描述 | 与本功能的关系 |
| --- | --- | --- |

### 5.2 用户故事

<!--
**Story N：** 作为 <角色>，我希望能 <动作>，以便 <价值>。
编号须与第七章 AC 分组编号一一对应。
-->

### 5.3 非目标

<!-- bullet list；无则写"暂无" -->

## 六、功能需求

- **6.1 原型**：<!-- 原型链接或版本号；如无则写"待补充" -->
- **6.2 功能需求**：

<!--
默认 6 列；章节标题已写明一级功能时省略"一级功能"列变 4 列；
回溯型 PRD 通常不需要"优先级"列；
回填原型素材时截图作为独立图片放进对应 §6.X「原型」节、不开表格列（见 prd-writing skill「原型列」段）；
明确不在本 PRD 范围、但读者会期望存在的功能行（如身份权限 PRD 中的"产品额度"）用 <del> 划线整行保留，不删除。
-->

| 一级功能 | 二级功能 | 三级功能 | 优先级 | 使用角色 | 需求描述 |
| --- | --- | --- | --- | --- | --- |

<!--
身份 / 权限 / RBAC / 账号体系类 PRD 可在此处插入两个专属章节，原 §七 / §八 / §九 顺延：

## 七、数据权限范围
（按平台分层给出"各角色在产品界面上能看到的数据范围"，含通用前提 / 各平台表格 / 通用边界说明）

## 八、角色权限清单
（权限矩阵大表：一级 / 二级 / 三级功能 × 各角色，✅/空 三档）

详见 prd-writing skill 「身份 / 权限类 PRD 专属可选章节」段。
-->

## 七、验收标准（Acceptance Criteria）

<!--
**Story [编号]：[故事名称简述]**
- [可验证的结果状态]

普通项目符号即可，勿嵌引用块 / 勿用 `- [ ]` 复选框（飞书发布会吞掉该结构）。
-->

## 八、非功能性需求

<!-- 如无则写"暂无" -->

## 九、附件（可选）

<!-- 分类表 / 字段字典 / 枚举值等；无此章节则不写 -->

## 📎 参考材料

<!--
 attachments AI 接管引用 section。caller SKILL（prd-writing）按
`.req-meta.json:attachments_seen` 列表渲染本 section（按 registered_at 升序），
PM chat 上传过的附件自动出现在这里；无引用则整段省略不写。

详见 `skills/_shared/pm-view/attachments-upload.md` §5 引用 section 渲染规则。
-->

