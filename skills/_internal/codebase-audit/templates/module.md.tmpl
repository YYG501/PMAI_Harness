<!-- 状态：草稿 | 未经 PM 确认 | 生成时间：{{DATE}} -->

# {{MODULE_NAME}} 模块规格

> 本文件是 **{{MODULE_NAME}} 模块的活文档** —— 记录该模块当前「是什么」，跨模块工作持续累积、长期演化。
> **只写长期为真的模块事实。** 带本次工作时态的内容（本轮范围 / 本批不做 / 本次验收标准）
> 属当前模块 discussion 或按需 PRD，**不进本文件**。

## 摘要
<!-- 1-3 句：模块解决什么问题、服务谁、在系统中处于什么位置。不写"本轮""本批"等单次工作时态范围 -->

## 一、模块定位

### 1.1 模块目标
<!-- 一句话说明模块在系统中的位置 -->

### 1.2 使用角色

| 角色 | 与本模块的关系 | 关键动作 |
|------|--------------|---------|

### 1.3 核心职责
<!-- bullet list：模块长期承担的职责 -->

### 1.4 职责边界
<!-- 本模块管什么、不管什么、和上下游如何分界 -->

## 二、功能清单（硬约束）

> 本节是硬约束：功能行为、数据规则、角色权限必须严格遵循。
> 视觉 / 布局 / 微交互的硬约束在 `DESIGN.md`（视觉基线 + 共享组件 inventory）；
>
> 填写规则（PM-VIEW-RULES §5.1，4 列表格 + 续行 rowspan + 需求描述列内联编号）：
> - 用一级章节承接模块内的业务功能分组，便于后续 build / build-close 对账。
> - 每个三级功能用一个功能块：`#### N · [三级功能名]` + 可选 `> **使用角色**：[...]` blockquote + 4 列表格（二级 / 三级 / 使用角色 / 需求描述）。
> - 同一三级功能的多条需求 → 第 2 条起的前 3 列留空（视觉等同 rowspan）。
> - 字段定义、计算口径、边界规则等直接写进需求描述列编号项；**不另起字段口径独立表**。
> - 需求描述编号项只回答 what（业务命题 / 角色权限 / 数据闭环 / 可执行业务动作）；过 §5.2 判别简则「换 UI 还成立吗」。
> - 字段值口径用业务动词（累加 / 统计 / 取 / 减 / 按 X 归并）；不用 sum / count / map / filter / reduce 等英文工程词。
> - 视觉细节（字号 / 颜色 / 间距 / 像素值 / 组件库类名）一律不在功能清单内出现；如需视觉规范，引用 `DESIGN.md` 对应章节。

<!-- 一级章节名示例：账号登录 / 权限管理 / 报表查看 -->

### [一级章节名]

#### 1 · [三级功能名]

> **使用角色**：[当本节整体角色统一时写在此 blockquote；逐行不同则只填表内列，本行删除]

| 二级功能 | 三级功能 | 使用角色 | 需求描述 |
|---------|---------|---------|---------|
| [二级功能名] | [三级功能名] | [角色名 — 角色限定条件] | 1. [业务命题或规则] |
| | | | 2. ... |
| [二级功能名] | [另一三级功能名] | [角色名] | 1. ... |
| | | | 2. ... |

> **注**：[可选，写明本节额外约束 / 边界；不需要时整行删除]

## 三、页面与交互范围

| 页面/视图 | 入口 | 核心区块 | 关键状态 |
|----------|------|---------|---------|

<!-- 关键状态至少考虑：空状态、加载态、异常态、无权限态、跨模块占位态 -->

## 四、硬约束
<!-- 跨功能 / 跨页面的模块级不可违反规则。
     功能内部的规则已写在 §二 功能清单的需求描述列，此处不重复——
     只写需要跨多个功能统一遵守的约束。无此类约束则整节留空。 -->

## 五、跨模块依赖与占位策略
<!-- 依赖哪些外部模块；模块当前对未实现依赖如何占位（占位是模块的当前真相，依赖落地后更新此节）-->
