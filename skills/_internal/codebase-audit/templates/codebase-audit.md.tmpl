# {{PROJECT_NAME}} — 代码现状档

> **这是什么**：brownfield（已有代码库）接入框架时，由 `/pmai-init-project` 的已有代码分支触发 codebase-audit 子流程产出的
> **代码现状档** —— 7 维度盘点已有代码库，作为后续内联方向讨论的实况语境
> （被现状档喂着，和新项目一样定方向）。
> **与 GSD 对照**：同构于 GSD 的 `map-codebase → new-project`。
> **落盘路径**：`docs/CODEBASE-AUDIT.md`。
> ⚠️ **防 secret**：本文件**禁止**写入任何密钥 / token / 密码 / 连接串 —— 扫到的敏感值
> 一律以 `<redacted>` 占位，只记「这里有一个 X 类密钥、在 Y 文件」。

## 1. 技术栈（stack）

<!-- 语言 / 框架 / 运行时 / 包管理器 / 构建工具；版本号。从 package.json / 锁文件 / 配置读。 -->

## 2. 外部集成（集成）

<!-- 依赖的外部服务 / API / 数据库 / 第三方 SDK；各自用途。
     ⚠️ 连接串 / API key 一律 <redacted>，只记「集成了什么」。 -->

## 3. 架构（架构）

<!-- 整体架构形态：单体 / 前后端分离 / 微服务 / 等；主要分层；数据流向。 -->

## 4. 目录结构（结构）

<!-- 顶层目录树 + 每个关键目录一句话职责。不贴全树，列关键节点。 -->

## 5. 代码约定（约定）

<!-- 命名约定 / 文件组织约定 / 状态管理 / 错误处理 / 注释风格 等可观察的约定。 -->

## 6. 测试现状（测试）

<!-- 有无测试 / 测试框架 / 覆盖面 / 怎么跑测试。 -->

## 7. 隐患（隐患）

<!-- 扫码发现的技术债 / 风险点：过时依赖 / 无测试的关键路径 / 硬编码 / 重复代码 /
     安全隐患（含「发现 N 处疑似硬编码密钥，已 redact，位置见…」）。 -->

---

> **下一步**：本现状档产出后，PM 过目确认 → 当前接入流程在同一流程内内联跑方向讨论
> （读本档作实况语境，和新项目一样定方向、产出 `PRODUCT.md` + `TODO.md`），不用 PM 手敲第二个命令。
