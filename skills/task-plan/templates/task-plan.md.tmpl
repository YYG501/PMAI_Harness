# Task Plan — {{REQ_NAME}}

> 本文件是 task 拆分的**单一真相源**：拆分摘要、task 列表、执行顺序、风险，加末尾轻量"自检与状态摘要"（反模式自检结论 / 验收 GAP 索引 / 模块规格状态）。
> 写作规则参见 `skills/_shared/PM-VIEW-RULES.md`，特别是：
> - §三 PM 视图写作规则
> - §七 章节顺序约束（本模板按此节锁定的顺序）
> - §九 输入流约束（必读项目底座 `docs/PRODUCT-STATE.md` / `docs/DESIGN.md` + 本次 `req-plan.md`（范围清单 WHAT + 决策页 WHY）+ `prototype/` 主原型代码）

- 创建日期：{{CREATED_DATE}}
- 创建者：{{AUTHOR}}
- 分支：{{REQ_BRANCH}}
- 依赖：`req-plan.md`（本次范围清单 + 关键决策页，拆 task 的直接依据）+ 项目底座 `docs/PRODUCT-STATE.md` / `docs/DESIGN.md` + `prototype/` 主原型代码
- 阶段：范围确认（task-plan 是范围确认阶段产物；拍板后由 `/pmai-next` 推进进 build）
- Status：DRAFT（待 PM 确认拆分 → 进 build）

---

## 📌 拆分摘要

<!-- 填写规则：
- 1-2 句：本 req 拆成几个 task / 总工作量预估 / 推荐起跑顺序
- 不写"为什么这样拆"的设计理由 → 进 §四 自检与状态摘要的反模式自检
- 字数控制在 100 字以内
-->

[1-2 句拆分摘要]

---

## 一、Task 列表

<!-- 填写规则：
- 表格列：id / title / 所属模块 / 所属模块章节 / summary / order / risk
- "title" 用产品视角的功能名（与 task spec 顶部标题保持一致）
- "所属模块" = 业务模块名 / 基础设施
- "所属模块章节" 业务模块 task 必填，基础设施 task 留空
- "summary" 一句话讲清本 task 交付什么 + 验收角度（不写实现细节）
- "order" = task 在执行链路中的顺序号
- "risk" = 标关联到 §三 风险表的 R 编号；无关联写"无"
- 不允许写超过一行的 summary（细节进 build）
- 每条 task 除「交付什么」外，还要在下方「每个 task 的范围细节」里写清四字段（执行深度 / PM 确认什么 / 不做什么 / 并行性），决定 PM 怎么看 demo 验收、build 时怎么排（见 task-plan SKILL 步骤 2）
- 结构决策（合并 / 拆开 / 重排 task）由步骤 3.5「PM 拍板执行模式门」在 §二 处理，AI 不自决；本表不再设 per-task「决策类型」列
-->

| id | title | 所属模块 | 所属模块章节 | summary | order | risk |
|----|-------|---------|------------|---------|-------|------|

### 每个 task 的范围细节（SKILL 步骤 2 四字段）

<!-- 每个 task 一段，写清四字段（防覆盖审计误判为「漏」、防 build 越界、定 PM demo 验收角度）：
- 执行深度：页面 / 字段 / 按钮 / tab / 状态做到什么程度（跑通主流程 / 含边界态 / 含空态错误态）
- PM 确认什么：这块 demo 上要 PM 拍板的方向问题（一句话）
- 不做什么：本 task 明确划出去、本次不做的部分
- 并行性：能否与其它 task 同时在各自原型分支跑（无 merge 冲突 + 不互相依赖），还是必须排在某 task 之后
-->

**task-001**：执行深度 [...] / PM 确认 [...] / 不做 [...] / 并行性 [...]

**未纳入本 plan 的范围清单条目**（如有，写一段说明为什么不纳入；如无写"无"）：

[列出 req-plan 范围清单里提到但未拆为 task 的条目 + 处理路径（"留待 PM 反馈后追加" / "归到 task-XXX" / "本次原型不实现 → req-plan 决策页 D-NN"）]

---

## 二、执行顺序与并行性

<!-- 填写规则：
- 用 ASCII 流程图 + 文字说明
- 标注串行 / 并行关系
- 标注 worktree 类型（req worktree 改 docs / task worktree 改代码）
- 给出 PM 启动建议（先启动哪个、什么时候并发）

- 「执行模式」是 task 级结构决策：
  AI 不自决；task-plan skill 步骤 3.5 主动 prompt PM 拍板（串行 / 并行 / 混合）
  + AI 给倾向 + 理由（同文件冲突 / 互相参照规范段 / task 数 / PM 走查负担）
  PM 拍板后写回下方「执行模式（PM 拍板）」行 + append req-events.jsonl decision
-->

**执行模式（PM 拍板）**：<串行 / 并行 / 混合>

```text
[流程图：task-001 → task-002 ↘
                 task-003 ↗ → 收尾]
```

- **Lane A（[worktree 类型], 串行 / 并行）**：[task 列表]
- **Lane B**：[同上]

**PM 启动建议**：
- 先启动 [task-XXX]
- [task-XXX] 完成后可同时启动 [task-YYY] / [task-ZZZ]
- 全部 task 完成后由 `/pmai-next` 推进进复审 / 沉淀

---

## 三、风险

<!-- 填写规则：
- 表格列：# / 风险描述 / 触发场景 / 应对路径
- 每条 1-3 句，正向描述
- "应对路径" 写"出问题时怎么处理"（不写"禁止 X"反向约束）
- 仅列与 task 拆分相关的风险（数据模型一致性、跨 task 接口、串行冲突等）
-->

| # | 风险描述 | 触发场景 | 应对路径 |
|---|---|---|---|
| R1 | [正向描述风险] | [什么情况下触发] | [出问题时怎么处理] |

---

## 四、自检与状态摘要

> 本节是 task-plan 自身的轻量自检结论 + task-spec / 沉淀（close-req）会消费的状态索引。
> **不是**给 PM 读的产品决策内容；PM 看一眼"反模式 5/5 PASS"即可。

### 4.1 反模式自检（5 条）

按 task-plan skill §2.2 五条反模式逐条自检。每条只写一行结论；命中时一句话说明合并 / 重构后的处理。**5 条全 PASS 才能进入 build**。

**反模式 A 命中时必须分流**（SKILL §2.2 反模式 A "判断"两分支）：
- **重构类** → 合并入 task-N（写明 N）
- **文档 / 规格 / 契约类**（含 req-plan 关键决策页明文要求的"建立 X 规范段 / 字段字典 / 权限矩阵"等主线规范产物，产物归宿是项目底座文档 `docs/PRODUCT-STATE.md` / `docs/DESIGN.md`）→ **不合并入 task**、**不把项目底座文档写进任何 task 的「执行范围」allowlist**；由首个相关业务 task 收尾后在沉淀（close-req）补对应章节（写明 N + 目标文件路径）

| # | 反模式 | 状态 | 命中处理（仅命中时填，含分类）|
|---|---|---|---|
| A | 纯前置 task（重构 / 文档 / 规格 / 契约） | 未命中 / 命中（已处理）| 类型: 重构类 → 合并入 task-N / 文档类 → task-N 收尾后在沉淀（close-req）补 docs/PRODUCT-STATE.md / docs/DESIGN.md 对应章节 |
| B | 横切质量 task | 未命中 / 命中（已处理）| — |
| C | 共生对拆成两个 | 未命中 / 命中（已处理）| — |
| D | 同文件串行多 task | 未命中 / 命中（已处理）| — |
| E | 业务功能 task 没有模块归属 | 未命中 / 命中（已处理）| — |

**结论**：[5 条全部 PASS / 命中 X 条已处理] → 可 / 不可进入 build

### 4.2 验收 GAP 清单

<!-- 填写规则（来源 = req-plan 范围清单；solution.md / prd.md §七 / implementation-design SIMP 均已不是来源）：
- 来自 req-plan.md 范围清单逐条审视，编号 G1, G2, ...（每条都要能在 build 完被覆盖审计对照原型代码验到）
- 每条三种处置之一：
  1. 由 task-NNN 接住（默认） → 「应落到哪个 task 验收」列填 task-NNN
  2. 已被 task 列表完全覆盖 → 整表写「无 GAP」
  3. 本次原型不实现（整块功能不做，PM 已在 req-plan 关键决策页拍板）→ 列填「本次原型不实现 → req-plan 决策页 D-NN」，
     引用 req-plan 关键决策页对应那条决策
- 处置 1 由 build 期 task-spec 按编号接住 / 处置 3 由沉淀（close-req）据 req-plan 决策页在 PRODUCT-STATE 标注「本功能本次不做」，供覆盖审计排除、不误报为漏
-->

| GAP # | GAP 描述 | 应落到哪个 task 验收 |
|---|---|---|
| G1 | [req-plan 范围清单的某条] | [task-XXX] / 本次原型不实现 → req-plan 决策页 D-NN |

### 4.3 模块规格状态

<!-- 填写规则：
- 列出本 req 涉及的每个业务模块在 PRODUCT-STATE.md 里的当前状态
- 状态：已存在-完整 / 已存在-待补 / 不存在-待创建
- 影响 build 期 task-spec 判断（要不要新建模块章节）
-->

| 模块 | 当前规格状态 | 本 req 涉及的章节 |
|---|---|---|

---

## 📁 历史档案

### 变更记录

| 日期 | 变更内容 | 负责人 |
|---|---|---|
| {{CREATED_DATE}} | 初稿 | {{AUTHOR}} |
