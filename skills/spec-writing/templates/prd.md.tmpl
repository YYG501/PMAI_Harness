# {{MODULE}} PRD

<!--
本文档是指导研发实现和验收的最终目标合同。
内容按 content-modules.md 选择，并叠加适用 Profile；本模板只负责完整 PRD 的章节编排。
范围、规则、字段、交互和验收来自已确认规格与决定；原型、mockup 和代码只作证据，不能缩小需求。
正文只保留最终要求，不写讨论过程、备选方案、mock / 占位或实现覆盖状态。
-->

## 摘要

<!-- 1-3 句话：要解决什么问题、最终覆盖什么范围、交付后谁得到什么结果。 -->

## 一、文档信息

| 字段 | 值 |
| --- | --- |
| 文档版本 | V |
| 创建日期 | |
| 编写人 | |
| 当前状态 | 草稿 / 评审中 / 已发布 |
| 上游依据 | Product Proposal（如有）、模块决定或项目级产品规则 |

## 二、变更日志

<!-- 每条一句，通常 20 字内、最多不超过 30 字；只写改了什么，不写原因和讨论过程。 -->

| 时间 | 版本 | 变更人 | 主要变更内容 |
| --- | --- | --- | --- |

## 三、背景、目标与范围

### 3.1 背景与问题

### 3.2 本次目标

### 3.3 范围

### 3.4 非目标

<!--
定位、目标用户、价值链、AI 必要性、平台/应用边界或 MVP 假设缺失时，停止成文并返回 /pmai-proposal。
模块对象、规则、任务路径、权限或关键交互缺失时，停止成文并返回 /pmai-design。
-->

## 四、用户、角色与场景

### 4.1 用户与角色

| 角色 | 核心任务 | 权限或责任差异 |
| --- | --- | --- |

### 4.2 核心场景

<!-- 谁在什么情况下进入、要完成什么、谁确认、完成后得到什么。 -->

## 五、信息模型

<!--
先定义稳定对象，再写功能动作。覆盖对象身份、关系、生命周期、状态、权威来源和历史保留。
字段很多时使用字段表；只在某动作中出现的字段放到第六章对应动作下。

企业平台 Profile：补租户/组织边界、账号/身份/成员/角色/授权区分、版本与外部接入。
AI Profile：补输入依据、AI 推断/建议、人工确认、正式生效对象、评测对象与版本。
-->

## 六、核心动作

<!--
按用户目标和业务动作组织，不按页面、弹窗、Tab 或接口组织。

每个动作按需写：
- 目标与角色
- 触发与前置条件
- 输入与校验
- 处理规则
- 结果与状态变化
- 权限与数据范围
- 失败与恢复

复杂 CRUD 管理后台可在本章开头增加“4 列动作索引”，但索引只帮助扫描，不承担完整需求：
| 动作组 | 核心动作 | 使用角色 | 详情位置或一句摘要 |
每个索引项仍需在下文写完整动作详情。简单 PRD 不生成索引。
-->

### 6.1 {{ACTION_GROUP}}

#### 6.1.1 {{CORE_ACTION}}

- 目标与角色：
- 触发与前置条件：
- 输入与校验：
- 处理规则：
- 结果与状态变化：
- 权限与数据范围：
- 失败与恢复：

## 七、异常与空状态

<!--
至少检查无数据、无匹配结果、校验失败、提交时状态/权限变化、外部服务失败、
重复提交、并发冲突、数据缺失/冲突，以及操作中断后的重试和保持不变规则。
每项说明用户看到什么、系统保留什么、能否重试、由谁处理。
-->

## 八、验收标准

<!--
按核心场景或动作分组。每条包含前置条件、动作/事件和可观察结果；必要时补不应发生的结果。
正常路径、权限边界、状态变化、异常与恢复都要覆盖。

启用 AI Profile 时，按需追加：端到端评估维度与红线、评测集要求、主 Case Rundown 和高风险支持 Case。
-->

## 九、非功能要求

<!-- 按需覆盖性能、安全、可用性、可观测性、兼容迁移、数据保留和合规；没有可靠阈值时写验证方法，不编造数字。 -->

## 十、附件与参考材料（可选）

<!--
权限矩阵、字段字典、枚举、错误码、评测样本和已确认设计资产按需放这里。
附件不能替代正文中的信息模型、动作规则、异常或验收。

attachments AI 接管引用：caller 按 `.work-meta.json:attachments_seen` 列表渲染，
按 registered_at 升序；无引用则省略参考材料内容。
详见 `skills/_shared/pm-view/attachments-upload.md` §6。
-->
