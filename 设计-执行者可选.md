# 设计：Execution Agent 可替换

> **状态**：设计定稿（v1），待实现
> **更新时间**：2026-04-22
> **决策基础**：四个根本决策 + 五个次级决策（均已与 PM 敲定）
> **关联**：`需求.md §8.4`、`设计.md §4 / §9.3`、`skills/task-execute/SKILL.md`

---

## Context

需求文档 `需求.md:300-303` 声明 Execution Agent "可被替换（Cursor / Gemini / 其他）"，但 v1 设计没落实 —— `skills/task-execute/SKILL.md` 全部假设执行者就是 Claude Code。

> **v1 范围：** 本次实施仅支持 `claude-code / codex / cursor-agent / manual` 四个执行者。Gemini 暂不实现（无具体使用场景，需要实测后再加）。

上次用 codex 跑 task-002 踩在空档上：Codex CLI 默认 sandbox 写不进项目文件；临场切 general-purpose agent 是"在症状上打补丁"，且把"用 codex 写代码"的初衷也丢了。

本轮补齐这层抽象。

**范围**：只让 Execution Agent 可替换，**Suborchestrator 永远是 Claude Code**。自审链路（gstack `/review /qa /design-review`）不动。

---

## 决策快照

| # | 决策点 | 结果 |
|---|--------|------|
| 根 1 | 可替换的是哪一层 | **只 Execution Agent**；Suborch 永远是 Claude |
| 根 2 | 执行者的维度 | **一维**扁平列表 |
| 根 3 | 选择粒度 | **per task + 继承默认值**（task → req → project） |
| 根 4 | 一等公民名单 | `claude-code` / `codex` / `cursor-agent` / `manual`（gemini 延后） |
| 次 1 | 默认路由策略 | 一律默认 `claude-code`，PM 手动改；不预设 routing 表 |
| 次 2 | 字段名 | `**executor：**`（英文键 + 中文冒号） |
| 次 3 | Adapter CLI 参数 | 实测定稿（见 §4） |
| 次 4 | Manual 中断恢复 | 标记文件 + 幂等重入；不做关键词识别 |
| 次 5 | 失败后 task 状态 | 自动回落"待确认" |
| 次 6 | Model 配置 | 独立字段 `executor_model`，per-executor 默认值配在 settings |

---

## 1. 执行者清单

| 名称 | 后端 | 调用形式 | 关键权限 |
|------|------|---------|---------|
| `claude-code` | Suborch 自己写（默认） | 主流程 inline，no dispatch | 现有 `check-branch.sh` hook |
| `codex` | Codex CLI | `node codex-companion.mjs task --write` | 必须 `--write`，cwd=task worktree |
| `cursor-agent` | Cursor Agent CLI | `cursor-agent -p --force --trust --workspace <path>` | `-p` 自带写权限；`--trust` 跳 workspace 信任提示 |
| `manual` | PM 本人 / 任何其他工具 | Adapter 写标记后立即退出，靠幂等重入 | PM 自负 |

> **注**：Gemini CLI 暂不实现。v1 实测中如果出现"claude-code / codex / cursor-agent 都不合适"的场景再补。

---

## 2. 数据结构变化

### 2.1 task 文件模板（`templates/task.md.tmpl`）

在 frontmatter 增加两个字段（放在「审查工具」之后、「分支」之前），字段**下方附 inline 提示**（模板里写死，PM 打开任一 task 都能看到——discoverability 的主入口）：

```markdown
**状态：** 待确认
**审查工具：** /qa, /design-review
**executor：** claude-code
**executor_model：**
<!--
可选 executor：claude-code / codex / cursor-agent / manual
model 留空 = 用 settings 默认
claude-code 仅支持：opus / sonnet / haiku（Agent tool 枚举）
cursor-agent：实验性，无沙箱，越界保护仅限 worktree 内
放弃 manual：python3 .claude/scripts/task-transition.py <本文件> --cancel-manual
暂缓 manual 提醒：python3 .claude/scripts/task-transition.py <本文件> --snooze-manual --days 3
-->
**分支：** task-NNN-<slug>
```

字段匹配正则：
- `re.search(r'\*\*executor：\*\*\s*(\S+)', text)`
- `re.search(r'\*\*executor_model：\*\*\s*(\S*)', text)` —— `\S*` 允许空值

**`executor_model` 空值语义**：走 `settings.json.executor.models[<executor>]` 的默认；默认也未设则让 CLI 用自身默认 model。

**claude-code 的 model 传递路径**：Suborchestrator 是通过 Agent tool 启动的 subagent，Agent tool 支持 `model` 参数。因此 `executor_model` 对 claude-code 执行者**也是有效的**——实施时把字段值传给 Agent tool 的 `model` 参数即可。

### 2.2 `.req-meta.json`

**不加 req 级 `executor_default` 字段**。PM 若想整个 req 用非默认执行者，在该 req 下每个 task 文件里显式填 `executor` 字段即可（通常一个 req 3-8 个 task，成本可接受）。简化继承链深度。

### 2.3 `.claude/settings.json`

新增顶层 `executor` 配置：
```json
"executor": {
  "default": "claude-code",
  "models": {
    "codex": "gpt-5.4",
    "cursor-agent": "gemini-3.1-pro"
  }
}
```

- `default`：全项目默认执行者
- `models.<executor>`：每个执行者的默认 model。可缺省——缺省时 adapter 不传 `--model` flag，让各 CLI 用自己的默认
- **不预设 routing 表**。PM 用一段时间、发现稳定偏好后再补（`"routing": {"纯后端": "codex", ...}`）

**当前默认值说明：**

| 执行者 | 默认 model | 说明 |
|--------|------------|------|
| claude-code | 不填 | 走 Agent tool 继承（默认从 Suborchestrator agent 定义或父会话），填了会传给 Agent tool 的 `model` 参数（`opus` / `sonnet` / `haiku`） |
| codex | `gpt-5.4` | 传给 `codex -m`；`service_tier=fast` 由用户 `~/.codex/config.toml` 控制，adapter 不覆盖 |
| cursor-agent | `gemini-3.1-pro` | 传给 `cursor-agent --model` |

### 2.4 继承链

**executor（谁）**：

```
task 文件 executor 字段
  ↓ 未填则继承
settings.json executor.default
  ↓ 未填则
claude-code
```

**executor_model（用什么 model）**：

```
task 文件 executor_model 字段
  ↓ 未填则继承
settings.json executor.models[<resolved_executor>]
  ↓ 未配置则
该 executor CLI 的默认 model（adapter 不加 --model flag）
```

> executor 和 model 都不在 req 层设置——per-req 切换需求罕见，省掉一层复杂度。需要时显式在 task 里填，或后续加 req 层（零破坏性扩展）。

由 `scripts/resolve-executor.py` 提供统一查询（返回 `{executor, model}` 两个值），供 task-execute 调用。

---

## 3. 流程改动

### 3.1 `/task-plan`

Step 5 生成 task 文件时：填 `executor：claude-code`，`executor_model：` 留空（走 settings 默认）。不做 routing。

### 3.2 `/task-confirm`

**展示方式（默认压单行，非默认展开）**：

默认态（executor=claude-code 且 executor_model 空）：
```
Task-003：添加用户登录
  执行方式：claude-code（默认）
  审查工具：/review, /qa
```

非默认时展开：
```
Task-003：添加用户登录
  执行方式：codex / gpt-5.4 (project 默认)
  审查工具：/review, /qa
```

**交互式切换执行者**（PM 不用再切回编辑 task 文件）：
```
是否切换执行方式？可选：claude-code / codex / cursor-agent / manual
输入新 executor（回车保持默认）：
model（留空=用默认，claude-code 仅支持 opus/sonnet/haiku）：
```

PM 输入后 skill 直接 sed 更新 task 文件字段 + 重新打印摘要。

**Spawn 流程（关键：先 ack 再切状态）**：

```
1. 状态保持「待确认」
2. 调用 Agent tool spawn Suborchestrator
   model 参数 = resolve-executor 得到的 model（若 executor=claude-code 且有效）
   payload 显式含 { resolved_executor, resolved_model, task_file_path }
3. 若 spawn 失败（例如 model 不可用）
   append event: suborch_spawn_failed { reason }
   告知 PM：「Suborchestrator 启动失败（reason）。状态保持待确认，可改 executor/model 后重试 /task-confirm。」
   exit skill（不转状态）
4. 若 spawn 成功
   subagent ack → append event: suborch_spawn_started
   /task-confirm 调 task-transition.py --to 执行中
   把控制权交给 Suborch 跑 /task-execute
```

**Suborch spawn payload 必含硬约束**：
> 当 resolved_executor != "claude-code" 时，Suborchestrator **只做 orchestration**（调 /task-execute dispatch），**禁止直接写业务代码**。

Skill 需扩展：检测 task worktree 已存在时跳过创建（支持失败后重试场景）。

### 3.3 `/task-execute`（核心改动）

#### 3.3.1 Prompt 契约（非 claude-code 执行者必备）

拼给 adapter 的 prompt 必须包含 **6 个 section**，缺一不可：

1. **元信息**：task 文件绝对路径、task_worktree 路径、task 分支名、task 描述
2. **启动前必读（按顺序）**：task 文件的「启动前必读」列表 + 每项绝对路径
3. **执行范围**：
   - **允许写入**：allowlist（来自 task 文件「执行范围」字段）
   - **禁止写入**：denylist（`docs/` + gitignore 匹配的生成物 + worktree 外一切路径）
4. **未处理的 PM 反馈**：task 文件「反馈」section 未标记已处理的条目
5. **验收标准**：task 文件「验收标准」section 原文
6. **写回职责**：
   - 执行完成后必须更新 task 文件「执行日志」「文档偏差」section
   - 禁止 `git commit` / `git add`（commit 由 Suborchestrator 统一做，见 3.3.3）

由 `scripts/build-execution-prompt.py <task_file>` 生成，stdout 是完整 prompt，写入临时文件后作为 `PROMPT_FILE` 传给 adapter。`claude-code` 执行者主流程 inline 实现时也应从同一 script 生成 prompt（保证一致）。

#### 3.3.2 主流程伪代码

```
# Step 3: Execution dispatch
{executor, model} = resolve_executor_and_model(task_file, settings)

# 3a. 幂等检查（manual 续跑）
lock_dir = ".runs/.lock-<task>"
if not mkdir(lock_dir):
    abort("另一个 /task-execute 正在运行此 task")
trap "rmdir lock_dir" EXIT

if exists($MAIN_REPO_ROOT/.runs/.pending-manual-<task>.json):
    pending = load(pending_file)
    告知 PM:
      "✓ 检测到 Task-<id> 的 manual 续跑标记（since <started_at>）。
       将保留当前 worktree 的现有改动，不会重新调用任何执行器。
       直接进入自审阶段。"
    baseline_sha = pending.baseline_sha   # 用于越界检查的对照
    run preflight:
        - git diff <baseline_sha> HEAD 看到的改动是否落在 allowlist
        - task 文件「执行日志」「文档偏差」是否更新
        - 未通过则提示 PM 补齐后再重入（不删标记）
    preflight 通过 → delete pending marker + append execution_manual_completed
    → goto step 4

# 3b. 正常 dispatch
baseline_sha = git rev-parse HEAD   # 记下基线供越界检查 + rollback
append event: execution_started { executor, model, baseline_sha }
PROMPT_FILE = $(mktemp)
python3 scripts/build-execution-prompt.py <task_file> > $PROMPT_FILE

invoke_executor(executor, PROMPT_FILE, task_worktree, model)
# invoke_executor 内部：
#   claude-code  → 当前 Suborch inline 实现（model 已在 spawn 时生效）
#   codex        → bash scripts/exec-adapters/codex.sh
#   cursor-agent → bash scripts/exec-adapters/cursor-agent.sh
#   manual       → bash scripts/exec-adapters/manual.sh（写 pending + exit skill）

if executor == "manual":
    # manual adapter 立即退出；pending 文件含 baseline_sha
    exit skill

if exit_code != 0:
    classification = classify_exit_code(exit_code)  # sandbox_denied / model_not_found / network / unknown
    rollback_to(baseline_sha)
    append event: execution_failed { executor, model, exit_code, classification, log: <path> }
    task-transition.py --fail-execution <task> --reason "<classification>"
    output_diagnostic(<见 §6>)
    exit skill

# 3c. 越界写保护
allowlist = parse_task_execution_scope(task_file)
denylist = ['docs/*'] + parse_gitignore() + ['!*']  # 最后一项表示未在 allowlist 的 catch-all
changed = git status --porcelain -z
  → 分类：tracked_modified / tracked_deleted / untracked / ignored
bad_files = changed - allowlist - ignored
if bad_files:
    rollback_paths(bad_files, baseline_sha)
    # 具体命令：
    #   tracked:   git restore --source=<baseline_sha> --worktree --staged -- <paths>
    #   untracked: rm -f <paths>
    append event: execution_failed { reason: "越界写入", bad_files }
    task-transition.py --fail-execution <task> --reason "越界写入"
    output_diagnostic(§6)
    exit skill

# 3d. 零改动检查
if len(changed) == 0:
    append event: execution_failed { reason: "no_changes" }
    task-transition.py --fail-execution <task> --reason "no_changes"
    output_diagnostic("执行者退出码 0 但 worktree 无改动")
    exit skill

append event: execution_completed { executor, files_changed: [...] }
# 继续 step 4 - step 10
```

#### 3.3.3 Commit 纪律

- **adapter 只写文件，不 `git add` / `git commit`**（adapter 契约强制，见 §4）
- claude-code inline 路径同样不在 step 3 结束前 commit
- **Suborchestrator 在 step 10（转待验收前）做统一 commit**：
  - `git add -A && git commit -m "task-<id>: <改动摘要>"`（改动摘要从执行日志拿）
  - commit 之前所有改动保持 unstaged，供 /task-execute 重跑时 rollback 到 `baseline_sha`
- 失败回落时 `/task-execute` 已用 `git restore + rm` 回到 baseline，worktree 干净，close-task.sh 不会拒绝后续重试

Step 4-10（dev server / 执行日志 / 偏差 / 自审 / 转状态）**保持不变**，只在 step 10 增加 commit 动作。

---

## 4. Adapter 脚本

5 个脚本放在 `scripts/exec-adapters/`，统一接口：

**入参**（环境变量）：
- `TASK_FILE`：task 文件绝对路径
- `TASK_WORKTREE`：task worktree 绝对路径
- `PROMPT_FILE`：Suborch 拼好的完整 prompt 的临时文件路径（由 `build-execution-prompt.py` 生成，含 §3.3.1 的 6 个 section）
- `EXECUTOR_MODEL`：要使用的 model 名（可为空，空时 adapter 不加 `--model` flag）
- `MAIN_REPO_ROOT`：主仓根路径（manual.sh 用于决定 pending 文件写入哪里）

**产出**：
- 代码改动已落 task worktree，**但保持 unstaged**
- 日志写到 `$MAIN_REPO_ROOT/.runs/execution-<task>-<executor>.log`（由 caller 重定向，adapter 输出走 stdout/stderr）
- 退出码（0=成功 / 非 0=失败）

**契约约束**：
- adapter **禁止 `git add` / `git commit` / `git checkout`**，改动保留在 unstaged 由 Suborchestrator 统一处理
- adapter 不修改主仓外路径（codex workspace-write 已隔离；cursor-agent `--force --trust` **无沙箱**，依赖 §3.3.2 越界检查兜底）
- 退出码分类（由 Suborch 解释）：
  - `0`：成功
  - `10`：sandbox 拒绝写入
  - `11`：model 不可用 / 未授权
  - `12`：网络 / rate limit
  - 其它非 0：未分类失败

### 4.1 `claude-code.sh`

**不需要此脚本**。task-execute 检测 executor==`claude-code` 时直接走现有主流程，不 shell-out。保留 `claude-code` 作为字段值是为了让字段有明确值而非空。

**Model 处理**：Suborchestrator 是通过 Agent tool 启动的 subagent。Agent tool 接受 `model` 参数（`opus` / `sonnet` / `haiku`），可覆盖 agent 定义中的 frontmatter model。

因此 `executor_model` 对 claude-code **是有效的**——实施时 `/task-confirm` 或 `/task-execute` 分派 Suborchestrator 时，把 `executor_model` 字段值传给 Agent tool 的 `model` 参数即可让该 task 用指定 model 跑。

**注意限制**：
- Agent tool 的 model 参数只接受 `opus` / `sonnet` / `haiku` 枚举值，不接受具体 ID（如 `claude-opus-4-7`）。`resolve-executor.py` 需校验这个约束
- 如果 `executor_model` 为空，Agent tool 按默认继承（来自 agent frontmatter 或父会话）

### 4.2 `codex.sh`

```bash
#!/usr/bin/env bash
set -euo pipefail
cd "$TASK_WORKTREE"
PROMPT="$(cat "$PROMPT_FILE")"

MODEL_ARGS=()
[ -n "${EXECUTOR_MODEL:-}" ] && MODEL_ARGS=(--model "$EXECUTOR_MODEL")

node "${CLAUDE_PLUGIN_ROOT}/scripts/codex-companion.mjs" task --write "${MODEL_ARGS[@]}" "$PROMPT"
```

> ⚠️ 实施时确认 `CLAUDE_PLUGIN_ROOT` 的稳定性。若升级 plugin 后路径变化，改调 `codex exec --sandbox workspace-write`。

### 4.3 `cursor-agent.sh`

```bash
#!/usr/bin/env bash
set -euo pipefail
PROMPT="$(cat "$PROMPT_FILE")"

MODEL_ARGS=()
[ -n "${EXECUTOR_MODEL:-}" ] && MODEL_ARGS=(--model "$EXECUTOR_MODEL")

cursor-agent -p --force --trust \
  --workspace "$TASK_WORKTREE" \
  --output-format text \
  "${MODEL_ARGS[@]}" \
  "$PROMPT"
```

### 4.4 `gemini.sh`

> **延后实现**。命令参考（保留以便未来补实现）：
> ```bash
> gemini -p "$PROMPT" --approval-mode yolo --output-format text [-m "$EXECUTOR_MODEL"]
> ```

### 4.5 `manual.sh`

```bash
#!/usr/bin/env bash
set -euo pipefail

TASK_ID=$(basename "$TASK_FILE" .md | sed -E 's/^(task-[0-9]+).*/\1/')
REQ_ID=$(basename "$(dirname "$(dirname "$TASK_FILE")")")
NOW=$(date -Iseconds)
BASELINE_SHA=$(git -C "$TASK_WORKTREE" rev-parse HEAD)
# 落在主仓，避免 cwd 漂移时 preamble 扫不到
PENDING_FILE="$MAIN_REPO_ROOT/.runs/.pending-manual-${TASK_ID}.json"

cat > "$PENDING_FILE" <<EOF
{
  "task_id": "$TASK_ID",
  "task_file": "$TASK_FILE",
  "task_worktree": "$TASK_WORKTREE",
  "req_id": "$REQ_ID",
  "executor": "manual",
  "started_at": "$NOW",
  "baseline_sha": "$BASELINE_SHA",
  "snoozed_until": null
}
EOF

python3 "$MAIN_REPO_ROOT/.claude/scripts/task-events.py" append "$TASK_FILE" \
  --type execution_manual_waiting

cat <<EOF
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Task-${TASK_ID} 已设为 manual 执行

请在以下 worktree 完成实现：
  $TASK_WORKTREE
参考 task 文件：$TASK_FILE
  必读文档、执行范围、验收标准、写回职责都在文件里

完成后跑：
  /task-execute $TASK_ID
会检测到 manual 标记 + 显式告知你"不会重跑执行器，直接进自审"。

如果决定放弃这个 task：
  python3 .claude/scripts/task-transition.py <task-file> --cancel-manual
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

EOF

exit 0
```

---

## 5. 配套改动

### 5.1 越界写保护

`check-branch.sh` hook 只拦 Claude 自己的 Edit/Write；codex / cursor-agent 通过 Bash 跑，它们写文件绕过 hook。

**两层保护**：

1. **Prompt 层**：Prompt 契约（§3.3.1）明确列 allowlist / denylist
2. **事后层**（见 §3.3.2 step 3c）：adapter 返回后 Suborch 用 `git status --porcelain -z` 精确分类 tracked/untracked/ignored，结合 task 文件「执行范围」allowlist 判断越界，用 `git restore --source=<baseline_sha>` + `rm` 精准回滚

**不再硬编码 `docs/` 禁止**：改成按 task 的 allowlist 判断；doc-update 类合法 task 把 `docs/` 加入 allowlist 即可。gitignore 匹配的生成物（`.next/` / `dist/` / `node_modules/.cache/`）自动忽略。

### 5.2 Manual 标记文件

位置：**`$MAIN_REPO_ROOT/.runs/.pending-manual-<task-id>.json`**（必须用主仓绝对路径，避免 cwd 漂移时观察层扫不到）。

Schema（见 §4.5）：`task_id / task_file / task_worktree / req_id / executor / started_at / baseline_sha / snoozed_until`。

**不走 24h 自动清理**。删除时机：
- `/task-execute` 重入，preflight 通过
- `task-transition.py --cancel-manual`
- `task-transition.py --to 待确认` 连带清理（见 §5.5）

### 5.3 `skill-preamble.sh` 扩展（汇总式 + 年龄降级 + snooze）

```bash
PENDING_DIR="$MAIN_REPO_ROOT/.runs"
pending_count=0
old_count=0
NOW=$(date +%s)
for f in "$PENDING_DIR"/.pending-manual-*.json; do
  [ -e "$f" ] || continue
  SNOOZED=$(jq -r '.snoozed_until // ""' "$f")
  if [ -n "$SNOOZED" ]; then
    SNOOZED_TS=$(date -j -f "%Y-%m-%dT%H:%M:%S%z" "$SNOOZED" +%s 2>/dev/null || echo 0)
    [ "$SNOOZED_TS" -gt "$NOW" ] && continue   # 在 snooze 期内，不提醒
  fi
  STARTED=$(jq -r '.started_at' "$f")
  STARTED_TS=$(date -j -f "%Y-%m-%dT%H:%M:%S%z" "$STARTED" +%s 2>/dev/null || echo "$NOW")
  AGE_DAYS=$(( (NOW - STARTED_TS) / 86400 ))
  pending_count=$((pending_count + 1))
  [ "$AGE_DAYS" -gt 7 ] && old_count=$((old_count + 1))
done

if [ "$pending_count" -gt 0 ]; then
  if [ "$old_count" -gt 0 ]; then
    echo "⚠️  有 $pending_count 个 manual task 等待中（$old_count 个超过 7 天）。运行 /task-status 查看详情。"
  else
    echo "ℹ️  有 $pending_count 个 manual task 等待中。运行 /task-status 查看详情。"
  fi
fi
```

**汇总不逐条**。Preamble 负责"你家里还有几件事没做"，详情在 `/task-status`。

### 5.4 `status-view.py` 扩展

`/task-status` 输出新增 Manual 节（包含下一步命令）：

```
Manual 等待中：
  Task-003（等待 7 天，2026-04-16 起）
  Task-005（等待 2 天，2026-04-21 起）

下一步：
  完成手工实现后： /task-execute task-003
  暂时不想管：     python3 .claude/scripts/task-transition.py <task-file> --snooze-manual --days 3
  放弃该 task：    python3 .claude/scripts/task-transition.py <task-file> --cancel-manual
```

### 5.5 `task-transition.py` 扩展

- **新增 flag `--fail-execution --reason <text>`**：
  - 前置：当前状态 `执行中`
  - 动作：检查 `.pending-manual-*.json` 存在则清理，转 `待确认`，append `execution_failed` 事件（如果调用方没 append），写 note
  - **这是唯一合法的 `执行中 → 待确认` 路径**（state_machine.py 加这条转换）
  - `/task-execute` 失败时必须调这个，不直接走 `--to 待确认`
- **新增 flag `--cancel-manual`**：
  - 前置：当前状态 `执行中` 且存在 `$MAIN_REPO_ROOT/.runs/.pending-manual-<task>.json`
  - 动作：删标记 + 转 `待确认` + 记 note（PM 主动放弃）
- **新增 flag `--snooze-manual --days N`**：
  - 前置：存在 pending 文件
  - 动作：把 `snoozed_until` 设为 `NOW + N days`，不改状态
- `--to 待确认` 时若存在 manual 标记，**决定性地总是清理**（不留开放语义）

### 5.6 `task-events.py` 新事件类型

| 事件 | payload |
|------|---------|
| `suborch_spawn_started` | `{executor, model}` |
| `suborch_spawn_failed` | `{reason}` |
| `execution_started` | `{executor, model, baseline_sha}` |
| `execution_completed` | `{executor, model, exit_code, files_changed}` |
| `execution_failed` | `{executor, model, exit_code, classification, reason, log_path}` |
| `execution_manual_waiting` | `{baseline_sha}` |
| `execution_manual_completed` | `{}` |

Model / baseline_sha / log_path 记入事件，便于后续审阅和调试。

`task-transition.py` 转"待验收"时仍**只校验 `review_completed`**（manual 场景不保证有 `execution_completed`）。

---

## 6. 失败 fallback（诊断式 + 可复制命令）

执行者失败（adapter 非 0 退出 / 越界写 / 零改动）后 Suborch：

1. 先 rollback worktree 到 `baseline_sha`（tracked 用 `git restore --source=<sha>`，untracked 用 `rm`）
2. 追加 `execution_failed` 事件（含 exit_code / classification / reason / log_path）
3. 调 `task-transition.py --fail-execution <task> --reason <classification>` 转待确认
4. 输出诊断（**不是菜单**，是带路径和可复制命令的报告）：

```
Task-003 执行失败，已回退到「待确认」。

执行者：codex
失败阶段：adapter 启动（exit code 10）
分类：sandbox_denied
原因：codex 沙盒拒绝写入 task worktree
日志：.runs/execution-task-003-codex.log

下一步可直接选一项：

1. 同执行者重试（修沙盒后）：
   检查 ~/.codex/config.toml，确认 sandbox_mode = "workspace-write"
   /task-confirm requirements/active/req-xxx/tasks/task-003-xxx.md

2. 改用 claude-code：
   sed -i '' 's/\*\*executor：\*\* codex/**executor：** claude-code/' <task-file>
   /task-confirm requirements/active/req-xxx/tasks/task-003-xxx.md

3. 改为 manual（自己实现）：
   sed -i '' 's/\*\*executor：\*\* codex/**executor：** manual/' <task-file>
   /task-confirm requirements/active/req-xxx/tasks/task-003-xxx.md

4. 先搁置：
   /task-status 查看其它待办
```

**分类 → 建议映射**（由 `scripts/classify-failure.sh` 输出）：

| classification | 原因描述 | 典型建议 |
|----------------|---------|---------|
| sandbox_denied | 执行器无权限写入 | 检查 CLI 配置 / 换执行者 |
| model_not_found | model 名非法 / 未订阅 | 检查 `executor_model`，或改用 settings 默认 |
| network | 网络 / rate limit | 稍等重试，或换执行者 |
| boundary_violation | 写到 task 执行范围外 | 检查 task「执行范围」字段是否漏列 |
| no_changes | 执行器退 0 但无文件改动 | 检查 prompt 是否被执行器误解为"只分析" |
| unknown | 其它非 0 退出 | 看 log_path 诊断 |

PM 仍可选"搁置"——对应不再 /task-confirm 该 task，状态停留在「待确认」，下次 `/task-status` 可见。

skill 结束后状态已到「待确认」，worktree 干净，PM 按报告复制一条命令即可。

**不自动降级到 claude-code**。PM 必须显式决定。

---

## 7. 实现顺序

详见 `实施计划-执行者可选.md`。概要：

1. Spike（批次 2）先固定 `invoke_executor()` seam 和 `build-execution-prompt.py` 契约，hardcode 最小 dispatch 跑通 codex + manual 路径
2. 模板（批次 3）加字段 + inline 提示
3. 基础设施（批次 4）：resolver + 事件 + `build-execution-prompt.py` + `classify-failure.sh`
4. Adapter 抽出（批次 5）
5. 状态流转（批次 6）：`--fail-execution` / `--cancel-manual` / `--snooze-manual` + state machine 加 `执行中→待确认`
6. 主流程完整化（批次 7）：resolver 接入、prompt 契约落地、commit 纪律、越界用 `git restore`+`git clean`、幂等重入带 preflight + ack、诊断式失败文案
7. 上游（批次 8）：/task-confirm 交互式执行者选择 + 默认压单行 + spawn ack + spawn 失败不转状态
8. 观察层（批次 9）：preamble 汇总 + 年龄降级 + snooze
9. 回归（批次 10）：adapter unit + task-execute 集成（越界 / 零改动 / spawn 失败 / manual 重入 / claude-code 无 regression）

---

## 8. 暂不做

- **Routing 表**：次 1 定"不预设"。出现稳定偏好后再补
- **Suborchestrator 可替换**（决策 1 的 B 方案）：另一份独立设计
- **关键词自动识别 manual 完成**：PM 显式 `/task-execute <task>` 重入即可
- **Adapter 结构化输出解析**：当前只看 exit code；未来 `--output-format json` 可用于更精细的失败归类
- ~~task 级动态切换 Claude Code 的 model~~：**已接通**。Suborchestrator 用 Agent tool 启动，`executor_model` 字段传给 `model` 参数（见 §4.1）
- **Gemini 执行者**：Gemini CLI 暂不实现。v1 实测中如果 claude-code / codex / cursor-agent 都不合适再补（命令参考保留在 §4.4）

---

## 9. 待实测点

- `CLAUDE_PLUGIN_ROOT` 在 codex adapter 的稳定性（plugin 升级后路径变化）
- cursor-agent 跑复杂 task 的实际耗时，是否需要 adapter 级超时
- 越界检测对"新增生成文件"（`.next/`, `node_modules/.cache/` 等）是否误报
- Manual pending 文件 glob 在 `.runs/` 较大时的性能
