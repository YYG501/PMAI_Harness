# PM AI 工作流框架 v1 设计文档

> **状态**：v2（Eng Review 后），等 PM 确认
> **更新时间**：2026-04-12
> **基础**：需求文档 v4 + CEO 审核 + Eng 审核 + Codex Outside Voice
> **范围**：只覆盖 v1 核心循环（init → req 生命周期 → task 生命周期 → worktree 隔离）
> **相关文档**：关键脚本的数据安全不变式见 [INVARIANTS.md](INVARIANTS.md)；测试套件见 `tests/README.md`

---

## 1. 设计概述

框架是一个 Claude Code skill/script 包。PM 通过 `/init-project` 创建业务项目，项目内通过 `/new-req`、`/task-confirm` 等 skill 推进工作流。流程由 Python/Shell 脚本强制（状态机、分支保护），通过 Claude Code 的 hook 机制拦截非法操作。

**技术栈**：Shell 脚本（worktree/git 操作）+ Python 脚本（状态机/事件流）+ Markdown skill 文件（工作流引导）+ JSON 配置（hook/权限）。

**运行时依赖**：v1 运行在 Claude Code 上。v2+ 计划支持 Codex 等其他 AI CLI 做主 Orchestrator。为此，核心逻辑放在脚本中（不依赖 Claude Code 特有能力），skill 只是 Claude Code 的薄封装。

**约束机制分层**：
- **主要约束**：CLAUDE.md 规则 + skill 文件引导 agent 行为
- **补充约束**：PreToolUse hook 拦截 Edit/Write 的非法操作
- **核心逻辑**：全在 Python/Shell 脚本中（req-transition.py, task-transition.py 等），不依赖 skill 系统
- **已知局限**：Bash 命令理论上可绕过 hook，这是 Claude Code 平台的当前限制

---

## 2. 框架仓库目录结构

```
PM-AI-Workflow/                      # 框架仓库（生成器）
├── CLAUDE.md                        # 框架开发用
├── 需求.md / 需求-v0-原始草稿.md
├── 设计.md                           # 本文件
│
├── templates/                       # 模板文件，init 时复制到业务项目
│   ├── CLAUDE.md.tmpl               # 业务项目 agent 入口
│   ├── CONTEXT.md.tmpl              # 项目背景骨架
│   ├── DESIGN.md.tmpl               # 视觉规范空骨架
│   ├── prd.md.tmpl                  # PRD 骨架
│   ├── task.md.tmpl                 # Task 文件模板
│   ├── settings.json.tmpl           # Hook + 权限配置
│   └── gitignore.tmpl               # .gitignore 模板
│
├── scripts/                         # 脚本，init 时复制到 .claude/scripts/
│   ├── init-project.sh              # 项目创建入口
│   ├── skill-preamble.sh            # 统一 preamble：所有 skill 共享的启动逻辑
│   ├── req-transition.py            # Req stage 转换（单一入口）
│   ├── task-transition.py           # Task 状态转换（单一入口）
│   ├── check-branch.sh              # PreToolUse hook：分支保护
│   ├── create-req-worktree.sh       # 创建 req 级 worktree
│   ├── create-task-worktree.sh      # 创建 task 级 worktree + symlink + 端口
│   ├── close-task.sh                # Task 关闭：归档 + merge + 清理
│   ├── close-req.sh                 # Req 关闭：merge + 归档
│   ├── task-events.py               # Append-only JSONL 事件流
│   └── status-view.py               # 全局状态总览生成
│
└── skills/                          # Skill 定义，init 时复制到 .claude/skills/
    ├── init-project/SKILL.md
    ├── new-req/SKILL.md
    ├── req-stage-gate/SKILL.md
    ├── task-plan/SKILL.md
    ├── task-confirm/SKILL.md
    ├── task-execute/SKILL.md         # 含实现 + 自审（合并了原 task-review）
    ├── task-submit/SKILL.md
    ├── close-task/SKILL.md
    ├── close-req/SKILL.md
    ├── doc-update/SKILL.md
    ├── prd-writing/SKILL.md
    └── status/SKILL.md
```

---

## 3. 生成的业务项目结构

```
<project>/
├── .claude/
│   ├── settings.json                # Hook + 权限
│   ├── scripts/                     # 复制自框架 scripts/
│   └── skills/                      # 复制自框架 skills/（不含 init-project）
│
├── docs/
│   ├── prd.md                       # 产品说明（持续维护）
│   ├── CONTEXT.md                   # 项目背景
│   ├── DESIGN.md                    # 视觉规范（空骨架，first req stage 4 填充）
│   └── modules/                     # 模块规格
│
├── requirements/
│   ├── active/
│   │   └── req-NNN-<slug>/
│   │       ├── .req-meta.json       # Req 元数据
│   │       ├── brief.md             # Stage 1
│   │       ├── analysis.md          # Stage 2
│   │       ├── solution.md          # Stage 3（可选）
│   │       ├── task-plan.md         # Stage 5
│   │       ├── close-report.md      # Stage 7
│   │       └── tasks/
│   │           ├── task-001-<slug>.md
│   │           ├── task-002-<slug>.md
│   │           └── _archived/       # 归档产物（含 .runs/ 副本）
│   └── closed/
│
├── prototypes/                      # 原型 app 代码（单 app）
│
├── .runs/                           # 运行时元数据（gitignored）
│   ├── <task-id>.json
│   └── events/<task-id>.jsonl
│
├── .worktrees/                      # Worktree 目录（gitignored）
├── .dev-port                        # 项目基础端口
├── CLAUDE.md                        # Agent 入口
└── .gitignore
```

---

## 4. Skill 清单与职责（13 个）

| # | Skill | 谁调用 | 功能 | 输出 |
|---|-------|--------|------|------|
| 1 | `/init-project` | PM | 创建新业务项目 | 项目目录 + git 仓库 |
| 2 | `/new-req` | PM | 开始新需求（含 stage 1 引导问题） | req 目录 + worktree + brief.md |
| 3 | `/req-stage-gate` | Orchestrator | 推进 req 到下一 stage | Stage 转换 |
| 4 | `/task-plan` | Orchestrator | Stage 5：模块规格 + 拆 task | task 文件 + task-plan.md |
| 5 | `/task-confirm` | PM | 确认 task 开始执行 | Task→执行中 + task worktree |
| 6 | `/task-execute` | Suborchestrator | 在 task worktree 中实现 + 自审 + 维持 dev server | 代码 + 执行日志 + 自审记录 |
| 7 | `/task-submit` | Orchestrator | 呈交 PM 验收 | 验收信息包 |
| 8 | `/close-task` | Orchestrator | Task 关闭（强制检查文档偏差） | 归档 + merge task→req + 清理 |
| 9 | `/close-req` | Orchestrator | Req 关闭 | merge req→main + 归档 |
| 10 | `/cancel-req` | PM | 废弃当前 req | 清理 worktree/分支，标记 cancelled |
| 11 | `/doc-update` | Orchestrator | 反向文档更新 | 文档修改写入 req 分支 |
| 12 | `/prd-writing` | Orchestrator | 更新 prd.md | 更新后的 prd.md |
| 13 | `/status` | PM | 全局状态总览（含执行中进度） | 当前 req/task 状态 + 最后事件 + 下一步 |

**调用链**：`/new-req` → `/req-stage-gate`(1→2→3→4→5) → `/task-plan` → `/task-confirm` → `/task-execute` → `/task-submit` → `/doc-update` → `/close-task` → `/close-req`
**退出路径**：`/cancel-req`（任何 stage 可调用）

---

## 5. 脚本清单与职责

### 5.1 Shell 脚本

| 脚本 | 功能 | 调用者 |
|------|------|--------|
| `init-project.sh` | 检测 gstack → 复制模板 → git init → 推导端口 | `/init-project` |
| `check-branch.sh` | PreToolUse hook：4 个检查门（见 §7） | Claude hook 系统 |
| `create-req-worktree.sh` | `git worktree add -b req-NNN .worktrees/req-NNN main` | `/new-req` |
| `create-task-worktree.sh` | `git worktree add -b task-NNN .worktrees/task-NNN <req-branch>` + symlink 依赖 + 推导端口 | `/task-confirm` |
| `close-task.sh` | 检查文档偏差 → 归档 .runs/ → merge task→req 分支 → 删分支 → 清 worktree → 杀 dev server | `/close-task` |
| `close-req.sh` | cd 到主仓 → merge req→main → 删分支 → 清 worktree → 移目录到 closed/ | `/close-req` |
| `cancel-req.sh` | 清理所有 task worktree/分支 → 清理 req worktree/分支 → 标记 req 状态为 cancelled | `/cancel-req` |

### 5.2 Python 脚本

| 脚本 | 功能 | 调用者 |
|------|------|--------|
| `req-transition.py` | Req stage 转换单一入口（验证顺序、跳过规则、更新 .req-meta.json） | `/req-stage-gate` |
| `task-transition.py` | Task 状态转换单一入口（验证状态机、前置条件、更新 task 文件状态字段、追加事件） | 多个 skill |
| `task-events.py` | 追加事件到 `.runs/events/<task>.jsonl` | `task-transition.py` `/task-execute` |
| `status-view.py` | 读取 req/task 状态，输出全局总览（含执行中 task 的最后事件） | `/status` |

**`/status` 输出格式**：

```
当前 Req：req-002-review-system（stage 6 - task 执行）
Task 状态：
  ✅ task-001 数据模型 — 已完成
  🔄 task-002 API 接口 — 执行中（最后活动：自审中 - /qa 通过，等待 /design-review）
  ⏳ task-003 前端组件 — 待确认
  ⏳ task-004 集成测试 — 待确认
下一步：等待 task-002 自审完成后验收
```

---

## 5.3 统一 Preamble（skill-preamble.sh）

所有 skill 的 preamble bash 块统一调用 `skill-preamble.sh`，避免每个 SKILL.md 重复写启动逻辑。

**skill-preamble.sh 职责**：
1. 检测当前工作位置（main / req worktree / task worktree）
2. 读取当前活跃 req 的 .req-meta.json（如有）
3. 读取当前活跃 task 的状态（如有）
4. 检测中断恢复标记（见 §5.4）
5. 输出环境变量供 skill 使用：`REPO_ROOT`, `BRANCH`, `WORKTREE_TYPE`(main/req/task), `ACTIVE_REQ`, `ACTIVE_REQ_STAGE`, `ACTIVE_TASK`, `ACTIVE_TASK_STATUS`

**每个 SKILL.md 的 preamble 只需**：
```bash
source "$(git rev-parse --show-toplevel 2>/dev/null || echo .)/.claude/scripts/skill-preamble.sh"
echo "SKILL: <skill-name>"
```

---

## 5.4 中断恢复

当 skill 执行过程中 agent 崩溃或用户中断时，系统需要能检测到并提示 PM。

**机制**：
- skill 开始时创建标记文件：`.runs/.pending-<skill-name>`，内容为 `{"skill": "<name>", "started_at": "<timestamp>", "req": "<req-id>", "task": "<task-id>"}`
- skill 正常结束时删除该标记
- `skill-preamble.sh` 启动时检测 `.runs/.pending-*` 文件：
  - 如果存在，说明上次执行中断了
  - 输出警告并建议 PM 检查状态：`"检测到上次 /<skill-name> 执行中断。运行 /status 查看当前状态。"`
  - 删除过期标记（创建时间超过 24 小时的自动清理）

**不阻塞**：中断恢复只是提示，不阻止当前 skill 执行。

---

## 6. 状态机设计

### 6.1 Req Stage（.req-meta.json）

```json
{
  "id": "req-001",
  "name": "user-auth",
  "branch": "req-001-user-auth",
  "worktree": ".worktrees/req-001-user-auth",
  "stage": 1,
  "stage_history": [
    {"stage": 1, "entered_at": "2026-04-12T10:00:00+08:00"}
  ],
  "is_first_req": true,
  "status": "active"
}
```

**转换规则**：

- 正向推进：1→2→3→4→5→6→7
- **支持回退**：PM 可从当前 stage 回退到之前任意 stage
- Stage 3（方案设计）：非 first req 时由 PM 在确认门显式决定是否跳过
- Stage 4（设计系统）：系统自动检测 docs/DESIGN.md 是否有实质内容，有则自动跳过
- First req：stage 1-4 全部必选
- 每个 stage 需要前一 stage 的产出文件存在（正向推进时检查）

**回退规则**：

| 从 | 回退到 | 条件 |
|----|--------|------|
| stage 2-5 | 任意更早 stage | 无条件，保留已产出文件（PM 在回退后的 stage 可修改或重写） |
| stage 6（task 执行） | stage 5 或更早 | 所有 task 必须先关闭或取消（不能有执行中/待验收的 task） |
| stage 7（close） | 不可回退 | merge 到 main 后不可逆 |

**命令**：
- 正向：`python3 .claude/scripts/req-transition.py <req-dir> --to <stage> [--skip-stage 3]`
- 回退：`python3 .claude/scripts/req-transition.py <req-dir> --to <stage> --rollback`

### 6.2 Task 状态机

```
待确认 ──(PM确认)──→ 执行中 ──(自审通过)──→ 待验收 ──(PM通过)──→ 已完成
                       ↑                              │
                       └──────(PM打回)─────────────────┘
```

**前置条件**：

| 转换 | 条件 |
|------|------|
| 待确认→执行中 | PM 显式确认 + **同 req 下无其他 task 处于执行中/待验收**（v1 串行强制） |
| 执行中→待验收 | 文档偏差 section 已填（或"无偏差"）；自审记录 section 有至少一条记录；事件流中 `review_completed` 事件覆盖所有审查工具 |
| 待验收→已完成 | PM 显式"通过" |
| 待验收→执行中 | PM 显式"打回" + 反馈已记录到 PM 反馈 section |

**审查工具校验**：task-transition.py 读取 `.runs/events/<task>.jsonl` 中的 `review_completed` 事件，与 task 文件的"审查工具"字段对比。事件由 `/task-execute` skill 通过 `task-events.py` 追加：
```json
{"event": "review_completed", "tool": "/qa", "result": "pass", "timestamp": "2026-04-12T15:00:00+08:00"}
```

**命令**：`python3 .claude/scripts/task-transition.py <task-file> --to <target> [--note <text>]`

### 6.3 Task 重开

重开 = 创建一个新 task（带 `-fix` 后缀），从 req 分支创建新 worktree，引用原 task 的验收标准和 PM 反馈。不恢复旧分支。

### 6.4 Req 废弃（/cancel-req）

PM 在任何 stage 可以废弃当前 req。`cancel-req.sh` 执行：
1. 关闭所有该 req 下处于执行中的 task（杀进程、清 worktree/分支）
2. 清理 req worktree 和分支
3. 将 .req-meta.json 的 status 设为 `cancelled`
4. req 目录移到 `requirements/closed/`（保留记录但不 merge 到 main）

**不 merge 到 main**，main 零污染。

---

## 7. Hook 配置（check-branch.sh）

`.claude/settings.json`：

```json
{
  "permissions": {
    "allow": [
      "Bash(bash .claude/scripts/*)",
      "Bash(python3 .claude/scripts/*)",
      "Bash(git *)",
      "Bash(npm *)",
      "Bash(bun *)",
      "Bash(lsof *)",
      "Bash(kill *)",
      "Skill(*)"
    ]
  },
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "bash .claude/scripts/check-branch.sh"
          }
        ]
      }
    ]
  }
}
```

**check-branch.sh 的 4 个检查门**：

1. **Task 状态直改拦截**：编辑 task 文件且修改了 `**状态：**` 字段 → DENY，提示使用 task-transition.py
2. **Req stage 直改拦截**：编辑 .req-meta.json 且修改了 stage 字段 → DENY，提示使用 req-transition.py
3. **Main 分支代码保护**：在 main 分支编辑 prototypes/ 下的文件 → DENY
4. **Worktree 作用域保护**：
   - task worktree 里编辑 docs/ → DENY（文档改动走 req 分支）
   - req worktree 里编辑 prototypes/ → DENY（代码改动走 task 分支）

**检测 worktree 类型**：通过当前分支名前缀判断（`req-` = req worktree，`task-` = task worktree，`main` = 主仓）。

---

## 8. Worktree 管理

### 8.1 两级分支结构

```
main（主仓，永驻）
  └── req-001-user-auth（req worktree，从 main 创建）
        └── task-001-login-ui（task worktree，从 req 分支创建）
```

### 8.2 Orchestrator 工作位置

| 状态 | Orchestrator 在哪 |
|------|-------------------|
| 没有活跃 req | 主仓 main |
| req 活跃（stage 1-5, 7） | req worktree |
| task 执行中（stage 6） | suborchestrator 在 task worktree；orchestrator 仍在 req worktree |

### 8.3 Task 文件修改位置

Task 文件始终在 task worktree 中修改（状态转换、执行日志、自审记录等）。task 分支 merge 回 req 分支时，这些改动一并带回。

### 8.4 端口分配

无全局状态，用项目路径 hash 推导：

```python
import hashlib
base = 3000 + (int(hashlib.md5(project_path.encode()).hexdigest(), 16) % 7000)
task_port = base + task_number
```

- 项目基础端口存在 `.dev-port`
- task 端口 = 基础端口 + task 序号

### 8.5 依赖 Symlink（通用化）

`create-task-worktree.sh` 创建 worktree 后：
- 检测项目根目录的包管理配置：
  - `package.json` → symlink `node_modules`
  - `Gemfile` → symlink `vendor/bundle`
  - `go.mod` → symlink `vendor`
  - 其他项目类型按需扩展
- 排除构建缓存目录（`.next`、`dist` 等），task 各自独立生成

### 8.6 清理

| 操作 | close-task.sh | close-req.sh |
|------|---------------|--------------|
| 文档偏差检查 | 检查偏差 section，有未处理记录则阻塞并提示跑 /doc-update | 无 |
| 归档 | 复制 .runs/<task>.json + events/ 到 tasks/_archived/<task>/ | 无（已在 task close 时归档） |
| merge | task→req 分支 | req→main（先 cd 到主仓执行） |
| 删分支 | task 分支 | req 分支 |
| 删 worktree | task worktree | req worktree |
| 杀进程 | task 端口的 dev server | 无 |
| 移目录 | 无 | req 目录移到 closed/ |
| 清 .runs/ | 删原件 | 无 |

---

## 9. 关键流程时序

### 9.1 init-project

```
PM 在框架仓库运行 /init-project
  → 问项目名、目录、背景
  → init-project.sh：
    a. 检测 gstack（hard dep，失败则报错退出）
    b. 检测 gstack 版本（不是最新给警告，不阻塞）
    c. 创建项目目录
    d. 复制 templates/ → 替换占位符
    e. 复制 scripts/ → .claude/scripts/
    f. 复制 skills/ → .claude/skills/（排除 init-project）
    g. 生成 .claude/settings.json
    h. 创建 docs/ 骨架（DESIGN.md 空骨架）、requirements/、prototypes/
    i. 生成 .gitignore
    j. git init && git checkout -b main
    k. 推导基础端口（项目路径 hash）→ .dev-port
    l. git add . && git commit -m "init: <project-name>"
  → "项目初始化完成。cd <project-dir>，运行 /new-req 开始第一个需求。"
```

### 9.2 Req 生命周期

```
PM: /new-req "实现用户登录"
  → 扫描 requirements/ 确定下一个 req 编号
  → 判断是否 first req
  → 创建 req 目录 + .req-meta.json
  → create-req-worktree.sh
  → cd 到 req worktree
  → 进入 stage 1

Stage 1（感受问题）
  → /new-req skill 用引导问题帮 PM 结构化描述需求：
    - 这次要解决什么问题？用户遇到了什么痛点？
    - 成功的标准是什么？做完后用户能做到什么？
    - 有没有明确不做的事情？
    - 有没有时间约束或外部依赖？
  → 根据 PM 回答写 brief.md
  → 确认门："是否进入 stage 2（需求分析）？"

Stage 2（需求分析）
  → 读 brief.md + docs/CONTEXT.md + docs/prd.md
  → 第一性原理分析
  → 写 analysis.md
  → 确认门：first req 直接进 3；后续 req 问"是否进入 stage 3 或跳过？"

Stage 3（方案设计，非 first req 可跳过）
  → 系统分层、模块边界
  → 写 solution.md
  → 自动运行 /plan-ceo-review 审阅
  → 确认门：显示文件路径 + 一句话摘要；review 发现直接贴 chat
  → PM 要求改 → 改 solution.md → 重新 review → 再确认

Stage 4（设计系统建立，DESIGN.md 已有内容时自动跳过）
  → 运行 /design-consultation（gstack）
  → 建立 docs/DESIGN.md
  → PM 确认
  → 确认门

Stage 5（模块规格 + task 拆分）
  → /task-plan：
    读 analysis.md + solution.md（如有）+ DESIGN.md
    更新/创建 docs/modules/<module>.md
    写 task-plan.md
    为每个 task 生成 task 文件（状态=待确认）
  → 确认门

Stage 6（task 执行）
  → 逐个 task 串行执行（见 9.3）
  → **中途追加 task**：PM 可在 stage 6 要求 orchestrator 创建新 task 文件（状态=待确认），
    同时更新 task-plan.md 记录追加原因。不需要回到 stage 5，直接在 stage 6 继续执行。
  → 所有 task 关闭后 → req-transition.py → stage 7

Stage 7（req close）
  → /close-req：
    写 close-report.md
    更新 prd.md（如有变化）
    commit 所有改动
    close-req.sh → cd 到主仓 → merge req→main → 清理
    → 回到主仓 main
```

### 9.3 Task 生命周期

```
[Task 文件在 stage 5 创建，状态=待确认]

PM: /task-confirm <task-file>
  → 展示 task 摘要，PM 确认
  → task-transition.py → 执行中
  → create-task-worktree.sh → 创建 task worktree + symlink + 端口
  → 写 worktree 路径和端口到 task 文件

Suborchestrator 在 task worktree（/task-execute）：
  → 读 task 文件（绝对路径）
  → 读"启动前必读"列出的文档
  → 实现代码
  → 启动 dev server（UI 类 task），写端口到 task 文件，保持运行
  → 写执行日志 section
  → 写文档偏差 section（或"无偏差"）
  → 自审：跑审查工具 → 每个工具通过 task-events.py 追加 review_completed 事件
  → 写自审记录 section → 修复问题
  → task-transition.py → 待验收
  → dev server 保持运行（PM 验收需要访问）

Orchestrator（在 req worktree）：
  → /task-submit → 呈交 PM
    UI 类：验收 URL + 摘要 + 自审 + 检查项 + 验收清单 + 偏差
    非 UI 类：diff/测试结果 + 摘要 + 自审 + 验收清单
  → PM 决策：
    "通过" → task-transition.py → 已完成
    "打回" → 反馈记录 → task-transition.py → 执行中 → 重新进入 task worktree 修复

PM 通过后：
  → /doc-update（如有偏差）
  → /close-task → close-task.sh：归档 → merge task→req → 删分支 → 清 worktree → 杀进程
```

---

## 10. Task 文件模板

```markdown
# Task NNN: [任务名称]

**状态：** 待确认
**审查工具：** /qa, /design-review
**分支：** task-NNN-<slug>
**worktree：** （执行时由 create-task-worktree.sh 填写）
**开发服务器：** （执行时填写）
**创建时间：** YYYY-MM-DD

## 启动前必读
1. docs/modules/<module>.md
2. docs/DESIGN.md
3. docs/CONTEXT.md

## 任务描述
[描述，不复制文档原文，只引用]

## 执行范围
- 新建：[文件列表]
- 修改：[文件列表]
- 不动：[排除文件]

## 验收标准
- [ ] [条件 1]
- [ ] [条件 2]

## 依赖
无

---

## 执行日志
### 执行报告 - [YYYY-MM-DD HH:MM]
**改动摘要：**
**新建文件：**
**修改文件：**
**验收标准完成情况：**

---

## 文档偏差
| 文档位置 | 文档原文 | 实际实现 |

---

## 自审记录
### 自审 1 - [YYYY-MM-DD HH:MM]
**工具：**
**结果：**
**详细发现：**
**遗留问题：**

---

## PM 反馈
### 反馈 1 - [YYYY-MM-DD]
**问题描述：**
**要求修改：**
**处理结果：** 待处理
```

---

## 11. CLAUDE.md 模板要点

业务项目的 CLAUDE.md 包含以下区域：

### 11.1 框架规则（不可修改）

- 角色定义：PM / 主 Orchestrator / Suborchestrator / Execution Agent
- 主 Orch 位置：req 活跃时在 req worktree，否则在主仓 main
- 状态转换受控入口：`req-transition.py`、`task-transition.py`
- **约束机制分层**：CLAUDE.md 规则是主要约束，hook 是补充检查
- 确认门规则：每个 stage 结束显式问 PM
- 分支结构图：main → req → task
- 文档引用原则：只引用不复制

### 11.2 项目信息

- 项目背景（init 时填入）
- 文档位置映射（docs/、requirements/）
- 当前状态：提示运行 `/status`

### 11.3 gstack 配置

- 可用 skill 列表（/qa、/design-review、/browse 等）

---

## 12. 实现顺序

按依赖关系排序，从基础设施到上层 skill：

| 批次 | 内容 | 依赖 |
|------|------|------|
| 1 | templates/ 所有模板文件 | 无 |
| 2 | skill-preamble.sh, task-events.py | 无 |
| 3 | req-transition.py, task-transition.py | task-events.py |
| 4 | check-branch.sh | 无 |
| 5 | create-req-worktree.sh, create-task-worktree.sh | 无 |
| 6 | close-task.sh, close-req.sh, cancel-req.sh | 无 |
| 7 | status-view.py | 无 |
| 8 | init-project.sh | templates/ |
| 9 | Skills: /init-project, /new-req, /req-stage-gate | scripts batch 2-5 |
| 10 | Skills: /task-plan, /task-confirm, /task-execute, /task-submit | scripts batch 2-6 |
| 11 | Skills: /close-task, /close-req, /cancel-req, /doc-update, /prd-writing, /status | scripts batch 2, 6-7 |

---

## 13. 验证方案

端到端验证流程：

**Happy path：**
1. 在框架仓库运行 `/init-project` 创建测试项目
2. cd 到测试项目，运行 `/new-req "添加首页"`
3. 走完 stage 1-4（first req 包含设计系统建立），确认每个确认门正常
4. 确认 task 文件生成正确
5. `/task-confirm` 确认一个 task，验证 worktree 创建和端口
6. 在 task worktree 中模拟实现，验证 hook 分支保护
7. 尝试直接修改 task 状态字段，验证 hook 拦截
8. `/task-execute` 完成实现+自审 → `/task-submit` → PM 通过 → `/close-task`
9. 验证归档产物完整（.runs/ 已复制到 req 目录内 _archived/）
10. 验证 merge 到 req 分支正确
11. `/close-req` 验证 merge 到 main + 归档
12. `/status` 验证状态总览

**Unhappy path：**
13. 尝试在 task-001 未 close 时 confirm task-002，验证串行强制拦截
14. PM 打回 task → 验证回到执行中 → 重新执行 → 重新提交 → 通过
15. close-task 时有未处理文档偏差，验证阻塞提示
16. `/cancel-req` 验证废弃 req 的完整清理（worktree/分支/标记）
17. close-task 后验证归档产物完整性
18. `/status` 验证执行中 task 显示最后事件

---

## 14. Eng Review 决策记录

| # | 问题 | 决策 |
|---|------|------|
| 1A | init-project 中 DESIGN.md 建立时机 | 从 init 拆出，放到 first req 方案设计之后的独立 stage 4 |
| 2A | task 文件修改位置 | 始终在 task worktree 中修改，merge 带回 req |
| 3A | /task-execute + /task-review | 合并为 /task-execute（含实现+自审） |
| 4B | 审查工具校验 | 保持严格校验，用事件流 JSONL 做结构化证据 |
| 5B | v1 是否保留 task worktree | 保留两级 worktree 结构 |
| Codex-1 | hook 可被 Bash 绕过 | 承认局限，CLAUDE.md 规则是主要约束，hook 是补充 |
| Codex-3 | 归档和删除打架 | close-task.sh 先归档到 req 目录内，再删除 |
| Codex-5 | 端口分配 | 用项目路径 hash 推导，去掉全局 registry |
| Codex-6 | 锁死 Node 项目 | symlink 策略通用化，按包管理配置自动检测 |
| Codex-7 | task 重开 | 最小方案：新建 -fix task，不恢复旧分支 |
| PM-1 | 缺 /cancel-req | 加 cancel-req.sh + skill |
| PM-2 | v1 串行无硬性强制 | task-transition.py 加串行检查 |
| PM-3 | doc-update 非强制 | close-task.sh 加文档偏差检查 |
| PM-4 | stage 6 中途加 task | 允许手动创建 task 文件 + 更新 task-plan.md |
| PM-5 | dev server 责任不清 | /task-execute 明确启动和维持 dev server |
| PM-6 | stage 1 缺引导 | /new-req 加引导问题 |
| PM-7 | /status 缺进度 | status-view.py 展示最后事件 |
| PM-8 | Req stage 不能回退 | 加回退支持，stage 6 回退需先关闭/取消 task，stage 7 不可回退 |
| PM-9 | 主 Orch 只支持 Claude | v1 只支持 Claude，v2+ 加 Codex。核心逻辑放脚本，skill 是薄封装 |
| GS-1 | gstack 定位矛盾 | 质量增强层，不是核心执行层 |
| GS-2 | Stage 1 引导问题 | 所有 req 用 /office-hours 替代框架 4 问 |
| GS-3 | Stage 4 自动跳过不合理 | 改为问 PM 是否需要更新 |
| GS-4 | plan 映射不清 | solution.md 和 task-plan.md 是传给 review 的"plan" |
| GS-5 | /review Ask PM 破坏自审 | suborchestrator 自行处理所有发现 |
| GS-10 | gstack skill 运行位置 | stage 1-5 在 req worktree，stage 6 在 task worktree |
| GS-11 | 三份文档并存 | 合并到设计.md |

---

## 15. gstack Skill 集成

### 15.1 定位

gstack skill 是框架的**质量增强层**，不是核心执行层。

- **核心执行层**：Python/Shell 脚本（状态机、worktree、stage 推进）。框架没有 gstack 也能跑完整流程
- **质量增强层**：gstack skill 在关键节点提高产出质量
- gstack 是 hard dependency（init 时检测），但单个 review skill 的失败不阻塞 stage 推进

### 15.2 Stage × gstack Skill 映射

| Stage | gstack skill | 做什么 | 调用方式 | 运行位置 |
|-------|-------------|--------|---------|---------|
| 1 感受问题 | `/office-hours` | 替代框架引导问题，六问深挖需求，产出复制到 brief.md | 所有 req 自动 | req worktree |
| 2 需求分析 | 无 | orchestrator 自行分析 | — | req worktree |
| 3 方案设计 | `/plan-ceo-review` | solution.md 写完后自动审阅 | 所有 req 自动 | req worktree |
| 4 设计系统 | `/design-consultation` | 建立或更新 DESIGN.md | 见规则 | req worktree |
| 5 task 拆分 | `/plan-eng-review` | task-plan.md 写完后审阅 | first req 自动，后续 req 建议 | req worktree |
| 6 task 执行 | `/review` + `/qa` + `/design-review` | 代码审查 + 功能测试 + 视觉审查 | 所有 task 自动 | task worktree |
| 7 req close | 无 | — | — | — |

### 15.3 各 Stage 详细规则

**Stage 1（/office-hours）**：所有 req 自动调用。产出 design doc 复制到 brief.md。brief.md 是唯一真相源。重跑覆盖。

**Stage 2**：无对应 skill。PM 可手动调 /plan-ceo-review 或 /codex。

**Stage 3（/plan-ceo-review）**：solution.md 写完后自动运行 /plan-ceo-review 审阅。所有 req 都自动，不可跳过。PM 看 review 结果后确认或要求修改，修改后重新 review。

**Stage 4（/design-consultation）**：
- DESIGN.md 不存在：自动调用
- DESIGN.md 已有内容：**问 PM** 是否需要更新。PM 说不用则跳过

**Stage 5（/plan-eng-review）**：传 task-plan.md 给 review。first req 自动，后续建议。追加 task 不影响之前的 review。

**Stage 6（按 task 类型选择自审工具）**：

自审工具由 task 文件的「审查工具」字段决定，task-plan 生成时按类型填默认值，PM 在 task-confirm 时可调整：

| task 类型 | 审查工具默认值 |
|----------|--------------|
| 纯后端 | `/review` |
| 纯 UI（样式/文案） | `/qa, /design-review` |
| 全栈新功能 | `/review, /qa, /design-review` |
| 纯配置/文档 | `(无)`，跳过自审 |
| 重构 | `/review` |

自审链路：

```
task 实现完成
  → 按「审查工具」字段逐个运行（若为空则跳过）
    - /review：suborchestrator 自行处理所有发现，不 Ask PM
    - /qa：测试 dev server URL
    - /design-review：对照 DESIGN.md 检查视觉
  → task-events.py 追加 review_completed 事件
  → task-transition.py → 待验收（校验所有列出工具都有 review_completed）
```

### 15.4 回退场景

回退到某个 stage 后，该 stage 的 gstack review 需要重跑。之前 stage 的 review 不受影响。

### 15.5 未绑定到 stage 的 gstack skill

PM 可随时手动调用：`/autoplan`、`/plan-design-review`、`/plan-devex-review`（gstack 自带）、`/codex`、`/browse`、`/investigate`、`/ship`
