# 框架同步设计文档

> **状态**：v1 定稿，待实施
> **更新时间**：2026-04-26
> **关联 TODO**：`TODOS.md` UP 条目（"现有 v1 项目升级到 stage 5/6 重设计 v2 的完整路径"）
> **依赖**：stage5-6 plan 实施完毕（v4 A4 已完成）
> **触发实战**：2026-04-26 手动同步生成器 → ExampleConsumerApp main，commit `d0aa6c1`，覆盖 38 个上游 commit / 23 文件

## 1. 背景

PM-AI-Workflow（生成器）的 `scripts/` `skills/` `templates/` `agents/` 是 source of truth。下游消费者项目（ExampleConsumerApp / ExampleConsumerB 等）通过把对应资产挂到自己仓的约定路径来用框架。生成器迭代后，下游需要"同步"才能用上新版。

历史做法：PM 手动找上次同步 commit、对照 commit log 决定改动范围、逐目录 `cp`、写 commit message。流程不可重复、容易漏。

### 这次（4-26）手动同步暴露的 6 个痛点

| # | 痛点 | 这次的处理 |
|---|---|---|
| **P1** | "上次同步到哪个 commit" 没显式锚点 | grep 历史 commit message "同步框架" 反推 (`476bc75`) |
| **P2** | rename 检测靠脑子（`status` → `task-status`） | 读 commit `d429bd3` 才发现 |
| **P3** | 路径映射靠脑子（4 条） | 凭历史 commit 推断 |
| **P4** | 消费者本地自定义没有保护 | AC4 刚好没自定义；rsync --delete 一旦撞上会无声覆盖 |
| **P5** | worktree 影响只能口头提醒 | 嘴上说一句，没列具体 worktree / SKILL 受影响清单 |
| **P6** | commit message 要 PM 回顾上游 commits 自己分类 | 这次 38 commit 我帮你写了，分了 6 大工作线 |

---

## 2. 定位

**`scripts/sync-to-consumer.sh` 是生成器主动 push 模型的同步工具**：

- 在生成器仓里跑，传入消费者路径作为参数
- 单向：generator → consumer，**不反向**
- 不读消费者侧的"想要什么"，只按 manifest 推
- 不自动 commit / 不自动 push，只 rsync + 暂存 + 给出 commit message 草稿

**不做的事**：

- 不搞"消费者注册表"（PM 单人，多消费者用 wrapper for 循环即可）
- 不做"生成器订阅 / 推送给所有下游"
- 不做"消费者主动 pull"
- 不替 PM 决定改动是否安全（worktree impact 只列清单 + 建议，不阻断）

---

## 3. 三个子项的范围

| 子项 | 内容 | 落地位置 |
|---|---|---|
| **S1** | sync 脚本 + manifest | `scripts/sync-to-consumer.sh` + `framework-manifest.yaml` |
| **S2** | 进行中 req 接续路径 | sync 脚本内置 worktree impact 报告子命令 |
| **S3** | 旧 module 规格 lazy migration | `skills/doc-update/SKILL.md` + `scripts/check-module-template.py` |

S1+S2 一同实施（S2 是 S1 的一个 phase），S3 单独跟在 doc-update 流程里。三者共享设计文档，便于 PM 一次审完。

---

## 4. S1: manifest 结构

### 4.1 文件位置 / 格式

`framework-manifest.py`，**生成器仓根目录**（与 README.md 同级）。

**为何选 python module 而非 yaml/toml/json**（决议见 §13）：

- 零依赖。macOS 默认 python 没装 PyYAML；tomllib 要 python 3.11+；json 不能写注释
- 和 `scripts/*.py` 风格一致，sync 脚本里 `import framework_manifest as m` 即可，不需要解析器
- PM 改起来就是改顶层变量赋值（dict / list），缩进语义和 yaml 几乎一样
- 后期如要加条件逻辑（如「针对某 consumer 跳过某条 rename」）有路径

### 4.2 字段

```python
# framework-manifest.py
"""框架同步 manifest。每次生成器加新 sync 路径 / 做 rename 时同 commit 改这里。
sync-to-consumer.sh 通过 import 读取这些常量。"""

SCHEMA_VERSION = 1

# 同步路径映射：generator 路径 → consumer 路径
# rsync -a --delete 语义；trailing slash 必带（rsync 行为约定）
SYNC_PATHS = {
    "scripts/":   ".claude/scripts/",
    "skills/":    ".claude/skills/",
    "templates/": "templates/",
    "agents/":    ".claude/agents/",
}

# 历史 rename。sync 时如果 consumer 上 from 路径还在、to 不在，自动 git mv
# rename 不会"跨 sync_paths"，from/to 都是 consumer 侧路径
RENAMES = [
    {
        "from":   ".claude/skills/status",
        "to":     ".claude/skills/task-status",
        "since":  "d429bd3",
        "reason": "codex plugin /status 冲突",
    },
]

# 消费者侧的本地自定义资产。rsync --delete 时排除，不被清理
# 用 consumer 侧路径
EXCLUDE_FROM_CONSUMER = [
    ".claude/skills/prd-writing",   # 当前生成器也有，但 PM 项目可能本地改过
]

# 生成器自身资产，永远不进入 sync 集
# 用 generator 侧 glob，匹配的文件 sync 时跳过
NEVER_SYNC = [
    "INVARIANTS.md",
    "TODOS.md",
    "README.md",
    "CLAUDE.md",
    "设计-*.md",
    "需求-*.md",
    "实施计划-*.md",
    "tests/",
    ".git/",
    "EXIT",
    ".worktrees/",
]
```

bash 侧通过小辅助 `scripts/_manifest-parser.py` 暴露这些常量给 sh（`--get sync_paths` 输出 `key1=val1\nkey2=val2`），见 §9 实现清单。

### 4.3 manifest 维护规则

- 生成器加新 sync 路径（如未来加 `hooks/`） → 加一行 `sync_paths`
- 生成器做 rename → 同 commit 加一条 `renames`
- 消费者本地自定义新 skill → PM 在 manifest 加 `exclude_from_consumer`（可由 PM 自行加，sync 脚本不强校验）
- 生成器加根目录的新设计文档 → glob `设计-*.md` 已覆盖，无需改 manifest

---

## 5. S1: sync 脚本流程

### 5.1 调用形式

```bash
# 仓根目录跑
scripts/sync-to-consumer.sh <consumer-path> [--apply] [--from-sha <sha>] [--force]

# 默认 dry-run，只输出 plan，不动文件
scripts/sync-to-consumer.sh ~/Projects/ExampleConsumerApp

# 实际执行（修改文件 + git add，不 commit）
scripts/sync-to-consumer.sh ~/Projects/ExampleConsumerApp --apply

# 显式指定上次同步 sha（首次 sync 或锚点丢失时用）
scripts/sync-to-consumer.sh ~/Projects/ExampleConsumerApp --from-sha 476bc75 --apply

# 跳过 consumer 工作树干净检查（不推荐，仅救急）
scripts/sync-to-consumer.sh ~/Projects/ExampleConsumerApp --apply --force
```

### 5.2 步骤

```
[1/9] 校验
  - consumer-path 存在 + 是 git repo
  - consumer 当前在 main 分支（避免污染 worktree 分支）
  - consumer 工作树干净（除非 --force）
  - 生成器工作树干净（保证 source 一致）

[2/9] 读 manifest
  - 找不到 → 报错
  - schema_version 不支持 → 报错

[3/9] 定位上次 sync sha
  优先级：
  a. --from-sha 显式参数
  b. consumer git log --grep "^framework-sync:" -n 1 → 提取 trailer sha
  c. consumer git log 含"同步框架" / "sync 框架" 的最近 commit → 取那次 commit 的"对应生成器 sha"
     （这条只用于 v0 锚点未建之前；建好后只用 a/b）
  全失败 → 要求 --from-sha

[4/9] 列上游变化
  cd generator && git log --oneline <from-sha>..HEAD -- $(yq sync_paths keys)
  按 conventional commit type 分组：
    feat:  / fix: / refactor: / chore: / docs:
  输出文件总数 + insertions/deletions（git diff --shortstat）

[5/9] 本地修改保护（P4）
  对每条 sync_paths.values()：
    git -C consumer diff --stat <consumer-path>
    git -C consumer log <from-sha>..HEAD -- <consumer-path>
  - 有未提交改动 → 列文件 + 报错停下
  - 有未上游的 commit（consumer 自己改过 sync 范围内的文件而生成器没追同步）
    → 列 commit + 警告，要求 --force 跳过

[6/9] 应用 renames
  对每条 renames：
    if consumer/<from> 存在 and consumer/<to> 不存在:
      git mv from to
    else:
      skip + log

[7/9] rsync
  对每条 sync_paths：
    rsync -a --delete \
      $(for ex in exclude_from_consumer: --exclude=$ex) \
      generator/<src>/ consumer/<dst>/

  对 never_sync 的 glob：rsync 默认不会进入（因 sync_paths 不含它们），
  但如果将来 sync_paths 扩到根（不推荐），在这里加 --exclude

[8/9] worktree impact 报告（S2，§6 详）
  生成 framework-sync-impact.md 写到 consumer 仓根目录（gitignore 不要它）

[9/9] commit message 草稿
  写到 stdout + .git/SYNC_COMMIT_MSG（让 PM 直接 git commit -F .git/SYNC_COMMIT_MSG）
  格式：
    chore: 同步框架 — <自动按 type 分组的 1-2 句概括>
    
    从 PM-AI-Workflow 同步自上次 sync (<from-sha>, <date>) 以来
    <N> 个上游 commit，覆盖 <M> 大工作线。
    
    1) <type>: <commit subject>
    2) <type>: <commit subject>
    ...
    
    ⚠️ <如有 worktree>，列 worktree 影响概要
    
    framework-sync: <generator HEAD sha>
```

### 5.3 commit trailer 锚点

每次 sync commit 必带 trailer：

```
framework-sync: 5a39159
```

值 = 生成器 HEAD 的完整 sha（或前 7 位）。下次 sync 用这个锚点。

**v0 锚点**：在 AC4 commit `d0aa6c1` 上 amend 加 trailer，把生成器当时的 HEAD（`5a39159`）写进去。

---

## 6. S2: worktree impact 报告

### 6.1 触发时机

sync 脚本 step 8。无论 dry-run 或 apply 都生成。

### 6.2 检测

```bash
git -C consumer worktree list --porcelain
```

逐个 worktree（除主 worktree）：

```
worktree /Users/.../ExampleConsumerApp/.worktrees/req-001-tenant-console-redesign
HEAD <sha>
branch refs/heads/req-001-tenant-console-redesign
```

### 6.3 状态采集

对每个 worktree 路径：

1. 读 `.req-meta.json`（如果有）拿 stage / req-id
2. 跑 `python3 .claude/scripts/status-view.py --summary <worktree-path>`（生成器加这个支持）
3. 列 task 数量 + 当前 stage

### 6.4 SKILL 影响匹配

从 step 4 的上游 commits 里抽出"涉及 skills/* 的 commit"列表，按 skill 名称聚合。每个 worktree 的报告里附：

```
worktree: req-001-tenant-console-redesign (stage 5, task-002 in progress)
  本次同步改写的 SKILL：
    - task-confirm  (5b96c7a feat(v4 A2): 6 个 SKILL 改写实施 v4 工作流核心)
    - task-execute  (5b96c7a, d760174)
    - task-plan     (5b96c7a, 7510692)
  当前 worktree 用得到的 SKILL：task-confirm, task-execute（推断自 stage 5）
  
  💡 建议：
    - 完成当前 task-002 再 rebase main
    - 或者：保持 worktree 不动，新 task 等 rebase 后再开
```

### 6.5 报告输出

写 `<consumer>/framework-sync-impact.md`，**不入库**（消费者 .gitignore 加一行）。每次 sync 覆盖。PM 看完即弃。

不阻断 sync。只列清单，让 PM 自己决定。

---

## 7. S3: module 规格 lazy migration

### 7.1 问题

template 改了，但 `docs/modules/<module>.md` 已经写过了。下次 doc-update sink 时，沉淀位置可能找不到对应 H2 / 表格列。

### 7.2 触发时机

doc-update skill 运行时。**不在 sync 流程内执行**——sync 只更新 template，迁移在使用 template 时再做。

### 7.3 检测机制

新增 `scripts/check-module-template.py <module-md-path>`：

- 解析 `templates/module.md.tmpl` 提取期望的 H2 段落 + 关键表格列名集合
- 解析目标 module.md，比对结构
- 输出：
  - `OK`：结构匹配，doc-update 直接 sink
  - `NEEDS_MIGRATION`：缺 H2 段 / 缺表格列，附 diff
  - `INCOMPATIBLE`：解析失败 / 不像 module.md，要 PM 手动处理

### 7.4 doc-update 集成

在 doc-update SKILL.md 加一段「前置：模板对齐」step：

```
0. 模板对齐
   对涉及的每个 module.md 跑 check-module-template.py
   - OK → 继续
   - NEEDS_MIGRATION → 走 reorganize 子流程：
       a. 把旧内容映射到新模板的 section
       b. 缺失 section 留 placeholder + 注释 "<!-- migrated from old template, fill in -->"
       c. 加 commit "chore(modules): align <module> to template v<N>"
       d. 然后再做正常 sink
   - INCOMPATIBLE → 停下，要 PM 手动处理
```

### 7.5 模板版本号

`templates/module.md.tmpl` 头部加注释行：

```markdown
<!-- template-version: 2 -->
# {{module_name}}
...
```

每次 module.md.tmpl 大改（H2 增删、关键表格列增删）→ 版本号 +1。
check-module-template.py 比对 module.md 顶部记录的 template-version 与当前 tmpl 版本。

不需要 semver；只是个递增整数。

---

## 8. 边界与红线

### 8.1 不会被 sync 改的（硬规则）

- consumer 仓根目录的 `CLAUDE.md` / `INVARIANTS.md` / 任何 `*.md`（除非显式在 sync_paths 里）
- consumer 仓的 `requirements/` `prototypes/` `docs/` 等业务目录
- consumer 仓的 `.git/` `.runs/` `.dev-port` `.worktrees/`
- consumer 仓任何 worktree 的工作树（sync 只改主 worktree / main）
- 生成器侧 `never_sync` glob 命中的文件

### 8.2 sync 出错时的恢复

sync 脚本不自动 commit。失败 / 不满意时：

```bash
cd consumer
git restore .         # 恢复未 add 的改动
git restore --staged . # 取消暂存
# rsync 已删除的文件靠 git 恢复（因为还没 commit）
```

### 8.3 不支持的场景

- consumer 不是 git repo → 报错（依赖 git 做 rollback / 锚点）
- consumer 在非 main 分支 → 报错（避免污染 worktree 分支；用户要在 main 上 sync）
- 生成器在非 main 分支 → 报错（锚点必须从 main commit 来）

---

## 9. 实现清单

| # | 文件 | 动作 |
|---|---|---|
| 1 | `framework-manifest.yaml` | 新建（按 §4 字段） |
| 2 | `scripts/sync-to-consumer.sh` | 新建（按 §5 流程；bash） |
| 3 | `scripts/_manifest-parser.py` | 新建（解析 manifest，给 sh 提供 key 取值；见 §11 Q1）|
| 4 | `scripts/status-view.py` | 加 `--worktree-path <path>` 支持，让外部仓也可以查询其他 worktree 的状态 |
| 5 | `scripts/check-module-template.py` | 新建（按 §7.3） |
| 6 | `templates/module.md.tmpl` | 头部加 `<!-- template-version: 2 -->` |
| 7 | `skills/doc-update/SKILL.md` | 加「0. 模板对齐」step |
| 8 | `tests/sync/` | 新增测试目录（按 §10） |
| 9 | ExampleConsumerApp commit `d0aa6c1` | amend 加 `framework-sync: 5a39159` trailer，建立 v0 锚点 |
| 10 | ExampleConsumerApp `.gitignore` | 加 `framework-sync-impact.md` |

---

## 10. 测试清单

15 个场景：

1. 干净 consumer，正常 sync → diff 符合预期 + commit trailer 写入
2. consumer 有未提交改动覆盖到 sync path → 报错停下，列文件
3. consumer 有未上游的 commit 改了 sync 范围内文件 → 警告，要 --force
4. consumer 在非 main 分支 → 报错
5. consumer 不是 git repo → 报错
6. consumer 路径不存在 → 报错
7. dry-run → 不动文件，输出 plan
8. 生成器 HEAD 和 from-sha 一致（无新改动） → 输出 "up to date" 退出
9. 没有 framework-sync trailer 锚点 + 无 --from-sha → 报错要求 --from-sha
10. rename 已应用过（task-status 已存在） → skip rename 不报错
11. exclude_from_consumer 项 → 不被 --delete 清理
12. consumer 有 worktree → 输出 framework-sync-impact.md，含每个 worktree 的 stage + SKILL 影响
13. mode change（生成器 100644 vs consumer 100755） → 跟随上游
14. doc-update 运行 lazy migration：检测到旧模板 module.md → 自动 reorganize
15. doc-update 运行：新模板 module.md → 跳过 reorganize 直接 sink

---

## 11. 已决问题（原未决，2026-04-26 由 PM 全权委托决议）

### Q1: manifest 用什么格式？→ **python**

零依赖（macOS 默认 python 没 PyYAML；tomllib 要 3.11+），和 scripts/*.py 风格一致，`import framework_manifest` 即用，PM 改起来就是改 dict。详见 §4.1。

### Q2: 多消费者怎么管？→ **YAGNI**

当前只有 AC4。第二个消费者（ExampleConsumerB）首次迁移单独开 req，不进常规 sync 队列。多消费者批量同步用 PM 自己写的 wrapper：

```bash
for path in ~/Projects/ExampleConsumerApp ~/Projects/ExampleConsumerB; do
  scripts/sync-to-consumer.sh $path --apply
done
```

遇到第三个消费者再考虑 manifest 加 `CONSUMERS = [...]` 或 `--all` 命令。

### Q3: ExampleConsumerB 这种 v1 项目首次迁移？→ **本设计不覆盖**

ExampleConsumerB 可能没 `agents/`、可能 skill 集合差很多、可能 module.md 结构完全是 v1。直接走 sync 脚本会触发大量 rename / delete，不安全。

策略：先把 sync 脚本用 AC4 跑稳；之后**单独开 req**设计 v1→v4 的一次性迁移脚本（不复用 sync-to-consumer.sh，因为迁移是一次性的、要做大量 ad-hoc 转换）。迁移完成后 ExampleConsumerB 进入常规 sync 队列。

### Q4: framework-sync trailer 是 amend 到 d0aa6c1 还是从下次开始？→ **amend**

建立锚点 v0，否则下次还要 grep 反推。AC4 main 未 push，amend 安全。amend 步骤纳入 §9 实现清单（第 9 项）。

### Q5: sync 脚本要不要支持 `--no-worktree-impact`？→ **不加，YAGNI**

worktree 报告轻量（只读 git worktree list + status-view），先不加 opt-out。

### Q6: doc-update 的 lazy migration 谁触发？→ **PM 走 stage 6 时自然触发**

不主动触发。模板升级后已沉淀的 module.md 暂时无害（无人读），等下次有 req 进 stage 6 → doc-update 跑 → 检测到旧结构 → reorganize → sink。符合 TODO "lazy" 本意。

---

## 12. 实现顺序

按依赖关系，分 4 phase：

| Phase | 内容 | 验证 |
|---|---|---|
| **P1** | manifest 格式拍板 (Q1) + 写 manifest yaml/py 文件 | manifest 文件存在 + 字段正确 |
| **P2** | `sync-to-consumer.sh` 主流程（step 1-7+9，跳过 step 8） | dry-run vs AC4 输出符合预期；apply 后 git diff 等同手工 sync |
| **P3** | step 8 worktree impact 报告（S2 完整功能） | dry-run vs AC4 生成 framework-sync-impact.md，列出 3 个 worktree 状态 |
| **P4** | doc-update lazy migration（S3） | 构造一个旧结构 module.md，跑 doc-update 自动 reorganize |

P1+P2 一同实施可以最快 ship 替代手工 sync；P3 让 worktree 影响可见；P4 独立加在 doc-update 流程里。

---

## 13. 决策记录

- 2026-04-26：PM 拍板"方案 B（manifest + 脚本），三个子项一起做，commit trailer 锚点 amend 到 d0aa6c1，manifest 放生成器根目录"
- 2026-04-26：PM 全权委托 AI 决议 Q1-Q6
  - Q1 manifest = **python**（零依赖、和 scripts/ 风格一致）
  - Q2 多消费者 = **YAGNI**（PM 自己写 wrapper）
  - Q3 ExampleConsumerB 首次迁移 = **本设计不覆盖**（单独 req）
  - Q4 framework-sync trailer = **amend 到 d0aa6c1**
  - Q5 worktree impact opt-out = **不加**
  - Q6 lazy migration 触发 = **PM 走 stage 6 时自然触发**
