#!/usr/bin/env bash
# pmai-release — generator-maintainer release workflow

set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec python3 "$SCRIPT_DIR/../scripts/release.py" "$@"
