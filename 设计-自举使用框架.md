# 让本仓用自己的框架管自己（自举）

## Context

PM-AI-Workflow 是个生成器，产出是一套给 PM 用的 AI 协作工作流。目前仓库本身的开发用的是"普通软件项目"方式（讨论 → 改动 → 提交），没有走自己产出的那套 stage lifecycle / req / task 流程。

**为什么要自举：**

1. **发现可用性问题最快的方式就是自己用**。在别人项目里部署之前，PM 自己先用这套框架开发这套框架，PM 每次遇到的摩擦都是真实的 DX bug。
2. **当前仓库的"需求"其实很适合**：每一次能力增强（本轮功能规格模板、上一轮组件复用）都是一个 req，里面可以拆成 1-N 个 task。
3. **避免生成器和产出脱节**：如果生成器的 skill 改了但没有在自己身上试过，很容易产出漏洞（比如 `init-project.sh` 漏拷 `module.md.tmpl` 这种坑，自举就会马上发现）。

**错配的地方：**

框架默认目标项目是有 UI 的产品项目。本仓是纯框架仓，没有 UI，没有前端代码。这带来几个不匹配：

- 不需要 `docs/DESIGN.md`（没有视觉规范）
- 不需要 `prototypes/`（没有原型）
- `/qa` 和 `/design-review` 对本仓无意义，要用其他 gate 代替（如 `tests/run-all.sh`）
- `check-branch.sh` 的白名单是按产品项目的目录布局写的（`docs/` = 文档，其他 = 代码），本仓的布局不一样

## 改动清单

### 1. `.claude/` 目录（symlink + 新增配置）

```
.claude/
├── settings.json           # 新建（基于 templates/settings.json.tmpl 改写）
├── settings.local.json     # 已有，保持
├── scripts  -> ../scripts  # symlink
├── skills   -> ../skills   # symlink
└── templates -> ../templates  # symlink
```

**为什么用 symlink：** 本仓的 `scripts/`、`skills/`、`templates/` 是 source of truth，`.claude/*` 只是框架的约定挂载点。复制会产生双份，symlink 保证改一处即可。

注意：symlink 要相对路径，方便 `cp -r` 或 worktree 创建时保持相对结构。

### 2. `.claude/settings.json`

基于 `templates/settings.json.tmpl`，调整：

- **保留 hooks**：`PreToolUse` 跑 `.claude/scripts/check-branch.sh`（按需调整规则见第 3 点）
- **permissions 白名单**：允许 `bash .claude/scripts/*.sh`、`python3 .claude/scripts/*.py`、`bash tests/*.sh`、`git *`、基础 shell
- **不需要**：`bun`、`npm`、`lsof`、`kill`（本仓无 dev server）

### 3. 调整 `scripts/check-branch.sh` 的分支白名单（本仓特化）

这是最关键的一步。框架默认白名单是给产品项目的，本仓布局不同。需要让 `check-branch.sh` 识别本仓的项目类型。

**方案 A（推荐）：配置驱动**

在根目录加一个 `.repo-profile`（或直接放 `.claude/repo-profile.json`），声明本仓类型：

```json
{
  "profile": "framework-self-hosted",
  "main_whitelist": [
    "CLAUDE.md",
    "README.md",
    "INVARIANTS.md",
    ".gitignore",
    ".claude/**",
    "requirements/**",
    "docs/**"
  ],
  "req_branch_paths": [
    "requirements/active/req-*/**",
    "docs/**"
  ],
  "task_branch_paths": [
    "scripts/**",
    "skills/**",
    "templates/**",
    "tests/**"
  ]
}
```

`check-branch.sh` 读 profile，按 profile 白名单校验。默认 profile（产品项目）仍然保留，零回归。

**方案 B：不改脚本，只在本仓加绕过**

`.claude/settings.json` 里把 hook 直接关掉或改成 no-op。

**推荐 A**：B 失去了校验能力，自举的意义被削弱。A 需要改 `check-branch.sh`，多 30-50 行，但长远有用（其他非标准布局的项目也能用）。

### 4. `docs/CONTEXT.md`

新建，内容大致：

```markdown
# PM-AI-Workflow 项目背景

## 定位
PM 单人使用的 LLM 协作工作流生成器。产出是一套 .claude/ 文件，部署到目标项目后，PM 可以用 /new-req、/task-plan、/task-execute 等 skill 推进工作。

## 目标用户
- 产出的使用者：PM，单人，用 Claude Code
- 本仓的开发者：PM 本人（即 dogfood 自举）

## 开发原则
- 简洁优先：能少一层就少一层
- 不为团队 SOP 设计
- 旧仓 ${LEGACY_REPO_ROOT} 只作只读参考，不复用其流程

## 不做什么
- 不做 UI / 前端
- 不做多人协作
- 不做 CI / 部署
```

### 5. `docs/modules/` 初始模块规格

按本仓自然的模块划分，初始生成（先占位，具体内容在第一个 req 里填）：

- `docs/modules/scripts.md` — 脚本模块（`scripts/*.sh`、`scripts/*.py`）
- `docs/modules/skills.md` — Skills 模块（`skills/*/SKILL.md`）
- `docs/modules/templates.md` — 模板模块（`templates/*.tmpl`）
- `docs/modules/tests.md` — 测试模块（`tests/*.sh`）

每个文件按新版 `module.md.tmpl` 的 5 section 结构起始。

### 6. `requirements/` 目录

```
requirements/
├── active/      # 空
└── closed/      # 空（后续把已完成的 req 挪进来）
```

**把现有的"设计-*.md"归档到哪里？**

两个选择：
- **(a)** 挪到 `requirements/closed/req-000-bootstrap/` 之类的假 req 下，只为了保留历史
- **(b)** 保留在根目录，仅作为"前-自举"时期的参考文档

推荐 (b)。(a) 会虚构不存在的 req 编号，污染 req 历史。

### 7. `CLAUDE.md` 更新

追加 session bootstrap section（参照 `templates/CLAUDE.md.tmpl`）：

```markdown
## Session Bootstrap

每次会话开始：
1. 读本文件（CLAUDE.md）
2. `source .claude/scripts/skill-preamble.sh`
3. 跑 `/task-status` 看当前 req / task 状态

## 状态转换

不要直接改 task 文件的「状态」字段或 .req-meta.json 的 stage 字段。用：
- `python3 .claude/scripts/req-transition.py <req-dir> --to <stage>`
- `python3 .claude/scripts/task-transition.py <task-file> --to <status>`
```

现有的"根因优先"、"gstack"规则保留。

### 8. 不引入的部分

- **`docs/DESIGN.md`**：没有 UI，不需要
- **`docs/prd.md`**：单人项目，不需要 PRD，需求直接落在 req 的 analysis.md / solution.md
- **`prototypes/`**：不需要
- **`.dev-port`**：不需要（无 dev server）
- **`/qa`、`/design-review` skill 的使用**：skill 保留（给其他项目用），但本仓的 task 默认「审查工具」字段只放 `/review`

## 冷启动顺序

1. 创建 symlink（`.claude/skills` → `../skills` 等）
2. 写 `.claude/settings.json`
3. 改 `scripts/check-branch.sh` 支持 profile（方案 A）
4. 加 `.claude/repo-profile.json`
5. 写 `docs/CONTEXT.md` 和 `docs/modules/*.md` 占位
6. 建 `requirements/active/`、`requirements/closed/`
7. 更新 `CLAUDE.md`
8. **第一个 req 自举试跑**：`/new-req`，主题为"验证本仓自举流程能走通"，分析 → 设计 → 拆 task → 执行 → 关闭。过程中记录所有摩擦点，后续作为新的 req 输入。

## 风险

- **递归修改问题**：如果 req 本身的目标是"改 check-branch.sh"，那 task 分支里改这个脚本时，当前正在运行的 hook 就是老版本。这不会阻塞（hook 只管 Edit 目标路径），但要注意 merge 后新规则生效。
- **旧设计文档保留**：根目录的 `需求.md`、`设计.md`、`设计-xxx.md` 都是自举前产物。保留在根目录作为历史，不挪位置。README 或 CONTEXT.md 里说明一下来历。
- **init-project.sh 的自引用**：本仓的 `.claude/scripts/init-project.sh` 是用来初始化"目标项目"的，不是用来初始化本仓的。保留不动。

## 验证

1. 创建所有 symlink 和配置文件，不破坏 `bash tests/run-all.sh` 现有通过率
2. 跑一次 `source .claude/scripts/skill-preamble.sh`，确认环境变量正确（`REPO_ROOT`、`WORKTREE_TYPE=main`）
3. 试 `/task-status`，应该显示"无 active req"
4. 跑 `/new-req`，能创建 req worktree，进入 stage 1
5. 整个流程走到 close-req，确认能回到 main

## 不改的部分

- `scripts/init-project.sh` 本身
- 所有 skill 定义（它们是 source of truth，本轮不改）
- `tests/` 已有测试
- 根目录的历史设计文档
