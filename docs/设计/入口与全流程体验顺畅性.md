<!--
设计文档：入口与全流程体验顺畅性
合并范围：
  M1 = init-project 一气呵成（原 D-iv，§0 已锁 2026-05-24）
  M2 = Banner + Next Up 块（gsd #4 借鉴）
  M3 = 跨 skill auto-advance chain（gsd #6 借鉴）
  M4 = AskUserQuestion 严格化收敛（gsd #11 借鉴）
  M5 = Resume / Start fresh 闸门（gsd #7 借鉴）
合并理由：5 模块共同指向同一根因 —— "PM 单人工作流体验断点多 / 锚点缺 / 续接靠人工"
PM 2026-05-25 显式要求合并成一个设计文档
-->

# 入口与全流程体验顺畅性 (v0)

> **状态**：M1 §0 已锁（PM confirm 2026-05-24）；M2-M5 §0 初稿待 PM 共写后锁
> **日期**：2026-05-25
> **作者**：PM + AI
> **对齐参考**：gsd workflow patterns（new-project / discuss-phase / execute-phase / autonomous / transition）
> **历史**：原 `init-project-入口一气呵成.md`（D-iv v0.1）合并升级；M1 = 原 D-iv 内容，M2-M5 = gsd 深度研究后新增

---

## §0 原始痛点（PM + AI 共写，后续 review **不可反向修改**）

> ⚠️ 任何 review / autoplan 不能给本节加东西、不能重新定义痛点。
> M1 痛点 2026-05-24 已锁，原文照搬不改。
> M2-M5 是 2026-05-25 新写的初稿，**PM 共写并锁后**才能进 §X review。

### §0.1 痛点清单（按模块编号）

---

#### §0.1.M1 init-project 入口三步分裂（已锁 2026-05-24）

PM 第一次开新项目时，要先在终端跑 `bash scripts/init-project.sh ...`（脚手架），再在 Claude Code 里发 `/project-solution`（定方向），再发 `/new-req`（起需求）——三步靠 echo / README 文案串接。其中 `/project-solution` 在 4 处用户入口（init 脚本 --help / init 脚本末尾 echo / README §1 / README §2）的"下一步"提示里**全部漏写**，导致 AI 和 PM 都把流程当两步（init → /new-req）。即使 `/new-req` 步骤 3.5 有 mini-fill 兜底把 PROJECT.md 补上，`roadmap.md` 永远不被填，项目级方向讨论缺失。

**触发场景**：

| # | 场景 | 实证 |
|---|---|---|
| 1 | AI 答"框架使用流程"漏 `/project-solution` | 本次对话（2026-05-24）；PM 追问才补正 |
| 2 | PM 跑完 init-project.sh 终端只看"下一步：/new-req" | `scripts/init-project.sh:38, 275`；`README.md:63, 65` |
| 3 | gsd 一个命令把骨架+方向+多份文档全跑完，我们三步分裂 | PM 提供 gsd ChatBuilder 对话(2026-05-24) |
| 4 | mini-fill 兜底 PROJECT.md 但 roadmap.md 永远漏 | `skills/new-req/SKILL.md:68-102` 只查 PROJECT 6 节 |

**根因**：`init-project.sh` 是 shell 脚本，跑完即返回终端，**没有跟 PM 对话的能力**——所以方向讨论必须拆出独立 skill，两段中间靠 echo / README 文案粘合 → 文案就会漂。修法：把入口改成 **agent 化 skill**，agent 内部串起「骨架 → 方向讨论 → 落档 → 下一步」全流程。

---

#### §0.1.M2 PM 跑到第三步常忘自己在哪（PM 锁 2026-05-25）

PM 在 req 流程跑到 stage 3 PRD 后，看不到统一的"我现在在 stage 3，下一步是 stage 4 gap-check"显式锚点。`status-view.py` 只在 `skill-preamble.sh` 启动时打印一次，**中途无阶段 banner、无"Next Up"块**；PM 切窗口回来要靠回忆或重发 `/req-stage-gate` 才知道在哪。

**触发场景**（初稿，PM 校验）：

| # | 场景 | 实证 |
|---|---|---|
| 1 | PM 在 task 窗口跑完 task-execute 后，回 req 窗口忘了下一步是 close-task 还是直接 close-req | [ASSUMED] —— 待 PM 实证 |
| 2 | 跨天回来续接，要重新跑 `status-view.py` 才知道状态 | [ASSUMED] —— skill-preamble 仅 skill 启动时跑 |
| 3 | gsd 每步打 banner `GSD ► AUTONOMOUS ▸ Phase 2/5: Analysis [██░░] 40%` + 结尾必出 `## ▶ Next Up — ...` 块；我们无此机制 | gsd `autonomous.md:62-69, 155-163` / `execute-phase.md:1725-1730` |

**根因**：进度可视化只在 skill 启动时一次性 render（status-view.py），无"过程中的位置感"。PM 体验是离散点查询，不是持续可见。修法：在每个 stage 转换 + skill 退出处加固定格式 banner + Next Up 块。

---

#### §0.1.M3 每个 stage 都要 PM 喂"继续"（PM 锁 2026-05-25）

PM 在一个 req 里跑完 stage 2 analysis 后，要手动发 `/req-stage-gate` 推进 stage 3；stage 3 PRD 后再发一次推 stage 4；…stage 7 spec 才到 task-confirm。每个 stage 边界都要 PM 主动喂命令，**即使前一步刚刚 PM 已说"通过"**。

**触发场景**：

| # | 场景 | 实证 |
|---|---|---|
| 1 | PM 在 stage 2 答完未决问题闸门确认通过，仍要手动敲 `/req-stage-gate` 才推 stage 3 | `skills/req-stage-gate/SKILL.md`（619 行，stage 间手动触发）|
| 2 | gsd 用 AskUserQuestion 在闸门处给"创建 PROJECT.md / 继续探索"二选一，PM 一次拍板下一步；我们闸门答"通过"后等命令 | gsd `new-project.md:368-380` Decision gate pattern |
| 3 | PM 痛感：闸门答"通过"后还要再敲一个 slash command 才推下一步，冗余且打断思路 | PM 2026-05-25 confirm 痛点真实存在 |

**根因**：闸门答"通过"后没主动推进，要 PM 再敲 slash command —— 因为闸门答案是"通过 / 继续 / OK"语义模糊，框架不敢假设 PM 想推下一步。修法：**闸门写法改成 gsd Decision gate pattern** —— AskUserQuestion 明确动作二选一（推进选项 = 下一步具体动作描述 / 留守选项 = 继续完善当前 stage + Loop 回路），PM 一次拍板下一步，不依赖"通过"的隐含语义、**不需要 `--auto` flag**。

---

#### §0.1.M4 AskUserQuestion 答题规则散乱（PM 锁 2026-05-25）

PM 答题门规则散在各 skill 各处：

- "未决问题闸门"在 stage 1→2（已落 memory `feedback_open_questions_gate`）
- task-spec 步骤 10"执行前确认"在 task-spec 内部
- task-execute 验收呈交在 task-execute 内部
- 每处自定义答题分支、自定义"PM 没答怎么办"

**没统一规则**："PM 没答怎么办 / 空答案怎么办 / 二选一怎么强制 / 是否禁止默认落盘"。gsd 通过一条全局 `#3018` 规则统一约束（AskUserQuestion 空答 → STOP wait next message，不重试不默认；没拿到答案禁止落盘 artifact）。

**触发场景**（初稿，PM 校验）：

| # | 场景 | 实证 |
|---|---|---|
| 1 | task-spec 步骤 10 vs req-stage-gate stage 1→2 闸门答题规则不一致 | grep `skills/task-spec/SKILL.md` + `skills/req-stage-gate/SKILL.md` 验证 |
| 2 | PM 没答 AI 默认走 "通过"分支导致 task 跳过 PM 拍板（历史 commit 07a3a09 处理）| memory `feedback_close_default_flow.md` |
| 3 | gsd 一条 `#3018 failure mode` 通用规则跨所有 workflow 引用 | gsd `discuss-phase.md:95-102` / SKILL.md:29-35 |

**根因**：没有 `_shared/pm-view/askuser-rules.md` 这种统一约束文件，每 skill 各自实现答题分支 → 规则漂移、PM 体验不一致。修法：把 gsd `#3018` 规则照搬，落到 `_shared/pm-view/askuser-rules.md`，所有用 AskUserQuestion 的 skill 顶部加指针引用。

---

#### §0.1.M5 session 起始 AI 不主动播报工作进度（PM 锁 2026-05-25）

> **v2 重定义**（PM 2026-05-25 主动决策更新 §0）：原 v1 痛点（"半成品续接 / Resume 闸门"借鉴 gsd #7）PM 实证 NULL —— PM 用 Claude Code conversation history 恢复机制已兜底"半成品"问题。真痛点是 v2：**session 起始 AI 不主动结构化播报工作进度**。

PM 切窗口 / 隔天回来 / 开新 chat session 时，Claude Code 恢复了 **对话历史** 但 AI 不**主动结构化播报工作进度**。PM 要么记住自己在哪，要么主动问"我在哪"，要么主动跑 `/status-view`。希望的体验：PM 一开窗口，AI 第一句就说："上次你做到 req-003 stage 3 PRD §四，下一步是 gap-check；要继续吗？"

**触发场景**：

| # | 场景 | 实证 |
|---|---|---|
| 1 | PM 开新窗口 conversation history 已恢复，AI 第一句不主动说"上次你在 stage X"，要 PM 主动问 | PM 2026-05-25 confirm 痛点真实 |
| 2 | gsd `progress.md` workflow 提供"phase.complete / disk_status / roadmap checkbox"聚合，用户敲一个命令看全 | gsd `progress.md` |

**根因**：我们 `status-view.py` 已经做了进度聚合，**但只在 PM 敲 skill 时由 `skill-preamble.sh` 触发**；PM 不敲 skill 直接聊天 → AI 没自动播报。`.req-meta.json` 数据齐备，缺的是**"session 起始就播报"的触发点**+ **AI 可直接念的 narrative 输出格式**。修法：① `status-view.py` 加 `--narrative` 模式（list → 段落叙述，AI 直接念给 PM 听）② `CLAUDE.md` 加章程规则「session 起始 AI 必须先跑 `status-view.py --narrative` 并播报」。

**与 M2 banner 关系**：M2 = 过程中位置感（每 stage 转换打 banner）；M5(v2) = session 起始位置感（开窗口时 AI 主动播报）；互补。

---

### §0.4 不解决什么（防 review 拉进来）

| # | 衍生 / 假设场景 | 为什么不在 §0 范围 |
|---|---|---|
| M1.1 | 扩展 init 产出到 gsd 7 份（research/ARCHITECTURE/FEATURES/TECH/RISKS/STATE/AGENTS）| Q2 分析：stage 体系已覆盖 ARCHITECTURE/FEATURES/TECH；项目级 RISKS 第一次写空对空；STATE 在 req 并行模型反误导；AGENTS = CLAUDE.md 已注入 |
| M1.2 | 删 init-project.sh 改纯 skill | 保留脚本作骨架构建器被 agent 用 Bash 调；纯 skill 化让 chmod / git init / 模板替换变 agent 写代码，错误率高 |
| M1.3 | 业务仓也能跑 `/init-project` | 只在生成器仓里跑（init-project.sh 拷贝时已排除 init-project skill）|
| M1.4 | 修 new-req mini-fill 加 `/project-solution` 漏跑检测 | mini-fill 是 legacy 兜底；根因修在入口不在兜底 |
| M1.5 | 整体改名 `/init-project` → `/new-project` | Q1 决议：沿用 `/init-project` |
| M2.1 | banner 加颜色 / emoji 美化 | 纯文本 ASCII 即可，跨终端兼容；美化跟功能无关 |
| M2.2 | banner 加进度百分比（gsd 有）| 我们 stage 7 个 + task 不定数；百分比意义不大，用 "Stage 3/7" 即可 |
| M3.1 | 跨 req 自动起新 req（任何"自动推进"机制都不能跨 req）| 新 req = PM 主动决策（市场 / 老板 / 客户输入），不能自动 |
| M3.2 | 自动跳过 PM 验收 task（任何"自动推进"机制都不能跳验收）| task 验收是 PM 把关核心节点，绝不自动 |
| M3.3 | `--auto` chain flag 整套机制（`.req-meta.json auto_chain_active` 字段 / PM 提前承诺批量推进） | gsd 有但叠加层；PM 单人产品工作流没 batch run 场景；M3 用 Decision gate 闸门解决，不引入 chain flag |
| M4.1 | 把所有 AskUserQuestion 改成相同模板 | 不同场景需要不同选项数 / 措辞；M4 范围限"答题规则约束"，不强制 UI 模板 |
| M4.2 | AskUserQuestion 答案做 LLM 校验 | 过度工程；M4 范围限"空答 / 没答 / 落盘前置"三条硬规则 |
| M5.1 | gsd #7 风格 checkpoint json / Resume 闸门 | v1 PM 实证 NULL（Claude Code conversation history 已兜底）；v2 真痛点是"主动播报"不是"半成品续接" |
| M5.2 | 自动决定下一步动作（不问 PM 直接续上）| 播报 ≠ 决策；M5(v2) 只解决"AI 主动报告进度"，下一步推进归 M3 闸门 |
| M5.3 | 把 conversation transcript 存进 status | 过度存储；播报靠 `.req-meta.json` 推断即可 |

→ **review 中任何 finding 指向以上场景，默认 DEFER**（除非 PM 显式接受拉进 §0）

---

## §1 方案概述（5 模块综合）

5 模块共同指向**"PM 看到的是一条直线，框架在背后做所有状态判断"**（gsd punchline）。模块间关系：

```
M1 (init-project 一气呵成)     ←── 入口体验
                              ↓
M2 (Banner + Next Up)         ←── 视觉锚点 [横切，所有 skill]
                              ↓
M3 (auto-advance chain)       ←── 持续意图 [横切，跨 stage / 跨 skill]
                              ↓
M4 (AskUser 严格化)            ←── 答题规则 [横切，所有用 AskUser 的 skill]
                              ↓
M5 (session 播报)             ←── 回来即知 [session 起始 AI 主动播报进度]
```

**实施分两批**（PM 可选择只做批 1 或两批都做）：

- **批 1：init-project 闭环（M1）** —— 仅修 init-project skill + 阶段内 banner 雏形；6 个 vp，~4.75h
- **批 2：横切普推（M2-M5）** —— 全仓扩散 banner / Decision gate / askuser 严格化 / session 播报；7 个 vp，~4.75h

---

## §2 与现有机制的关系

| 现有组件 | 改造方案 | 涉及模块 |
|---|---|---|
| `scripts/init-project.sh` | 保留作骨架构建器；删掉指向 `/new-req` 的 echo "下一步"文案（入口改了）| M1 |
| `skills/init-project/SKILL.md` | 重写 Workflow：从"调 shell 脚本"扩张到"4 阶段 agent 流程"| M1 |
| `skills/project-solution/SKILL.md` | 强化定位：4 个独立调用场景（重做 / 规划 / 新方向 / brownfield）+ 被 init-project 阶段 C 字面调用 | M1 |
| `skills/req-stage-gate/SKILL.md` | 加 banner（stage 转换处）+ 退出闸门改 Decision gate pattern（明确动作二选一）| M2 / M3 |
| `skills/new-req/SKILL.md` | 加 Next Up 块 + 阶段 banner + 退出闸门 Decision gate | M2 / M3 |
| `skills/task-confirm` / `task-execute` / `close-task` / `close-req` | 加 banner + Next Up + 退出闸门 Decision gate | M2 / M3 |
| `scripts/status-view.py` | 不动（基线 render）；用作 banner 数据源 | M2 |
| **新** `_shared/pm-view/banner-rules.md` | banner 格式规范 + Next Up 块格式 + 调用约定 | M2 |
| **新** `_shared/pm-view/gate-pattern.md` | gsd Decision gate 3 条硬规则：label=动作 / description=一句话 / 留守有 Loop | M3 |
| **新** `_shared/pm-view/askuser-rules.md` | gsd #3018 规则照搬：空答 STOP / 不默认 / 没答禁止落盘 | M4 |
| `scripts/status-view.py` | 加 `--narrative` 模式（list → 段落叙述，AI 直接念给 PM）| M5 |
| **新** `CLAUDE.md` 章程章节 | 加规则：「session 起始 AI 必须先跑 `status-view.py --narrative` 并播报」| M5 |
| `templates/PROJECT.md.tmpl` / `roadmap.md.tmpl` | 不动 | - |
| `skills/new-req/SKILL.md` 步骤 3.5 mini-fill | 不动（legacy 兜底）| M1 |
| `README.md` / `RUNTIME.md` | 更新快速开始 + 新窗口续接命令 | M1 / M2 / M3 |

---

## §3 实施清单（13 个 vp）

按 vp（vertical prototype）切片，每片独立可测、可 commit。**vp 在同一 worktree 顺序做**（不是并行）。

### 批 1 · M1（init-project 闭环）—— ~4.75h

#### vp-1：阶段 A + B（前置检查 + 骨架建设）—— 1.5h

- 改 `skills/init-project/SKILL.md`：扩 Workflow 阶段 A + B
- 阶段 A：用 AskUserQuestion 分批问参数（项目名 / 路径 / 背景 / intent）
- 阶段 A 加 brownfield 检测（Q3=加）：目标目录已含 `.git/` 或代码文件 → 拒绝 + 提示走 `/codebase-audit`
- 阶段 B：agent 用 Bash 调 `bash scripts/init-project.sh ...`
- 改 `scripts/init-project.sh:36-39, 268-276, 38`：删掉指向 `/new-req` 的 echo "下一步"文案

#### vp-2：阶段 C（字面调用 /project-solution）—— 0.75h

- 阶段 C 段写明「agent 用 Skill 工具 invoke `/project-solution`」
- 不重复实现提问法 —— 真相源只在 `/project-solution` 一处
- **前置技术验证**（vp-2 实施前）：写最小可行测试 skill A invoke skill B，确认 Claude Code Skill 工具支持 skill-in-skill
- 异常处理：sub-skill 失败 / PM 中断 / 部分完成场景

#### vp-3：阶段 D（终态汇总 + 下一步）—— 0.25h

- PROJECT.md / roadmap.md / commit 由 `/project-solution` 已完成
- 阶段 D 在收到 sub-skill 返回后做汇总输出
- 终态输出格式：`✅ <name> 已就绪 / 下一步(复制): cd <target> && /new-req "..."`

#### vp-4：文档同步 —— 0.5h

- 改 `README.md:55-94`（快速开始）：删两步改单步
- 改 `README.md:65`（"5 分钟你会看到"）
- 改 `RUNTIME.md` 新窗口续接命令
- 更新 `CHANGELOG.md` 未发布段

#### vp-5：测试 + 端到端验证 —— 1h

- 跑 `tests/run-all.sh` 确认 398/0 没变
- 跑 `bash scripts/measure-tthw.sh` 计时（期望 ≤ 30 分钟）
- 在 ExampleConsumerApp 之外起一个新项目跑全流程

#### vp-6：`/project-solution` 定位强化 —— 0.75h

- 重写 `skills/project-solution/SKILL.md` frontmatter / When To Use
- 4 个独立调用场景：A 项目方向重做 / B 季度规划 / C 老板新方向 / D brownfield 接入 / E 被 init-project 阶段 C 调用
- 删除"老项目同步兜底"等弱定位

---

### 批 2 · M2-M5（横切普推）—— ~6h

#### vp-7：M2 banner 规范 + status-view 复用 —— 1h

- 新 `skills/_shared/pm-view/banner-rules.md`：定义 banner 格式
  - 阶段 banner：`━━━ PMAI ► <SKILL> ▸ Stage <N>/<T>: <Name> ━━━`
  - Next Up 块：`## ▶ Next Up — <command> <hint>`
- 改 `scripts/status-view.py` 加 `--banner-only` 模式（输出当前 stage banner 一行）
- 改 `scripts/_lib/state.py` 暴露 `get_current_stage_banner(req_dir)` 函数

#### vp-8：M2 banner 在核心 skill 落地 —— 1h

- 改 `skills/req-stage-gate/SKILL.md`：每个 stage 转换前后打 banner
- 改 `skills/init-project/SKILL.md`：4 阶段（A/B/C/D）打 banner
- 改 `skills/new-req` / `task-confirm` / `task-execute` / `close-task` / `close-req`：入口 banner + 退出 Next Up 块
- 跑 `tests/run-all.sh` 确认无回归

#### vp-9：M3 闸门 Decision gate pattern —— 0.75h

- 新 `skills/_shared/pm-view/gate-pattern.md`：3 条硬规则
  1. **label = 动作描述**（例："写 PRD" / "继续完善 analysis"），禁用 "OK" / "Proceed" / "Continue" 模糊词
  2. **description = 一句话解释**（例："我会开始写 prd.md，进入 stage 3"），不是文档化长说明
  3. **留守选项有 Loop 回路**：选了"继续完善 X"自动回到当前 stage 工作态，不退出
- 改 `skills/req-stage-gate/SKILL.md`：每个 stage 退出闸门改 Decision gate 二选一
- 改 `skills/new-req` / `task-confirm` / `close-task` / `close-req` 的退出闸门同款
- 加测试：grep 验证闸门 label 不含 "OK" / "Proceed" / "Continue" 等模糊词

#### vp-10：M4 AskUser 严格化 —— 0.5h

- 新 `skills/_shared/pm-view/askuser-rules.md`：3 条硬规则
  1. 空答 / 没答 → STOP wait next message，不重试不默认
  2. 没拿到答案前禁止落盘 artifact（"#3018 failure mode"）
  3. AskUserQuestion 不支持的 runtime 退化为编号列表，仍 wait
- 改 7 个用 AskUser 的 skill 顶部加指针：`> AskUser 规则见 [@_shared/pm-view/askuser-rules.md]`
- 收敛现有散落规则到该文件（合并 `feedback_open_questions_gate` + 7a3a09 close-task 经验）

#### vp-11：M5 session 起始播报 —— 0.5h

- 改 `scripts/status-view.py`：加 `--narrative` 模式
  - 当前 list 表格保留（兼容 skill-preamble）
  - `--narrative` 输出"上次你做到 req-003 stage 3 PRD §四（提交 abc123，2 天前），下一步是 gap-check"这种 AI 可直接念的段落
  - 数据源：`.req-meta.json` 现有字段 + 最近 commit + stage_history
- 改 `CLAUDE.md` 加章程章节「session 起始播报」：「任何新 session 起始（PM 第一句话），AI 必须先跑 `bash .claude/scripts/status-view.py --narrative` 并把输出主动播报给 PM，再回应 PM 的具体请求」
- 加测试：`tests/test-narrative-mode.sh` 验证 narrative 输出格式 + 内容覆盖（active req / stage / last commit / next step）

#### vp-12：批 2 文档同步 + 端到端测试 —— 0.5h

- 改 `README.md` 加 `--auto` 用法说明
- 改 `RUNTIME.md` 加 chain flag 状态说明
- 改 `CHANGELOG.md` 未发布段加 M2-M5 条目
- 跑 `tests/run-all.sh` 确认无回归
- 在 ExampleConsumerApp 跑一次完整 req（带 `--auto`）验证体感

#### vp-13：消费仓端到端验证 —— 0.5h

- 同步到 ExampleConsumerApp（按 `框架同步-SOP.md`）
- PM 验收：banner 是否清晰 / chain 是否流畅 / Resume 是否准确 / AskUser 是否一致

**总估时**：批 1 ~4.75h + 批 2 ~4.75h = **~9.5h**（PM 验收时间另算；vp-9 砍 `--auto` 1.5h→0.75h；vp-11 M5 重定义 1h→0.5h）

---

## §4 砍掉的机制清单（防被 review 加回来）

| # | 砍掉的 | 为什么砍 | 来源 |
|---|---|---|---|
| C1 | 扩 init 产出到 7 份（research/STATE/AGENTS）| §0.4.M1.1：跟 stage 体系重叠 | M1 原 D-iv §4 |
| C2 | 删 init-project.sh 改纯 skill | §0.4.M1.2：保留脚本更稳 | M1 原 D-iv §4 |
| C3 | mini-fill 加 `/project-solution` 漏跑检测 | §0.4.M1.4：根因修在入口 | M1 原 D-iv §4 |
| C4 | 改名 `/init-project` → `/new-project` | §0.4.M1.5 + Q1=a | M1 原 D-iv §4 |
| C5 | 业务仓允许跑 `/init-project` | §0.4.M1.3 | M1 原 D-iv §4 |
| C6 | 删 `/project-solution` skill / 降级为内部子流程 | PM 2026-05-24 挑战：4 个独立调用场景 | M1 原 D-iv §4 |
| C7 | 阶段 C 内嵌一份提问法副本 | 双份维护必漂移（与"四处文案漂移"同 pattern）| M1 原 D-iv §4 |
| C8 | banner 加颜色 / emoji / 进度百分比 | §0.4.M2.1, M2.2：纯 ASCII + Stage N/T 即可 | M2 新增 |
| C9 | `--auto` chain flag 整套机制（`.req-meta.json auto_chain_active` 字段 / 跨 stage 自动推 / PM 提前承诺批量推进）| §0.4.M3.3：gsd 有但叠加层；PM 单人产品工作流没 batch run 场景；闸门 Decision gate（M3）已解决 stage 推进 | **PM 2026-05-25 challenge** 后定向 |
| C10 | 跨 req 自动起新 req / 自动跳过 PM 验收 task | §0.4.M3.1 + M3.2：未来任何"自动推进"机制都不能跨这两条 PM 把关边界 | M3 边界硬约束 |
| C11 | AskUserQuestion 统一 UI 模板 | §0.4.M4.1：不同场景不同措辞 | M4 新增 |
| C12 | AskUser 答案做 LLM 校验 | §0.4.M4.2：过度工程 | M4 新增 |
| C13 | gsd #7 checkpoint json + Resume / Start fresh 闸门 | §0.4.M5.1：v1 PM 实证 NULL（Claude Code conversation history 已兜底"半成品续接"）| **PM 2026-05-25 否决 v1 → 重定义 v2** |
| C14 | 自动决定下一步动作 / 自动续上 | §0.4.M5.2：M5(v2) 只播报不决策；下一步推进归 M3 Decision gate | M5(v2) 边界 |
| C15 | gsd #5 heartbeat checkpoint 行 | PM-AI-Workflow 流程没那么长，价值密度低 | gsd 对照 |
| C16 | gsd #8 Orchestrator 红线 | 仅 1 个 subagent，场景对不上 | gsd 对照 |
| C17 | gsd #9 wave 并行 + 文件 overlap 降级 | 无并行 | gsd 对照 |
| C18 | gsd #2 thin wrapper（SKILL.md = 适配器）| 短期单 runtime，价值未到付钱时；扩 runtime 时再做 | gsd 对照 |
| C19 | gsd #1 lazy-load mode overlays | 配合 C18 一起做，本轮不付 | gsd 对照 |
| C20 | gsd #10 canonical_refs 累积器 | 高价值但范围独立，应走另一 D-* 文档（开新条目）| gsd 对照 |
| C21 | gsd #3 init JSON 单次注入 | 我们 status-view.py 已等价，本轮不重做 | gsd 对照 |

---

## §5 风险与待验

### 风险

| # | 风险 | 涉及 | 缓解 |
|---|---|---|---|
| R1 | Skill 工具在 skill 内 invoke 另一个 skill 技术可行性 | M1 vp-2 | **vp-2 前先做技术验证**；不行 fallback 阶段 C 提示 PM 自己发 |
| R2 | agent 调 shell 后 cwd 问题 | M1 vp-1 | 用绝对路径 `<target-dir>/...` 绕开，不真 cd |
| R3 | 阶段 C 太长 PM 没耐心 | M1 vp-2 | 分批问 + 选项简写答 + 收敛即止 |
| R4 | sub-skill 异常处理 | M1 vp-2 | 设计返回值约定；PM 中断告知"下次 `/project-solution` 续上" |
| R5 | Decision gate 闸门 label 写得不够明确，PM 仍歧义 | M3 vp-9 | gate-pattern.md 3 条硬规则 + 测试 grep "OK"/"Proceed"/"Continue" 模糊词；PM 选了仍可随时打字"停 / 等下"覆盖 |
| R6 | banner 在不同终端宽度下乱排 | M2 vp-7 | 固定宽度（80 字符）+ ASCII 字符；不用 box-drawing 重型字符 |
| R7 | narrative 模式幻觉（status-view 编造没有的进度）| M5 vp-11 | narrative 严格基于 `.req-meta.json` 字段 + git log 输出，不让 AI 自由发挥；测试覆盖空 req 场景必须输出"无 active req"而非编造 |
| R8 | askuser-rules 改动影响 7 个 skill 现有行为 | M4 vp-10 | 收敛前先 grep 现有规则差异，列差异表给 PM 确认 |
| R9 | 批 2 改动跨 7+ skill，回归风险大 | M2-M5 | 每 vp 跑 `tests/run-all.sh`；批 2 同步消费仓后单独跑一轮 PM 验收 |

### 待验

| # | 待验项 | 验证方式 | vp |
|---|---|---|---|
| V1 | PM 新流程实际体验对齐 gsd | 端到端跑新项目 | vp-5 |
| V2 | 阶段 C 5 组提问 PM 耐心阈值 | measure-tthw 计时 | vp-5 |
| V3 | brownfield 边界误伤 | 3 测试场景：空 / 含 git / 含代码无 git | vp-1 |
| V4 | **Skill 工具 skill-in-skill 技术可行性** | vp-2 前最小可行测试 | vp-2 |
| V5 | `/project-solution` 被调用 vs 独立调用体验一致性 | 端到端测两次 | vp-5 |
| V6 | banner 跨终端兼容 | macOS Terminal / iTerm2 / Claude Code 内置 | vp-7 |
| V7 | Decision gate label 在实战中是否一次拍板（无回头追问）| 跑 3 个完整 req 观察闸门答题率 / 是否 PM 答完后还要补打字 | vp-9, vp-13 |
| V8 | narrative 播报是否真让 PM 感觉"AI 记住我的进度"| 跑 3 次新 session 场景 + PM 自评（"够了 / 不够 / 多余"）| vp-11, vp-13 |
| V9 | askuser-rules 收敛后现有 skill 行为对齐 | grep 现有规则 diff + PM 确认 | vp-10 |

---

## §6 实证支撑

| 数据点 | 来源 | 涉及 |
|---|---|---|
| 4 处入口文案漏 `/project-solution` | grep：`scripts/init-project.sh:38, 275`、`README.md:63, 65` | M1 |
| AI 第一次答漏 `/project-solution` | 本次对话（2026-05-24）| M1 |
| mini-fill 设计意图 vs 实际遮蔽 | `skills/new-req/SKILL.md:68-102` | M1 |
| gsd 单一入口体验对比 | PM 提供 ChatBuilder 对话（2026-05-24）| M1 |
| 三步分裂体验 | `skills/init-project/SKILL.md:58` 唯一指引藏在 SKILL.md | M1 |
| gsd banner + Next Up pattern | `autonomous.md:62-69, 155-163` / `execute-phase.md:1725-1730` / `transition.md:494-509` | M2 |
| gsd Decision gate pattern | `new-project.md:368-380`（"Ready?" / "Create PROJECT.md" / "Keep exploring" 二选一 + Loop）| M3 |
| gsd `#3018 failure mode` | `discuss-phase.md:95-102` / `gsd-discuss-phase/SKILL.md:29-35` | M4 |
| gsd progress 聚合 workflow | `progress.md`（phase.complete / disk_status / roadmap checkbox 聚合）| M5(v2) |
| 我们 status-view.py 仅启动 render | `scripts/status-view.py`（150 行，纯 render）| M2 |
| 我们 req-stage-gate 内置续跑限内部 | `skills/req-stage-gate/SKILL.md`（619 行）| M3 |
| 我们 AskUser 规则散乱 | memory `feedback_open_questions_gate` / `feedback_close_default_flow` | M4 |

---

## §7 决策路径

| 阶段 | 决议 | 影响 |
|---|---|---|
| 2026-05-24 | D-iv M1 §0 锁定（init-project 三步分裂）| 进入 M1 方案 review |
| 2026-05-25 | PM 要求合并 M2-M5（gsd 借鉴）到一个设计文档 | 合并升级，文档改名 `init-project-入口一气呵成.md` → `入口与全流程体验顺畅性.md` |
| 待 PM 决策 | M2-M5 §0 是否锁定 / 是否合 M1 一起做 | 决定批 2 是否纳入 |

---

## §X Review Findings

> 等 M2-M5 §0 PM 共写并锁后，autoplan / eng-review / ceo-review 跑完落这里。
> **PAIN_LINK = NONE 且 EVIDENCE = ASSUMED 的 finding 默认 [DEFER]**。

### Round 0 — 2026-05-25 — 设计文档合并初稿

| # | Severity | Finding | PAIN_LINK | EVIDENCE | 决议 |
|---|---|---|---|---|---|
| — | — | （待 review）| — | — | — |

---

## §Y 决议日志（PM 与 AI 共审后写）

| 日期 | 决议 | 影响 |
|---|---|---|
| 2026-05-24 | Q1=a 沿用 `/init-project` 名字 | skill 文件名 / 入口命令不动 |
| 2026-05-24 | Q2=a 保持产出范围（PROJECT + roadmap，不扩 7 份）| 阶段 D 范围锁定 |
| 2026-05-24 | Q3=加 brownfield 检测 | vp-1 阶段 A 加检测分支 |
| 2026-05-24 | Q4=b 先写设计文档 | 本文件 |
| 2026-05-24 | PM 挑战澄清：`/project-solution` 是项目方向规划入口 | §1 阶段 C 改字面调用 + §2 表更新定位 + §3 vp-2/vp-3/vp-6 重估 + §4 加 2 条砍机制 + §5 加 Skill-in-skill 技术风险 |
| 2026-05-25 | PM 要求合并 gsd 借鉴的 M2-M5 到本设计文档 | 文档改名 + 5 模块并表 + 砍机制扩 C8-C21 + 实施扩 vp-7~vp-13 |
| 2026-05-25 | PM 锁 M2 §0（banner + Next Up）| M2 进 review |
| 2026-05-25 | PM 挑战 `--auto` chain flag 不合理 → 改 gsd Decision gate pattern；PM 锁 M3 §0 | vp-9 估时 1.5h→0.75h；§2 砍 chain-flow.md / auto_chain_active 字段 / .pm-workflow config 改动；C9/C10 重写为砍 `--auto` 整套；总估时 10.75h→10h |
| 2026-05-25 | PM 锁 M4 §0（AskUser 严格化）| M4 进 review |
| 2026-05-25 | PM 否决 M5(v1) gsd #7 checkpoint（实证 NULL）→ 主动决策更新 §0 重定义 M5(v2) "session 起始播报"+ 机制选 C（status-view narrative + CLAUDE.md 规则）| vp-11 估时 1h→0.5h；§2 砍 resume-rules.md + last_checkpoint 字段，加 status-view --narrative + CLAUDE.md 章程规则；C13/C14 重写；R7/V8 重定义；总估时 10h→9.5h |

---

**End of 入口与全流程体验顺畅性 v0**（M1 已锁；M2-M5 待 PM 共写 §0）
