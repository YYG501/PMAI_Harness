# **一、产品级功能需求**

**1. 项目接入与初始化**

- 系统需要支持 2种开始工作的方式：新产品启动；已有产品有新需求。
- 系统需要能从模板仓复制出真实项目仓。
- 系统需要在初始化时发现 gstack 是否已安装、是否可用，是否是最新版本，因为项目是强依赖gstack，所以一定需要安扎滚。。

**2. 阶段化项目推进**

- 系统需要支持从阶段 0 到阶段 6 的完整项目生命周期。
- 每个阶段都有明确输入、明确产出、明确边界。
- 每个阶段结束后必须等待 PM 确认，不能AI自动进入下一阶段。
- 系统需要支持可选阶段，如 solution-design，但不影响主流程可继续。
- 对于新项目，需要从阶段0开始推进。
- 对于已有产品有新需求，可以直接跳过前面的阶段，进入task。

**3. 文档驱动协作**

- 系统需要以结构化文件作为协作真相源，而不是靠对话上下文
- 系统需要保证 task 只引用文档，不在 task 里复制压缩后的长上下文，不要产生task传递时的信息损耗。

**二、模块与任务管理需求**

**5. Task 拆分与索引**

- 系统需要维护 tasks/TASKS.md 作为任务索引。
- 系统需要为每个任务生成独立 task 文件，记录目标、执行范围、验收标准、依赖、审查工具和状态。
- 系统需要支持 task 关闭后重开。

**6. Task 状态流转**

- 系统需要支持 task 从 待确认 -\> 执行中 -\> 待验收 -\> 已完成的生命周期。
- 系统需要强制通过受控入口修改状态，不能让 agent 直接手改状态字段。
- 系统需要在每个状态节点限制可执行动作，例如 待确认 时不能直接改代码。

**三、执行 harness 功能需求**
**7. Orchestrator / Suborchestrator / Execution Agent 分工**

- 系统需要区分 PM、主 orchestrator、suborchestrator、execution agent 四类角色。
- 主 orchestrator 负责流程推进、文档维护、验收呈报，不应直接承担标准 task 的实现。
- suborchestrator 需要对单 task 负责，包括 preflight、实现推进、自审、收口。
- execution agent 只是具体写代码的执行器，可被替换。

**8. Worktree 隔离执行**

- 系统需要为每个 task 创建独立 worktree。
- 系统需要保证主仓常驻 main，不要在主仓切 feature 分支。
- 系统需要把依赖目录如 node\_modules symlink 到 worktree，减少重复安装。
- 系统需要为每个 task 分配确定性的开发端口，便于 PM 验收和主仓对比。

**10. 并行执行**

- 系统需要允许多个 task 并行运行。

**四、Review 与验收需求**
**11. 自审机制**

- 系统需要让 suborchestrator 在实现完成后自行进入自审。
- 自审必须在 worktree 中执行，不能回主仓做。
- 自审工具要按 task 声明执行，而不是写死。

**12. PM 验收**

- 系统需要在 待验收 时向 PM 提供具体验收地址、实现内容、审查结果、重点检查项。
- 系统需要让 PM 做显式“通过 / 打回”决策。
- PM 打回后系统需要把 task 送回可继续执行状态。

**13. 收尾关闭**

- 系统需要在 PM 通过后自动完成 close：合并分支、删除分支、清理 worktree、归档 task、删除 runtime。
- 系统需要保存 task snapshot、日志、事件流等归档产物。
- 系统需要支持 close 前后的文档偏差检查与必要更新。

**七、经验与框架反馈需求**
**20. 经验沉淀**

- 系统需要记录仓库级经验和用户级个人偏好。
- 系统需要在 task 开始前召回经验，在 task 结束后沉淀经验。

**21. 框架问题反馈**

- 系统需要支持把框架 bug、hook 误拦、规则缺失等问题提交到框架 issue 系统。
- 系统需要支持框架维护会话读取 issue、修复、关闭 issue。
- 系统需要支持业务项目通过升级脚本获得框架修复。