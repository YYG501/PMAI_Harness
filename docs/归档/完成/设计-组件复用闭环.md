# 组件复用机制（v3）

## 问题

AI 实现新页面时不复用项目中已有的组件和页面样式。

## 根因

AI 在实现时看不到已有组件的代码。当前 task 的「启动前必读」只列文档（DESIGN.md、CONTEXT.md、模块规格），不列已有的源码文件。AI 对项目中已存在什么组件一无所知。

## 解法

**在 task-plan 生成 task 文件时，把相关的已有页面/组件源码加入「启动前必读」列表。**

工作流中已有这个机制：
- task-plan 步骤 5 生成 task 文件，填写「启动前必读」字段
- task-execute 步骤 2 按列表逐个读取

缺的只是：task-plan 在填「启动前必读」时没有把已有源码文件放进去。

## 改动

### 1. `skills/task-plan/SKILL.md` — 步骤 5 增加源码参考

在步骤 5「生成 task 文件」中，「启动前必读」的填写规则里增加：

```markdown
**启动前必读（填写规则）：**

按以下优先级填写，文档在前、源码在后：

1. `docs/modules/<module>.md` — 模块工作规格（必填）
2. `docs/DESIGN.md` — 视觉规范（UI task 必填）
3. `docs/CONTEXT.md` — 项目背景（首个 task 必填，后续可省略）
4. **已有参考源码**（UI task 必填，非 UI task 按需）：
   - 用 Glob 扫描项目中的页面和组件目录，找到与本 task 最相关的已有实现
   - 优先选择：同类型页面（如本 task 做列表页，找已有的列表页）、共享布局组件、共享 hooks
   - 每个 task 列 2-5 个参考文件，不要贪多（AI 会自行探索发现更多）
   - 标注参考理由，如：`src/pages/UserList.tsx — 参考列表页布局和筛选模式`
```

### 2. `templates/task.md.tmpl` — 「启动前必读」增加源码占位

```markdown
## 启动前必读（按顺序，读完再执行）

1. `docs/modules/{{MODULE_NAME}}.md` — 模块工作规格
2. `docs/DESIGN.md` — 视觉规范
3. `docs/CONTEXT.md` — 项目背景
4. （参考源码，由 task-plan 根据已有代码填写）
```

### 3. `skills/task-execute/SKILL.md` — 步骤 2 明确源码参考的用法

步骤 2 当前只说"理解模块规格和接口约定"。增加：

```markdown
### 步骤 2：读取必读文档

按「启动前必读」列表，逐个读取文档内容。理解：
- 模块规格和接口约定
- 设计系统规范（DESIGN.md）
- 项目背景（CONTEXT.md）
- **参考源码中的组件结构、props 接口、样式模式、布局方式**

如果列表中包含已有页面或组件的源码，在实现时：
- 复用其中的共享组件（import，不要重写）
- 遵循其布局模式和样式约定
- 如果需要新建组件，先确认参考源码中没有可复用的
```

## 不改动的部分

- `scripts/` — 不需要新校验
- `skills/close-task/SKILL.md` — 不需要维护步骤
- `skills/req-stage-gate/SKILL.md` — 不需要改动
- `templates/DESIGN.md.tmpl` — 保持纯视觉规范
- 不新增任何 skill、模板或脚本

## 为什么这个方案比 v1/v2 好

| 维度 | v1（注册表闭环） | v2/v2.1（autoContext Skill） | v3（启动前必读） |
|------|-----------------|---------------------------|----------------|
| 新增文件 | 0 | 1 模板 | 0 |
| 改动文件数 | 5 | 4 | 3 |
| 新增概念 | 组件注册表、复用计划、复用审计 | component-guide skill、autoContext 分层 | 0（用现有机制） |
| source of truth | 手工维护的表格 | 手工维护的 YAML 列表 | 代码库本身（每次 task-plan 重新扫描） |
| 过期风险 | 高（注册表不更新就失效） | 中（autoContext 列表不更新就失效） | 无（每次 task-plan 实时发现） |
| 维护成本 | 每次 close-task 复杂流程 | 每次 close-task 追加路径 | 零（无需维护） |
| AI 感知深度 | 摘要行 | 自动加载源码 | task-plan 选择的 2-5 个最相关源码 |

**核心优势：没有需要维护的中间状态。** 每次 task-plan 都从代码库实时扫描，发现最相关的已有实现。不存在过期、漏填、死路径的问题。

## 局限

- **依赖 task-plan 的判断质量**：task-plan 可能选错参考文件或遗漏。但这比"完全看不到"好得多，且 AI 在实现时仍可自行探索。
- **不是硬约束**：AI 看到了参考代码不代表一定会复用。但 /review 和 /design-review 自审步骤会检查视觉一致性和代码质量。
- **首个 req 无参考**：第一次建项目时没有已有代码可参考，这是正常的。

## 验证

1. 修改 3 个文件
2. 运行 `tests/` 确认无回归
3. 在 ExampleConsumerB 下一个 UI task 中验证：task-plan 是否在「启动前必读」中列出了已有源码，task-execute 是否基于参考实现了复用
4. 同步部署到 ExampleConsumerB
