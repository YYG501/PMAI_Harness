# 设计 — 原型与系统双模式

## 一、背景与问题

### 现状

PM-AI-Workflow 生成器目前已有 stage / skill / hook / template 完整体系，并且在 `solution.md` / `solution.engineering.md`、`task.md` / `task.engineering.md` 上已经做了 "engineering 后缀" 的双轨——区分 PRD 视角与开发视角的产出。

但目前框架只有**单一完整度档位**：spec 按完整功能清单写，下游 task 按完整功能清单派生，最终代码按完整功能清单实现。

### 痛点

按现状跑出来的产物，在"原型阶段"会失控。参考案例（旧仓 / 类似生成方式产出）：

- 50,408 行代码 / 300 文件 / 75 个路由页
- 三端各实现一份 CRUD（tenant / ops / main-ops）
- 单页过长：`TenantDepartmentDetailPage.tsx` 1373 行、`TenantLicensesPage.tsx` 1197 行
- framework 抽象层 3000 行 / 33 文件

这个体量已经超过"配合文档给团队讲清功能和页面交互"的原型语义，向 MVP 雏形漂移——PM 既不敢大改也达不到上线质量。

### 需求

- **prototype 模式**：快速推进、代码量小、复杂度低，但仍保证组件复用与交互质量
- **system 模式**：保持现有的工作模式与产出质量

---

## 二、核心设计原则

1. **mode 只影响两件事**：spec 消费段 + task 拆分规则
2. **stage 流程、skill 名称、hook、确认门**——全部共用，不分叉
3. **mode 在 `new-req` 阶段一次性决定**，中途不换档（要换 = 重开 req）
4. **mode 与 engineering 后缀正交**：
   - engineering 后缀维度 = "开发视角 / PRD 视角"
   - mode 维度 = "原型 / 系统"
   - 两个维度独立，不混合到同一个字段

> 这条原则是为了避免框架本身因支持双模式而膨胀。如果在每个 stage / skill 里都加 `if mode == prototype / else` 分支，框架会变成它自己最讨厌的样子。差异必须集中。

---

## 三、双模式覆盖范围

| 维度 | prototype | system |
|---|---|---|
| 组件复用 | ✅ 强制 | ✅ 强制 |
| 交互质量门（主路径走通、状态切换正确） | ✅ 强制 | ✅ 强制 |
| spec 消费段 | §六 原型演示路径 | §五 功能清单 |
| task 粒度 | 粗（一路径/一交互 = 一 task） | 细（按 task 拆分反模式约束） |
| task 间依赖序 | 弱（路径间基本独立） | 强（数据 → API → UI → 测试） |
| 架构决策前置（权限模型 / 数据层 / API 契约） | ❌ 不要求 | ✅ 在 brief / analysis 阶段钉死 |
| 测试 / 类型 / lint 硬质量门 | ❌ 不要求 | ✅ 强制 |
| 边界态 / 空数据 / 错误态 task | ❌ 不生成（写在 §五 文档里即可） | ✅ 生成 |

**叠加结构**：system = prototype + 5 条追加项。两个模式共用前两条基础能力。

---

## 四、改造分块

### 块 0 — 前置依赖：worktree 文档同步（必须先做）

#### 0.1 问题描述

执行 task 时，codex / cursor-agent 在 task worktree 里看不到完整 spec / analysis / 其他需求文档。

**物理证据示例**：
- task worktree HEAD = `f51c448`（早期一次 "Merge main into req-001" commit）
- task worktree 在 `task-003` 分支
- `git ls-tree HEAD requirements/active/req-001-xxx/tasks/` 在 task worktree 里**找不到** task-003 spec 文件
- 但 req worktree 里同一文件完好（588 + 990 行），且 git status 不显示 modified（说明已 commit 进 req 分支）

#### 0.2 根因

`scripts/create-task-worktree.sh` 第 73-77 行的逻辑：

```bash
if 分支已存在:
  git worktree add <worktree> <branch>          # 用 task 分支已有的 HEAD
else:
  git worktree add -b <branch> <worktree> <REQ_BRANCH>   # 新建 task 分支，基于 req
```

**关键缺陷**：task 分支**只在首次创建**时与 req 分支同步。task 分支创建之后，req 分支上的任何更新（spec 修订、新增 analysis、component-registry 增长、其他 task 文件 commit）**都不会同步**到已存在的 task worktree。

`scripts/build-execution-prompt.py` 显式拒绝 inject 文件内容（理由：避免 drift，文件是 single source of truth）。这个设计有个隐含假设：**task worktree 里能按路径读到这些文件**。task 分支落后于 req 分支时，假设破裂——执行者按 prompt 给的路径读，但 task worktree 里那个路径根本不存在或是旧版本。

#### 0.3 修复方向

**核心策略**：每次 task 执行前，从 req 分支强制 sync **两层文档路径**到 task worktree，但**不 commit 进 task 分支**（task 分支不承载需求文档与项目级文档的版本）。

**两层 sync 白名单**：

```
# 第一层：req 级（每个 task 必须看到）
requirements/active/<req-id>/

# 第二层：项目级（task worktree 也要读到最新）
DESIGN.md
CLAUDE.md
component-registry.md   ← 块 D 实施时新增（项目级单文件）
```

第二层**从 req 分支拉**（不是 main）：原型阶段允许快速迭代项目级文档，task 立刻看到。多 req 并行时的漂移问题，纪律放在 `close-req` 阶段卡（合并 main 时校验），不在 `task-execute` 阶段卡。

**具体实施**：

新增 `scripts/sync-req-docs.sh`：

```bash
#!/usr/bin/env bash
# 从 req 分支同步需求目录与项目级文档到 task worktree（保持 unstaged）
set -euo pipefail

WORKTREE="$1"     # task worktree 绝对路径
REQ_BRANCH="$2"   # req 分支名
REQ_ID="$3"       # e.g. req-001-tenant-console-redesign

PATHS=(
  "requirements/active/$REQ_ID/"
  "DESIGN.md"
  "CLAUDE.md"
  "component-registry.md"
)

for p in "${PATHS[@]}"; do
  # 文件可能在某些 req 还不存在（如 component-registry.md 块 D 上线前）
  if git -C "$WORKTREE" cat-file -e "$REQ_BRANCH:$p" 2>/dev/null; then
    git -C "$WORKTREE" checkout "$REQ_BRANCH" -- "$p"
    git -C "$WORKTREE" reset HEAD -- "$p" 2>/dev/null || true
  fi
done
```

**调用时机（两处都要）**：

1. `scripts/create-task-worktree.sh` 末尾：worktree 创建后立即 sync 一次
2. `skills/task-execute/SKILL.md` 启动步骤：每次执行 task 前重新 sync（覆盖 worktree 创建之后的更新）

**为什么不用 merge / rebase**：
- merge 会引入 commit 噪音并污染 task 分支历史
- rebase 风险更大，可能丢 task 分支的代码改动
- `git checkout <req-branch> -- <path>` 是精准操作：只动白名单路径，不动代码改动，零冲突风险

**为什么保持 unstaged**：
- task 提交时（task-submit）只 commit task worktree 里的代码改动
- 需求文档与项目级文档由 req worktree / req 分支维护，task 分支不参与版本管理
- 这一点要在 `scripts/check-task-scope.py` 加校验：禁止 task commit 包含 sync 白名单路径下的改动

**事件记录（不是 lock）**：

并行 task 执行不需要 lock——每个 task worktree 是独立物理目录，git index 也独立，`git checkout <branch> -- <path>` 互不抢资源。

但要在 `scripts/audit-task-events.py` 加一项：每次 sync 记录 `req-branch HEAD hash + sync 后白名单内文件 hash`。出问题时能追溯"这次执行用的是哪个版本的 spec / DESIGN / registry"。

**历史 commit 处理**：旧 task 分支可能历史性 commit 过需求文档。**不回溯改写**——sync 是覆盖式 checkout，下次 task-execute 自动用 req 分支最新内容覆盖，旧 commit 不影响新行为。这条与 memory "codex 前 commit checkpoint，不给事后救回幻觉" 同源原则。

#### 0.4 与双模式改造的依赖关系

块 A / B / D / E 都依赖"task worktree 能读到最新需求文档"：

- 块 B（spec 双段落） → §六 演示路径修订必须能传到 task
- 块 D（组件复用闭环） → component-registry 增长必须能被后续 task 看到
- 块 E（交互质量门） → 闸门校验依赖 spec 最新内容

**块 0 不修复，其他块的产物 task 都拿不到。** 必须最先做。

#### 0.5 验证

修复后，跑以下测试：

1. 创建 req-001，new-task 生成 task-001
2. 在 req worktree 修改 task-001 spec（追加内容）+ commit 到 req 分支
3. 在 task-001 worktree 触发 task-execute
4. **期望**：task worktree 能读到 task-001 spec 的最新内容（包含步骤 2 的追加部分）

修复前，第 4 步会读到旧版本（task 分支落后）；修复后必须读到最新。

---

### 块 A — mode 入口

**目标**：让 mode 字段稳定贯穿到所有 skill 与 script。

**mode 落点**：独立 `req.meta.json`，放在 req 根目录：

```
requirements/active/req-001-xxx/
├── req.meta.json         ← 新增 { "mode": "prototype" | "system", "stage": "...", "created_at": "..." }
├── analysis.md
├── solution.md
├── modules/
│   └── module-xxx.md
└── tasks/
```

**为什么不用 CONTEXT.md**：CONTEXT.md 是项目级文档（项目名 / 技术栈 / 约束 / 风险），不是 req 元数据。把 req 级的 mode 塞进去会让 CONTEXT 在每个 req 下被改写、污染语义。

**为什么不用 markdown frontmatter**：mode 被 script 频繁读取（`req-transition.py` / `task-transition.py` / `task-plan` skill），json 解析比 markdown 抽字段稳。req.meta.json 还能扩展承载未来其他 req 级元数据。

**改动点**：

- `skills/new-req/SKILL.md`：brief 阶段强制选 `mode: prototype | system`，**无默认值**（避免 PM 漏选默认走重的）
- `scripts/init-project.sh` / 新建 req 流程：生成 `req.meta.json`
- `scripts/req-transition.py` / `scripts/task-transition.py`：每次 transition 时读取并透出 mode 字段
- `scripts/status-view.py`：在 req 状态展示中显示 mode（PM 视觉提示）
- 后续所有 skill 从 `req.meta.json` 读取 mode，**不允许 AI 自行推断**

**老 req 兼容**：`req.meta.json` 不存在时，script 默认按 `system` 模式处理（兼容档），保持现有行为。改造完成后老 req 自然 close，进入纯新流程。

**验证**：跑一遍 `new-req`，确认 `req.meta.json` 生成且 mode 字段能被 `req-stage-gate` 读取。

---

### 块 B — spec 双段落

**目标**：spec 同时承载"开发契约"与"原型范围"，两份产物从同一份 spec 派生。

**事实定位**：
- §五 功能清单实际挂在 `templates/module.md.tmpl` 第三章"功能清单（硬约束）"
- `req-prd.md.tmpl` 是 stage 6 终态产出（"一次性产出，定稿后不修订"），是开发契约文档，**不承担原型派生职责**
- 因此双段落都挂在 `module.md.tmpl`

**改动点**：

修改 `templates/module.md.tmpl`，在第三章（§五 功能清单）之后插入新章节，原"四、页面与交互范围"及之后章节顺延：

```
## 三、功能清单（硬约束）         ← 现有 §五，不变
## 四、页面与交互范围              ← 现有，不变

## 五、原型演示路径                ← 新增 §六（重新编号为五）

> 仅 prototype 模式消费。system 模式可标记"全部演示"或与 §三 同范围。
> 必须按"路径"组织，不是按"功能点"——否则等于复读 §三。
> task-plan 在 prototype 模式下读这一节，不读 §三。

### 路径 1 · [路径名]
- 起点 → 屏幕 A → 操作 → 屏幕 B → 关键状态切换 → 终点
- 沿路径要演示的状态：[loading / 成功 / 空态 / ...]

### 路径 N · ...

### 不演示（仅在 §三 文档化，不进原型代码）
- [功能点]：原因

## 六、硬约束                      ← 原"五、硬约束"顺延
## 七、本批不做                    ← 顺延
## 八、验收标准                    ← 顺延
## 九、跨模块依赖与占位策略       ← 顺延
## 十、Task 拆分提示              ← 顺延（引用新章节）
```

`skills/req-analysis/SKILL.md` + `skills/req-solution/SKILL.md`：产出要求增加新章节必填，且对 prototype 模式必须明确"不演示"清单。

**反模式防线**（关键）：

AI 倾向把 §六 写成 §五 的复读版——这样双段落形同虚设。必须在 skill few-shots 里写一个**反例**：

```
错误示范：
§六 原型演示路径
  - 用户能新建部门
  - 用户能编辑部门
  - 用户能删除部门
  ...（与 §五 完全同构）

→ 这等于没分段。§六 必须按"路径"组织，不是按"功能点"。
```

正例：

```
§六 原型演示路径
  - 路径 1：首页 → 部门列表 → 新建弹窗 → 填名称 → 保存 → 列表刷新 → 点详情 → 看到成员
    关键状态：保存中 loading / 保存成功 toast / 列表无数据空态
  - 路径 2：（不做 — 删除部门相关交互在 §五 文档说明，原型不演示）
```

---

### 块 C — task-plan 双规则

**目标**：根据 mode，task 拆分读不同段、用不同粒度规则。

**改动点**：

`skills/task-plan/SKILL.md` 按 mode 分支：

```
if mode == prototype:
  source = spec.§六 原型演示路径
  granularity = 粗（一路径/一交互 = 一 task）
  dependency = 弱
  generate_test_task = false
  generate_edge_state_task = false

if mode == system:
  source = spec.§五 功能清单
  granularity = 细（按 task 拆分反模式约束 — 见 memory）
  dependency = 强（数据 → API → UI → 测试）
  generate_test_task = true
  generate_edge_state_task = true
```

`scripts/check-task-scope.py`：按 mode 调阈值（原型允许更粗的 task，系统按现有阈值）。

---

### 块 D — 组件复用闭环（两模式共有 / 收益最大）

**目标**：解决"三端各做一份 / 1373 行详情页"这类根因——重复造轮子。

**这是改造里收益最大的一块。** 原型代码体量是否能真的轻下来，主要看这块。

**改动点**：

1. 新增 `templates/component-registry.md.tmpl`：项目级单文件（**不按模块拆分**），路径为项目根 `component-registry.md`
   - 单文件原因：原型阶段组件库共享性最高、按模块拆会重复登记；YAGNI，体量大了再拆
   - 维护职责：跨 req 共享资产，由块 0 的 sync 第二层白名单负责传到 task worktree
2. `skills/task-plan/SKILL.md` **硬挂一步"复用检查"**：
   - 读 component-registry
   - 对每条待生成 task，回答："这个交互/视图是否已有组件覆盖？"
   - 命中 → task 标记 `reuse: <component-id>`，不新建
   - 未命中 → task 生成时同步登记新组件到 registry
3. `scripts/check-task-scope.py` 增加一项：检测 task 描述是否暗示"重复造轮子"（同名组件、同结构页面），命中则阻断

> ⚠️ 这一步必须做成 **skill 硬步骤**，不能只在 prompt 里"建议 AI 检查复用"。参考已有 memory：AI 倾向用 ad-hoc 替代多步 skill 流程。

**与既有设计对齐**：根目录已存在 `设计-组件复用闭环.md`，本块实施前需与之合并，避免另起炉灶。

---

### 块 E — 交互质量门

**目标**：原型不能"看起来像但点不动"——这是原型的命门。

**最小闸门接口**：闸门只校验"接口"，不绑定工具品牌。证据用什么形式（gstack-browse 自动跑、手动截图、录屏、其他），由 PM 按 task 类型自决。

**为什么不做 evidence-adapter 模式**：与 exec-adapters（codex / cursor-agent / manual）的差别——exec-adapters 的不同执行者**调用协议、prompt 格式、写回路径都不同**，需要适配层；evidence 不管哪种工具产出，**最后都是"文件 + 描述"，本质同构**，加 adapter 是反向膨胀。

**改动点**：

`skills/close-task/SKILL.md`（或 `skills/task-submit/SKILL.md`，根据当前收尾流程挂载位置）加两条校验：

```
两种模式都校验：
  1. requirements/active/<req-id>/tasks/<task-id>.evidence/ 目录非空（任何文件即可）
  2. close-task 闸门里 PM 填一段"演示证据描述"：自由文本，写"演示了哪条路径 / 走通了什么"

system 模式额外保留：
  - 现有质量门（测试 / 类型 / lint）
```

**闸门提示文字**（让 PM 知道有哪些工具可选）：

```
请提交本次原型交互的演示证据到 <task-id>.evidence/ 目录。
推荐工具：
  - gstack-browse 自动跑路径，截图 + console log
  - 手动截图 / 录屏
  - 任何能让你和团队看清交互的方式

写法：套未决问题闸门写法，不留 FORCE 逃生舱（闸门必须填证据描述）
```

**per-project 协议**：项目想固定用某个工具，PM 自己在项目 CLAUDE.md 写一句约定即可。**不进框架代码**——这是 PM 自己的协议，不是框架要承载的事。

---

## 五、不动的边界

为避免后续 AI / 自己改写时把 mode 分支扩散到不该去的地方，显式列出**不分叉**的部分：

- `skills/req-stage-gate/SKILL.md`
- `skills/close-req/SKILL.md` / `skills/cancel-req/SKILL.md`
- `skills/quick-fix/SKILL.md`
- stage 数与 stage 之间的转换关系
- review-bundle / finalize-review 流程
- engineering 后缀双轨（与 mode 正交，独立保留）
- task-execute / task-submit 的执行框架（执行者可选、worktree、event 系统）

---

## 六、落地顺序

### 前置稳定性依赖（块 0 之前必修）

诊断显示当前 main 分支上有 5 条与"新两文件 task 格式"相关的 P0/P1 bug，已经 sanity-check 验证全部存在：

- `task-transition.py` 找不到新格式的"文档偏差 / 自审记录" section（已跨文件到工程合同）
- `check-branch.sh` 不识别新表格状态字段，hook 拦截被绕过
- `close-req.sh` / `cancel-req.sh` 漏掉新格式 task
- `build-execution-prompt.py` 给外部执行器错的 worktree
- 测试 fixture 仍生成旧单文件，新格式无覆盖

**不修这些，块 0 的 sync 机制写完无法验证**——task-transition 卡死、close/cancel 漏 task、外部执行器拿错 worktree。双模式建立在不稳的 task 闭环上没有意义。

详细设计见 `设计-新两文件格式对齐.md`，其中明确了根因（缺 shared task parser）与改造方案（集中收口到 `scripts/_lib/task_parser.{py,sh}`）。工作量估计 2-3 天。

### 完整落地顺序

按"依赖关系 / 风险递增"排序：

1. **格式对齐（前置）** — 见 `设计-新两文件格式对齐.md`。完成后 task 闭环重新可用。
2. **块 0（worktree 文档同步）** — 所有双模式块的前置。不修这个，块 B/D/E 的产物到不了 task worktree，整个改造空转。
3. **块 A（mode 入口）** — 改动最小，给后续块提供分支依据。验证 mode 字段能稳定贯穿到所有 skill / script。
4. **块 B（spec 双段落）** — 改 1-2 个模板 + 改 2 个 skill 的产出要求 + 写反例 few-shot。
5. **块 D（组件复用闭环）** — 新增 registry 模板 + task-plan skill 硬步骤 + check-task-scope 阻断。这块自带验证：跑一次自举看 task 数 / component 数比例。
6. **块 C（task-plan 双规则）** — 在 A / B / D 稳定后再上，依赖前几块的产物。
7. **块 E（交互质量门）** — 最后接上，套现有未决问题闸门写法即可。

---

## 七、四个最容易翻车的点

### 7.0 task 分支创建后 req 分支文档更新被忽略（块 0 范围）

**症状**：spec / analysis / component-registry 在 req 分支已更新并 commit，但 task worktree 里读到的还是旧版本（甚至文件根本不存在）。
**后果**：执行者按陈旧 / 缺失文档干活，产物与最新 spec 不一致，且无法察觉。
**防线**：
- `create-task-worktree.sh` 末尾必须调用 `sync-req-docs.sh`
- `task-execute` skill 启动步骤必须再调用一次 `sync-req-docs.sh`（覆盖 worktree 创建之后的更新）
- `check-task-scope.py` 校验：task commit 不允许包含 `requirements/active/<req-id>/` 路径
- `audit-task-events.py` 增加一项：执行前后 spec 文件 mtime / hash 对比，异常时记录事件

### 7.1 §六 演示路径写成 §五 复读版

**症状**：§六 的内容仅仅是把 §五 的功能点逐条罗列。
**后果**：双段落形同虚设，原型仍按完整功能清单铺开。
**防线**：
- skill few-shots 必须有反例
- skill 流程里加自检步骤"§六 是按路径组织还是按功能点组织？"
- 第一次跑 prototype req 时 PM 亲自审 §六

### 7.2 组件复用步骤被 AI 跳过

**症状**：task-plan 没读 component-registry，直接按 spec 生成新组件。
**后果**：原型仍然重复造轮子，块 D 失效。
**防线**：
- skill 流程做成多步硬挂，不靠提示
- check-task-scope.py 检测到疑似重复造轮子时**阻断**而非"提醒"
- close-req 时审计 registry 增长率（合理：随路径数线性增长；异常：随 task 数线性增长）

### 7.3 PM 中途想换档

**症状**：prototype 跑到一半，PM 说"我想升级到 system 继续做"。
**后果**：把原型档的债务带进系统档，比从头做系统档还重。
**防线**：
- 显式禁止：no in-place upgrade
- 提供迁移路径："另开一个 system 模式的 req，复用 §五，不复用任何原型代码 / task"
- 这个原则与 memory 中"codex 前 commit checkpoint，不给事后救回幻觉"同源

---

## 八、验证方式

改造完成后，用框架**自己**跑一次自举验证：

### 验证 0：worktree 文档同步（块 0 单独验证）

在做后续验证之前，先单独验证块 0：

1. 创建 req-001，new-task 生成 task-001
2. 进入 req worktree，修改 task-001 spec（追加一段可识别内容）+ commit 到 req 分支
3. 进入 task-001 worktree，触发 task-execute
4. ✅ **期望**：task worktree 能读到 task-001 spec 的最新内容（包含步骤 2 的追加段）
5. 反向验证：在 task-001 worktree 尝试修改 spec 并 commit
6. ✅ **期望**：被 `check-task-scope.py` 阻断（task 分支不该 commit 需求文档）

### 验证 1：prototype req

跑一个简单需求（如"用户管理后台"）的 prototype 模式：

- ✅ task 数应在个位数 / 低十位数（一条主路径几个 task）
- ✅ component-registry 命中率应较高（同类交互复用）
- ✅ 单 task 描述简短（粗粒度）
- ✅ 无测试 / 边界态 / 空数据 task
- ✅ 主路径能点通

### 验证 2：system req

跑同一个需求的 system 模式：

- ✅ task 拆分粒度按反模式约束
- ✅ 测试 / 类型 / lint task 齐全
- ✅ task 间依赖序明确（数据 → API → UI → 测试）
- ✅ 架构决策（权限 / 数据 / API）在 analysis 阶段已落
- ✅ 现有所有质量门保持

两个模式都通过 → 改造收口。

---

## 九、与已有设计文档的关系

本设计需要与以下既有设计文档对齐 / 合并：

- `设计-组件复用闭环.md` → 块 D 的具体实施需要继承这份文档的设计
- `设计-功能规格文档增强.md` → 块 B 的 spec 双段落需要与这份文档的增强方向协调
- `设计-stage2-3-skill改造.md` → 块 B 涉及 req-analysis / req-solution，需要与 stage 2-3 改造保持一致
- `设计-stage5-6-task循环.md` → 块 C / E 涉及 task-plan / close-task，需要与 stage 5-6 循环保持一致

落地实施前需先做**设计文档对齐 review**，确认无冲突再动 skill / template / script。

---

## 十、决议记录

实施前的 8 个待回答问题已全部决议。以下是决定与理由摘要，作为后续实施时的参考依据。

### 块 0 相关

| # | 问题 | 决议 | 理由 |
|---|---|---|---|
| 1 | `sync-req-docs.sh` 同步白名单 | 两层：①`requirements/active/<req-id>/` 全目录；② 项目级 `DESIGN.md` / `CLAUDE.md` / `component-registry.md` | 仅同步 req 目录漏掉跨 req 共享资产；项目级文件需要 task worktree 也能读到最新 |
| 1.1 | 第二层从哪个分支拉 | 从 req 分支拉（不是 main） | 原型阶段允许快速迭代项目级文档；多 req 并行漂移由 close-req 阶段卡，不在 task-execute 卡 |
| 2 | 历史 commit 处理 | 不回溯改写，新规则只对新 task 生效 | sync 是覆盖式 checkout，下次 task-execute 自动用最新内容覆盖；与 memory "不给事后救回幻觉" 同源 |
| 3 | 并行执行 lock | 不需要 lock，但加事件记录 | 每个 worktree 物理目录与 git index 独立、不抢资源；事件记录用于追溯"用的是哪个版本的 spec" |

### 块 A-E 相关

| # | 问题 | 决议 | 理由 |
|---|---|---|---|
| 4 | mode 字段落点 | 独立 `req.meta.json`，放 req 根目录 | CONTEXT.md 是项目级文档不该承载 req 元数据；mode 被 script 频繁读，json 比 markdown 抽字段稳 |
| 5 | §六 挂哪份模板 | 挂 `module.md.tmpl`，作为新章节"五、原型演示路径"，原"五"及之后顺延 | §五功能清单实际就是 module.md 的"三、功能清单"；req-prd.md 是 stage 6 终态产出不承担派生职责 |
| 6 | component-registry 粒度 | 项目级单文件（`component-registry.md` 在仓库根） | 原型阶段组件库共享性最高，按模块拆会重复登记；YAGNI |
| 7 | 交互质量门证据形式 | 最小闸门接口：evidence 目录非空 + PM 描述非空，工具 PM 自决 | 不绑定具体工具品牌；不做 adapter 模式（与 exec-adapters 不同：所有证据本质同构，无需适配层） |
| 8 | 老 req 处理 | 块 0 全应用、块 A-E 仅新 req（兼容档 = system） | 块 0 是 bug 修复对所有 req 应用；块 A-E 是新流程，老 req 让其自然 close |

### 实施起点

8 条决议已落到对应改造块的实施细节中（见 §四）。落地顺序仍按 §六：**前置格式对齐 → 块 0 → 块 A → 块 B → 块 D → 块 C → 块 E**。

---

## 十一、轻重平衡自检

PM 视角对当前框架最大的反馈之一是"流程重 / 心智成本高 / v4 手动协调成本大"。本设计新增了若干物件：

- `req.meta.json`（块 A）
- `module.md` 第五章新增"原型演示路径"（块 B）
- `component-registry.md`（块 D）
- `<task-id>.evidence/` 目录（块 E）

这些会不会让框架更重？

### 自检通过的理由

1. **mode 是 PM 在 `new-req` 阶段一次性选**，不是每个 task 都要选。日常 task 心智成本未增加。
2. **`req.meta.json` 由 script 维护，PM 不用手写**。读取由 `status-view` 等工具呈现，PM 不用主动查。
3. **`component-registry.md` 是块 D 的强制产物，任何项目都受益**（不只是双模式）——这是底层能力增益，不是双模式的额外税。其同步成本由块 0 的 sync 第二层白名单消化。
4. **evidence 目录是 close-task 一次提交**，与现有"task 完成时的提交说明"等惯例同构，不增加新协议；且最小闸门接口已尽量轻——目录非空 + 一段描述。
5. **spec 双段落（块 B）只在 prototype 模式下消费第五章**，system 模式行为与现状一致——**system 模式 PM 心智零增加**。

### 仍可能的隐性成本

- **prototype 模式下 PM 要学会"按路径"写演示路径**（不是按功能点）。这是新心智，但被 §五功能清单兜底（写不成路径就强制反例提示），不会让 PM 卡住。
- evidence 目录维护对 PM 来说是新动作。已用最小闸门接口压到 "目录非空 + 自由文本描述"。

### 红线（设计护栏）

如果未来扩展时发现 mode 分支扩散到 §五"不动的边界"以外的 stage / skill / hook（违反 §二 原则 1 "mode 只影响两件事"），**必须停下来重新审视，而不是继续叠加**。这一条作为本设计的护栏——一旦突破，框架会回到"为支持双模式而膨胀"的失败模式。

### 与现有反馈的呼应

诊断里 PM 视角"框架重 / 心智成本高"的判断主要指向当前主分支的格式对齐 bug 与 v4 手动协调成本——这些**与双模式正交**：

- 格式对齐：见前置稳定性依赖小节，独立处理
- v4 手动协调：是执行模式选择问题，与双模式无关，不在本设计范围

本设计不让这些既有问题变重，也不假装能解决它们。
