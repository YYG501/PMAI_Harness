<!-- TODO — PM 待办池（本包内）

固定路径：TODO.md
作用：PM 提过 / 讨论过想做的事，随手记一笔。无序，不排顺序。
      这不是「规划真相源」（项目方向真相源是 PRODUCT.md），就是 PM 自己的待办清单。

谁在何时改：
  - /pmai-init-project 的首次初始化 / 已有代码接入分支，只记 PM 主动提过 / 讨论过的待办。
  - 自动 finalize 只写 PM 在当前工作中明确留下的后续项；工作流自己完成同步，不要求再跑 record。
  - 没有 active work 时，PM 明确说「记一下」可由 `/pmai-record` 补录一条已经确认的后续事项。
    标题带一句话自包含（建议目标 + 涉及模块 / 文件），下次起步直接看懂。
    AI 不从代码、竞品或历史归档反推填充，也不替 PM 排优先级。
    遗留真相源单一在本池子；收尾报告只渲染指针「已转入 TODO 第 N 条」。
  - PM 随时手动增删（想到新的就记一条，不想做了就划掉）。
  - PM 要起新需求时，AI 提醒「池子里有这些待办」让 PM 挑一个，
    不替 PM 判断「下一个该做 X」。

状态取值（三态，简单）：
  - todo   — 讨论过 / 提过想做，还没开始
  - doing  — 正在做（已进入 /pmai-design 或 /pmai-build）
  - done   — 做完了（已 close）

字段：工作标识（进入完整工作后可回填，未起前留空）/ 标题 / 状态
      —— 没有「排序」列，待办池不排顺序。
-->

# PMAI E2E Fixture · 待办池 (TODO)

> PM 提过 / 讨论过想做的事。无序，不排顺序 —— 要做时挑一个。
> 状态：todo（想做）/ doing（在做）/ done（做完）。

| 工作标识 | 标题 | 状态 |
|---|---|---|
|  | <你想到要做的第一件事> | todo |
