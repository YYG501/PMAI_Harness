# 发布前变更确认：决定

## D1 两态确认状态

- 结论：首版只使用“待处理”和“已确认”两种状态。
- 依据：测试夹具的产品范围确认；回退路径是后续若实际需要表达拒绝，再重新进入 design。
- PM 依据：测试夹具在 `pmai-e2e-design` 会话中明确选择两态。
- 授权：decision gate `gate-861a8f0600ca`，已消费为 D1。

## 设计共识

- 结论：首版展示变更详情，产品经理可确认或保留待处理，之后能回看结果和时间。
- 授权：shared-understanding gate `gate-e8aff8f112d5`，已消费。
