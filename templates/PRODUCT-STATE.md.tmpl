# {{PROJECT_NAME}} — 产品现状（PRODUCT-STATE）

> **这是什么**：产品的「现状层」—— 现在长什么样、做到哪了、哪些是真系统哪些还是 mock。AI 进项目**第一个读它**，就知道你产品当前的样子。
> **为什么有它**：治"每次都要重新跟 AI 解释产品定位 / 功能 / 现状"。它是产品上下文的心脏。
> **谁写**：**只在事实收敛时更新**（防腐铁律——别处随手改它，它必然漂成假现状）。完整 build 落入 main 后由自动文档编译写入新事实（兼容 `/pmai-build-close` 复用同一逻辑）；没有 active work 时，`/pmai-record` 只能依据 main 已提交事实纠正文档漏记 / 错记，不能把讨论、design、mock 或未提交实现写成现状。
> **项目决策档案不算这里**：`docs/decisions/` 带日期、写一次不维护、从不声称是现状——它是历史坐标，不在防腐铁律约束内。
> **谁读**：每个新需求的 design、build、自适应验收和反向出 PRD，开头都先读它。
> **它不装什么**：定位 / 角色 / 术语 → `PRODUCT.md`；跨功能产品规则 → `PRODUCT-RULES.md`；视觉规范 → `DESIGN.md`；完整文档入口 → `docs/INDEX.md`。**本文件只装动态现状，不兼职总索引。**

---

## 产品定位（一句话）

<!-- 一句话现状定性即可；完整定位 / 用户画像见 PRODUCT.md -->

## 当前功能 / 能力

<!-- 产品现在能做什么。每条 ≤ 一行。随每次沉淀累积。 -->

| 功能 / 能力 | 状态 | 备注 |
|---|---|---|

## 当前界面 / 产品形态

<!-- 当前已经 landed 的 prototype 或 product 有哪些页面、入口和主要区域。代码位置以
     `.pm-workflow/project.yml` 为准；project.yml 尚未生成时保持为空，不猜。 -->

| 页面 / 区域 | 有什么（分区 / 主块） | 备注 |
|---|---|---|

## 实现深度状态（mock / 真实能力）

<!-- 按层记当前是 mock 还是真实能力；具体深度以 project.type 与模块规格为准。
     某层从 mock 转真在哪个需求做的，也记一笔，方便追溯。 -->

| 层 | 当前 | 何时转的 |
|---|---|---|

## 文档入口

完整文档索引见 `docs/INDEX.md`；建造对象、技术栈和代码入口见 `.pm-workflow/project.yml`（首次可建造 design 定稿后生成）。
