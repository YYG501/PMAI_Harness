# {{PROJECT_NAME}} — 产品现状（PRODUCT-STATE）

> **这是什么**：产品的「现状层」—— 现在长什么样、做到哪了、哪些是真系统哪些还是 mock。AI 进项目**第一个读它**，就知道你产品当前的样子。
> **为什么有它**：治"每次都要重新跟 AI 解释产品定位 / 功能 / 现状"。它是产品上下文的心脏。
> **谁写**：**只在事实收敛时更新**（防腐铁律——别处随手改它，它必然漂成假现状）。仅有两个 sanctioned 写入口：完整 build 落入 main 后的自动文档编译（兼容 `/pmai-build-close` 复用同一逻辑）+ `/pmai-record`（轻量记录：main 直接改或 design 后暂不 build），都是收敛点触发的原子写。
> **项目决策档案不算这里**：`docs/decisions/` 带日期、写一次不维护、从不声称是现状——它是历史坐标，不在防腐铁律约束内。
> **谁读**：每个新需求的范围确认、原型 build、覆盖审计、反向出 PRD，开头都先读它。
> **它不装什么**：定位 / 角色 / 术语 → `PRODUCT.md`；跨功能产品规则 → `PRODUCT-RULES.md`；视觉规范 → `DESIGN.md`；完整文档入口 → `docs/INDEX.md`。**本文件只装动态现状，不兼职总索引。**

---

## 产品定位（一句话）

<!-- 一句话现状定性即可；完整定位 / 用户画像见 PRODUCT.md -->

## 当前功能 / 能力

<!-- 产品现在能做什么。每条 ≤ 一行。随每次沉淀累积。 -->

| 功能 / 能力 | 状态 | 备注 |
|---|---|---|

## 主原型现状

<!-- prototype/ 现在有哪些页面、大致分区结构。目的：让 AI 能说出"当前原型长什么样、缺哪块"，
     从而在新需求时主动指出"这是新东西、现状里没有"。 -->

| 页面 / 区域 | 有什么（分区 / 主块） | 备注 |
|---|---|---|

## 实现深度状态（mock / 真系统）

<!-- 按层记当前是 mock 还是真系统（对应工程结构约束里的层）。
     某层从 mock 转真在哪个需求做的，也记一笔，方便追溯。 -->

| 层 | 当前 | 何时转的 |
|---|---|---|
| 数据层 | mock | — |
| 权限层 | mock | — |
| 会话层 | mock | — |
| API 契约 | mock | — |

## 文档入口

完整文档索引见 `docs/INDEX.md`；主原型代码见 `prototype/`。
