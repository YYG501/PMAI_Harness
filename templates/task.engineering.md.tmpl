# Task {{TASK_NUMBER}} Engineering: {{TASK_NAME}}

> 本文件是 **工程合同**（task-execute / task-confirm / task-submit / agent 用）。PM 默认不看。
> PM 视图见 [`task-{{TASK_NUMBER}}-{{TASK_SLUG}}.md`](./task-{{TASK_NUMBER}}-{{TASK_SLUG}}.md)。
>
> 本文件允许出现所有工程内容：组件路径、props 签名、reducer action、字段名、像素值、颜色码、emoji、反向约束、单测条目、grep 校验等。
> **唯一原则**：本文件不重复 PM 视图已有的功能行为描述；本文件只补充实现层细节、反向约束、agent 行为约束。

- 关联文件：`task-{{TASK_NUMBER}}-{{TASK_SLUG}}.md`（同目录）
- 创建时间：{{CREATED_DATE}}

<!-- synced_pm_view_hash: {{PM_VIEW_HASH}} -->
<!-- 生成与维护规则见 skills/_shared/PM-VIEW-RULES.md §9.6（双文件 lazy sync）。
     PM 视图改动时本 hash 不立即更新，PM 在 task-spec 步骤 12 选 A 后由 reconcile 步骤刷新。
     hash 计算：shasum -a 256 task-NNN-<slug>.md | cut -c1-12 -->

---

## 1. 元信息扩展（agent 用）

**executor：** claude-code
**executor_model：**
<!--
可选 executor：claude-code / codex / cursor-agent / manual
model 留空 = 用 settings 默认
claude-code 仅支持：opus / sonnet / haiku（Agent tool 枚举）
cursor-agent：实验性，无沙箱，越界保护仅限 worktree 内
放弃 manual：python3 .claude/scripts/task-transition.py <PM 视图文件路径> --cancel-manual
暂缓 manual 提醒：python3 .claude/scripts/task-transition.py <PM 视图文件路径> --snooze-manual --days 3
-->
**审查工具：** {{REVIEW_TOOLS}}
<!-- 「审查工具」字段语义：推荐 PM 在 task-execute 步骤 7 自跑的工具（I-RV1）。
     AI 不得自动调用本字段列出的任何 skill；PM 跑完后报告结果，AI 才 append review_completed 事件作为审计记录。
     哨兵值 (无)/无/空/N/A 都表示无推荐。 -->

---

## 2. 状态转换说明（agent 必读）

> ⚠️ **agent 读取 PM 视图文件后第一步：检查「状态」字段，按下表行动。**

| 状态 | 谁行动 | 做什么 |
|------|--------|--------|
| `待执行` | orchestrator | 等 PM 通过 `/task-confirm` 创建 worktree；可只读代码（preflight），不得写代码 |
| `执行中` | task window / execution agent | 在 task worktree 中实现 + 自审；呈交和 PM 验收期间仍保持此状态 |
| `已完成` | — | PM 已通过，不再修改 |
| `已废弃` | — | PM 决定不做。文件已 `git mv` 到 `tasks/discarded/`，task worktree + 分支已清理 |

**状态转换（受控入口）：**
`待执行` →(PM 在 task 窗口启动 `/task-execute`)→ `执行中` →(PM 通过呈交块)→ `已完成`
PM 打回 → 保持 `执行中`
执行失败 / 放弃 manual → `待执行`
`{待执行, 执行中}` →(PM 废弃)→ `已废弃`（专用入口 `--discard`，不走 `--to`）
`已完成` 不可废弃（代码已合入 req 分支）；如需撤销，开新 task revert 或 `/cancel-req` 整体取消

**禁止直接编辑 PM 视图文件的状态字段。使用：**
`python3 .claude/scripts/task-transition.py <PM 视图文件路径> --to <目标状态>`
废弃 task：`python3 .claude/scripts/task-transition.py <PM 视图文件路径> --discard --reason "<一句话>"`

---

## 3. 启动前必读（按顺序，读完再执行）

<!-- 填写规则：
- 列出 agent 在 task worktree 启动前必读的文件路径
- 按读取优先级排序（最相关的在前）
- 每条：路径 + 一句话说明读它做什么
- 至少包含：
  - solution.md（PM 视图）的相关章节
  - solution.engineering.md 的相关章节
  - task-plan.md 的相关 GAP / 风险条目
  - 同模块已完成 task 的 PM 视图 + 工程合同
  - docs/CONTEXT.md / docs/DESIGN.md / docs/modules/<module>.md
  - prototypes/ 涉及的现有文件
-->

1. `requirements/active/{{REQ_ID}}-{{REQ_SLUG}}/solution.md` §X — [一句话说明]
2. `requirements/active/{{REQ_ID}}-{{REQ_SLUG}}/solution.engineering.md` §Y — [一句话说明]
3. `requirements/active/{{REQ_ID}}-{{REQ_SLUG}}/task-plan.md` §三 风险 + §四 自检与状态摘要（反模式 / 验收 GAP）— [一句话说明]
4. `docs/CONTEXT.md` — 项目背景
5. `docs/DESIGN.md` — 视觉规范
6. `docs/modules/<module>.md` — 当前模块规格
7. 同模块已完成 task 的 PM 视图 + 工程合同 — [复用 / 避免重复]
8. `prototypes/src/.../X.tsx` — 当前实现现状

---

## 4. 功能清单工程版（实现层细节）

<!-- 填写规则：
- 与 PM 视图功能清单一一对应，编号同步（§4.1 对应 PM 视图功能 1）
- 每节列出实现层细节：字段名 / props 接口 / reducer action / 函数签名 / 调用链
- 不重复 PM 视图已写的功能行为，只补"怎么实现"
- 用代码块表达类型 / 接口
-->

### 4.1 [对应 PM 视图功能 1] - 实现层细节

[字段 / props / 算法 / 调用链]

### 4.2 [对应 PM 视图功能 2] - 实现层细节

...

---

## 5. 实现指引

<!-- 填写规则：
- 给 agent 实现时的行动指导
- 分四块：组件复用 / 状态覆盖 / 关键逻辑 / 易错点 / 禁止项
- 不复述"启动前必读"已经列过的文件
- 历史踩坑 / vitest stub 限制 / dev console 信号规范等都放在这里
-->

### 5.1 组件复用

[列出本 task 应该复用 / 参考的现有组件 + 路径]

### 5.2 状态覆盖

[Empty / Loading / Error / Partial / Success 各状态的实现要点]

### 5.3 关键逻辑

[复杂或特殊的实现要点：异步、跨组件通信、状态机、性能优化等]

### 5.4 vitest stub 限制 / dev console 信号规范（如适用）

[项目级的工程约定（如 expect API 收窄、console.warn vs error）]

---

## 6. 易错点 / 禁止项

<!-- 填写规则：
- 反向描述允许：禁止 X / 不允许 Y / 不要 Z
- 每条按"行为 → 后果"格式
- 来源标注：源 PM 反馈 / 源 plan-eng-review / 源历史 task 踩坑
- 来自同模块已完成 task PM 反馈中的"反向约束"类条目（PM-VIEW-RULES §9.4 三类分流）
-->

| # | 禁止行为 | 后果 / 理由 | 来源 |
|---|---|---|---|

---

## 7. plan-design-review / plan-eng-review 沉淀

<!-- 填写规则：
- 每条 review 沉淀编号 V1, V2, ...，按 review 时间顺序
- 三段式：决策 / 理由 / 落地约束
- 视觉沉淀（V1-V10 类）/ 架构沉淀（V11-V20 类）/ 状态-动效沉淀（V21+）分小节
- 与 §4 / §5 冲突时本节为准，需在 §4 / §5 末尾标注"本条已被 V_X 取代"
-->

### 7.1 视觉沉淀（plan-design-review V1-V10）

#### V1 [决策标题]
**决策**：[简短描述]
**理由**：[为什么这样选]
**落地约束**：[实现时必须遵守的具体规则]

### 7.2 架构沉淀（plan-eng-review V11-V20）

[同上格式]

### 7.3 状态/动效沉淀（plan-design-review V21+）

[同上格式]

---

## 8. a11y / 视口 / 视觉规范细则

<!-- 填写规则：
- 像素值、颜色码、字号、间距、emoji 视觉等所有具体视觉参数
- 视口断点（1280px / 1024px）下的布局变化
- a11y 要求（role / aria-label / tab order / focus trap / 键盘导航）
- 引用 docs/DESIGN.md §X，不复制粘贴 DESIGN 内容
-->

### 8.1 视觉参数

| 元素 | 规格 | 备注 |
|---|---|---|

### 8.2 视口断点

| 断点 | 布局变化 |
|---|---|

### 8.3 a11y 要求

| 区域 | a11y 要求 |
|---|---|

---

## 9. 工程层验收清单（自审用，agent 自动化校验）

<!-- 填写规则：
- agent 自审 / eng review / qa 跑通后逐条勾选
- 包含：grep 校验、单测条目、引用稳定性测试、覆盖率阈值、性能指标
- 与 PM 视图的「✅ 验收清单」分工：PM 视图是 demo 走查、本节是工程层断言
- 每条："grep / 命令 / 测试条目"
-->

- [ ] [grep 校验：grep -E "..." 在 X 文件返回 0 命中（理由：...）]
- [ ] [单测条目：xxx.test.ts 覆盖 N 个场景全 PASS]
- [ ] [引用稳定性测试：reducer view-only action 后 state.X 引用相等]
- [ ] [性能指标：toggle 单行展开时其他行不重渲染（React DevTools 验证）]
- [ ] [覆盖率：纯函数模块测试覆盖率 ≥ N%]

---

## 10. 文档偏差（execution agent 填写，工程层视角）

<!-- 填写规则：
- 实现过程中发现的工程层偏差（与 solution.engineering / docs/modules 不一致处）
- 业务行为偏差仍写在 PM 视图的「📁 历史档案」section
- orchestrator 合并前读取此 section，呈交 PM 决定是否更新文档
-->

| 文档位置 | 文档原文 | 实际实现 |
|---------|---------|---------|

---

## 11. 自审记录（PM 跑完 review 后由 orchestrator 机械追加）

<!-- 填写规则：
- Review 工具一律由 PM 自跑（I-RV1）。AI 不得自动调用 /review /qa /design-review。
- PM 在 chat 报告结论后，AI 在本 section 追加一条记录（保留 PM 原话或转写），并 append 事件作为审计：
  python3 .claude/scripts/task-events.py append <PM 视图文件路径> --type review_completed --tool /qa --result pass
- 事件流仅作审计记录，不当 task-transition gate（I-RV2）。
- PM 全跳 review：本 section 至少留一条 `**结果：** PM 选择不跑 review` placeholder。
- plan 阶段的 /plan-eng-review / /plan-design-review 用 --type plan_review_completed，由 /task-spec 步骤 8 在 PM 报告结论后追加。
-->

### 自审 1 - [YYYY-MM-DD HH:MM]
**工具：** /qa, /design-review
**结果：** PM 报告：通过 / 发现 N 个问题 / 选择不跑 review

**详细发现：**
- F-001: [文件:行号] [问题描述] → [修复方式] ✅已修复
- 或：未发现问题
- 或：N/A（PM 未跑此工具）

**遗留问题：** 无 / [列出]
