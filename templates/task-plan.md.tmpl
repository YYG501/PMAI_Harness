# Task Plan — {{REQ_NAME}}

> 本文件是 task 拆分的**单一真相源**：拆分摘要、task 列表、执行顺序、风险，加末尾轻量"自检与状态摘要"（反模式自检结论 / 验收 GAP 索引 / 模块规格状态）。
> 写作规则参见 `skills/_shared/PM-VIEW-RULES.md`，特别是：
> - §三 PM 视图写作规则
> - §七 章节顺序约束（本模板按此节锁定的顺序）
> - §九 输入流约束（必读 brief.md / **stage 2 真相源** (A 分支 analysis.md / B 分支 stage2-office-hours.md) / prd.md / implementation-design.md / docs/{PROJECT,DESIGN}.md / docs/modules/\*.md / prototypes/）

- 创建日期：{{CREATED_DATE}}
- 创建者：{{AUTHOR}}
- 分支：{{REQ_BRANCH}}
- 依赖：`brief.md` + **stage 2 真相源**（A 分支 `analysis.md` / B 分支 `stage2-office-hours.md`，由 `.req-meta.json:stage2_source` 决定）+ `prd.md` + `implementation-design.md`（v2 / C2：管线重构后 solution.md 砍除，依赖改 prd + implementation-design；D-i v4 改 stage 2 双源）
- 当前 stage：5（task 拆分）
- Status：DRAFT（待 PM 确认进入 stage 6）

---

## 📌 拆分摘要

<!-- 填写规则：
- 1-2 句：本 req 拆成几个 task / 总工作量预估 / 推荐起跑顺序
- 不写"为什么这样拆"的设计理由 → 进 §四 自检与状态摘要的反模式自检
- 字数控制在 100 字以内
-->

[1-2 句拆分摘要]

---

## 一、Task 列表

<!-- 填写规则：
- 表格列：id / title / 所属模块 / 所属模块章节 / summary / order / risk / 决策类型
- "title" 用产品视角的功能名（与 task spec 顶部标题保持一致）
- "所属模块" = 业务模块名 / 基础设施
- "所属模块章节" 业务模块 task 必填，基础设施 task 留空
- "summary" 一句话讲清本 task 交付什么 + 验收角度（不写实现细节）
- "order" = task 在执行链路中的顺序号
- "risk" = 标关联到 §三 风险表的 R 编号；无关联写"无"
- 不允许写超过一行的 summary（细节进 task spec）

- 「决策类型」列（speed mode 用 — 见 skills/req-stage-gate/SKILL.md「speed mode」段）：
  - **结构**：本行 task 涉及以下任一情况（speed mode 自动推进期间命中 → 当场停下问 PM）：
    * 合并：本 task 把 PRD §七 / GAP 清单的多条验收项 / 多个 solution 候选合在一起
    * 拆开：把 PRD 单一功能拆成多个 task（如按文件 / 按子流程）
    * 重排：本 task 的 order 不是按 PRD §六 自然顺序、是 PM / AI 重排过的
    * 反模式 A 命中已处理（纯前置文档 / 规格 / 契约 task 已合并到业务 task）
  - **机械**：本 task 完全是 PRD §七 验收某一条的直译，无合并 / 无拆开 / order 按自然顺序
  - **拿不准 → 默认填「结构」**（保守 / 宁可多停一次）
-->

| id | title | 所属模块 | 所属模块章节 | summary | order | risk | 决策类型 |
|----|-------|---------|------------|---------|-------|------|---------|

**未纳入本 plan 的 solution 候选**（如有，写一段说明为什么不纳入；如无写"无"）：

[列出 solution 中提到但未拆为 task 的候选 + 处理路径（"留待 PM 反馈后追加" / "归到 task-XXX" / "不在本 req 范围"）]

---

## 二、执行顺序与并行性

<!-- 填写规则：
- 用 ASCII 流程图 + 文字说明
- 标注串行 / 并行关系
- 标注 worktree 类型（req worktree 改 docs / task worktree 改代码）
- 给出 PM 启动建议（先启动哪个、什么时候并发）

- 「执行模式」是 task 级结构决策（speed mode 2026-05-26）：
  AI 不自决；task-plan skill 步骤 3.5 主动 prompt PM 拍板（串行 / 并行 / 混合）
  + AI 给倾向 + 理由（同文件冲突 / 互相参照规范段 / task 数 / PM 走查负担）
  PM 拍板后写回下方「执行模式（PM 拍板）」行 + append req-events.jsonl decision
-->

**执行模式（PM 拍板）**：<串行 / 并行 / 混合>

```text
[流程图：task-001 → task-002 ↘
                 task-003 ↗ → 收尾]
```

- **Lane A（[worktree 类型], 串行 / 并行）**：[task 列表]
- **Lane B**：[同上]

**PM 启动建议**：
- 先启动 [task-XXX]
- [task-XXX] 完成后可同时启动 [task-YYY] / [task-ZZZ]
- 全部 task 完成后进入 stage 7 验收 / 收尾

---

## 三、风险

<!-- 填写规则：
- 表格列：# / 风险描述 / 触发场景 / 应对路径
- 每条 1-3 句，正向描述
- "应对路径" 写"出问题时怎么处理"（不写"禁止 X"反向约束）
- 仅列与 task 拆分相关的风险（数据模型一致性、跨 task 接口、串行冲突等）
-->

| # | 风险描述 | 触发场景 | 应对路径 |
|---|---|---|---|
| R1 | [正向描述风险] | [什么情况下触发] | [出问题时怎么处理] |

---

## 四、自检与状态摘要

> 本节是 task-plan 自身的轻量自检结论 + task-spec / doc-update 会消费的状态索引。
> **不是**给 PM 读的产品决策内容；PM 看一眼"反模式 5/5 PASS"即可。

### 4.1 反模式自检（5 条）

按 task-plan skill §2.2 五条反模式逐条自检。每条只写一行结论；命中时一句话说明合并 / 重构后的处理。**5 条全 PASS 才能进入 stage 6**。

| # | 反模式 | 状态 | 命中处理（仅命中时填）|
|---|---|---|---|
| A | 纯前置 task（重构 / 文档 / 规格 / 契约） | 未命中 / 命中（已处理）| — |
| B | 横切质量 task | 未命中 / 命中（已处理）| — |
| C | 共生对拆成两个 | 未命中 / 命中（已处理）| — |
| D | 同文件串行多 task | 未命中 / 命中（已处理）| — |
| E | 业务功能 task 没有模块归属 | 未命中 / 命中（已处理）| — |

**结论**：[5 条全部 PASS / 命中 X 条已处理] → 可 / 不可进入 stage 6

### 4.2 验收 GAP 清单

<!-- 填写规则（v2 / C1 / C2：solution.md 已砍，来源改 prd.md）：
- 来自 prd.md 「§七 验收标准」逐条审视，编号 G1, G2, ...
- 每条三种处置之一：
  1. 由 task-NNN 接住（默认） → 「应落到哪个 task 验收」列填 task-NNN
  2. 已被 task 列表完全覆盖 → 整表写「无 GAP」
  3. 原型本次不实现（kind 2 整块功能不做）→ 列填「原型不实现 → 反向写回 implementation-design.md
     段 1.5 SIMP-NN」+ 同步去 implementation-design.md 段 1.5 追加 SIMP 行（来源列 = kind 2）
- 由 stage 6 task-spec 按编号接住（处置 1）/ close-req §2a 据 SIMP 行标注 PRD（处置 3）
-->

| GAP # | GAP 描述 | 应落到哪个 task 验收 |
|---|---|---|
| G1 | [prd.md §七 验收标准的某条] | [task-XXX] / 原型不实现 → SIMP-NN |

### 4.3 模块规格状态

<!-- 填写规则：
- 列出本 req 涉及的每个业务模块的当前规格状态
- 状态：已存在-完整 / 已存在-待补 / 不存在-待创建
- 影响 task-spec 步骤 4 判断（读取当前模块规格）
-->

| 模块 | 当前规格状态 | 本 req 涉及的章节 |
|---|---|---|

---

## 📁 历史档案

### 变更记录

| 日期 | 变更内容 | 负责人 |
|---|---|---|
| {{CREATED_DATE}} | 初稿 | {{AUTHOR}} |
