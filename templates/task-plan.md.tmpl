# Task Plan — {{REQ_NAME}}

> 本文件是 **PM 视图**（拆分摘要、task 列表、执行顺序、风险）。
> 工程实现细节（反模式自检全文、验收 GAP 清单、模块规格状态、拆分依据等）见 `task-plan.engineering.md`。
> 写作规则参见 `skills/_shared/PM-VIEW-RULES.md`，特别是：
> - §三 PM 视图写作规则
> - §七 章节顺序约束（本模板按此节锁定的顺序）
> - §九 输入流约束（必读 brief.md / analysis.md / solution.md（PM 视图）/ docs/{CONTEXT,DESIGN,prd}.md / docs/modules/\*.md / prototypes/）

- 创建日期：{{CREATED_DATE}}
- 创建者：{{AUTHOR}}
- 分支：{{REQ_BRANCH}}
- 依赖：`brief.md` + `analysis.md` + `solution.md`
- 当前 stage：5（task 拆分）
- Status：DRAFT（待 PM 确认进入 stage 6）

---

## 📌 拆分摘要

<!-- 填写规则：
- 1-2 句：本 req 拆成几个 task / 总工作量预估 / 推荐起跑顺序
- 不写"为什么这样拆"的设计理由 → 进 .engineering.md（反模式自检会覆盖）
- 字数控制在 100 字以内
-->

[1-2 句拆分摘要]

---

## 一、Task 列表

<!-- 填写规则：
- 表格列：id / title / 所属模块 / 所属模块章节 / summary / order / risk
- "title" 用产品视角的功能名（与 task spec 顶部标题保持一致）
- "所属模块" = 业务模块名 / 基础设施
- "所属模块章节" 业务模块 task 必填，基础设施 task 留空
- "summary" 一句话讲清本 task 交付什么 + 验收角度（不写实现细节）
- "order" = task 在执行链路中的顺序号
- "risk" = 标关联到 §三 风险表的 R 编号；无关联写"无"
- 不允许写超过一行的 summary（细节进 task spec）
-->

| id | title | 所属模块 | 所属模块章节 | summary | order | risk |
|----|-------|---------|------------|---------|-------|------|

**未纳入本 plan 的 solution 候选**（如有，写一段说明为什么不纳入；如无写"无"）：

[列出 solution 中提到但未拆为 task 的候选 + 处理路径（"留待 PM 反馈后追加" / "归到 task-XXX" / "不在本 req 范围"）]

---

## 二、执行顺序与并行性

<!-- 填写规则：
- 用 ASCII 流程图 + 文字说明
- 标注串行 / 并行关系
- 标注 worktree 类型（req worktree 改 docs / task worktree 改代码）
- 给出 PM 启动建议（先启动哪个、什么时候并发）
- 不写技术依赖（"reducer 必须先于 dialog"等）→ 进 .engineering.md
-->

```text
[流程图：task-001 → task-002 ↘
                 task-003 ↗ → 收尾]
```

- **Lane A（[worktree 类型], 串行 / 并行）**：[task 列表]
- **Lane B**：[同上]

**PM 启动建议**：
- 先启动 [task-XXX]
- [task-XXX] 完成后可同时启动 [task-YYY] / [task-ZZZ]
- 全部 task 完成后进入 stage 7 验收 / 收尾

---

## 三、风险

<!-- 填写规则：
- 表格列：# / 风险描述 / 触发场景 / 应对路径
- 每条 1-3 句，正向描述
- "应对路径" 写"出问题时怎么处理"（不写"禁止 X"反向约束）
- 仅列与 task 拆分相关的风险（数据模型一致性、跨 task 接口、串行冲突等）
- 通用工程风险（reducer 性能 / mock 缺样例等）→ 进 .engineering.md
-->

| # | 风险描述 | 触发场景 | 应对路径 |
|---|---|---|---|
| R1 | [正向描述风险] | [什么情况下触发] | [出问题时怎么处理] |

---

## 📁 历史档案

> 工程实现细节（反模式自检 / 验收 GAP / 模块规格状态 / 拆分依据 / autoplan 修订点）见 [`task-plan.engineering.md`](./task-plan.engineering.md)

### 变更记录

| 日期 | 变更内容 | 负责人 |
|---|---|---|
| {{CREATED_DATE}} | 初稿 | {{AUTHOR}} |
