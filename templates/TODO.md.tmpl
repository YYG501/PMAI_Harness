<!-- TODO — PM 待办池（本包内）

固定路径：docs/TODO.md
作用：PM 提过 / 讨论过想做的事，随手记一笔。无序，不排顺序。
      这不是「规划真相源」（项目方向真相源是 docs/PRODUCT.md），就是 PM 自己的待办清单。

谁在何时改：
  - /pmai-project-solution / /pmai-codebase-audit 首次产出时，只记 PM 主动提过 / 讨论过的待办。
    AI 不从代码、竞品、requirements/closed 反推填充 —— 池子里只有 PM 真说过想做的。
  - PM 随时手动增删（想到新的就记一条，不想做了就划掉）。
  - PM 要起新需求时，AI 提醒「池子里有这些待办」让 PM 挑一个，
    不替 PM 判断「下一个该做 X」。

状态取值（三态，简单）：
  - todo   — 讨论过 / 提过想做，还没开始
  - doing  — 正在做（已 /pmai-new-req 起 req）
  - done   — 做完了（req 已 close）

字段：req-id（起 req 后回填，未起前留空）/ 标题 / 状态
      —— 没有「排序」列，待办池不排顺序。
-->

# {{PROJECT_NAME}} · 待办池 (TODO)

> PM 提过 / 讨论过想做的事。无序，不排顺序 —— 要做时挑一个。
> 状态：todo（想做）/ doing（在做）/ done（做完）。

| req-id | 标题 | 状态 |
|---|---|---|
|  | <你想到要做的第一件事> | todo |
