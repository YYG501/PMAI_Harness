<!-- TODO — PM 待办池（本包内）

固定路径：docs/TODO.md
作用：PM 提过 / 讨论过想做的事，随手记一笔。无序，不排顺序。
      这不是「规划真相源」（项目方向真相源是 docs/PRODUCT.md），就是 PM 自己的待办清单。

谁在何时改：
  - /pmai-strategy / /pmai-codebase-audit 首次产出时，只记 PM 主动提过 / 讨论过的待办。
    AI 不从代码、竞品或历史归档反推填充 —— 池子里只有 PM 真说过想做的。
  - 沉淀时（/pmai-close 或 /pmai-deposit）PM 选「推下个功能」的遗留，自动写入一条，
    标题带一句话自包含（建议目标 + 涉及文件），下个需求起步直接看懂、不靠翻 closed。
    这仍是 PM 此刻拍过的遗留，不是 AI 凭空反推 —— "不反推"纪律不破。
    遗留真相源单一在本池子；close-report 只渲染指针「已转入 TODO 第 N 条」。
  - PM 随时手动增删（想到新的就记一条，不想做了就划掉）。
  - PM 要起新需求时，AI 提醒「池子里有这些待办」让 PM 挑一个，
    不替 PM 判断「下一个该做 X」。

状态取值（三态，简单）：
  - todo   — 讨论过 / 提过想做，还没开始
  - doing  — 正在做（已进入 /pmai-design 或 /pmai-build）
  - done   — 做完了（已 close）

字段：工作标识（进入完整工作后可回填，未起前留空）/ 标题 / 状态
      —— 没有「排序」列，待办池不排顺序。
-->

# {{PROJECT_NAME}} · 待办池 (TODO)

> PM 提过 / 讨论过想做的事。无序，不排顺序 —— 要做时挑一个。
> 状态：todo（想做）/ doing（在做）/ done（做完）。

| 工作标识 | 标题 | 状态 |
|---|---|---|
|  | <你想到要做的第一件事> | todo |
