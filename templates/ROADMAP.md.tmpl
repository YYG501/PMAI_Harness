<!-- roadmap — PM 规划视图（本包内）

固定路径：docs/ROADMAP.md
作用：项目的 req 全表（历史 done + 当前 active + 计划 planned）—— PM 唯一规划视图，没有"叙事性里程碑"的副本。
颗粒度：到单个 req。

谁在何时改：
  - /pmai-project-solution 首次产出时填初始队列（PM 已知的待做需求）
  - PM 随时手动增删（想到新需求 / 砍掉旧需求）
  - 一个 req 走完后，PM 把对应行状态推进到 done
  本包内 roadmap 是 PM 自己维护的规划视图；机器消费者（如 /pmai-new-req 自动挑下一个）后续落地，本包不依赖它被机器读。

状态取值（固定三态）：
  - planned  — 已规划、还没开始
  - active   — 正在做（已 /pmai-new-req 起 req）
  - done     — 已完成（req 已 close）

字段：req-id（req 起后回填，未起前留空）/ 标题 / 状态 / 排序（数字，小的先做）
-->

# {{PROJECT_NAME}} · roadmap

> 计划态 req 队列。状态 planned / active / done；排序数字小的先做。

| 排序 | req-id | 标题 | 状态 |
|---|---|---|---|
| 1 |  | <第一个要做的需求标题> | planned |
