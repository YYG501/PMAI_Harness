# Task Plan Engineering — {{REQ_NAME}}

> 本文件是 **工程合同**（生成器 / stage gate / agent 用）。PM 默认不看。
> PM 视图见 [`task-plan.md`](./task-plan.md)。
>
> 本文件允许出现所有工程内容：反模式自检全文、模块规格状态、拆分依据论证、autoplan 修订点等。
> **唯一原则**：本文件不重复 PM 视图已有的 task 列表 / 执行顺序描述；本文件只补充拆分依据与生成器约束。

- 关联文件：`task-plan.md`（同目录）
- 创建日期：{{CREATED_DATE}}
- 当前 stage：5（task 拆分 - 工程合同）

---

## 1. 反模式自检声明

<!-- 填写规则：
- 来源：task-plan skill §3.2 反模式 A-E
- 每条逐一勾选并给出"未命中 / 命中（已处理）"的判定
- "命中（已处理）" 必须说明合并 / 重构后的结果
- 5 条全部 PASS 才能进入 stage 6
-->

按 task-plan skill §3.2 五条反模式逐条自检：

### 反模式 A · 纯重构前置 task
- 状态：[未命中 / 命中（已处理）]
- 判定：[逐 task 检查的结论]

### 反模式 B · 横切质量 task
- 状态：[未命中 / 命中（已处理）]
- 判定：[a11y / 响应式 / i18n / 埋点是否在每个 UI task 内验收]

### 反模式 C · 共生对拆成两个
- 状态：[未命中 / 命中（已处理）]
- 判定：[逐 task 问"单独跑完后能端到端验证业务价值吗"]

### 反模式 D · 同文件串行多 task
- 状态：[未命中 / 命中（已处理）]
- 判定：[同文件 + 串行的 task 是否写了"拆多个 vs 合并"权衡结论]

### 反模式 E · 业务功能 task 没有模块归属
- 状态：[未命中 / 命中（已处理）]
- 判定：[各 task 模块归属是否符合硬规则]

**自检结论**：[N 条全部 PASS / 命中 X 条已处理] → 可 / 不可进入 stage 6

---

## 2. stage 6 task-spec 验收 GAP 清单

<!-- 填写规则：
- 来自 solution.md 的"验收标准"逐条审视，标注哪些点需要在 stage 6 task-spec 阶段细化
- 编号 G1, G2, ... 供 task-spec 在生成各 task 时引用
- 每条："GAP 描述 / 应该落到哪个 task 的验收"
- 如本 req 验收点已被 task spec 完全覆盖，写"无 GAP"
-->

| GAP # | GAP 描述 | 应落到哪个 task 验收 | 原因 |
|---|---|---|---|
| G1 | [solution.md 验收标准的某条 PM 走查点] | [task-XXX] | [为什么需要在 task spec 阶段细化] |

---

## 3. 模块规格状态

<!-- 填写规则：
- 列出本 req 涉及的每个业务模块的当前规格状态
- 状态分类：
  - 已存在 - 完整：docs/modules/<module>.md 已写完整规格
  - 已存在 - 待补：现有规格存在但本 req 涉及的章节缺失
  - 不存在 - 待创建：模块规格文件尚未创建（待 doc-update 沉淀）
- 影响 task-spec 步骤 4（读取当前模块规格）的判断
-->

| 模块 | 当前规格状态 | 本 req 涉及的章节 | 备注 |
|---|---|---|---|

---

## 4. 拆分依据 / 原型代码现状分析

<!-- 填写规则：
- 来自 prototypes/ 现有原型代码的反向校验
- 列出本 req 涉及的现有页面 / 组件 / 已落地能力
- 标注哪些是"复用已有"、哪些是"扩展现有"、哪些是"全新建"
- 影响 task 拆分粒度（避免重复创建已有能力）
-->

### 4.1 现有原型代码涉及范围

| 文件路径 | 当前实现 | 本 req 处理方式 |
|---|---|---|
| `prototypes/src/.../X.tsx` | [当前职责 / 行数] | 复用 / 扩展 / 重写 / 不动 |

### 4.2 与上游文档的反向校验差异

[列出原型与上游文档（analysis / solution）描述不一致的地方 + PM-VIEW-RULES §9.3 的处理结论]

---

## 5. 共享数据 / 接口的 task 间约束

<!-- 填写规则：
- 列出多个 task 共享的数据结构、纯函数、组件接口
- 标注"由哪个 task 落定 / 哪个 task 消费"
- 防止后置 task 反向变形前置 task 的数据结构
-->

| 共享对象 | 落定 task | 消费 task | 不可反向变形约束 |
|---|---|---|---|

---

## 6. autoplan 修订点 / 决策表

<!-- 填写规则：
- 来自 autoplan 跑过 task-plan 后产出的决策追溯
- 原样保留 autoplan 输出
- PM 不看本节
- 如未跑 autoplan，本节填"未运行 autoplan"
-->

[autoplan 输出原文]

---

## 7. 工程层验收清单（task-plan 自身的）

<!-- 填写规则：
- 校验 task-plan 自身是否满足生成器约束（不是各 task 的验收）
- 包含：5 条反模式 PASS / GAP 清单完整 / 模块规格状态准确 / 单 req task 总数 ≤ 7 等
-->

- [ ] 反模式 A-E 五条自检全部 PASS（§1）
- [ ] 验收 GAP 清单与 solution.md "验收标准"一一对应（§2）
- [ ] 各业务模块 task 数量 ≤ 3（task-plan skill §3.3 软上限）
- [ ] 单 req 总 task 数 ≤ 7（task-plan skill §3.3 软上限）
- [ ] task 列表的 "所属模块章节" 字段对所有业务模块 task 已填
- [ ] 共享数据 / 接口的 task 间约束已列（§5）

---

## 8. 变更记录（工程合同侧）

| 日期 | 变更内容 | 负责人 |
|---|---|---|
| {{CREATED_DATE}} | 初稿 | {{AUTHOR}} |
