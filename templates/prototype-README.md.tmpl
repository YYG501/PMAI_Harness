# prototype/ —— 主原型说明

> **这是什么**：你产品的**单一主原型**。所有需求的原型都在这一条线上演进，**不留 fork / 变体 / 逃生舱**。
> **怎么演进**：每个需求在自己的后台暂存区改这份原型；你确认后并回主线。**任何时候，main 上只有一份"已确认主原型"。**
> **谁建**：项目 init 时用脚手架起一版能跑的（默认 Next.js + TS + Tailwind + shadcn，init 时可改栈）。
> **它和文档的关系**：视觉照 `docs/DESIGN.md`；实现深度（哪层 mock、哪层真）照 `工程结构约束-{prototype,system,custom}.md`；现状记在 `docs/PRODUCT-STATE.md`。

---

## 约定

- **每页 self-contained**：假数据按当前实现深度写死在文件顶部（mock 档不调真接口）。
- **视觉一致**：token（色 / 间距 / 字号）以 `docs/DESIGN.md` 为准；UI primitive 用 `components/ui`（shadcn）。
- **复用靠复制样板改改**，不预先抽象组件（原型阶段；真系统档再按需抽）。
- **不用浏览器原生弹框**：删除 / 二次确认用包装好的弹层组件（见 DESIGN.md「UI 习惯」）。

## 实现深度（mock / 真系统）

按 `工程结构约束-{prototype,system,custom}.md` 选的档走：
- **prototype 档**：全 mock（写死数据、不做权限 / 会话、happy path）—— 起步默认。
- **system 档**：真后端 / 数据 / 权限 / 会话。
- **custom 档**：按层混搭（比如权限转真、数据还 mock）。

某层从 mock 转真，是一次**显式拍板的模块工作**（在模块 `spec.md` / `decisions.md` 里记清），在这份主原型里**原地重写那一层**，不另开分叉。

## 目录大意

```
prototype/
├─ app/ 或 src/app/       页面（每页 self-contained）
├─ components/ui/         shadcn primitive（token 来自 DESIGN.md）
└─ （其余按所选技术栈）
```

> 代码组织的"保留 / 禁止"清单按 `工程结构约束-{档}.md` 走；本文件只说大意。
