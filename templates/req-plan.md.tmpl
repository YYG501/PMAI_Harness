# req-plan —— 本需求的实现文档（范围清单 + 关键决策）

> **这是什么**：本需求的「实现文档」—— build 前你拍板"要建成什么样 + 为什么"。**产品口径，不是工程怎么写**。
> **谁写 / 谁拍**：AI 在范围确认阶段产出（三条上坡路：思路清就直奔清单 / 有岔路就掰扯收敛 / 想看图就出几版草图挑），**PM 拍板**。
> **谁读**：build 照「范围清单」建；覆盖审计拿「范围清单」逐项对照；反向出 PRD 时从「关键决策」取"为什么"。
> **它不装什么**：工程实现细节（组件怎么拆 / 状态怎么管 / mock 数据结构 / 接口怎么 mock）——那些是 AI 后台干的活，不进这份、不用你逐行确认。
> **生命周期**：本需求在途的工作稿；沉淀时内容分流（现状 → PRODUCT-STATE / 规则 → PRODUCT-RULES / 范围+决策 → PRD）后归档。

---

## 一、范围清单（建什么、做不做）

<!-- 结构化到：分区 / 块 / 字段 / tab / 状态 / 交互形式 / 做不做。
     这是 build 完覆盖审计的锚点 —— 列得够细，才能逐项核对"建了 / 丢了 / 降级成占位"。
     边界：只写"有什么"（界面有哪些块、哪些字段、哪种交互形式）；
            别写"顺不顺"（信息挤不挤、流程顺不顺）—— 那是 build 后看着原型才调的，不在这纸上定。 -->

### <页面 / 模块名>

- **分区**：……
- **<块名>**
  - 字段：……
  - 交互形式：<下拉 / 输入框 / 弹窗 / 抽屉 / tab 切换 / 点击跳转 ……>
- **tab**（如有）：……
- **状态覆盖**：<loading / empty / success / error 这版要做哪些>
- **做 / 不做**：做【……】；这版不做【……】

## 二、关键决策（岔路 + 拍了什么 + 为什么）

<!-- 范围确认时掰过的关键岔路。是反向出 PRD 时"为什么这么设计"的依据。
     真系统口径的规则（权限为什么这么设、审批为什么走这条流程）写在这里 ——
     PRD 从这里取，绝不从 mock 原型的随手实现反推。 -->

### <决策一句话标题>

- **岔路**：<面临的几个选择>
- **拍板**：<选了哪个>
- **为什么**：<理由 —— 尤其权限 / 业务规则这类真系统语义>
