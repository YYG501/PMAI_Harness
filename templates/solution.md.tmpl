# Solution — {{REQ_NAME}}

> 本文件是 **PM 视图**（方案摘要、关键决策、交付清单、范围、风险、验收）。
>
> **📂 文档结构 — review 时必读以下全部文件**（任何 `/plan-ceo-review` / `/plan-eng-review` / `/plan-design-review` / `/qa` / `/review` 等 review skill 进入时，**禁止只读本文件就出 finding**——单文件评审不全）：
>
> - 本文件（PM 视图主文件）：方案摘要 / 关键决策 / 交付清单 / 范围 / 风险 / 验收
> - `solution.engineering.md`：数据结构 / 派生状态 / 组件路径 / mock / 算法 / 易错点 / a11y/视口/视觉规范 / plan-review 沉淀 / autoplan 输出 —— **review 始终双读，缺这份评审不完整**
> - `docs/DESIGN.md`：项目级视觉规范 —— design review 必读
> - `docs/modules/*.md`：模块规格 —— eng review 必读
> - `analysis.md` / `brief.md`：上游需求溯源 —— ceo review / 战略评估必读
>
> 写作规则参见 `skills/_shared/PM-VIEW-RULES.md`，特别是：
> - §三 PM 视图写作规则（明确指代 / 正向描述 / 禁工程词 / 禁像素颜色 / 禁反向约束）
> - §六 关键产品决策格式（必填章节）
> - §七 章节顺序约束（本模板按此节锁定的顺序）
> - §九 输入流约束（必读 brief.md / analysis.md / docs/{CONTEXT,DESIGN,prd}.md / docs/modules/\*.md / prototypes/）

- 创建日期：{{CREATED_DATE}}
- 创建者：{{AUTHOR}}
- 分支：{{REQ_BRANCH}}
- 依赖：`brief.md` + `analysis.md`
- 当前 stage：3（方案设计）
- Status：DRAFT（待 PM 确认进入 stage 4/5）

---

## 📌 方案摘要

<!-- 填写规则：
- 1-2 段，每段 2-3 句
- 第一句：本 req 解决什么问题（呼应 analysis.md 的根因结论）
- 第二句：方案的核心选择（一句业务语言概括）
- 第三句：本 req 边界（做什么 / 不做什么）
- 禁止：架构图、技术选型、组件路径、autoplan 修订点 → 这些进 .engineering.md
- 字数控制在 200 字以内；信息密度高、没有铺垫
-->

[2-3 句话讲清本 req 的问题、方案核心、边界]

---

## 📖 术语表

<!-- 填写规则：
- 录入范围：本 req solution 正文中出现 ≥2 次的业务专名
  - 业务对象（许可证 / 部门额度 / 已开通用户）
  - 业务状态枚举（"生效中" / "已过期" / "已分配"）
  - 自定义业务度量（"许可证使用率" = 已开通 / 总数）
- 不录入：
  - 通用词（用户 / 页面 / 操作 / 状态 / 模块）
  - 工程词（schema 字段名 / TS 类型 / reducer / dispatch）→ 这些归 .engineering.md 的字段映射
- "首次出现章节"用本文档自身章节名（如「📐 数据模型与状态」），便于读者跳读
- 无新增业务术语时，删掉表格写一行：本 req 无新增业务术语（沿用 docs/CONTEXT.md 业务术语表）
- 录入条目应当是 PM 在 demo / 走查时会念出来的词，工程同学一眼能对回字段
-->

| 术语 | 业务含义 | 首次出现章节 |
|---|---|---|
| [业务专名] | [一句业务语言释义，不写 schema / 类型] | [章节名] |

---

## 🎯 关键产品决策

<!-- 填写规则（PM-VIEW-RULES §六，必填章节）：
- 表格列：决策项 / 当前选择 / 备选方案
- "决策项"必须明确"在「具体页面 - Tab - 区域」对什么对象做什么决策"
- "当前选择"用完整业务描述，不写实现细节
- "备选方案"至少 1 个，标明取舍
- "共同理由"放表格下方一段集中说明，不在每行的"备选方案"里重复
- 如果本 req 没有 PM 决策点（纯执行型），删掉表格写一行：本 req 无关键产品决策（执行 analysis.md §X 已确定方案）
-->

| # | 决策项 | 当前选择 | 备选方案 |
|---|---|---|---|
| 1 | [在「具体页面 - 区域」对什么对象做什么决策] | [一句完整业务描述] | [1-2 种合理替代，注明为什么没选] |

> 决策的共同理由：[一段话集中说明决策依据]

---

## 📦 交付物清单

<!-- 填写规则：
- 每条交付物一行
- 列：# / 交付物 / 位置 / 工作量(S/M/L) / 本 req 内完成
- "交付物"用产品视角的业务名（"业务意图映射导览文档" / "UI 嵌入点 P0" / "现有 docs cross-link 增补"）
- "位置"写大致路径（"docs/modules/<module>/<filename>.md" / "prototypes/src/modules/<module>/pages/"），不写具体组件路径
- 工作量用 S（≤1 天）/ M（2-3 天）/ L（≥1 周）
- "本 req 内完成"列：✅ / ⚠️ 走 task worktree / ❌ 不在本 req 范围
-->

| # | 交付物 | 位置 | 工作量 | 本 req 内完成 |
|---|---|---|---|---|
| D1 | [产品视角的业务名] | [大致路径] | M | ✅ |

---

## 📐 数据模型与状态（PM 视角）

<!-- 填写规则：
- 用业务语言描述：核心实体、关键状态、状态转换关系、字段业务含义
- 关键状态用文字 + 状态机图（mermaid 或 ASCII，状态名用业务名："生效中"、"已过期"，不用 active/expired）
- 字段写"业务含义"（"许可证的剩余席位数 = 总数 - 已开通 - 已分配给子部门"），不写 TS 类型 / Record / discriminated union
- 不写：schema 定义、字段名 schema、enum 字面量、reducer action、字段类型签名 → 这些全部进 .engineering.md
- 如本 req 不涉及数据模型变更，写一行：本 req 不涉及数据模型变更
-->

### 核心实体

[用业务语言列出本 req 涉及的核心实体（如"许可证 / 部门额度 / 用户开通记录"）+ 每个实体在本 req 中的角色]

### 关键状态机

[如有状态变更，用业务语言列出状态枚举 + 状态转换关系。可用 ASCII 状态图或 mermaid。所有状态名用业务文案（"生效中"），不写 enum 字面量]

### 字段业务含义

[各字段的业务含义 + 字段间关系（如等式 / 派生关系）。不写 TS 类型]

---

## 🧩 模块职责与边界

<!-- 填写规则：
- 每个涉及的模块用一段（约 50-100 字）
- 描述：本模块负责什么 / 不负责什么 / 与其他模块的边界
- 用业务语言（"许可证管理模块负责许可证的导入、状态流转、失效检测"，不写"License 实体的 CRUD"）
- 多模块涉及时按业务链条排序
-->

### 模块 A · [业务名]
[职责 + 边界]

### 模块 B · [业务名]
[职责 + 边界]

---

## 🖼 页面 UI 骨架

<!-- 填写规则：
- 每个涉及的页面用 ASCII 线框图 + 简短文字说明
- 线框图只画信息层级 + 主要区块布局
- 禁止：像素值（24px）、字号、颜色码、组件库类名（Tailwind 等）
- 骨架代码块内**只写 admin / 用户在屏幕上实际看见的字**（PM-VIEW-RULES §3.10）：
  - ❌ 默认值标注（"席位（独占）"）/ 文档元注释（"（新增·简化版）"）/ 设计意图（"（弱化）"）
  - ❌ 字段口径括号（"（多证场景下出现）"）/ 折叠藏默认值（"[▸ 高级配置]"）
  - 这些一律剥离到骨架代码块**下方**的「关键交互说明」段，详见 §3.10 反例对比
- 文字说明每行：[页面名 - 区域名] 显示什么 / 用户可做什么
- 如不涉及 UI，写一行：本 req 不涉及 UI 改动
-->

### 页面 A · [完整页面名（如「产品访问管理 - 产品详情页」）]

```
┌─────────────────────────────────────┐
│ [顶部区域信息]                      │
├─────────────────────────────────────┤
│ [主区域分块]                        │
│ [主操作入口位置]                    │
└─────────────────────────────────────┘
```

[关键交互说明：admin 在该页面能做什么、各操作触发什么响应]

---

## 📋 规格文档变更范围

<!-- 填写规则：
- 表格列：文件 / 变更内容 / 工作量
- "文件"列写具体路径（docs/modules/<module>.md 等）
- "变更内容"用业务语言（"开篇增加 1 段读者引导" / "在元信息块尾部加 1 行 cross-link"）
- 不允许写"重组段落 / 删除原文" — 这应该在 analysis 已确定不做
-->

| 文件 | 变更内容 | 工作量 |
|---|---|---|

---

## 🔄 task 拆分预估

<!-- 填写规则：
- 表格列：Task / 交付物 / Owner 身份 / Worktree / 工作量
- "Task" 用产品视角的功能名（"业务意图映射导览主文档" / "UI 嵌入点 P0"）
- "Owner 身份" = PM + AI / AI execution agent / PM
- "Worktree" = req worktree（改 docs）/ task worktree（改代码）
- 不写技术依赖（"先实现 reducer 再做 dialog"等） → 这进 .engineering.md
- 表格下方一段说明 task 间依赖关系与优先级
-->

| Task | 交付物 | Owner 身份 | Worktree | 工作量 |
|---|---|---|---|---|
| T1 | [产品视角的功能名] | PM + AI | req worktree | M |

**依赖关系与优先级**：[一段说明 task 间的串行/并行关系，以及推荐执行顺序]

---

## 🚧 风险与未决事项

<!-- 填写规则：
- 风险表格列：# / 风险描述 / 可能性 / 预案
- "风险描述" 用正向句式（"X 场景下可能 Y"），不写"禁止 X / 不允许 Y"
- "可能性" = 高 / 中 / 低
- "预案" 一两句话讲清备选路径
- 未决事项用列表，每条标 PM 决策状态：[待答] / [已答 -> 选 X] / [可后置]
-->

### 风险

| # | 风险描述 | 可能性 | 预案 |
|---|---|---|---|
| R1 | [正向描述风险场景] | 中 | [备选路径] |

### 未决事项

- Q1. [PM 待决策项] [待答 / 已答 -> X / 可后置]

---

## ✅ 验收标准

<!-- 填写规则：
- PM 走查清单（不是工程层断言）
- 每条："admin / 研发 / PM 在 X 场景下能 / 做到 Y"
- 不写：grep 校验、单测断言、引用稳定性测试、覆盖率阈值 → 这些全部进 .engineering.md
- 颗粒度：PM 自己能在 demo 时勾选的条件
-->

- [ ] [PM 可走查的成功条件 1，含场景与预期反馈]
- [ ] [PM 可走查的成功条件 2]

---

## 🔧 本轮实现深度变更

<!-- 填写规则（4.5d.3）：
- 默认沿用项目级 `CLAUDE.md` 的「## 工程结构约束」段，本段填「无变更」
- 仅在本 req 要改造项目代码架构（如把数据层从 mock 升级到真实持久化、加完整 RBAC、抽 Template / hook 等）时显式列出变更项
- 自由文本（不是 enum 字段表）；写得越具体，AI 在 task-execute 写代码时越准
- close-req 时如本段非「无变更」，AI 会提示 PM 是否同步到项目级 CLAUDE.md
- 例：「本 req 把数据层从 mock 静态升级为真实 IndexedDB 持久化；权限从不做升级到完整 RBAC（含登录态 + 三级角色）」 -->

无变更

---

## 📁 历史档案

> 工程实现细节见 [`solution.engineering.md`](./solution.engineering.md)

### 变更记录

| 日期 | 变更内容 | 负责人 |
|---|---|---|
| {{CREATED_DATE}} | 初稿 | {{AUTHOR}} |

## 📎 参考材料

<!-- v5 attachments：本 stage 引用过的 attachments/<file> 在此列出（路径 — 重点）。无引用 → 删整段。 -->
