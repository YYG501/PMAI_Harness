# {{PROJECT_NAME}} — 设计规范（DESIGN）

> **这是什么**：产品的视觉与交互约定 —— 界面该长什么样、什么风格、哪些 UI 习惯必须守。
> **为什么写成"正向约束"而不是"禁止清单"**：告诉 AI"该怎么做"（用什么组件、什么节奏、什么密度）比列一堆"别做什么"更能逼出好视觉。**build 前 AI 必读它再动手。**
> **谁读**：原型 build（照着建）、视觉门（对照它审 + 截图给 PM）。
> **谁写**：项目 init 时起草（可借 gstack `design-consultation` 出初稿，也可手写），随产品演进 PM 手动调。

---

## 一、视觉基调（正向约束）

<!-- 每条是"该怎样"，不是"别怎样"。示例（按真实产品改）：
- 整体克制、留白充足，信息密度中等偏低
- 主色 <#色值>，强调色 <#色值>，中性灰阶 <…>
- 圆角 <N px>，阴影克制（只在悬浮 / 弹层用）
- 字号阶梯 <…>，正文 <N px> / 行高 <…>
- 间距统一用 4 / 8 的倍数 -->

## 二、产品化 demo 目标

<!-- build 出来的原型要达到的"可用感"门槛，不是线框稿： -->

- **首屏见价值**：打开就能看懂这个页面是干嘛的，核心信息在最显眼处。
- **每个主按钮最低可用闭环**：主操作点下去有真实反馈（哪怕是 mock 的成功态），不留死按钮。

## 三、UI 习惯（必须守）

<!-- 从实践沉淀的硬约定，build 时不可违： -->

- **不用浏览器原生弹框**：删除 / 高危 / 二次确认一律用项目包装好的弹层组件（如 Modal、确认弹层），不用 `window.alert` / `window.confirm`。
- **宽表处理横向滚动**：前置列该 sticky 的要 sticky；滚动条做基本美化，别露原生粗陋样式。
- **状态要全**：主路径至少覆盖 loading + 成功；empty / error 按需补（这版具体覆盖哪些，在模块 `spec.md` 里定）。

## 四、组件 / token 来源

- UI primitive：`prototype/` 里的 `components/ui`（shadcn）。
- 视觉 token（色 / 间距 / 字号）：以本文件为准，组件实现引用它。

## 共享组件 inventory

> **这是什么**：build 前组件复用查询底座。每个需求动手前逐组件查这里：
> **有 → 复用**；**没有 → 新建并加进本表**。req 间累积，越来越全，reuse 率随之上升。
> **谁管**：本框架（build 时累积 + `/close` 收尾沉淀）。gstack 不写这段。复审的覆盖审计 + 视觉门查这里。

| 组件名 | 用途 | 视觉 | 状态 | 交互 | 出处 req |
|---|---|---|---|---|---|
| <!-- build 时累积，目前为空 --> | | | | | |
