# 设计规范（DESIGN.md）

> **这是什么**：项目级**设计规范** —— 界面长什么样（视觉合约）+ **现有组件清单**（共享组件
> inventory）。是 stage 4 设计阶段 gap-check「逐组件判复用 vs 新建」的查询底座。
> **谁产出**：stage 4 由 `/design-consultation`（gstack skill）建立视觉基线；之后随每个 req
> 的 gap-check 累积新建组件（delta-9）。
> **一份累积、不每 req 重生成** —— 重新生成会打散共享组件清单、reuse 死。
> **性质 = 合约（Contract）**：每个值具体化（hex / px / size-weight），执行期不许偏离。

## 视觉风格

<!-- 一句话描述：例如 "极简、专业、暗色系，参考 Linear 的设计语言" -->

## 颜色方案

<!-- 每个值具体化为 hex。Primary / Secondary / Background / Surface / Error / Warning / Success 等。 -->

| 角色 | 值（hex） | 用途 |
|---|---|---|
| Primary | `#______` | 主操作 / 强调 |
| Background | `#______` | 页面底色 |
| Surface | `#______` | 卡片 / 面板 |
| Error | `#______` | 错误 / 危险操作 |

## 字体

<!-- 字号 + 字重具体化（size-weight）。 -->

| 层级 | 字号 / 字重 | 用途 |
|---|---|---|
| 标题 H1 | `__px / __` | 页面主标题 |
| 正文 | `__px / __` | 常规文本 |
| 代码 | `__px / mono` | 代码 / 等宽 |

## 布局合约

<!-- delta-9：纯视觉层布局约束（GSD UI-SPEC 借鉴）。 -->

- 基础间距单位：`__px`（间距一律取其倍数）
- 栅格系统：`__ 列`
- 最大内容宽度：`__px`
- 页面骨架：<!-- 顶栏 / 侧栏 / 主区 的布局约定 -->

## 响应式

<!-- 视口断点 + 各断点下的布局变化。 -->

| 断点 | 宽度 | 布局变化 |
|---|---|---|
| 桌面 | `≥ __px` | <!-- 默认 --> |
| 窄屏 | `< __px` | <!-- 侧栏折叠 等 --> |

## 无障碍

<!-- a11y 基线：焦点环 / 键盘导航 / 对比度 / aria 约定。 -->

- 焦点环：<!-- 可见焦点环样式 -->
- 键盘导航：<!-- tab order / 快捷键约定 -->
- 对比度：正文 ≥ 4.5:1，大字 ≥ 3:1

## 组件规范 — 共享组件 inventory

> **这是 stage 4 gap-check 查的那份清单**（delta-9）。每个 req 动手前逐组件查这里：
> **有 → 复用**；**没有 → 新建并加进本表**。inventory 随 req 累积越来越全，reuse 率随之上升。

<!-- 每个组件一条目。新建组件在 gap-check / close-task 时追加。 -->

| 组件名 | 用途 | 长什么样（关键视觉 + 行为） | 出处 req |
|---|---|---|---|
| <!-- 按钮 --> | <!-- 主 / 次操作 --> | <!-- 形态 / 状态 / 尺寸 --> | <!-- req-NNN --> |
| <!-- 输入框 --> | | | |
| <!-- 卡片 --> | | | |
| <!-- 对话框 --> | | | |

## 视觉层交互 / 文案呈现规则

<!-- delta-9：DESIGN.md 只管视觉层交互（hover / 动效 / 焦点环）与文案呈现规则；
     功能交互归 prd.md，文案 / 术语归 prd.md + CONTEXT.md 术语表。 -->

- hover / 动效：<!-- 时长 / 缓动 -->
- 文案呈现：<!-- 空状态 / 错误提示 的呈现风格 -->

## 创意自由度

> **为什么要明确这个：** 把约束区域和自由区域分开说清楚，agent 知道哪里可以发挥、哪里必须遵守。
> 这是解决"agent 一板一眼"的关键：不说清楚，agent 会把所有地方都做得中规中矩。
> （GSD UI-SPEC 没有这块，是本框架独有 —— 不丢。）

- **高自由度区域**：动效、微交互、图表样式、卡片排列方式、空状态文案
- **低自由度区域**：信息层级、关键操作按钮位置、状态标签颜色、色彩系统、间距基准
- **不可偏离**：模块规格 / prd.md 功能清单中的功能行为、数据规则、角色权限

## Checker Sign-Off

> delta-9：`DESIGN.md` 定稿过一道几维检查（借鉴 GSD `gsd-ui-checker` 6 维思路）。
> stage 4 定稿前 PM 逐维过一遍。

- [ ] 颜色 / 字体 / 间距值都已具体化（无 `__` 占位残留）
- [ ] 布局合约 / 响应式 / 无障碍 三块都已填
- [ ] 共享组件 inventory 至少覆盖本项目已有的核心组件
- [ ] 创意自由度三档都已明确
- [ ] 视觉层交互 / 文案呈现规则已填
- [ ] 与 `prd.md` 无功能交互重复（DESIGN.md 只管视觉层）
