# mockups/ —— 探索变体的家

> **这是什么**：你在探索期一口气试出来的那些**并行小原型**（不同方向的草图 / approach-A·B·C / 各种 skeleton）都放这儿。它和最终建造结果是两码事：真正代码根与入口由 `.pm-workflow/project.yml` 定义，`mockups/` 只是摊在桌上比来比去的草图集，不冒充已建产品。
> **为什么要有这个家**：以前这些草图散在各处、合并了一版剩下的就再也找不回，过几天想"上次那个左边导航的版本去哪了"翻不到。现在它们都有固定位置，且能翻得到。

---

## 三件东西

- **变体本身**（各个 mock 文件 / 目录）—— 所有生成出来或导入进来的都落这儿、都进版本库，**包括当场被你否掉的草图**。否掉不等于删掉，留着是为了下次还能翻回来看。
- **`manifest.json`** —— **清单，唯一的真相源**。每个值得留的方向在这儿登记一条：来自哪个需求和轮次、时间、核心做法、适用情境、主要取舍与状态。
- **`audits/`** —— 每轮生成前的设计依据，以及生成后的桌面 / 窄屏截图和逐条视觉验收结果。新版设计稿没有通过这层检查，不进入看版。
- **`index.html`** —— **看版导航页**，从 `manifest.json` 自动生成。**别手改它**（手改下次重生成就被覆盖）；要调内容改 `manifest.json` 再重生成。
- **`viewer.html`** —— **查看页**，从 `manifest.json` 自动生成。卡片点进去先到这里，顶部有“返回目录”和“打开页面”。

> 一句话：**清单是底账，看版是它的展示面。** 永远改账、不改面。

## 一条变体记什么

| 字段 | 记什么 |
|---|---|
| 来自哪个需求 | 这版设计稿属于哪个需求 / 模块；看版会先按它分组 |
| 标题 | 这张卡在看版里的方向名，不重复写轮次 |
| 路径 | 点开能看那个 mock 的相对路径 |
| 核心做法 | 这一版怎么组织信息和任务路径，一句话 |
| 适合 | 最适合哪类判断或使用情境，一句话 |
| 主要取舍 | 为了得到这个效果牺牲什么；不能只写优点 |
| PM 反馈 | 可选；PM 对这一版的明确意见，不和“适合”混写 |
| 状态 | `活跃` / `待合并` / `已退役` |
| 出自哪轮 | 第一轮 / 第二轮这类探索轮次；同轮多方向写相同值 |
| 本轮要判断 | 这一轮共同验证的产品问题 |
| 时间 | 首次生成时间和最后修改时间；看版按最近更新时间倒序 |
| 设计依据 | 本轮实际绑定的 `DESIGN.md`、现有页面 / 组件，以及必须继承、复用、允许改变和需要守住的设计要求 |
| 视觉验收 | 桌面与窄屏截图，以及设计原则、现有外壳复用、信息层级、任务路径、响应式和文字控件检查结果 |

还有一个 **精选** 标记：探索收敛、你挑定值得留的那几版打上精选，看版里会突出显示；没打精选的仍在目录里、能翻到，只是看版不高亮。看版先按需求，再按轮次组织；最新轮默认展开，旧轮折叠，同轮已选方向排在最前。

## 谁来登记

平时是 **AI 帮你记**：它生成 mock 或导入外部图片时顺手往清单写一条草稿，收敛时再问你哪些值得留、登记成精选；你过一眼、改不对的地方。你不用自己手敲清单。

## 外部设计稿怎么进来

- **gstack /design-shotgun**：可以用来做视觉方向探索；跑完后，AI 会把 gstack 生成的候选稿和挑定结果复制回本目录，再刷新看版。规格和 build 只引用这里的路径，不引用 `~/.gstack/...`。
- **PM 上传图片**：截图、设计图、竞品图可以作为设计素材导入本目录参与比选。图片能帮助判断方向，但不会自动变成规格事实；最后仍要回到 `/pmai-design` 把选定方向讲清楚。

## 状态怎么走

- **活跃 / 待合并**：还在比较 / 准备带进 build target 的，看版里高亮排在上面。
- **已退役**：亮点已经进入最终建造结果后，那一版标 `已退役`，并记一句"已进入建造结果（位置 / commit）"。**退役 = 收起来灰掉，不是删掉** —— 它仍在 `mockups/` 里、仍在清单里、看版里折叠着能翻开看。真想清理目录，你手动删。

## 看版怎么刷新

往 `manifest.json` 加 / 改一条之后，重生成看版即可（AI 在沉淀那一步会顺手做，你也可以让它重跑）：

```
python3 "$PMAI_HOME/scripts/gen-mock-board.py" <项目根目录>
```

读 `mockups/manifest.json`、写 `mockups/index.html` 和 `mockups/viewer.html`。清单空的也不报错，生成一张"暂无变体"的空看版。
