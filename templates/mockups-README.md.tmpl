# mockups/ —— 探索变体的家

> **这是什么**：你在探索期一口气试出来的那些**并行小原型**（不同方向的草图 / approach-A·B·C / 各种 skeleton）都放这儿。它和 `prototype/` 是两码事 —— `prototype/` 是**跑着的那一份主原型**（单一真相源），`mockups/` 是**摊在桌上比来比去的草图集**，静态、不塞进主原型。
> **为什么要有这个家**：以前这些草图散在各处、合并了一版剩下的就再也找不回，过几天想"上次那个左边导航的版本去哪了"翻不到。现在它们都有固定位置，且能翻得到。

---

## 三件东西

- **变体本身**（各个 mock 文件 / 目录）—— 所有生成出来的都落这儿、都进版本库，**包括当场被你否掉的草图**。否掉不等于删掉，留着是为了下次还能翻回来看。
- **`manifest.json`** —— **清单，唯一的真相源**。每个值得留的变体在这儿登记一条：来自哪个需求、标题、路径、探索什么、好东西、状态、出自哪轮。
- **`index.html`** —— **看版导航页**，从 `manifest.json` 自动生成。**别手改它**（手改下次重生成就被覆盖）；要调内容改 `manifest.json` 再重生成。

> 一句话：**清单是底账，看版是它的展示面。** 永远改账、不改面。

## 一条变体记什么

| 字段 | 记什么 |
|---|---|
| 来自哪个需求 | 这版设计稿属于哪个需求 / 模块；看版会先按它分组 |
| 标题 | 可选；这张卡在看版里的标题，不填就用“探索什么” |
| 路径 | 点开能看那个 mock 的相对路径 |
| 探索什么 | 这一版想试的方向，一句话 |
| 好东西 | 这一版值得合进主原型的亮点（合并候选），一句话 |
| 状态 | `活跃` / `待合并` / `已退役` |
| 出自哪轮 | 第一轮 / 第二轮这类探索轮次；需求归类看“来自哪个需求” |

还有一个 **精选** 标记：探索收敛、你挑定值得留的那几版打上精选，看版里会突出显示；没打精选的仍在目录里、能翻到，只是看版不高亮。

## 谁来登记

平时是 **AI 帮你记**：它生成 mock 时顺手往清单写一条草稿（毕竟 mock 是它建的），收敛时再问你哪些值得留、登记成精选；你过一眼、改不对的地方。你不用自己手敲清单。

## 状态怎么走

- **活跃 / 待合并**：还在比较 / 准备并进主原型的，看版里高亮排在上面。
- **已退役**：好东西并进主原型之后，那一版就标 `已退役`，并记一句"已并入主原型（位置 / commit）"。**退役 = 收起来灰掉，不是删掉** —— 它仍在 `mockups/` 里、仍在清单里、看版里折叠着能翻开看。真想清理目录，你手动删。

## 看版怎么刷新

往 `manifest.json` 加 / 改一条之后，重生成看版即可（AI 在沉淀那一步会顺手做，你也可以让它重跑）：

```
python3 "$PMAI_HOME/scripts/gen-mock-board.py" <项目根目录>
```

读 `mockups/manifest.json`、写 `mockups/index.html`。清单空的也不报错，生成一张"暂无变体"的空看版。
