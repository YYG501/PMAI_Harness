# Solution Engineering — {{REQ_NAME}}

> 本文件是 **工程合同**（agent / eng review / autoplan 用）。PM 默认不看。
> PM 视图见 [`solution.md`](./solution.md)。
>
> 本文件允许出现所有工程内容：TS 类型、字段名、reducer、props、像素值、颜色码、emoji 视觉、反向约束、autoplan / GSTACK REVIEW 沉淀等。
> **唯一原则**：本文件不重复 PM 视图已有的功能行为描述；本文件只补充实现层细节。

- 关联文件：`solution.md`（同目录）
- 创建日期：{{CREATED_DATE}}
- 当前 stage：3（方案设计 - 工程合同）

---

## 1. 数据结构定义

<!-- 填写规则：
- 写本 req 涉及的数据结构、TS 类型、Record / discriminated union / schema 定义 / enum 字面量
- 用 ```ts 代码块，不只是文字描述
- 列出新增字段、变更字段、废弃字段
- 字段名严格按代码层命名（snake_case / camelCase 按项目约定）
- 引用项目内现有类型时给文件路径
-->

### 1.1 新增 / 变更类型

```ts
// 示例
type LicenseRecord = {
  id: string;
  status: 'active' | 'pending' | 'expired' | 'disabled' | 'revoked';
  // ...
};
```

### 1.2 字段约束

[列出字段层级的约束：必填 / 默认值 / 校验规则 / 唯一性等]

### 1.3 废弃字段（如有）

[列出本 req 废弃的旧字段 + 替代字段]

---

## 2. 派生状态规则（代码层）

<!-- 填写规则：
- 写本 req 涉及的派生函数、状态计算逻辑、reducer 设计
- 包含：函数签名、参数类型、返回类型、副作用（如有）
- 性能优化点（useMemo 依赖、引用稳定性、避免无谓重渲染）
- 单 reducer / 多 reducer 的选择 + 理由
- 失败回滚路径（如适用）
-->

### 2.1 派生函数清单

| 函数名 | 签名 | 说明 |
|---|---|---|

### 2.2 reducer 设计（如适用）

[reducer 的 action 列表 / state shape / 引用稳定性约束]

### 2.3 性能优化点

[useMemo / React.memo / 引用稳定性 / nowBucket 等]

---

## 3. 组件实现路径

<!-- 填写规则：
- 列出本 req 新建 / 修改 / 不动的组件文件路径
- 用具体文件路径（prototypes/src/...），不是大致路径
- 标注每个组件的职责 + props 接口
- 标注复用关系（哪个组件引用哪个）
-->

### 3.1 新建组件

| 路径 | 职责 | 关键 props |
|---|---|---|

### 3.2 修改组件

| 路径 | 改动内容 | 影响范围 |
|---|---|---|

### 3.3 不动组件（明确排除）

[列出本 req 严格不动的组件 + 不动的理由]

---

## 4. Mock 数据改造清单

<!-- 填写规则：
- 列出 mock 文件路径
- 需要补充的 mock 样例：场景 / 触发条件 / 预期表现
- 已有 mock 是否需要修改
-->

### 4.1 mock 文件清单

| 路径 | 改动内容 |
|---|---|

### 4.2 需要补充的样例场景

| 场景 | 触发条件 | 预期 mock 表现 |
|---|---|---|

---

## 5. 关键算法消费规则

<!-- 填写规则：
- 列出本 req 涉及的关键算法 / 纯函数 + 调用方
- 函数签名 + 输入输出含义
- 调用链：哪些 UI / reducer 消费此函数
- vitest 单测条目
-->

### 5.1 纯函数清单

| 函数 | 签名 | 调用方 |
|---|---|---|

### 5.2 单测条目

| 函数 | 测试场景 | 期望结果 |
|---|---|---|

---

## 6. 易错点 / 反向约束

<!-- 填写规则：
- 反向描述允许：禁止 X / 不允许 Y / 不要 Z
- 每条按"行为 → 后果"格式（"X 行为会导致 Y 问题，所以禁止 X"）
- 来源标注：源 PM 反馈 / 源 plan-eng-review / 源历史 task 踩坑
-->

| # | 禁止行为 | 后果 / 理由 | 来源 |
|---|---|---|---|
| 1 | [具体禁止的实现做法] | [行为会导致什么问题] | [PM 反馈 / V1 / 历史 task] |

---

## 7. plan-eng-review / plan-design-review 沉淀

<!-- 填写规则：
- 每条 review 沉淀编号 V1, V2, ...，按 review 时间顺序
- 按"决策 / 理由 / 落地约束"三段式
- 视觉沉淀（V1-V10 类）/ 架构沉淀（V11-V20 类）/ 状态-动效沉淀（V21+）分小节
- 与 §1-§6 冲突时本节为准，需在 §1-§6 末尾标注"本条已被 V_X 取代"
-->

### 7.1 视觉沉淀（plan-design-review V1-V10）

#### V1 [决策标题]
**决策**：[简短描述]
**理由**：[为什么这样选]
**落地约束**：[实现时必须遵守的具体规则]

### 7.2 架构沉淀（plan-eng-review V11-V20）

[同上格式]

### 7.3 状态/动效沉淀（plan-design-review V21+）

[同上格式]

---

## 8. autoplan 修订点 / 决策表

<!-- 填写规则：
- 来自 autoplan 跑过后产出的 Decision Audit Trail / Voices Consensus / GSTACK REVIEW REPORT
- 原样保留 autoplan 输出，作为审计留痕
- PM 不看本节
-->

### 8.1 Decision Audit Trail

[autoplan 决策追溯]

### 8.2 Voices Consensus Tables

[autoplan voices 共识表]

### 8.3 User Challenges 决策表

[PM 在 autoplan 过程中的挑战与决议]

### 8.4 GSTACK REVIEW REPORT（如有）

[gstack review 输出原文]

---

## 9. a11y / 视口 / 视觉规范细则

<!-- 填写规则：
- 像素值、颜色码、字号、间距、间隔、emoji 视觉等所有具体视觉参数
- 视口断点（1280px / 1024px / mobile）下的布局变化
- a11y 要求（role / aria-label / tab order / focus trap / 键盘导航）
- 与 docs/DESIGN.md 对齐，不复制粘贴 DESIGN 内容；引用 DESIGN.md §X
-->

### 9.1 视觉参数

| 元素 | 规格 | 备注 |
|---|---|---|

### 9.2 视口断点

| 断点 | 布局变化 |
|---|---|

### 9.3 a11y 要求

| 区域 | a11y 要求 |
|---|---|

---

## 10. 工程层验收清单

<!-- 填写规则：
- agent 自审 / eng review / qa 跑通后逐条勾选的工程层断言
- 包含：grep 校验、单测条目、引用稳定性测试、覆盖率阈值、性能指标
- 与 PM 视图的「✅ 验收标准」分工：PM 视图是 demo 走查、本节是 agent 自动化校验
-->

- [ ] [grep 校验：grep -E "..." 在 X 文件返回 0 命中]
- [ ] [单测条目：xxx.test.ts 覆盖 N 个场景全 PASS]
- [ ] [引用稳定性测试：reducer view-only action 后 state.X 引用相等]
- [ ] [性能指标：toggle 单行展开时其他行不重渲染（React DevTools 验证）]
- [ ] [覆盖率：纯函数模块测试覆盖率 ≥ N%]
