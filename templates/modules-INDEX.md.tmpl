# 模块索引

> 本文件由 close-req §1.5 主 rewrite 完成后**单独 refresh** 维护（不进 REWRITE_COVERED_FILES metric）。
> 每条 ≤30 字，写模块**用途**，不写功能清单。
> AI 在 Stage 2 / 3 / 5 / 6 / 7.x 可读本文件快速判断"新需求落哪个模块"。

| 模块 | 简介 |
|---|---|
