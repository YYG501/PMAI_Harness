<!-- 项目决策记录（冻结档）模板 · runtime framework .tmpl
     skill 内部按 $PMAI_HOME/templates/ 直接调用（strategy / init-project step C / close 共用），不拷进消费仓。
     项目级冻结决策记录：写一次、带日期、不维护，当前真相仍回到脊柱文档。
     落点：消费仓 docs/decisions/<日期>-<slug>.md（命名 R5：目录 decisions/、中文类名「项目决策记录」）。
     用法：把所有 <尖括号占位符> 填掉；写一次就不动（方向变写新的一份，旧的留作历史坐标）。 -->

<!-- ╔══════════════════════════════════════════════════════════════╗
     ║  冻结档 · 写于 <日期> · 之后不维护                            ║
     ║  这不是"现状"——它是 <日期> 那一刻的判断快照。               ║
     ║  当前产品长什么样永远以 docs/PRODUCT-STATE.md（脊柱）为准。   ║
     ║  本档只装"为什么这么拼"的理路；事实一律用指针指向脊柱。       ║
     ╚══════════════════════════════════════════════════════════════╝ -->

# 项目决策记录：<一句话主题> （冻结于 <日期>）

> **性质**：冻结档，写一次不维护。读者看到的是「<日期> 那时的判断」——是历史坐标，不是当前真相。
> **当前真相去哪看**：产品现状 → `docs/PRODUCT-STATE.md`；定位/角色/术语 → `docs/PRODUCT.md`；视觉 → `docs/DESIGN.md`；跨功能规则 → `docs/PRODUCT-RULES.md`。
> **出自**：<strategy 方向讨论 / init-project 奠基 / 模块工作收尾产出的项目级理路>。

---

## 理路：为什么这么拼

<!-- 这是冻结档的主体，只装"跨文件的整体设计意图"，不复述离散事实。常见几类（按本轮实际有的写，没有的删）： -->

### 护城河 / 差异化论证

<!-- 为什么这个产品/这套机制有别人难复制的地方；当时怎么论证的。 -->

### 机制整体的设计意图

<!-- 几个字段/角色/状态/规则合起来想达成什么——单看每条规格看不出的"整体为什么这么咬合"。 -->

### 关键交互理念的咬合推导

<!-- 某个交互为什么这样设计、它和别的交互怎么互相支撑（推导链，不是结论清单）。 -->

### 演进故事（v2 / 将来往哪走）

<!-- 当时设想的演进方向 / 暂时不做但记下的取舍。-->

---

## 当时的关键事实（带日期的快照 + 指向当前真相源）

<!-- F-D1：只放指针拿不到"当时的判断语境"——读者会拿 then-why 配 now-what。
     所以这里并存：① 冻结当时的关键事实摘要（带日期，自包含读得懂） ② 指针指向当前真相源（可能已被后续需求改写）。 -->

| 当时的事实（<日期> 快照） | 当前真相源（可能已变） |
|---|---|
| <例：主角色是「部门管理员」，24 字段 ontology，owner 用颜色区分> | 以 `docs/PRODUCT.md` 用户画像 / `docs/modules/<模块>.md` 为准 |
| <...> | <...> |

---

<!-- 写完落 docs/decisions/<日期>-<slug>.md。这份不进"每次必读核心"，是脊柱的索引展开层：
     后续 AI 查"为什么这么拼"、范围触及本主题时才按需读回（见 templates/文档地图.md §二/§三）。 -->
