# Task {{TASK_NUMBER}}: {{TASK_NAME}}

<!-- ⚠️ 标题下方禁止加 frontmatter 段落。

  ✅ 正确：状态在 ## 📌 任务卡 表格里
    | **状态** | 待确认 |

  ❌ 错误（AI 常踩）：
    > 状态：「待启动」          ← 中文引号 + blockquote
    **状态：** 待执行            ← 标题下方 frontmatter 段
    | **状态** | 待启动 |        ← 派生显示标签当字段值

  合法状态值（5 态）：待确认 / 执行中 / 待验收 / 已完成 / 已废弃
  ("待启动 / 待执行" 是 status-view.py 派生显示标签，不是字段值)
  closed/ 下旧 task 是 v1 历史格式，新 task 不参考。
-->

> 本文件是 **PM 视图**（任务卡、关键决策、产物预览、功能清单、范围、验收清单、历史档案）。
> 工程实现细节（启动前必读、功能清单工程版、实现指引、易错点、plan-review 沉淀、工程层验收）见 [`task-{{TASK_NUMBER}}-{{TASK_SLUG}}.engineering.md`](./task-{{TASK_NUMBER}}-{{TASK_SLUG}}.engineering.md)。
>
> 写作规则参见 `skills/_shared/PM-VIEW-RULES.md`，特别是：
> - §三 PM 视图写作规则（明确指代 / 正向描述 / 禁工程词 / 禁像素颜色 / 禁反向约束）
> - §五 功能清单格式（N · 功能名 + 可选「使用角色」blockquote + 4 列表格「二级 / 三级 / 使用角色 / 需求描述」+ 续行前 3 格留空 rowspan + 需求描述列内联编号）
> - §六 关键产品决策格式
> - §七 章节顺序约束（本模板按此节锁定的顺序）
> - §九 输入流约束（必读 brief.md / analysis.md / task-plan.md / solution.md（PM 视图）/ docs/{CONTEXT,DESIGN,prd}.md / docs/modules/<module>.md / 同模块已完成 task / prototypes/）

---

## 📌 任务卡

| | |
|---|---|
| **状态** | 待确认 |
| **所属模块** | [模块名 / 多个模块逗号分隔 / 基础设施] |
| **所属模块章节** | [一级章节名；基础设施 task 留空] |
| **dev server** | （执行时填写） |
| **worktree** | （执行时由 create-task-worktree.sh 填写） |
| **创建时间** | {{CREATED_DATE}} |
| **分支** | task-{{TASK_NUMBER}}-{{TASK_SLUG}} |

**依赖**：

<!-- 结构化 task ID 列表（task-confirm / task-execute 依赖前置 gate 会机器解析）。
     格式：一行一个 - task-NNN (一句话说明)；无依赖写 "无"。
     不允许写成自然语言描述（如"依赖 task-2 和 task-3"），机器解析规则只提取 task-NNN。 -->
- 无

**做什么**（10 秒理解）：

<!-- 填写规则：
- 1-2 句话讲清本 task 交付什么、改哪个区域、不改哪个区域
- 不写技术细节（reducer / props / 字段名等）→ 进 .engineering.md
- 不写为什么这样选（决策理由）→ 进「关键产品决策」共同理由行
-->

[1-2 句任务描述]

---

## 🎯 关键产品决策

<!-- 填写规则（PM-VIEW-RULES §六）：
- 表格列：决策项 / 当前选择 / 备选方案
- "决策项"必须明确"在「具体页面 - Tab - 区域」对什么对象做什么决策"
- "当前选择"用完整业务描述
- "备选方案"至少 1 个，标明取舍
- "共同理由"放表格下方一段集中说明
- 如本 task 无关键产品决策（纯执行型）：删掉表格，写一行：
  本 task 无关键产品决策（执行 solution.md §X 已确定方案）
-->

| # | 决策项 | 当前选择 | 备选方案 |
|---|---|---|---|
| 1 | [在「具体页面 - 区域」对什么对象做什么决策] | [完整业务描述] | [1-2 种合理替代] |

> 决策的共同理由：[一段话集中说明]

---

## 📐 产物预览

<!-- 填写规则（按 task 类型生成）：
- UI task（审查工具含 /design-review，或描述涉及 页面/组件/界面/前端/UI/view）→ ASCII 线框图
- 纯文档 task（审查工具为空，描述含 文档/PRD/规格/说明/spec）→ markdown bullet 树形大纲
- 其他（基础设施 / 后端 / 全栈）→ 写「无（[类型] task）」

UI 线框图填写规则：
- 用 markdown code block + ASCII 字符表达信息层级 + 主要区块布局
- 标注顶部信息区 / Tab / 主区域分块 / 关键操作入口位置

ASCII 框图代码块内硬约束（PM-VIEW-RULES §3.10：只允许"屏幕上看得见的字"；任何释义、注释、设计意图 → 全部下沉到框图代码块下方的「关键交互说明」段）：
- ❌ 默认值标注：「席位（独占）」/「1（独占，固定）」→ 系统恒定属性，直接写值「1」，"独占"语义进 §交互说明
- ❌ 文档元注释：「（新增·简化版）」/「（仅租户管理员可见）」→ 给文档读者的，admin 屏幕上看不见，整段删除
- ❌ 设计意图：「（已吊销，弱化）」/「（弱化）」→ 文案保留状态名「已吊销」；视觉弱化进 §交互说明，引 docs/DESIGN.md
- ❌ 字段口径解释：「（多证场景下出现）」/「（按 X 维度集动态显示）」→ 屏幕字保留入口名；触发条件 / 数据派生进 §交互说明
- ❌ 折叠藏默认值：「[▸ 高级配置]」← 默认值藏在这里 → 默认值直接渲染；override 走单独按钮（如「改为共享部门额度」）
- ❌ 像素值 / 颜色码 / 字号 / Tailwind 类名 / 组件库类名 → 进 .engineering.md §8

大纲填写规则：
- markdown bullet list 树形（嵌套 -），章节层级 2-3 级
- 章节名用产品视角的业务名（不是文件名 / 类名）
-->

```
[ASCII 线框图]
```

[关键交互说明：管理员（租户管理员 / 部门管理员）在该产物上能做什么、各操作触发什么响应]

---

## 📋 功能清单

<!-- 填写规则（PM-VIEW-RULES §5.1，4 列表格 + 续行 rowspan + 需求描述列内联编号）：

结构：
- 标题：N · 功能名 — 必须含完整指代前缀（如「产品详情页 - Tab 1 额度分配视图」）
- 紧接标题（可选）：> **使用角色**：[...]（当本节整体角色统一时写在 blockquote；逐行不同则只填表内列）
- 4 列功能表格：二级功能 / 三级功能 / 使用角色 / 需求描述
- 同一三级功能的多条需求 → 第 2 条起的前 3 列**留空**（视觉等同 rowspan）
- 需求描述列内联数字编号：每条 1. / 2. / 3. ...
- 字段定义、计算口径、边界规则等直接写进需求描述列某条编号项；**不另起字段口径独立表**
- 正向句式：「系统提供 X」/「管理员 在 Y 场景执行 Z 后系统反馈 W」
- 表后（可选）：> **注**：[本节额外约束 / 边界]

需求描述列编号项硬约束（§5.2 判别简则：「换一套 UI 还成立吗？」成立 = 业务规则；不成立 = UI 描述，挪走）：
  ❌ UI 排版 / 视觉容器：灰底 / 圆角 / 2 列 grid / 上下结构 / 顶部 / 底部 / 副标题 / 主按钮 / 二级按钮 → 进 §📐 产物预览 + .engineering.md §8
  ❌ 元素位置 / 顺序：「第 1 行 X、第 2 行 Y」/「字段顺序：A / B / C」/「右侧依次是 ...」 → 进 §📐 产物预览
  ❌ UI 控件类型：segmented control / kebab / tree connector / 折叠区 / sticky / overlay → 进 .engineering.md §8
  ❌ 样式参照：「与 X 同款」/「与 Y 对齐」/「一致样式」 → 进 .engineering.md §8
  ❌ 反向约束：「禁止 X」/「不允许 Y」/「不准 Z」 → 进 .engineering.md §6
  ❌ 设计意图：「（避免双重计数）」/「（防止角色爆炸）」 → 进 §🎯 关键产品决策的「共同理由」
  ❌ 工程词：reducer / dispatch / props / hook / useState / schema / topContent / event.stopPropagation → 进 .engineering.md §4
  ❌ 反引号代码引用（§3.8）：`useState` / `getXxx()` / `field.attribute` / `onClick` 等 → 一律不出现在 PM 视图
  ❌ 中英混杂业务词（§3.8）：admin / scope / CRUD / review / active / pending / null / undefined / existing / sum / count / map / filter / reduce → 改为日常中文（管理员 / 管辖范围 / 创建-编辑-删除 / 评审 / 已激活 / 待生效 / 无值 / 未设置 / 现有记录 / 累加 / 统计 / 按 X 归并 / 按 X 筛选 / 合并）
  ❌ 数学符号（§3.8）：∩ / ∪ / ⊊ / ⊆ / 严格小于 → 改业务语言（"两者交集" / "完全包含且至少少一个"）

字段值口径写作（§5.3，当字段定义出现在需求描述列时）：
- 用业务动词：累加 / 统计 / 取 / 减 / 按 X 归并 / 按 X 筛选；不用 sum / count / reduce / map / filter
- 状态字面量用中文：已激活 / 待生效 / 无值 / 未设置；不用 active / pending / null / undefined

已弃格式（§5.5 反例）：3 列表 + 需求列表脱表（同一节多角色时角色对应关系丢失；二级 / 三级容易凑层）

基础设施 task：本 section 填「无（基础设施 task）」
-->

### 1 · [产品视角的功能名 — 必须含完整指代前缀]

> **使用角色**：[当本节整体角色统一时写在此 blockquote；逐行不同则只填表内列，本行删除]

| 二级功能 | 三级功能 | 使用角色 | 需求描述 |
|---------|---------|---------|---------|
| [二级功能名] | [三级功能名] | [角色名 — 角色限定条件] | 1. [业务命题或规则；过「换 UI 还成立吗」判别简则] |
| | | | 2. ... |
| [二级功能名] | [另一三级功能名] | [角色名] | 1. ... |
| | | | 2. ... |

> **注**：[可选，写明本节额外约束 / 边界；不需要时整行删除]

### 2 · ...

---

## 🚦 跨功能产品规则

<!-- 填写规则：
- 来自同模块已完成 task 的 PM 反馈中"正向规则"类条目（PM-VIEW-RULES §9.4 三类分流）
- 跨多个功能的共同业务规则（统一文案 / 统一行为 / 数据闭环关系等）
- 每条 1-2 句完整描述，不缩略
- 如本 task 无跨功能规则：删掉本 section
-->

1. [跨功能产品规则 1，正向描述]
2. [跨功能产品规则 2]

---

## 📦 范围

<!-- 填写规则：
- 「改」列出本 task 涉及的页面 / 区域（用产品视角的业务名，不写文件路径）
- 「不改」列出明确排除的页面 / 区域 + 排除理由
- 文件级路径列表进 .engineering.md
-->

**改**
- [产品视角的区域名 1]
- [产品视角的区域名 2]

**不改**
- [明确排除的区域 + 排除理由]

---

## ✅ 验收清单（PM 走查）

<!-- 填写规则：
- PM 走查清单（不是工程层断言）
- 每条："管理员（租户管理员 / 部门管理员）/ PM 在 X 场景下能 / 做到 Y"
- 不写：grep 校验、单测断言、引用稳定性测试、覆盖率阈值 → 进 .engineering.md §9
- 颗粒度：PM 自己能在 demo 时勾选的条件
- 业务模块 task 必填；基础设施 task 也必填可验证条件（脚手架可启动、脚本可运行等）
-->

### 主路径
- [ ] [PM 可走查的成功条件 1]
- [ ] [PM 可走查的成功条件 2]

### 边界路径
- [ ] [边界场景 1]

### 文案
- [ ] [文案统一性条件]

### 视觉与可访问性
- [ ] [视觉走查条件，不写像素值]

---

## 📁 历史档案（验收时打开看最新一轮）

> 工程实现细节（启动前必读 / 实现指引 / 易错点 / plan-review 沉淀 / 工程层验收）见 [`task-{{TASK_NUMBER}}-{{TASK_SLUG}}.engineering.md`](./task-{{TASK_NUMBER}}-{{TASK_SLUG}}.engineering.md)

### 执行日志（agent 填写，不要删除历史记录）

#### 执行报告 - [YYYY-MM-DD HH:MM]
**改动摘要：** [做了什么]
**新建文件：** [列表，没有则填"无"]
**修改文件：** [列表]
**遇到的问题：** [如无则填"无"]
**文档对齐预告：** [本轮原型改动可能影响 task md 哪些业务字段；首次实现 / 无影响填"无"]
- §📋 字段口径表 第 N 行 → 改成 ...（举例）
- §🎯 决策 #N → 反转为方案 B（举例）
**验收标准完成情况：**
- [x] 条件 1
- [ ] 条件 2（未完成原因：...）

> **「文档对齐预告」字段说明**：
> - task-execute 反馈循环里，AI 改原型代码后在此预告可能影响的 task md 段
> - close-task §0 读这个字段当对齐线索，跟原型代码做最终 batch 对齐
> - 反馈循环规则禁止 AI 直接改 §🎯/§📐/§📋/§🚦/§✅ 正文，**只在这里预告**
> - 首次执行（非反馈循环）填"无"

---

### PM 反馈（orchestrator 代为追加，agent 重跑前必读）

#### 反馈 1 - [YYYY-MM-DD]
**问题描述：** [发现了什么问题]
**要求修改：** [具体怎么改]
**分类（PM-VIEW-RULES §9.4）**：[正向规则 / 反向约束 / 决策记录 / 视觉规范]
**处理结果：** 待处理 / 已处理

> PM 反馈分类规则：
> - 正向规则（"统一用 X" / "全文用 Y"）→ task-spec 同步入下一个 task 的「跨功能产品规则」
> - 反向约束（"禁用 X" / "不要 Y"）→ task-spec 同步入下一个 task 的工程合同 §6 易错点 / 禁止项
> - 决策记录（"二审改 X" / "重做为 Y"）→ task-spec 同步入下一个 task 的「关键产品决策」备选方案列
> - 视觉规范（"spinner 改 +/-" / "ⓘ 用 lucide" / "chip 改 box" / "弹窗 footer baseline" 类控件/间距/图标/配色决策）→ close-task 步骤 1.5 反推到 `docs/DESIGN.md`，PM 逐条选 Y-rule / Y-task-note / N
>
> ⚠️ 视觉规范类反馈**禁止塞到反向约束**（会进工程合同 §6 task-local sink，下一个 task 起新组件时 LLM 没参考、重复踩坑）

---

### 业务层偏差（task 实证发现 req / 项目级文档需修订处）

<!-- 填写规则：
- 默认填「无」（多数 task 不会推翻已有文档）
- 当本 task 实证发现 brief.md / analysis.md / solution.md（PM 视图）/ prd.md / docs/modules/<module>.md
  内容需修订时，按下方格式列出
- 工程层偏差（接口签名 / 字段命名 / 组件路径不一致）写工程合同 §10 文档偏差，不在此重复
- close-task 时调用 /doc-update 对账模式扫此段 + §10 → 呈交 PM 决定逐条修订原文
- 推翻 / 修订都按"文档位置 + 文档原文 + 实证发现 + 建议改法"四列描述
-->

| 文档位置 | 文档原文 | 实证发现 | 建议改法 |
|---|---|---|---|

无
