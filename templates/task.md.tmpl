# Task {{TASK_NUMBER}}: {{TASK_NAME}}

<!-- task_format: single-typed-v3 -->

<!-- ⚠️ 标题下方禁止加 frontmatter 段落。

  ✅ 正确：状态在 ## 📌 任务卡 表格里
    | **状态** | 待执行 |

  ❌ 错误（AI 常踩）：
    > 状态：「待启动」          ← 中文引号 + blockquote
    **状态：** 待执行            ← 标题下方 frontmatter 段
    | **状态** | 待启动 |        ← 派生显示标签当字段值

  合法状态值（4 态）：待执行 / 执行中 / 已完成 / 已废弃
  ("待启动" 是 status-view.py 派生显示标签，不是字段值)
-->

> 本文件是 task 的 **typed contract**（单文件，delta-3）—— 内部分三区，各区有各自的 lint：
>
> - **PM 确认区**：任务卡 / task 级范围 / task 级验收清单 / PM 反馈承接清单。
>   PM 在确认门**只读这一区**即可判 scope；跑 scoped PM-view lint（`check-doc-pm-view.py`）。
> - **执行区**：启动前必读 / 实现规格 / 实现设计引用 / 约束与易错 / 自测说明 / 工程层验收 /
>   状态转换说明。允许所有工程内容（TS 类型 / 字段 / 像素 / 反向约束），不跑 PM-view lint。
> - **审计区**：文档偏差 / 自审记录 / 历史档案。executor 填，不 lint。
>
> review 时（`/plan-eng-review` / `/plan-design-review` / `/qa` / `/review` 等）读本文件全文 +
> `docs/DESIGN.md`（项目级视觉规范）+ `docs/modules/<本 task 所属模块>.md`（模块规格）。
>
> 写作规则参见 `skills/_shared/PM-VIEW-RULES.md`：§三 PM 视图写作规则（仅约束 PM 确认区）/
> §七 章节顺序。

<!-- ╔══════════════════════════════════════════════════════════════╗
     ║  PM 确认区 — PM 在确认门读这一区；scoped PM-view lint 校验本区 ║
     ╚══════════════════════════════════════════════════════════════╝ -->
<!-- region: PM-CONFIRM begin -->

## 📌 任务卡

| | |
|---|---|
| **状态** | 待执行 |
| **所属模块** | [模块名 / 多个模块逗号分隔 / 基础设施] |
| **所属模块章节** | [一级章节名；基础设施 task 留空] |
| **executor** | claude-code |
| **executor_model** |  |
| **审查工具** | {{REVIEW_TOOLS}} |
| **dev server** | （执行时填写） |
| **worktree** | （执行时由 create-task-worktree.sh 填写） |
| **创建时间** | {{CREATED_DATE}} |
| **分支** | task-{{TASK_NUMBER}}-{{TASK_SLUG}} |

<!--
executor 取值：claude-code / codex / cursor-agent / manual；executor_model 留空 = settings 默认。
claude-code 仅支持 opus / sonnet / haiku（Agent tool 枚举）。
「审查工具」= 推荐 PM 在 task-execute 步骤 7 自跑的工具（I-RV1）；哨兵值 (无)/无/空/N/A 表示无推荐。
-->

**依赖**：

<!-- 结构化 task ID 列表（task-confirm / task-execute 依赖前置 gate 会机器解析）。
     格式：一行一个 - task-NNN (一句话说明)；无依赖写 "无"。
     不允许写成自然语言描述（如"依赖 task-2 和 task-3"），机器解析规则只提取 task-NNN。 -->
- 无

**做什么**（10 秒理解）：

<!-- 1-2 句话讲清本 task 交付什么、改哪个区域；不写技术细节（reducer / props / 字段名）。 -->

[1-2 句任务描述]

---

## 📦 范围（改 / 不改）

<!-- task 级范围（这个 task 改哪 / 不改哪）；req 级范围在 prd.md。
     用产品视角的业务名（不写文件路径，文件级路径进执行区）。 -->

**改**
- [产品视角的区域名 1]

**不改**
- [明确排除的区域 + 排除理由]

---

## ✅ 验收清单（PM 走查）

<!-- task 级验收（PM 自己能在 demo 时勾选的条件）；req 级验收标准在 prd.md §七。
     不写 grep 校验 / 单测断言 / 覆盖率阈值 → 那些进执行区·工程层验收。 -->

### 主路径
- [ ] [PM 可走查的成功条件 1]

### 边界路径
- [ ] [边界场景 1]

### 文案 / 视觉
- [ ] [文案统一性 / 视觉走查条件，不写像素值]

---

## 📥 PM 反馈承接清单

<!-- delta-3 §2.4：task-spec 扫前序「已完成」task 的「PM 反馈」段，按 relevance 二分。
     每条前序反馈一行；确认门摘要输出「承接 X 条 / 不适用 Z 条」，PM 一眼可见、可纠误判。
     无前序反馈 → 写「无前序 PM 反馈」。 -->

| 来源 task | 原文摘要 | relevance | 处理结果 | 理由 |
|---|---|---|---|---|
| task-NNN | [一句话摘要] | 适用 / 不适用 | 承接（写进执行区·约束与易错）/ 不适用（留原 task 不动）| [一句理由] |

无前序 PM 反馈

<!-- region: PM-CONFIRM end -->

<!-- ╔══════════════════════════════════════════════════════════════╗
     ║  执行区 — executor 实现依据；允许所有工程内容；不跑 PM-view lint ║
     ╚══════════════════════════════════════════════════════════════╝ -->
<!-- region: EXEC begin -->

## 🔁 状态转换说明（agent 必读）

> ⚠️ **agent 读取本文件后第一步：检查「状态」字段，按下表行动。**

| 状态 | 谁行动 | 做什么 |
|------|--------|--------|
| `待执行` | orchestrator | 等 PM 通过 `/task-confirm` 创建 worktree；可只读代码（preflight），不得写代码 |
| `执行中` | task window / execution agent | 在 task worktree 中实现 + 自审；呈交和 PM 验收期间仍保持此状态 |
| `已完成` | — | PM 已通过，不再修改 |
| `已废弃` | — | PM 决定不做。文件已 `git mv` 到 `tasks/discarded/`，task worktree + 分支已清理 |

**状态转换（受控入口）：**
`待执行` →(PM 在 task 窗口启动 `/task-execute`)→ `执行中` →(PM 通过呈交块)→ `已完成`
PM 打回 → 保持 `执行中`；执行失败 / 放弃 manual → `待执行`
`{待执行, 执行中}` →(PM 废弃)→ `已废弃`（专用入口 `--discard`）

**禁止直接编辑「状态」字段。** 用：
`python3 .claude/scripts/task-transition.py <task 文件路径> --to <目标状态>`
废弃：`python3 .claude/scripts/task-transition.py <task 文件路径> --discard --reason "<一句话>"`

---

## 🚦 启动前必读（按顺序，读完再执行）

<!-- 列出 agent 在 task worktree 启动前必读的文件路径 + 一句话说明。至少含：
- prd.md 的相关章节（req 级 WHAT）
- implementation-design.md 的相关 HOW-ID 行（见下「实现设计引用」段已挑出）
- task-plan.md 的相关 GAP / 风险条目
- docs/CONTEXT.md / docs/DESIGN.md / docs/modules/<module>.md
- 同模块已完成 task — 复用 / 避免重复 -->

1. `requirements/active/{{REQ_ID}}-{{REQ_SLUG}}/prd.md` §X — [一句话说明]
2. `requirements/active/{{REQ_ID}}-{{REQ_SLUG}}/task-plan.md` §三 风险 + §四 自检与状态摘要
3. `docs/CONTEXT.md` / `docs/DESIGN.md` / `docs/modules/<module>.md`
4. 同模块已完成 task — [复用 / 避免重复]

---

## 🔧 实现规格

<!-- 本 task「怎么做」—— 由 task-spec 从 prd.md 切片（WHAT）转写 + 补实现层细节。
     与 prd.md 分工：prd.md 是 req 级功能规格，本段是 task 级可执行规格。
     允许工程内容（字段名 / props / 函数签名 / 调用链）；用代码块表达类型 / 接口。 -->

### 1 · [功能名]

[task 级实现要求 + 实现层细节]

---

## 🧩 实现设计引用（HOW）

<!-- task-spec 从 implementation-design.md 按 HOW-ID + 适用关键词挑出当前 task 相关行，
     写在这里（不让 task-execute 跨文件回查）。每条带原 HOW-ID 便于追溯。
     task-scoped 占位值也内联在本段（executor 不跨文件回查 prd.md）。 -->

| HOW-ID | 决策 / 文件·模式 | 本 task 落地要点 |
|---|---|---|
| HOW-NN | [从 implementation-design.md 挑出的架构决策 / 文件路径] | [对本 task 的具体含义] |

**task-scoped 占位值**（如本 task 含 UI 占位）：

| 占位词 | 实际值 / 硬约束 |
|---|---|
| `[占位词]` | [实际值 + 必要约束] |

---

## ⚠️ 约束与易错

<!-- 易错点 / 反向约束（允许反向描述：禁止 X / 不允许 Y）+ a11y / 视口 / 视觉规范细则。
     来源标注：源 PM 反馈（含承接自前序 task 的 relevance=适用 条目）/ plan-review / 历史 task。
     像素细则体量大时，按 delta-8 §3.4 同款判断分流 docs/DESIGN.md。 -->

| # | 禁止行为 / 易错点 | 后果 / 理由 | 来源 |
|---|---|---|---|
| 1 | [具体禁止的实现做法] | [会导致什么问题] | [PM 反馈 / plan-review / 历史 task] |

---

## 🧪 自测说明（task 级流程化 UAT，task-verify 自动跑）

<!-- task-spec 从 prd.md §七 验收标准派生 task-scoped 自测说明（task 文件对 task-verify 自包含）。
     UI task 必填；非 UI task 写「无（[类型] task）」。
     每个流程一个 `### 流程 N: <流程名>`；动作 + 期望用 PM 视角自然语言，不写 selector / assert。 -->

### 流程 1: [流程名]
1. [动作]
2. 期望：[期望可观察结果]
artifact: verify/flow-1.png

### 流程 N: 冷启动 smoke（task-spec 自动注入）
1. 杀掉 dev server；清本地 storage
2. 重启 dev server，访问 /
3. 期望：起得来 + 首屏无报错
artifact: verify/cold-start.log

---

## ✔️ 工程层验收（自审用，agent 自动化校验）

<!-- 与 PM 确认区·验收清单分工：那边是 PM demo 走查、本段是 agent 自动化断言。
     grep 校验 / 单测条目 / 引用稳定性 / 覆盖率阈值 / 性能指标。 -->

- [ ] [grep 校验 / 单测条目 / 引用稳定性 / 性能指标]

<!-- region: EXEC end -->

<!-- ╔══════════════════════════════════════════════════════════════╗
     ║  审计区 — executor / PM 填；不 lint                            ║
     ╚══════════════════════════════════════════════════════════════╝ -->
<!-- region: AUDIT begin -->

## 📋 文档偏差

<!-- ⚠️ 本 section 必须保留 —— task-transition.py「执行中→已完成」gate 读它。
     execution agent 填：实现过程发现的、与 prd.md / implementation-design.md /
     docs/modules 不一致处。四列：文档位置 / 文档原文 / 实际实现 / 建议改法。
     无偏差填「无」。close-task Phase 2 把本段 promote 成 req `adjustment` 事件。 -->

| 文档位置 | 文档原文 | 实际实现 | 建议改法 |
|---|---|---|---|

无

---

## 🔍 自审记录

<!-- ⚠️ 本 section 必须保留 —— task-transition.py「执行中→已完成」gate 读它。
     PM 跑完 review 后由 orchestrator 机械追加（I-RV1：review 工具 PM 自跑）。
     PM 全跳 review：至少留一条 `**结果：** PM 选择不跑 review` placeholder。 -->

### 自审 1 - [YYYY-MM-DD HH:MM]
**工具：** [/qa, /design-review 等，或 PM 未跑]
**结果：** PM 报告：通过 / 发现 N 个问题 / 选择不跑 review
**详细发现：** [F-001: ... ✅已修复 / 未发现问题 / N/A]
**遗留问题：** 无 / [列出]

---

## 📁 历史档案（验收时打开看最新一轮）

<!-- 本段允许写入：
- ✅ 执行日志（agent 在 task-execute 自动写）
- ✅ PM 反馈（PM 在 close-task / review 后手动写）
- ✅ plan-review 沉淀（plan 阶段 review 沉淀，三段式 决策/理由/落地约束）
- ❌ AI 维护性动作（hash 对齐等）禁止写入 —— 单文件已无双文件 hash 同步机器（delta-3）。 -->

### 执行日志（agent 填写，不要删除历史记录）

#### 执行报告 - [YYYY-MM-DD HH:MM]
**改动摘要：** [做了什么]
**新建文件：** [列表，没有则填"无"]
**修改文件：** [列表]
**遇到的问题：** [如无则填"无"]
**文档对齐预告：** [本轮原型改动可能影响 task md 哪些业务字段；首次实现 / 无影响填"无"。close-task §0 对齐扫描优先读本字段]
**验收标准完成情况：**
- [x] 条件 1
- [ ] 条件 2（未完成原因：...）

---

### PM 反馈（orchestrator 代为追加，agent 重跑前必读）

#### 反馈 1 - [YYYY-MM-DD]
**问题描述：** [发现了什么问题]
**要求修改：** [具体怎么改]
**处理结果：** 待处理 / 已处理

> close-task 收尾会扫本段：视觉规范类 → `docs/DESIGN.md`；全项目跨功能产品行为规则 →
> `docs/PRODUCT-RULES.md`（PM-selective promote）；task-local / 同模块前瞻 → 留本段，
> 由后续 task 的 task-spec 按 relevance 二分承接（写进「PM 反馈承接清单」）。

---

### plan-review 沉淀

<!-- plan 阶段 /plan-eng-review / /plan-design-review 沉淀，三段式：决策 / 理由 / 落地约束。
     与执行区冲突时本段为准。 -->

<!-- region: AUDIT end -->

## 📎 参考材料

<!-- v5 attachments：本 task 引用过的 attachments/<file> 在此列出（路径 — 重点）。无引用 → 删整段。 -->
