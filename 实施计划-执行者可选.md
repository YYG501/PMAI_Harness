# 实施计划：执行者可选

> 派生自 `设计-执行者可选.md`。本文档是执行手册——每个批次列出**改什么文件、做什么事、如何验收**。设计问题不在这里争论，改设计回到 `设计-执行者可选.md` 先。

## 关联文档

- **设计来源**：`设计-执行者可选.md`
- **主框架设计**：`设计.md`
- **需求出处**：`需求.md` §8.3（Suborchestrator）/ §8.4（Execution Agent）

## 前置条件

- 当前工作区无 staged 改动冲突（`git status` 干净；已有 `M` 文件可保留但实施时注意 diff 边界）
- `codex-cli` / `cursor-agent` / `claude` CLI 本机可用（已确认）
- `CLAUDE_PLUGIN_ROOT` 变量在 codex-companion.mjs 调用时能解析
- Gemini 执行者延后，本计划不实现（见设计 §8）

## 实施策略（核心思路）

**Spike 先行，但 Spike 只 hardcode resolver，不 hardcode 执行接口**。第一批就固定 `invoke_executor(executor, prompt_file, worktree, model)` seam 和 `build-execution-prompt.py` 契约——后续批次只填 seam 内部实现，不回头改 seam 形状。

避免 codex 审视里指出的风险："批次 2 inline dispatch，批次 7 整体重写"——那是先写一套注定报废的控制流。正确做法：Spike 写稳定的接缝，证明**主路径结构成立**；执行器的具体实现后续批次填进来。

## 总览

| 批次 | 目标 | 依赖 | 状态 |
|------|------|------|------|
| 1 | 设计文档 | — | ✅ 完成 |
| 2 | **Spike**：seam + prompt 契约 + 最小 dispatch 跑通 codex + manual | — | 未开始 |
| 3 | 模板层：task.md 加字段 + inline 提示，settings.json 加 executor 块 | 2 | 未开始 |
| 4 | 基础设施：`resolve-executor.py` / `build-execution-prompt.py` / `classify-failure.sh` + 新事件类型 | 3 | 未开始 |
| 5 | 适配器抽出：`scripts/exec-adapters/` + 3 脚本（commit 纪律契约） | 2（seam） | 未开始 |
| 6 | 状态流转：`task-transition.py` 加 `--fail-execution` / `--cancel-manual` / `--snooze-manual` + state machine 加 `执行中→待确认` | — | 未开始 |
| 7 | 主流程完整化：resolver 接入 + 越界用 git restore/clean + 幂等重入 preflight + 诊断式失败文案 + commit 纪律 | 4, 5, 6 | 未开始 |
| 8 | 上游：/task-confirm 交互式选 executor + 默认压单行 + spawn ack + spawn 失败不转状态 | 3, 6 | 未开始 |
| 9 | 观察层：preamble 汇总 + 年龄降级 + snooze；status-view 给下一步命令 | 6 | 未开始 |
| 10 | 回归：adapter unit + task-execute 集成（5 组场景） | 5, 7 | 未开始 |

---

## 批次 2：Spike（seam + 契约）

**目标**：固定两个接缝形状，最小跑通一条非 claude-code 路径。本批次是后续所有批次的接口锚点。

**改动**：

- **新建** `scripts/build-execution-prompt.py`（contract 版，最小实现）：
  - CLI：`python3 scripts/build-execution-prompt.py <task_file>` → stdout 是完整 prompt
  - 输出必含 6 个 section（见设计 §3.3.1）：元信息 / 启动前必读 / 执行范围 allow+deny / PM 反馈 / 验收标准 / 写回职责
  - Spike 阶段可以用"占位串接 task 文件内容"的简化实现，只要 section 结构定死

- **修改** `skills/task-execute/SKILL.md` step 3，加入 `invoke_executor` 接缝：
  ```bash
  EXECUTOR=$(grep '^\*\*executor：\*\*' "$TASK_FILE" | awk '{print $2}' | tr -d ' ')
  [ -z "$EXECUTOR" ] && EXECUTOR=claude-code
  MODEL=$(grep '^\*\*executor_model：\*\*' "$TASK_FILE" | awk '{print $2}' | tr -d ' ')
  PROMPT_FILE=$(mktemp)
  python3 scripts/build-execution-prompt.py "$TASK_FILE" > "$PROMPT_FILE"
  BASELINE_SHA=$(git rev-parse HEAD)
  
  invoke_executor() {
    local executor="$1" prompt_file="$2" worktree="$3" model="$4"
    case "$executor" in
      claude-code)
        # spike 阶段：走现有主流程 inline（不改）
        return 0
        ;;
      codex)
        cd "$worktree"
        node "${CLAUDE_PLUGIN_ROOT}/scripts/codex-companion.mjs" task --write \
          ${model:+--model "$model"} "$(cat "$prompt_file")"
        ;;
      manual)
        # spike 版：直接打印提示 + 写最小 pending 文件 + exit skill
        echo "Task 已设为 manual。请在 $worktree 完成后重跑 /task-execute"
        mkdir -p "$MAIN_REPO_ROOT/.runs"
        jq -n --arg id "$TASK_ID" --arg sha "$BASELINE_SHA" \
          '{task_id:$id, baseline_sha:$sha}' \
          > "$MAIN_REPO_ROOT/.runs/.pending-manual-${TASK_ID}.json"
        exit 0
        ;;
      *)
        echo "未知 executor: $executor" >&2
        return 1
        ;;
    esac
  }
  
  invoke_executor "$EXECUTOR" "$PROMPT_FILE" "$TASK_WORKTREE" "$MODEL"
  ```

- **不做**：
  - 不做 resolver（后续批次 4，spike 用 grep）
  - 不做 adapter 抽出（后续批次 5，spike inline 命令）
  - 不做越界保护（后续批次 7）
  - 不做失败 fallback（后续批次 6+7）
  - 不改 task-transition.py（后续批次 6）

**验收**：

- 手动造 task 文件（`executor：codex`）+ 必读 / 执行范围 / 验收 section 完整 → 跑 `/task-execute` → codex 在 task worktree 写文件、stdout 能看到 codex 输出
- 手动造 task 文件（`executor：manual`）→ 跑 `/task-execute` → 打印提示 + `.pending-manual-*.json` 出现在 `$MAIN_REPO_ROOT/.runs/`
- 老 task（无 executor 字段）走 claude-code 无 regression
- `build-execution-prompt.py` 对一个样本 task 输出的 prompt 包含全部 6 section（可人眼检查 + grep 验证）

**Spike 的产出**：不是"完整 dispatch"，而是 4 个关键证据：
1. `invoke_executor` seam 的入参足够（executor / prompt_file / worktree / model）
2. `build-execution-prompt.py` 生成的 prompt 被 codex 正确理解（code 写进 worktree 且没漂移）
3. codex `--write` 在 task worktree 不踩 sandbox
4. manual 提示 + pending 文件路径的 UX 能被 PM 理解

如果这四条出问题，停下来回设计；全通过才继续。

---

## 批次 3：模板层

**目标**：把 spike 里 PM 手填的 executor 字段正式化 + 把 discoverability 前置到模板。

**改动**：

- `templates/task.md.tmpl`
  - 在任务元信息 section（审查工具上方）插入字段 + HTML 注释提示（inline 帮助，PM 每次打开都看见）：
    ```markdown
    **executor：** claude-code
    **executor_model：**
    <!--
    可选 executor：claude-code / codex / cursor-agent / manual
    model 留空 = 用 settings 默认
    claude-code 仅支持：opus / sonnet / haiku（Agent tool 枚举）
    cursor-agent：实验性，无沙箱，越界保护仅限 worktree 内
    放弃 manual：python3 .claude/scripts/task-transition.py <本文件> --cancel-manual
    暂缓 manual 提醒：python3 .claude/scripts/task-transition.py <本文件> --snooze-manual --days 3
    -->
    ```

- `templates/settings.json.tmpl`（如仓内尚无则新建）
  - 顶层加 `executor` 块：
    ```json
    "executor": {
      "default": "claude-code",
      "models": {
        "codex": "gpt-5.4",
        "cursor-agent": "gemini-3.1-pro"
      }
    }
    ```

**验收**：

- `/task-plan` 生成的 task 文件包含字段 + HTML 注释提示
- 新项目 init 生成的 `.claude/settings.json` 含 `executor` 块
- 老 task 文件缺字段时不崩（批次 4 的 resolver 兜底）

---

## 批次 4：基础设施

**目标**：三个核心脚本 + 事件类型。

**改动**：

- **新建** `scripts/resolve-executor.py`
  - CLI：`python3 scripts/resolve-executor.py <task_file>`
  - 输出 JSON：`{"executor": "...", "model": "...|null"}`
  - 继承链（2 层）：task 字段 → `settings.json executor.default` → `claude-code`
  - 严格解析中文冒号字段
  - 无效 executor 值 → exit 1，stderr 给"人话"（`错误：无效 executor "cursor_agent"（注意中划线）。合法值：claude-code / codex / cursor-agent / manual。修复：编辑 <task_file> 把 executor 改为 cursor-agent`）
  - **claude-code 的 model 校验**：若 `executor=claude-code` 且 `executor_model` 非空，值必须是 `opus` / `sonnet` / `haiku`。错误信息：`claude-code 的 executor_model 只支持 opus / sonnet / haiku。当前值：gpt-5.4。如果你想用 gpt-5.4，请把 executor 改成 codex。`

- **升级** `scripts/build-execution-prompt.py`（从 spike 的简化版升级到正式版）
  - 严格从 task 文件解析 6 个 section
  - 新增「未处理的 PM 反馈」section：从 task 文件「反馈」section 筛出未标记已处理的条目
  - 新增「写回职责」明确写禁止 `git commit` / `git add`

- **新建** `scripts/classify-failure.sh`
  - 入参：`<exit_code>` + `<log_path>`
  - 输出 classification：`sandbox_denied` / `model_not_found` / `network` / `unknown`
  - 规则：
    - exit 10 → `sandbox_denied`
    - exit 11 → `model_not_found`
    - exit 12 → `network`
    - 非 10/11/12 → 扫 log 的 stderr 关键词作二次分类（"sandbox" / "rate limit" / "model not found"）
    - 匹配不到 → `unknown`

- **修改** `scripts/task-events.py`，在 `VALID_EVENT_TYPES` 加 7 种：
  - `suborch_spawn_started` / `suborch_spawn_failed`
  - `execution_started` / `execution_completed` / `execution_failed`
  - `execution_manual_waiting` / `execution_manual_completed`
  - `append` 支持自由 payload（`--payload '{"key":"value"}'`）

**验收**：

- `resolve-executor.py` 对 2 种继承场景（task 填 / settings 填）都返回正确值；无效 executor / 非法 claude-code model 报错信息是人话
- `build-execution-prompt.py` 对完整 task 文件输出包含全部 6 section 的 prompt
- `classify-failure.sh` 对各 exit code 输出正确分类
- `task-events.py` 接受 7 种新事件类型；无效类型被拒

**注意**：claude-code 的 model 枚举不硬编码在 `resolve-executor.py` 的列表常量里，读自 `scripts/executor-config.json`（或 `settings.json` 的 `executor.claude_code_model_allowlist`），方便上游加 tier 时改配置即可，不改代码。

---

## 批次 5：适配器抽出

**目标**：把 spike 的 inline 命令抽成统一 adapter 脚本，加 `cursor-agent`，强制 commit 纪律。

**改动**：

- 新建目录 `scripts/exec-adapters/`
- **`codex.sh`**（见设计 §4.2）
- **`cursor-agent.sh`**（见设计 §4.3）
- **`manual.sh`**（见设计 §4.5，pending 文件必须写到 `$MAIN_REPO_ROOT/.runs`，含 `baseline_sha` + `snoozed_until:null`）
- 每个脚本 `chmod +x`

**契约（环境变量入参）**：

- `TASK_FILE` / `TASK_WORKTREE` / `PROMPT_FILE` / `EXECUTOR_MODEL` / `MAIN_REPO_ROOT`

**契约（产出）**：

- 改动落 `TASK_WORKTREE`，**必须保持 unstaged**
- adapter **禁止** `git add` / `git commit` / `git checkout`
- stdout/stderr 直接输出；caller（task-execute）负责 `> .runs/execution-<task>-<executor>.log 2>&1` 重定向
- exit code 分类：0 / 10 sandbox / 11 model / 12 network / 其它未分类

**验收**：

- 每个 adapter 跑一个 trivial task 能成功，worktree 改动保持 unstaged
- 故意让 adapter 跑 `git add` 会失败（契约校验通过 bash trap 或脚本内显式 check）
- `EXECUTOR_MODEL=""` 时 adapter 不传 `--model` flag
- `manual.sh` 写出 pending 文件到 `$MAIN_REPO_ROOT/.runs/`，含 `baseline_sha`

---

## 批次 6：状态流转扩展

**目标**：state machine 支持 `执行中→待确认`；三个新 flag。

**改动**：

- **修改** `scripts/task-transition.py` + 对应 state machine 常量：
  - state_transitions 加 `执行中→待确认`（仅通过 `--fail-execution` 或 `--cancel-manual` 触发）
  - 新增 flag `--fail-execution <task> --reason <text>`
    - 前置：当前状态 `执行中`
    - 动作：若存在 pending 文件则清理 → 转 `待确认` → 若未 append `execution_failed` 则 append → 写 note
    - **这是唯一合法的 `/task-execute` 失败回退路径**
  - 新增 flag `--cancel-manual`
    - 前置：当前状态 `执行中` 且存在 `$MAIN_REPO_ROOT/.runs/.pending-manual-<task>.json`
    - 动作：删标记 + 转 `待确认` + note
  - 新增 flag `--snooze-manual --days N`
    - 前置：存在 pending 文件
    - 动作：把 `snoozed_until` 设为 `NOW + N days`；不改状态；不删标记
  - `--to 待确认` 时若存在 manual 标记 → **决定性地总是清理**（不留开放语义）

- **修改** worktree 创建逻辑（在现有 worktree 相关脚本内）
  - `/task-confirm` 重试场景：检测 task worktree 已存在时跳过创建
  - 具体触碰点需 grep 确认（可能是 `task-bootstrap.sh` 或 `task-transition.py` 内联）

**验收**：

- `--fail-execution` 对有/无 pending 的 task 都能正确回退到 `待确认` + 清理 + note
- `--cancel-manual` 对无标记的 task 报错；有标记时正确
- `--snooze-manual --days 3` 后 `jq .snoozed_until` 能看到未来 3 天的时间戳
- state machine 测试：`执行中→待确认` 只能通过两个 flag 触发，裸 `--to 待确认` 在 `执行中` 状态下被拒（或者不拒但连带清理 pending）
- 重复跑 `--to 执行中` 且 worktree 已存在不炸

---

## 批次 7：主流程完整化

**目标**：把 spike 的 seam 填成完整版——接入 resolver / adapter / 越界 / 幂等重入 / commit / 诊断失败文案。

**改动**（修改 `skills/task-execute/SKILL.md` step 3）：

- **Step 3a（lock + manual 幂等重入）**：
  ```
  lock_dir = ".runs/.lock-<task>"
  mkdir(lock_dir) || abort("另一个 /task-execute 正在运行此 task")
  trap "rmdir lock_dir" EXIT

  if exists($MAIN_REPO_ROOT/.runs/.pending-manual-<task>.json):
    pending = load(pending_file)
    output: "✓ 检测到 Task-<id> 的 manual 续跑标记（since <started_at>）。
            将保留 <worktree> 的现有改动，不会重新调用任何执行器。
            直接进入自审阶段。"
    # preflight
    diff = git diff pending.baseline_sha HEAD --name-only
    allowlist = parse_task_execution_scope(task_file)
    denylist_hit = [f for f in diff if not in allowlist]
    if denylist_hit:
      output: "⚠️ 检测到 manual 改动越出执行范围：<files>。补齐 allowlist 或回滚后再 /task-execute"
      exit  # 不删 pending
    if task_file['执行日志'].empty or task_file['文档偏差'].empty:
      output: "⚠️ 请先更新 task 文件的「执行日志」和「文档偏差」section 再 /task-execute"
      exit  # 不删 pending
    delete pending_file
    append execution_manual_completed
    goto step 4
  ```

- **Step 3b（normal dispatch）**：
  ```
  {executor, model} = JSON(scripts/resolve-executor.py task_file)
  baseline_sha = git rev-parse HEAD
  append execution_started {executor, model, baseline_sha}
  prompt_file = mktemp
  python3 scripts/build-execution-prompt.py task_file > prompt_file

  log_path = .runs/execution-<task>-<executor>.log
  if executor == claude-code:
    # inline 主流程（原有 step 3 逻辑），但不在此 commit
  else:
    MAIN_REPO_ROOT=$(git rev-parse --show-toplevel) \
    TASK_FILE=... TASK_WORKTREE=... PROMPT_FILE=... EXECUTOR_MODEL=model \
    bash scripts/exec-adapters/<executor>.sh > log_path 2>&1
    exit_code=$?

  if executor == manual:
    exit skill  # manual.sh 已写 pending，skill 结束

  if exit_code != 0:
    classification = bash scripts/classify-failure.sh exit_code log_path
    rollback_to_baseline(baseline_sha)  # 见 3c 回滚逻辑
    append execution_failed {executor, model, exit_code, classification, log_path}
    python3 scripts/task-transition.py <task> --fail-execution --reason "$classification"
    output_diagnostic(classification, log_path, task_file)  # 见设计 §6 模板
    exit skill
  ```

- **Step 3c（越界保护 + 回滚函数）**：
  ```
  allowlist = parse_task_execution_scope(task_file)   # 含 ['docs/'] 需 task 显式列
  denylist = parse_gitignore()   # 生成物视为合法忽略
  changed = git status --porcelain -z | parse
  bad_files = []
  for path, status in changed:
    if path matches denylist: continue  # gitignore 生成物跳过
    if path not in allowlist: bad_files.append((path, status))
  if bad_files:
    rollback_paths(bad_files, baseline_sha)
    append execution_failed {reason: "boundary_violation", bad_files}
    python3 scripts/task-transition.py <task> --fail-execution --reason "boundary_violation"
    output_diagnostic("boundary_violation", log_path, task_file)
    exit skill

  # 回滚实现
  rollback_to_baseline(baseline_sha):
    tracked = [p for p,s in changed if s in ('M','D','R','C')]
    untracked = [p for p,s in changed if s == '??']
    git restore --source=$baseline_sha --worktree --staged -- $tracked
    rm -f $untracked
  rollback_paths(bad_files, baseline_sha):
    # 同上但只作用于 bad_files
  ```

- **Step 3d（零改动检查）**：
  ```
  changed = git status --porcelain | count
  if changed == 0 and executor != manual:
    append execution_failed {reason: "no_changes"}
    python3 scripts/task-transition.py <task> --fail-execution --reason "no_changes"
    output_diagnostic("no_changes", log_path, task_file)
    exit skill

  append execution_completed {executor, model, exit_code: 0, files_changed: [...]}
  # 继续 step 4
  ```

- **Step 10（commit）** — 在现有 step 10（转待验收前）加 commit：
  ```
  git add -A
  git commit -m "task-<id>: <摘要 from 执行日志>"
  python3 scripts/task-transition.py <task> --to 待验收
  ```
  commit 只在自审全绿后做，失败时 step 3/3c/3d 的 rollback 已把 worktree 回到 baseline。

**验收**：

- 正向：codex 成功 → worktree 改动 unstaged → step 4-9 自审 → step 10 commit → 转待验收
- 越界：codex 写了 `docs/xxx.md`（未在 allowlist）→ 自动 rollback + 转待确认 + 诊断输出含 `boundary_violation`
- 零改动：codex exit 0 但 git status 空 → 转待确认 + 诊断 `no_changes`
- sandbox 错误：codex exit 10 → rollback + `--fail-execution --reason sandbox_denied` + 诊断含 `检查 ~/.codex/config.toml` 建议
- Manual 重入：PM 改完 worktree + 更新「执行日志」→ `/task-execute` 显示 "✓ 检测到 manual 标记..." + 进自审；PM 忘更新「执行日志」→ 提示补齐不删 pending
- 双 /task-execute 并发 → 第二个报"另一个在运行"

---

## 批次 8：上游流程（/task-confirm 重点）

**目标**：交互式选 executor；默认输出压单行；spawn ack 流程。

**改动**：

- **修改** `skills/task-plan/SKILL.md`
  - step 5 生成 task 文件时：填 `executor：claude-code`，`executor_model：` 留空；模板自带 inline 提示

- **修改** `skills/task-confirm/SKILL.md`
  - **默认态单行输出**：
    ```
    Task-003：添加用户登录
      执行方式：claude-code（默认）
      审查工具：/review, /qa
    ```
    非默认 executor 或非空 model 才展开为两行（`执行方式：codex / gpt-5.4 (project 默认)`）

  - **交互式切换**：
    ```
    是否切换执行方式？可选：claude-code / codex / cursor-agent / manual
    输入新 executor（回车保持默认）：
    model（留空=用默认，claude-code 仅支持 opus/sonnet/haiku）：
    ```
    输入非空后 sed 更新 task 文件；重新打印摘要

  - **Spawn 流程**：
    ```
    1. {executor, model} = JSON(scripts/resolve-executor.py task_file)
    2. 状态保持「待确认」（关键：不提前转）
    3. agent_call = Agent tool spawn Suborchestrator
         model = (executor == claude-code && model != "") ? model : unset
         payload.resolved_executor = executor
         payload.resolved_model = model
         payload.hard_constraint = if executor != claude-code:
           "当 resolved_executor != claude-code，Suborchestrator 只做 orchestration，
            禁止直接写业务代码。只调 /task-execute 让 dispatch 处理。"
    4. 若 spawn 异常：
         append suborch_spawn_failed {reason}
         output: "Suborchestrator 启动失败（reason）。状态保持待确认。
                  可能原因：executor_model=opus 但当前订阅不含，或 Agent tool 不可用。
                  修复后重试 /task-confirm。"
         exit skill（不转状态）
    5. spawn 成功且 subagent ack：
         append suborch_spawn_started {executor, model}
         python3 scripts/task-transition.py <task> --to 执行中
    ```

  - worktree 已存在时跳过创建（复用批次 6 检测）

  - **成功后 copy-pasteable 输出**：
    ```
    已启动 Task-003，执行方式：codex / gpt-5.4
    下一步（Suborchestrator 正在 <task-worktree> 自动推进）：
      tail -f .runs/execution-task-003-codex.log  （如需看执行进度）
      /status                                      （查看所有 task）
    ```

**验收**：

- 默认态 `/task-confirm` 执行方式一行显示；交互输入非默认值后展开为两行
- 交互式改 executor + model → task 文件被正确更新（中文冒号格式不破坏）
- Spawn 失败（mock：传 executor_model=non-existent-tier）→ 状态保持待确认 + 输出人话 + `suborch_spawn_failed` 事件
- Spawn 成功 → 状态转执行中 + `suborch_spawn_started` 事件
- Suborch spawn payload 含 hard_constraint（可由集成测试断言）

---

## 批次 9：观察层

**目标**：preamble 汇总式不打扰；status-view 给下一步命令。

**改动**：

- **修改** `scripts/skill-preamble.sh`
  - 现有 `.pending-<skill>` 扫描之后加 manual 汇总块（见设计 §5.3 脚本）：
    - 遍历 `$MAIN_REPO_ROOT/.runs/.pending-manual-*.json`
    - 检查 `snoozed_until` — 在 snooze 期内跳过
    - 计 `pending_count` 和 `old_count`（started_at > 7 天）
    - 只输出一行汇总（`ℹ️` 或 `⚠️`），不逐条打印

- **修改** `scripts/status-view.py`
  - 扫描 `$MAIN_REPO_ROOT/.runs/.pending-manual-*.json`
  - 新增 Manual section（见设计 §5.4 示例）：列出每个 task + 等待天数；末尾固定三行"下一步"可复制命令

**验收**：

- 1 个 manual pending → preamble 一行 `ℹ️ 有 1 个 manual task...`
- 2 个 manual pending 且 1 个 > 7 天 → preamble `⚠️ 有 2 个 manual task 等待中（1 个超过 7 天）`
- Snooze 3 天后的 task → preamble 不提醒该 task，计数减 1
- `/status` 输出 Manual section 含三行 copy-pasteable 命令

---

## 批次 10：回归 fixture

**目标**：adapter unit + task-execute 集成 + resolver 校验 unit。

**改动**：

- **新建** `tests/test-executors.sh`
  - **Adapter unit**：
    - codex / cursor-agent：跑 trivial task 写 hello-<executor>.txt；断言文件存在 + unstaged
    - manual：写 pending 文件到 `$MAIN_REPO_ROOT/.runs/`，断言 schema（含 baseline_sha）
  - **resolver unit**：
    - claude-code + executor_model=opus → 通过
    - claude-code + executor_model=gpt-5.4 → 报错人话
    - 无效 executor → 报错
    - 2 层继承各 2 例

- **新建** `tests/test-task-execute-integration.sh`（使用假 adapter + 假 spawn harness）
  - 5 组场景：
    1. `executor=claude-code` 原路径无 regression
    2. `executor=codex` + adapter exit 0 + 合法改动 → 进入 step 4，事件流完整
    3. `executor=codex` + adapter 写越界（假 adapter 写 `docs/a.md`）→ 自动回滚 + 转待确认 + `boundary_violation` classification
    4. `executor=codex` + adapter exit 10 → 转待确认 + `sandbox_denied` classification + 诊断文案含建议
    5. `executor=manual` → 写 pending → PM 修改 worktree（集成测试 stub 模拟）→ 重入 → preflight 通过 → 进 step 4

- **更新** `tests/test-req-transition.sh`（已有，改动中）— 确认增量不破坏

**注意**：
- CLI live 测试用 `SKIP_LIVE_TESTS=1` 默认跳过（避免 CI 付费 + 慢）
- 集成测试用 fake adapter（shell script 模拟不同 exit code + 写文件行为），不依赖真 CLI

**验收**：

- `bash tests/test-executors.sh`（含 resolver）全绿
- `SKIP_LIVE_TESTS=1 bash tests/test-task-execute-integration.sh` 5 组场景全绿
- 现有 `tests/test-req-transition.sh` 仍通过
- CI 不 break

---

## 实施节奏

- **Day 1**：批次 2 Spike（seam + prompt 契约 + 最小 dispatch）。通过三条关键证据检查。
- **Day 2**：批次 3（模板）+ 批次 6（状态流转，独立可并行）
- **Day 3**：批次 4（基础设施 3 脚本 + 事件）+ 批次 5（adapter 抽出 + commit 纪律）
- **Day 4-5**：批次 7（主流程完整化）—— 依赖最多，最后实施。
- **Day 6**：批次 8（上游 /task-confirm 交互 + spawn ack）+ 批次 9（观察层汇总）
- **Day 7**：批次 10 集成测试全绿 → release

**回滚点**：每批次结束跑 `tests/test-req-transition.sh`，失败立即定位。批次 7 结束 +  批次 10 集成测试是主 gate。

**Spike 失败的处理**：如果批次 2 暴露了 seam 形状不对（入参不够 / prompt 契约有漏）、codex 在 worktree 里有新 sandbox 问题、manual UX 不 OK——**停下来回设计**，不要强推后续批次。

**worktree 策略**：本实施本身在主仓直接做（非 task 流程），因为是在开发框架本身。后续 dogfood 时才会用框架的 req/task 流程来做新需求。

---

## 未纳入本计划的点

见 `设计-执行者可选.md` §8：

- Routing 表
- Suborchestrator 可替换（另一份独立设计）
- 关键词自动识别 manual 完成
- Adapter 结构化输出解析（`--output-format json`）
- Gemini 执行者

---

## 实测点（实施中验证）

- `CLAUDE_PLUGIN_ROOT` 稳定性
- cursor-agent 复杂任务耗时 / 无沙箱的实际越界率（靠事后层保护）
- 越界检测对生成物 false positive（靠 gitignore 兜底的效果）
- Manual pending glob 在 `.runs/` 较大时的性能
- `build-execution-prompt.py` 生成的 prompt 是否真的让 codex / cursor-agent 正确理解 write-back 职责（执行日志 / 文档偏差 section 是否被更新）
- Agent tool `model` 参数在 spawn 失败时的错误格式（用于 classify）
