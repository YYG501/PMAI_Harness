# 设计：并行 Task 执行架构（Map-Reduce）

> **状态**：方案草稿（v0.1），**未实施**——动手前需先定 north star
> **更新时间**：2026-04-24
> **关联**：
> - `TODOS.md` v3 条目（本文档的导航摘要）
> - `TODOS.md` v2 条目（**架构冲突项**，必须先抉择）
> - `skills/task-confirm/SKILL.md` 步骤 5（当前 Suborchestrator spawn 逻辑）
> - `skills/task-execute/SKILL.md` 步骤 3b（当前 adapter 调用逻辑）
> - `scripts/exec-adapters/codex.sh`（当前同步阻塞实现）
> - `设计-执行者可选.md`（v1 Execution Agent 抽象的上一代设计）

---

## Context

### 触发事件（2026-04-24）

admin console4 req-001 task-001 执行时触发如下链路：

```
PM 主会话
 └─ /task-confirm 调 Agent(subagent_type=...) spawn suborchestrator subagent
      └─ subagent 在 task worktree 调 /task-execute
           └─ /task-execute 调 scripts/exec-adapters/codex.sh（同步阻塞）
                └─ codex exec（high reasoning，10-30 分钟）
```

Claude Bash 工具最大 timeout 10 分钟 < codex 实际耗时 10-30 分钟，suborchestrator subagent 被迫 `run_in_background=true` + `Monitor` 等通知。subagent 一个 turn 结束即被回收，codex 进程还活着但无人接管后续（日志 / /review / 状态转换 / PM 通知）。

### 根因：机制与角色错配

当前生成器把 **Suborchestrator 角色**（单 task owner：preflight / 实现 / 自审 / 收口）绑定到 **Agent subagent 机制**上。这是错配：

| | Subagent 机制 | Suborchestrator 职责 |
|---|---|---|
| 生命周期 | **单次短命**：跑完 prompt → return → 回收 | 长跑（10-30 分钟起）|
| 上下文 | 独立一轮 | 跨多轮事件 |
| 用途 | 白纸视角评审 / 独立查询 / 探索 | 协调、监控、收口 |

类比：把"项目经理岗位"雇了"临时工"——会开完人就走，项目没人跟。

### 并行需求（2026-04-24）

PM 明确提出"未来要做并行"——一个 req 内多个 task（如 admin console4 req-001 的 task-003/004/005 分改不同页面）能同时推进，不是一个跑完再跑下一个。

这一条让"单会话直接承担 suborch 职责"（原方向 A）也被否决——主会话单线，跑 3 个 codex 需要 30-90 分钟串行等。

---

## 决策快照

| # | 决策点 | 结果 |
|---|--------|------|
| 根 1 | Suborchestrator 作为独立 Claude 角色 | **取消**。职责由主 Orchestrator 承担，执行交给非 Claude 后台进程 |
| 根 2 | 并行的粒度 | **task 级并行**（同 req 内）。不做跨 req 并行、不做多 Claude 实例 |
| 根 3 | 后台执行机制 | `nohup` / `setsid` 起独立 bash 进程跑 executor adapter |
| 根 4 | 主会话监控机制 | **双路**：`ScheduleWakeup`（周期轮询）+ `UserPromptSubmit` hook（PM 输入时兜底） |
| 根 5 | 完成信号载体 | **追加事件到 `.runs/events/<task>.jsonl`**（2026-04-24 Issue 2 修订；原草案用 `.runs/<task>.done.json` sentinel 文件，已废弃）|
| 次 1 | 并行度上限 | 软配置 `max_parallel_tasks`，默认 3，通过 `.claude/settings.json` 可改 |
| 次 2 | 后台进程失活检测 | 主会话每轮 wakeup 检查 pid 存活 + task 超时阈值（默认 60 分钟） |
| 次 3 | 收口阶段（/review / 状态转换） | 主会话串行处理，每个 task 独立进入 worktree 完成 |
| 次 4 | subagent 机制在别处的保留 | `analysis-reviewer`、探索型查询等短任务**继续用 subagent**——问题不在机制本身，在"拿 subagent 当长跑 orchestrator"这个误用 |
| 次 5 | max_parallel_tasks 超限行为 | 软警告 PM，不硬拦——PM 可以覆盖（并行 5 个要承担成本） |
| 次 6 | sentinel 写入原子性 | 先写 `.tmp` 再 `mv`，避免主会话读到半写状态 |

---

## 1. 新架构概览

```
┌─────────────────────────────────────────────────────────┐
│ PM 主会话（主 Orchestrator）                            │
│                                                          │
│  /task-confirm task-003/004/005 一起确认                │
│    │                                                     │
│    ├─ nohup codex-bg.sh task-003 &   ──────┐            │
│    ├─ nohup codex-bg.sh task-004 &   ──────┤            │
│    └─ nohup codex-bg.sh task-005 &   ──────┤            │
│                                             │            │
│  fire-and-forget 后立即释放                 │            │
│                                             │            │
│  ScheduleWakeup(15-20 分钟) ◄──────────────┼──          │
│    │                                        │  并行后台  │
│    └─ scan-task-done.sh                     │            │
│        │                                    │            │
│        ├─ PENDING: execution_completed_bg ◄─┤ 完成追加   │
│        │   (in events/task-004.jsonl)       │  事件      │
│        └─ 进入 task-004 worktree            │            │
│            跑 /review / 转状态 / 通知 PM    │            │
│                                             │            │
│  UserPromptSubmit hook 兜底 ────────────────┤            │
│    （PM 早回来输入 → 立即扫 sentinel）     │            │
└─────────────────────────────────────────────┼───────────┘
                                              │
┌─────────────────────────────────────────────┼───────────┐
│ 后台 bash 进程（非 Claude）                 │            │
│                                             ▼            │
│  codex-bg.sh：                                           │
│   1. codex exec（10-30 分钟，独立进程）                  │
│   2. task-events.py append execution_completed_bg        │
│      到 .runs/events/task-NNN.jsonl（flock 原子追加）    │
│   3. exit                                                │
└─────────────────────────────────────────────────────────┘
```

**关键属性**：
- **耗时部分真并行**：N 个 task 同时跑 codex，总墙钟时间 ≈ 单 task 耗时 + 收口串行尾巴
- **收口串行快速**：主会话一次处理一个 done sentinel，/review 通常 <1 分钟，不成瓶颈
- **主会话不阻塞**：fire-and-forget + 周期轮询，不持续等任何一个 task
- **两路监控**：wakeup 负责长时间无输入时的进度推进，hook 负责 PM 早回来时的立即响应

---

## 2. 组件详细设计

> ⚠️ **ILLUSTRATIVE ONLY**：本章所有 shell 片段均为架构草图，**不得直接照抄交付**。实施时必须遵循 §11 G13 的 shell 硬约束（`jq -n --arg` 生成 JSON；`${BASH_SOURCE[0]%/*}` 而非 `$0`；`trap 'rm -f "$TMP" 2>/dev/null' EXIT` 清理；PID 追踪通过 `PM_AI_TASK` 环境变量 + `pgrep` 二次确认工作进程而非 wrapper shell）。

### 2.1 后台 worker（`scripts/exec-adapters/codex-bg.sh`）

在现有 `codex.sh` 外包一层后台启动。或者给 `codex.sh` 加 `--background` flag（决策点）。

**完成信号写入 `.runs/events/<task>.jsonl`**（2026-04-24 eng review Issue 2 决议）——不新建 `.done.json`。worker 调用现有 `scripts/task-events.py append --type execution_completed_bg` 追加事件，与 I-CT7/I-CT8 审计流天然对齐；G5 flock 也只需保护这一个入口。

```bash
#!/usr/bin/env bash
# codex-bg.sh — ILLUSTRATIVE SKETCH, 交付前必须过 §11 G13 审核
#
# 调用方：/task-confirm 用 nohup setsid 起它
# 契约：
#   - 跑完 codex 后追加 execution_completed_bg / execution_failed_bg 事件到 .runs/events/<task>.jsonl
#   - 不转状态、不跑 /review——那是主 Orchestrator 的事
#   - stdout/stderr 重定向到 .runs/execution-<task>-codex.log
set -euo pipefail

: "${MAIN_REPO_ROOT:?}"
: "${TASK_FILE:?}"
: "${TASK_WORKTREE:?}"
: "${PROMPT_FILE:?}"
: "${PM_AI_TASK:?}"      # task-NNN；/task-abort 用 `pgrep -f "PM_AI_TASK=$PM_AI_TASK"` 精准定位

TASK_ID="$PM_AI_TASK"
START_TS=$(date +%s)

# 调现有 codex.sh（同步 blocking 没关系——我们整个已在后台进程里）
# BASH_SOURCE 代替 $0，详见 §11 G13
EXEC_EXIT=0
bash "${BASH_SOURCE[0]%/*}/codex.sh" || EXEC_EXIT=$?

END_TS=$(date +%s)
ELAPSED=$((END_TS - START_TS))

# 事件类型分流（崩溃由主会话 liveness 补写 execution_crashed_bg，worker 自己只负责 completed/failed）
EVENT_TYPE="execution_completed_bg"
[ "$EXEC_EXIT" -ne 0 ] && EVENT_TYPE="execution_failed_bg"

# 用 jq -n --arg/--argjson 生成 payload，任何字段含特殊字符都安全（G4/G13）
# task-events.py 内部负责 flock + 原子 append（G5）
PAYLOAD=$(jq -n \
  --arg executor "codex" \
  --argjson exit_code "$EXEC_EXIT" \
  --argjson elapsed "$ELAPSED" \
  --arg log_path "$MAIN_REPO_ROOT/.runs/execution-${TASK_ID}-codex.log" \
  --arg worktree "$TASK_WORKTREE" \
  --argjson pid "$$" \
  '{executor:$executor, exit_code:$exit_code, elapsed_seconds:$elapsed, log_path:$log_path, worktree:$worktree, pid:$pid}')

python3 "$MAIN_REPO_ROOT/scripts/task-events.py" append "$TASK_FILE" \
  --type "$EVENT_TYPE" \
  --payload-json "$PAYLOAD"
```

**同样为 `cursor-agent-bg.sh`** 写一份（或把现有脚本扩展一个 `BACKGROUND=1` 分支）。`claude-code` executor 不走后台（主会话直接承担，不 spawn 子进程）。`manual` executor 不需要后台（它本来就是 PM 手动操作）。

### 2.2 完成事件格式（取代原 sentinel 方案）

2026-04-24 Issue 2 决议——完成信号合并到现有事件流 `.runs/events/<task-id>.jsonl`，不再新建 `.done.json` / `.processed`。

**新增事件类型**：

| type | 写入方 | 含义 |
|---|---|---|
| `execution_completed_bg` | codex-bg.sh | worker 正常退出 (`exit_code == 0`) |
| `execution_failed_bg` | codex-bg.sh | worker 捕获到非零 exit，本身还活着 |
| `execution_crashed_bg` | 主会话 liveness 检查 | pid 已死但无 `execution_*_bg` 事件；由 §2.4 scan-task-done 补写 |
| `execution_aborted` | `/task-abort` 脚本 | 见 §2.7 |
| `reduced_bg` | 主 Orchestrator 收口 | 收口完成标记；同 task 下次扫描跳过 |

**payload 示例**（`execution_completed_bg`）：

```json
{
  "type": "execution_completed_bg",
  "timestamp": "2026-04-24T15:42:10Z",
  "payload": {
    "executor": "codex",
    "exit_code": 0,
    "elapsed_seconds": 1834,
    "log_path": ".runs/execution-task-003-codex.log",
    "worktree": ".worktrees/task-003-license-page-revamp",
    "pid": 57630
  }
}
```

**生命周期 / 扫描规则**：
- worker 完成 → 追加 `execution_*_bg` 事件 → 主 Orchestrator scan 发现 → 处理完追加 `reduced_bg` → 下次扫描跳过
- 判定"待收口"：task 的 events 流中最后一个 `execution_*_bg` 事件之后**没有**对应 `reduced_bg`
- 事件永不删（I-CT7/I-CT8 既有审计价值保持）

**为什么不用 sentinel 文件**（Issue 2）：
- 事件流已存在且承担 I-CT7/I-CT8 审计职责；新增 `.done.json`/`.processed`/`.pid` 会让 task 生命周期有 3 个 source of truth，原子 CAS 难保证
- flock（G5）只需保护 `task-events.py append` 一个入口，逻辑收敛
- `.pid` 仍保留但仅作运行时 liveness 用（非审计资产），task 收口时由 scan-task-done 清理

### 2.3 主 Orchestrator 启动多 task 流程

`/task-confirm` 新行为（多 task 模式）：

```bash
# 伪代码——放进 skills/task-confirm/SKILL.md
for TASK_FILE in "$@"; do
  # 原有 preflight：状态校验、worktree 创建、prompt 生成
  ...

  # 启动后台 worker（setsid 让 worker 独占 pgid，/task-abort 用 kill -TERM -$PGID 树杀）
  nohup setsid env \
    MAIN_REPO_ROOT="$MAIN_REPO_ROOT" \
    TASK_FILE="$TASK_FILE" \
    TASK_WORKTREE="$TASK_WORKTREE" \
    PROMPT_FILE="$PROMPT_FILE" \
    EXECUTOR_MODEL="$EXECUTOR_MODEL" \
    PM_AI_TASK="$TASK_ID" \
      bash "$MAIN_REPO_ROOT/scripts/exec-adapters/${EXECUTOR}-bg.sh" \
      > "$LOG_PATH" 2>&1 &
  WORKER_PID=$!
  disown

  # 记录 (pid, boot_id, lstart) 三元组供 liveness 检查（§11 G6）
  BOOT_ID=$(sysctl -n kern.boottime 2>/dev/null | sed -E 's/.*sec = ([0-9]+).*/\1/' || echo 0)
  LSTART=$(ps -o lstart= -p "$WORKER_PID" 2>/dev/null | tr -s ' ' '_' || echo unknown)
  printf '%s %s %s\n' "$WORKER_PID" "$BOOT_ID" "$LSTART" > "$MAIN_REPO_ROOT/.runs/${TASK_ID}.pid"

  # 转状态为"执行中" + 写 event
  python3 task-transition.py "$TASK_FILE" --to 执行中
  python3 task-events.py append "$TASK_FILE" \
    --type execution_started_bg \
    --payload "{\"executor\":\"$EXECUTOR\",\"pid\":$WORKER_PID}"
done

# 向 PM 汇报
echo "已启动 N 个后台 worker：task-003 / task-004 / task-005"
echo "下次 wakeup: 15 分钟后，或你再次输入时立即扫描"
echo ""
echo "/task-status 随时查看进度"

# 主会话调 ScheduleWakeup
ScheduleWakeup(delaySeconds=900, reason="定期扫描 task done sentinel")
```

### 2.4 扫描 + 收口（`scripts/scan-task-done.sh`）

```bash
#!/usr/bin/env bash
# ILLUSTRATIVE SKETCH, 交付前过 §11 G13
# 扫 .runs/events/*.jsonl 找待收口的 task
# 输出：每行一个 JSON（含 task_id），供主会话逐行处理
set -euo pipefail

MAIN_REPO_ROOT=$(git rev-parse --show-toplevel)
EVENTS_DIR="$MAIN_REPO_ROOT/.runs/events"
[ -d "$EVENTS_DIR" ] || exit 0

# flock 双入口互斥（§11 G5；wakeup 与 UserPromptSubmit hook 共用同一锁）
exec 9>"$MAIN_REPO_ROOT/.runs/reducer.lock"
flock -n 9 || exit 0   # 另一入口正在跑，此次 no-op

for EVENTS in "$EVENTS_DIR"/task-*.jsonl; do
  [ -f "$EVENTS" ] || continue
  TASK_ID=$(basename "$EVENTS" .jsonl)
  LAST_EXEC=$(jq -rc 'select(.type | test("^execution_(completed|failed|crashed|aborted)(_bg)?$"))' "$EVENTS" 2>/dev/null | tail -1)
  [ -z "$LAST_EXEC" ] && continue
  LAST_EXEC_TS=$(jq -r '.timestamp' <<< "$LAST_EXEC")
  REDUCED_AFTER=$(jq -rc --arg ts "$LAST_EXEC_TS" 'select(.type == "reduced_bg" and .timestamp > $ts)' "$EVENTS" 2>/dev/null | head -1)
  [ -n "$REDUCED_AFTER" ] && continue
  jq -c --arg task_id "$TASK_ID" '. + {task_id: $task_id}' <<< "$LAST_EXEC"
done

# Liveness：对 .runs/*.pid 里 pid 已死、三元组失配、但无任何 execution_*_bg 事件的，视为崩溃
for PID_FILE in "$MAIN_REPO_ROOT"/.runs/task-*.pid; do
  [ -f "$PID_FILE" ] || continue
  TASK_ID=$(basename "$PID_FILE" .pid)
  EVENTS="$EVENTS_DIR/$TASK_ID.jsonl"
  [ -f "$EVENTS" ] && jq -e 'select(.type | test("^execution_(completed|failed|crashed|aborted)(_bg)?$"))' "$EVENTS" > /dev/null 2>&1 && continue
  # 读 (pid, boot_id, lstart) 三元组（§11 G6）
  read -r PID BOOT_ID LSTART < "$PID_FILE" || continue
  CUR_BOOT=$(sysctl -n kern.boottime 2>/dev/null | sed -E 's/.*sec = ([0-9]+).*/\1/')
  if [ "$BOOT_ID" != "$CUR_BOOT" ]; then
    echo "{\"task_id\":\"$TASK_ID\",\"status\":\"CRASHED_REBOOT\"}"
    continue
  fi
  if ! kill -0 "$PID" 2>/dev/null; then
    echo "{\"task_id\":\"$TASK_ID\",\"status\":\"CRASHED\",\"pid\":$PID}"
    continue
  fi
  # TODO: 超时检查（§10 Q3 未决；默认 60 分钟）
done
```

主会话在 wakeup 和 hook 两个触发点都调这个脚本，对每条输出处理：
- 正常完成（`exit_code=0`）→ 进 worktree 跑 `/review` → 转 "待验收" → 追加 `reduced_bg` 事件 → 通知 PM
- 失败（`exit_code!=0`）→ 分类失败类型 → 转回 "待确认" + 诊断文案 → 追加 `reduced_bg { fail_type: ... }` 事件
- 崩溃（pid 死/boot 换代但无 execution 事件）→ 主会话**补写 `execution_crashed_bg` 事件** → 按失败分支处理并追加 `reduced_bg`

收口完成后一律追加 `reduced_bg` 事件（不再有 `.processed` 空文件）。

### 2.5 UserPromptSubmit hook 兜底

`.claude/hooks/task-done-check.sh`：

```bash
#!/usr/bin/env bash
MAIN_REPO_ROOT=$(git rev-parse --show-toplevel 2>/dev/null) || exit 0
[ -d "$MAIN_REPO_ROOT/.runs" ] || exit 0

PENDING=$(bash "$MAIN_REPO_ROOT/.claude/scripts/scan-task-done.sh" | head -20)
if [ -n "$PENDING" ]; then
  cat <<EOF
<system-reminder>
检测到 $(echo "$PENDING" | wc -l) 个 task 已完成待收口。先处理它们再响应 PM。

待收口清单（原始 JSON）：
$PENDING

处理方法：每条调 scripts/process-task-done.sh 进入 worktree 跑 /review 并转状态。
</system-reminder>
EOF
fi
```

注册到 `settings.json`：

```json
{
  "hooks": {
    "UserPromptSubmit": [
      { "matcher": "", "hooks": [{ "type": "command", "command": ".claude/hooks/task-done-check.sh" }] }
    ]
  }
}
```

**效果**：PM 早于 wakeup 输入了消息，hook 先扫 sentinel，把待收口清单注入到主会话上下文，主会话会在响应 PM 之前先处理它们。

### 2.6 ScheduleWakeup 协议

主会话在启动后台 worker 后**必须**调一次 ScheduleWakeup，在每轮 wakeup 结束后**判断是否还有 task 未完成**：有则再调一次，无则停止。

```
ScheduleWakeup(
  delaySeconds=900,
  prompt="<<autonomous-loop-dynamic>>",  # 或自定义 prompt
  reason="扫 task done sentinel"
)
```

Wakeup 点到达时，Claude 被唤醒并在系统提示里看到唤醒理由，执行 `scan-task-done.sh` → 处理 → 判断是否继续 → 调下一次 ScheduleWakeup。

### 2.7 `/task-abort` 契约（并发语义）

2026-04-24 eng review Issue 5 决议——§11 G14 硬约束。

**进程锚点**：§2.3 启动 worker 时已经 export `PM_AI_TASK=task-NNN` 并 `nohup setsid` 占用独立 pgid。abort 时用 `pgrep -f "PM_AI_TASK=$TASK_ID"` 精准找到该 task 的 wrapper，再 `ps -o pgid= -p <pid>` 拿到 pgid。

**PGID 树杀**：
1. `kill -TERM -$PGID`（负号 = 整个进程组，杀 wrapper 及其所有子孙：codex、hook subshell 等）
2. 30 秒后 wrapper 还未退 → `kill -KILL -$PGID`（TERM→KILL 窗口默认 30s，独立 config 不与 §10 Q3 task_timeout 混用）
3. 最后追加 `execution_aborted` 事件，payload：`{ signal, pgid, elapsed, by: "user" | "timeout" }`

**状态路径**（扩展 I-TT1）：
- `task-transition.py` 新增合法转换：**执行中 → 待确认**（仅 `/task-abort` 入口可用，其他入口调用仍拒绝）
- transition 要求 `--note` 参数（PM 输入 abort 原因），记录 `status_changed` 事件 `reason="aborted"`
- `tests/` 新增反例：普通转换路径调"执行中→待确认"必须被拒绝（保持 I-TT1 严格性）

**worktree 处置**：保留，**不** `git reset --hard`。PM 可能想检查 codex 半成品。下次 PM 对同 task 再 `/task-confirm` 时由执行者决定是否复用或重写。

**配额释放**：abort 脚本进入 `reducer.lock` flock，删除该 task 的 `.runs/<task-id>.pid`，追加 `reduced_bg { aborted: true }`。`max_parallel_tasks` 计数基于 `.runs/*.pid` 数量——pid 文件删掉即释放。幂等——重复 abort 产生至多一次 `execution_aborted` 事件（通过检查事件流最后一条是否已是 aborted）。

**UX**：
- `/task-abort task-NNN` 精准 abort
- `/task-abort` 无参 → 列出所有 `.runs/*.pid` 让 PM 选
- `/task-abort all` → 杀掉所有活跃 worker（3 个并行一次清）

**失败保护**：
- 拿不到 flock → 提示 PM 稍后重试，不做任何变更（fail-closed）
- TERM 后 30s 升 KILL 仍失败 → exit 1，PM 手动 `ps` + `kill`，脚本记 `execution_abort_failed` 事件
- transition 失败 → 事件已追加但状态没转 → PM 看到残留 "执行中" 状态且 worker 已死 → 下次 scan-task-done 的崩溃分支兜底

测试用例见 §11.5 T6。

---

## 3. 数据结构变化

### 3.1 `.runs/` 目录新文件

| 文件 | 作者 | 读者 | 生命周期 |
|---|---|---|---|
| `<task-id>.pid` | /task-confirm | scan-task-done.sh、/task-abort | 内容：`pid boot_id lstart` 三元组（§11 G6）；worker 启动时建，收口时由 scan-task-done 或 abort 删 |
| `execution-<task-id>-<executor>.log` | codex-bg.sh | 主会话在收口时读 | worker 完成时最终化，永久保留 |
| `reducer.lock` | scan-task-done.sh、/task-abort | wakeup + hook | flock 互斥锁（§11 G5），两入口共享，本身无内容 |

**完成信号不再落独立文件**（Issue 2 决议）——走现有 `events/<task-id>.jsonl`。`execution_completed_bg` / `execution_failed_bg` / `execution_crashed_bg` / `execution_aborted` / `reduced_bg` 五种新事件类型参见 §2.2。

~~`<task-id>.done.json`~~ / ~~`<task-id>.processed`~~（2026-04-24 删除约定，改走事件流）

### 3.2 settings.json 新配置项

```json
{
  "parallel_tasks": {
    "max_parallel": 3,
    "task_timeout_minutes": 60
  }
}
```

### 3.3 task 文件 frontmatter（无变化）

`executor`、`executor_model` 字段沿用现有；只是 `executor != claude-code` 时走后台分支。

---

## 4. 流程改动（各 skill）

### 4.1 `skills/task-confirm/SKILL.md`

**删除**：步骤 5 的 `Spawn Suborchestrator`（不再 `Agent(subagent_type=...)` 调用）

**改为**：
- 单 task 模式：创建 worktree → 转状态 → 对非 claude-code executor 用 `nohup` 起后台 worker → 调 ScheduleWakeup
- 多 task 模式（PM 输入多个 task 编号）：对每个 task 重复上面步骤 → 最后只调一次 ScheduleWakeup
- 超过 max_parallel 软警告

### 4.2 `skills/task-execute/SKILL.md`

**改变**：步骤 3b 的 codex/cursor-agent 分支**不再被主流程调用**（现在由后台 worker 直接调 `codex-bg.sh`）

**保留**：claude-code executor 的主流程——主会话自己写代码时不走后台（主会话本身就在"前台")

**新增**：step 3b-bg（文档层说明后台启动契约），便于未来接任者理解

### 4.3 `skills/task-status/SKILL.md`

**扩展**：展示"后台运行中"的 task：

```
当前 Req：req-001-xxx（stage 6 - task 执行）

🟢 后台运行中（3）：
  task-003 licenses-page    — codex，已跑 12m，无 done
  task-004 product-list     — codex，已跑 8m，无 done
  task-005 detail-prep      — codex，已跑 12m，无 done

📮 待收口（1）：
  task-002 mock-data        — codex，已完成 2m 前，exit 0，等主会话 /review

⏳ 待确认：
  task-006-010

下一步：等下次 wakeup（约 8 分钟）或你再次输入触发 hook
```

### 4.4 `templates/CLAUDE.md.tmpl` 角色表

**改为**：

| 角色 | 职责 | 工作位置 |
|------|------|---------|
| PM | 决策、确认、验收。不写代码 | 对话界面 |
| 主 Orchestrator | 流程推进、task 启动、监控、完成后收口（先前 Suborchestrator 的职责并入） | req 活跃时在 req worktree，否则在主仓 main |
| 后台 Worker | 非 Claude 角色；独立 bash 进程跑 executor，完成写 sentinel | 后台 |
| Execution Agent | 纯代码执行器（codex / cursor-agent / claude-code / manual） | task worktree |

---

## 5. 和 v2（worktree 物化）的架构冲突

TODOS.md v2 的核心设计：**serial 约束**——"同时只能有一个 worktree 处于可写"，通过 `git worktree lock` 或 `chmod -R a-w` 物化状态机。

v3 的核心需求：**多 task 并行**——多个 worktree 同时可写。

**冲突是正面的，不可调和**。必须在二者之间选 north star：

### 选项 (a)：并行优先（v3 为主，v2 降级）

- 放弃 serial worktree 约束
- 每个并行 task 独立 worktree，所有 worktree 都可写
- v2 的"防止跨 task 污染"用**task allowlist 严格校验**替代（v1 已有部分：`check-branch.sh`、`I-AD1/I-AD2` 越界检查；需加强）
- **代价**：失去 v2 的"fail-closed 物理约束"，重回 v1 的"agent 尊重规则"模型，但加强 allowlist 执行强度

### 选项 (b)：Serial 优先（v2 为主，v3 取消）

- 保留 serial 约束
- 放弃并行，接受单 task 串行
- **代价**：PM 反对——并行是明确诉求

### 选项 (c)：混合——同 req 内串行，跨 req 并行

- 一个 req 内的 task 还是 serial（v2 精神）
- 跨 req 可以同时推（e.g., req-001 跑时 req-002 也跑）
- **代价**：对 PM 单人工作流意义不大（PM 一次只做一个 req）；且引入"req 间上下文切换"的新复杂度

### 推荐

**选项 (a) + 加强 allowlist**。理由：
- PM 的并行需求是实际驱动的（admin console4 req-001 就明确能并行 3 个 task）
- v2 的 fail-closed 是 nice-to-have，不是硬需求——**v1 的防御在 2026-04-22 事件后 v1 也是"暂时足够"的判断**（见 v2 的 Context 段）
- allowlist 加强 + 后台 worker 的 `I-AD1/I-AD2` 越界回滚机制，可以覆盖大部分跨 task 污染风险

**需 PM 拍板**。未拍板前不写任何 v3 代码。

---

## 6. 失败 fallback

### 6.1 后台 worker 崩溃（kill -9 / OOM / 机器重启）

- PID 文件还在，done 文件没写
- scan-task-done.sh 发现 pid 死 → 输出 `"status":"CRASHED"`
- 主会话收口时分类为 `worker_crashed` → 写 event → 转回"待确认" + 提示 PM 重试或换 executor

### 6.2 主会话意外关闭（PM 关了 Claude Code）

- 后台 worker 继续跑（`nohup` + `disown` 保护）
- worker 完成写 sentinel——但没人处理
- PM 下次打开 Claude Code → `UserPromptSubmit` hook 触发 → 扫到待收口 → 注入上下文 → 主会话立即处理

### 6.3 ScheduleWakeup 不触发

- macOS/Linux 上 Claude Code 支不支持 ScheduleWakeup 需验证（见 §8 待实测）
- 如果不支持，退化到"纯 hook 兜底"模式——PM 不输入就永远不处理
- 可以配 cron 每 N 分钟给主会话发一个 `/process-task-done` prompt（重度 hack，最后手段）

### 6.4 完成事件写入中途失败

- Issue 2 决议后事件流承载完成信号，原子性由 `task-events.py` 内的 flock + 原子 append 保证
- 如果 codex 自己崩了（写事件之前 exit）→ worker 脚本捕获 exit code 写 `execution_failed_bg`
- 如果 worker 脚本本身崩了 → pid 死但无任何 `execution_*_bg` 事件 → §6.1 + §2.4 主会话补写 `execution_crashed_bg`
- 如果 `task-events.py append` 本身失败（磁盘满 / jq parse error）→ worker 脚本 exit code 传导，但事件未追加 → 纳入 §6.1 崩溃分支（等效于 pid 死无事件）

### 6.5 存量业务项目资产缺失（distribution 兜底）

2026-04-24 eng review Issue 1 决议——PM 选手动 migration，但 runtime 必须有可见兜底。

**症状**：已生成业务项目（admin console4 等）在 PM 升级生成器后、手动同步 v3 资产前，跑 `/task-confirm` 会进入 "skill 半新半旧"——新 skill 试图调 `codex-bg.sh`，但脚本不存在；hook 未在 `.claude/settings.json` 注册；task 状态已转但 worker 起不来；也无事件写入；scan-task-done 永远发现不了。PM 看到的是"点了 /task-confirm 什么都没发生"。

**守卫**：`/task-confirm` preflight 检测 v3 资产：

```bash
REQUIRED_V3_ASSETS=(
  "$MAIN_REPO_ROOT/scripts/exec-adapters/codex-bg.sh"
  "$MAIN_REPO_ROOT/scripts/scan-task-done.sh"
  "$MAIN_REPO_ROOT/.claude/hooks/task-done-check.sh"
)
MISSING=()
for ASSET in "${REQUIRED_V3_ASSETS[@]}"; do
  [ -f "$ASSET" ] || MISSING+=("$ASSET")
done
if [ ${#MISSING[@]} -gt 0 ]; then
  echo "⚠️  本项目缺少 v3 后台执行资产："
  printf '  - %s\n' "${MISSING[@]}"
  echo
  echo "你可以："
  echo "  A) 手动从生成器仓（PM-AI-Workflow）同步最新 scripts/ .claude/hooks/ 和 settings.json；"
  echo "  B) 本次 task 继续用旧 suborch 同步流程（Agent(subagent_type=...) spawn）；"
  echo "  C) 取消 /task-confirm。"
  # 等 PM 选择；B 走旧 suborch 分支；A 暂停等 PM 同步；C 直接 exit
fi
```

**不强制升级**——PM 随时可选 B 让当前 task 走旧 suborch 跑完，再择机 upgrade。这条守卫是 **fail-visible**（PM 看见并决策），不是 fail-closed（直接阻塞）。

**upgrade 脚本不在本 v3 PR 交付范围**（Issue 1 PM 选手动）。但守卫本身**必须**同 v3 首发上线——否则 admin console4 PM 遇到 silent failure，本 plan 的 DX 保证失效。

---

## 7. 实现顺序（north star 定了以后）

1. **`codex-bg.sh` + sentinel 格式**（最小可测单元）
   - 跑 1 个 task，验证原子写 + exit code 捕获
2. **`scan-task-done.sh`**
   - 手动创建几个 sentinel，验证扫描输出
3. **`/task-confirm` 后台启动分支**
   - 单 task 后台跑通
4. **主会话 wakeup + 收口流程**
   - 单 task 全链路跑通
5. **UserPromptSubmit hook 兜底**
   - 验证 PM 早回来时的唤醒效果
6. **多 task 并行**（核心目标）
   - 同时起 3 个，验证互不阻塞 + 收口顺序正确
7. **max_parallel 软警告**
8. **`/task-status` 扩展展示**
9. **`templates/CLAUDE.md.tmpl` 角色表更新**
10. **业务项目同步**（admin console4 等已生成项目 pull 新 skill）

---

## 8. 暂不做

- **多 Claude 实例并行**：真并行（每个 task 一个主会话）工程量太大，单人 PM 不需要
- **跨 req 并行**：选项 (c)，意义不大
- **后台 worker 的实时进度回传**：只关心"完成"事件，不做中间 progress 事件
- **sentinel 持久化到 SQLite/JSON db**：文件系统够用，避免引入新存储
- **`ScheduleWakeup` 非 dynamic 模式**：不用 cron-like 固定调度，用 dynamic 让 Claude 自己选延迟
- **Gemini executor**：和 `设计-执行者可选.md` 同步——暂不做

---

## 9. 待实测点

| # | 问题 | 验证方法 |
|---|---|---|
| 1 | Claude Code 的 skill 能否调 `ScheduleWakeup`？ | 🟡 **2026-04-24 60s POC 通过**：调用 `ScheduleWakeup(delaySeconds=60)` 成功排程并在 60s 后 fire 回原会话。**但 15-min 实际延迟 + `<<autonomous-loop-dynamic>>` 回抽场景未覆盖**——正式验证见 §11.5 T9（Phase A 硬前置） |
| 2 | `nohup` + `disown` 能在 Claude Bash 工具调用的子进程里真正脱离吗？ | 启动后 kill 主 bash，检查子进程是否存活 |
| 3 | Claude 主会话意外关闭时 `UserPromptSubmit` hook 的注入上下文能被下次会话读到吗？ | PM 关掉 Claude Code → worker 继续 → PM 重开 → 发消息 → 检查 hook 是否触发 |
| 4 | 3 个 codex 并发的本机资源开销（CPU / 内存 / 网络） | 实跑测 |
| 5 | 3 个 codex 并发的 API 成本估算 | 测 1 个 baseline → ×3 估算，看 PM 可接受度 |
| 6 | `UserPromptSubmit` hook 的 stderr 是否会泄露给 PM 界面？ | 故意让 hook 报错看显示 |
| 7 | sentinel 写入时如果被另一个 scan 读到半写状态？ | 原子 mv 理论无此问题，但需高并发测 |
| 8 | `git worktree` 多实例并发写不同 worktree，git metadata（`.git/worktrees/`）有无竞争？ | 3 个进程并发 commit 到各自 worktree 分支 |

---

## 10. 开放问题（动手前需回答）

1. ~~**north star 是 (a) 并行优先还是 (b) serial 优先？**~~ ✅ **2026-04-24 PM 决定：选 (a) 并行优先**（尽管 /autoplan 6 路声音一致反对，PM 仍选择推 v3 原方案，但接受以下 3 条加固）
2. ~~**max_parallel 默认值定多少？**~~ ✅ **默认 `max_parallel=1`（opt-in）**。首日 PM 跑 serial，显式改 settings.json 才启用并行
3. **task_timeout 阈值多少？** 默认 60 分钟是否合适（codex high reasoning 最长见过多少）？
4. **business project（admin console4）的迁移策略**：
   - 新 skill 上线后，它那边的 req-001 中途已启动的 task-001 要不要留着用旧 suborch 流程跑完？
   - 还是 kill 掉，换新流程重跑？
5. **ScheduleWakeup 轮询频率**：15 分钟起还是 20 分钟？频率太高耗 context，太低 PM 等不到。可能要做成 adaptive（启动初期密，后期稀）
6. **Subagent 机制在别处的保留清单**——要不要在 CLAUDE.md 写清楚"subagent 用在什么场景、不用在什么场景"，避免未来又犯"拿 subagent 当 orchestrator"的错？
7. ✅ **`/task-abort` 与 v3 同 PR 一起上**（2026-04-24 PM 决定）——同期交付 kill switch，避免并行 runaway codex 无停止手段

## 11. /autoplan 评审加固清单（必须在实施中落地）

虽然 PM 选了原方案，但 /autoplan 6 路评审暴露的 critical issues 必须在 Phase A-F 实施中一并修复，否则重现 2026-04-22 类事件：

| # | 加固项 | 来源 |
|---|---|---|
| G1 | **ScheduleWakeup POC 通过**（已验证，见 §9.1） | — |
| G2 | **`max_parallel=1` 默认 opt-in**（见 §10.2） | DX C1 |
| G3 | **同 PR 交付 `/task-abort`**（见 §10.7） | DX C2, Codex DX Issue 2 |
| G4 | **Shell injection 防御**：executor 白名单（`case ... in codex\|cursor-agent)`）、task-id regex `^task-[0-9]{3}$`、JSON 用 `jq -n --arg` 不用 heredoc | Eng C3, Codex Eng Issue 4 |
| G5 | **Reducer mutex**：wakeup 和 hook 的 `scan-task-done` 入口用 `flock .runs/reducer.lock` 或 `mkdir` 做原子 claim。重复 reduce 必须 no-op | Eng C2, Codex Eng Issue 2 |
| G6 | **PID + boot epoch**：`.pid` 文件存 `(pid, boot_id, process_start_time)`。`kill -0` 不够，必须验证三元组。macOS 用 `sysctl kern.boottime` + `ps -o lstart` | Eng edge #1+#4, Codex Eng Issue 3 |
| G7 | **Hook 项目边界**：`UserPromptSubmit` hook 首先检查 `.pm-ai-workflow/project.json` 是否存在——不是生成器产出的项目不触发。避免任何带 `.runs/` 目录的仓库被污染 | Eng edge #5, Codex Eng Issue 5 |
| G8 | **FSM 定义**：task 生命周期明确状态 `queued → starting → running → succeeded\|failed\|crashed → processing → accepted\|needs_retry`。每次转换原子 CAS。不允许 3 个松散文件事实 | Eng edge #5, Codex Eng Issue 5 |
| G9 | **Hook 不静默重排**：PM 输入消息时 hook 注入"有 N 个待收口，先处理还是先回答你？"问 PM，不直接强制收口优先 | DX C3, Codex DX Issue 3 |
| G10 | **Task 状态对 PM 透明**：`/task-status` 默认只展示业务语义（进行中 / 待验收 / 遇到问题）。exit code / sentinel 文件名 / pid 进 `--debug` 模式 | DX F4, Codex DX Issue 4 |
| G11 | **失败 UX 文案**：§6 的 6 个失败模式每个都写 PM-facing 文案（"可重试 / 可换 executor / 查看技术详情"）。动手前先定文案，再写代码 | DX 错误UX, Codex DX Issue 5 |
| G12 | **Umask + 权限**：`codex-bg.sh` 开头 `umask 077`，`.runs` 目录 `chmod 700`。避免日志泄漏到本机其他用户 | Eng 安全 #2 |
| G13 | **Shell 实现硬约束**（2026-04-24 eng review Issue 2+3）：(a) JSON 全部用 `jq -n --arg/--argjson`，禁止 heredoc 插值；(b) 脚本引用自身目录用 `${BASH_SOURCE[0]%/*}` 而非 `$(dirname "$0")`；(c) 任何临时文件必须 `trap 'rm -f "$TMP" 2>/dev/null' EXIT` 清理；(d) worker PID 追踪依托 `PM_AI_TASK` 环境变量 + `pgrep -f` 二次确认，不仅凭 `$!`；(e) 完成信号归并到 `.runs/events/<task>.jsonl`，不建 `.done.json` / `.processed` | eng review Issue 2+3 |
| G14 | **`/task-abort` 契约**（2026-04-24 eng review Issue 5）：`nohup setsid` 启动 + PGID 树杀；`PM_AI_TASK` env 锚点；扩展 `task-transition.py` I-TT1 仅由 `/task-abort` 入口允许"执行中→待确认"；worktree 保留供 PM 检查；`reducer.lock` flock 下幂等释放 `max_parallel` 配额；TERM→30s→KILL 降级。详 §2.7 | eng review Issue 5 |

---

## 11.5 实施前置测试清单

2026-04-24 eng review Issue 4 决议——延续 `INVARIANTS.md` 的"一条不变式配一个反例测试"模式。

Phase A 完成前，`tests/` 下必须包含下列 9 条自动化测试（runner 跟现有 bats/pytest 约定保持一致）：

| # | 目标 | 反例场景 | 期望结果 |
|---|------|----------|----------|
| T1 | 事件追加原子性 | `task-events.py append` 中途 SIGKILL / 磁盘满 | 事件流不出现半写条目；最后一条可 `jq` 解析 |
| T2 | G5 flock 双入口互斥 | wakeup 与 UserPromptSubmit hook 同秒触发 `scan-task-done.sh` | 仅一个入口进入 reduce 分支，另一个立即 exit 0 no-op |
| T3 | G6 PID + boot epoch 跨 boot 不误判 | 构造 pid 文件含历史 boot_id | liveness 判定为 `CRASHED_REBOOT`，不是 alive |
| T4 | G7 hook 项目边界 | 在非 PM-AI-Workflow 项目 CWD 跑 hook | hook 立即 exit 0，不读任何 `.runs/` |
| T5 | G4 task-id regex 防注入 | 恶意 task 文件名 `task-001; rm -rf` | adapter 启动时 exit 1，不调 codex |
| T6 | G14 `/task-abort` 原子 | 启动 worker 后立即 abort | pid 死、事件有 `execution_aborted`、task 状态回"待确认"、worktree 保留、`.pid` 删除（配额释放） |
| T7 | I-AD2 并发越界检测 | 3 个并行 task + 预埋"写别 task worktree"的 prompt | 越界 task 的 adapter 后置校验触发 fail，事件流记录 `execution_failed_bg` |
| T8 | G2 `max_parallel=1` 默认生效 | 未改 `settings.json`，连续起 2 个 task | 第 2 个被软警告并等待，PM 可 override |
| T9 | G1 ScheduleWakeup 15-min 实延触发 | 排程 900 秒（非 60s POC） | 触发时间误差 ±60 秒可接受；回 prompt 为 `<<autonomous-loop-dynamic>>` 或自定义值 |

**T1-T9 失败任何一条 → Phase A 不算完工，不允许开 Phase B**（G1-G14 硬约束任一未落地都等价于 2026-04-22 类事件复发风险未消除）。

对应 `tests/` 目录命名建议：`tests/v3_T1_event_append.bats`、`tests/v3_T2_flock_mutex.bats` ……实施时由 Phase A 实现者定最终文件名。

---

## 附录 A：术语对照

| 术语 | 含义 |
|---|---|
| **Suborchestrator** | 本项目之前定义的角色名：单 task owner，职责是 preflight → 实现 → 自审 → 收口。v3 后**消失** |
| **Subagent** | Claude Code 内置机制（`Agent` 工具）。一次性短命子会话。v3 后继续在合适场景使用（如 `analysis-reviewer`） |
| **后台 Worker** | v3 引入。非 Claude 角色，独立 bash 进程，跑 executor adapter |
| **Executor / Execution Agent** | 跨版本稳定概念。真正写代码的外部程序（codex / cursor-agent / claude-code / manual） |
| **主 Orchestrator** | PM 对话的主 Claude 会话。v3 后承担前 Suborchestrator 的职责 |
| **Sentinel 文件** | ~~`.runs/<task-id>.done.json`~~（v3 原草案，2026-04-24 Issue 2 决议废弃）。完成信号现以 `execution_completed_bg` / `execution_failed_bg` 等事件追加到 `.runs/events/<task-id>.jsonl` |

---

## 附录 B：决策溯源

- **为什么不做多 Claude 实例真并行？** PM 单人工作流不需要；工程成本 vs 收益比差
- **为什么双路（wakeup + hook）而不是单路？** 单 wakeup 怕 PM 等得久；单 hook 怕 PM 不输入就永远不处理；双路互补
- **为什么 sentinel 用文件而不是 socket/pipe？** 文件简单、跨进程、崩溃后还在、可审计；socket 需处理连接生命周期
- **为什么保留 `codex.sh`（同步）而新建 `codex-bg.sh`（后台）？** 主会话直接跑 codex 的场景（`claude-code` executor 或 PM 调试）还需要同步模式；不要把 `codex.sh` 强行后台化

---

## /autoplan 评审报告（2026-04-24，6 路独立声音）

| 阶段 | 声音 | 关键发现数 | 状态 |
|---|---|---|---|
| Phase 1 CEO | Claude subagent + Codex（gpt-5.5） | 10 战略盲点 + 5 premise challenges | issues_open |
| Phase 3 Eng | Claude subagent + Codex | 13 架构/安全问题（其中 critical 4 个） | issues_open |
| Phase 3.5 DX | Claude subagent + Codex | 5 DX critical + 评分 **4/10** | issues_open |
| Phase 2 Design | — | skipped（无 UI scope） | N/A |

### 三阶段 consensus（两路独立声音完全一致，无 disagree）

**CEO 一致结论**：north star（task 并行）**未经产品验证**，是从一次 10 分钟 Bash 超时事件过拟合推出来的架构决策。应先量化：多少 req 可并行？并行节省 vs 收口串行的真实收益？

**Eng 一致结论**：四层机制（nohup + sentinel + ScheduleWakeup + hook）**不是可组合架构，是脆弱补丁链**——每层都在弥补下层失败。`ScheduleWakeup` 是 critical 依赖但 §9.1 admit 未验证；无 mutex/claim/lock 让 wakeup 和 hook 会重复收口；`.pid` + `kill -0` 跨 boot epoch 不成立；shell 注入面未 sanitize。

**DX 一致结论**：parallelism 默认开启（max_parallel=3）不是 opt-in；没有 `/task-abort` 让 PM 无法停止失控 codex（money risk ×3）；`UserPromptSubmit` hook 静默重排 PM 对话优先级，违反用户意图；`/task-status` 暴露 `.done.json` / `exit 0` / `待收口` 等内部细节；失败态无 PM-facing 文案，只有工程分类。

### 6 路独立声音共同指出的 simpler alternative（plan 未评估）

| 候选 | 价值 | 成本 |
|---|---|---|
| **Python daemon + SQLite** | 真状态机、原子转换、可测试、不依赖 Claude session 做控制面 | 200 行 Python，1 天 spike |
| **launchd / systemd-user agent** | OS 级进程监管、内置 PID 追踪、自动重启、WatchPaths 触发 reducer | plist/unit 生成，平台差异 |
| **先量化再架构** | 收集 10 个历史 task 的耗时/失败分布；手动并行跑 3 个实 task；拿数据再决定 | 1 周观察期，零代码 |

§8 暂不做 段只用 1 行 dismiss 了 SQLite，没有对比成本/恢复语义/实现复杂度。6 路声音 verdict：**Simpler alternative actually evaluated? No.**

### Cross-Phase 高置信度主题

所有 3 phase 的两路声音都独立指向的主题：

1. **load-bearing unverified ScheduleWakeup**（CEO Issue 1 / Eng Issue 1 / DX F1）—— 30 分钟 POC 是动任何代码之前的硬前置
2. **fail-closed (v2) 不该被 parallelism 换掉**（CEO Issue 2 / Eng State Explosion / DX C1）—— §5 推荐 (a) 的理由仅"PM 想并行"，等价于放弃 2026-04-22 事件后补的物理约束
3. **并行需求未被量化证明**（CEO Challenge A / Eng 评审 / DX TTHW）—— 无数据支撑"PM 需要并行"
4. **应该把 orchestrator 移出 Claude session**（CEO Blind 6 / Eng Option C / DX C3 hook 重排）—— Claude session 本质不适合当调度器

### 评审产生的最终建议

**不是 approve / reject 二选一，而是三个抉择**：

#### 选项 X：停下，先做量化
- **先不动 v3 代码**
- 对 admin console4 已跑过的 task 做数据分析：耗时分布 / 并行潜力 / PM 等待时间
- 30 分钟 POC `ScheduleWakeup` 是否可用
- 拿到数据后重开方案讨论，north star 可能从"task 并行"改成"可靠异步单任务"
- **成本**：1-2 周，几乎全部是观察和写数据分析脚本

#### 选项 Y：降级 scope 先解"timeout bug"
- 放弃并行设计，**只做单 task 后台执行**（修复 codex 10 分钟超时）
- 保留 v2 的 serial worktree 约束（fail-closed 不动）
- 80% 的实际痛点解决，20% 的复杂度
- 后续 PM 真的积累并发等待痛时，再回头做 parallel 设计
- **成本**：1-3 天工程

#### 选项 Z：彻底换架构，按外部 supervisor 重写
- 丢弃 Claude session 作为控制面的设计
- 用 Python daemon + SQLite 或 launchd/systemd-user 做真 orchestrator
- Claude session 变成 thin client（CLI 调用 + 读状态）
- 保留 v2 的 serial 或 v3 的 parallel 作为调度策略（daemon 内可切换）
- **成本**：1 周 spike + 1-2 周实施

### 评审方法留痕

- 6 次独立外部声音：3 × Claude subagent（general-purpose，foreground）+ 3 × Codex（gpt-5.5 CLI，timeout 600）
- 0 disagreements across phases（CEO/Eng/DX 的两路声音完全一致，无需 taste decision 仲裁）
- 0 auto-decisions（评审本身没有 auto-decided 的 implementation issue——plan 在 premise 层就有问题，继续 auto-decide 细节无意义）
- 1 User Challenge（6 路声音都认为"north star 需要重新定义"——PM 最终拍板权）

---

## GSTACK REVIEW REPORT

| Review | Trigger | Why | Runs | Status | Findings |
|--------|---------|-----|------|--------|----------|
| CEO Review | `/plan-ceo-review` | Scope & strategy | 0 | — | — |
| Codex Review | `/codex review` | Independent 2nd opinion | 1 (via autoplan §11) | issues_found | 13 arch/sec issues (4 critical) |
| Eng Review | `/plan-eng-review` | Architecture & tests (required) | 2 | issues_open (PLAN) | 5 增量 issues，2 critical gaps（F5 distribution runtime、F7 I-TT1 缺 abort 路径）；4 unresolved decisions |
| Design Review | `/plan-design-review` | UI/UX gaps | 0 | — | — |
| DX Review | `/plan-devex-review` | Developer experience gaps | 1 (via autoplan §11) | issues_found | 5 DX critical, 评分 4/10 |

- **CODEX:** n/a（本轮跳过 outside voice——autoplan §11 已经有 3 路独立 Codex 声音覆盖）
- **CROSS-MODEL:** autoplan 6 路 consensus 选 Option Y/Z；PM 选 Option (a) 并加 G1-G12。本轮 eng review 不再 litigate north star，聚焦 autoplan 漏掉的 5 个增量点。
- **UNRESOLVED:** 4 条——(1) admin console4 migration 手动处理但缺 runtime 兜底；(2) §10 Q3 task_timeout 阈值；(3) §10 Q5 ScheduleWakeup 频率 adaptive 设计；(4) §10 Q6 subagent 使用场景文档
- **VERDICT:** **NOT CLEARED** — eng review has open issues。需先落地本轮 5 条决策（见下）才可开工 Phase A。

### 本轮 plan-eng-review 接受的 5 条 plan 修订（已全部落地）

| # | 决策点 | 落地位置 | 状态 |
|---|---|---|---|
| 1 | admin console4 migration：PM 选"手动"，F5 runtime 兜底 | §6.5 新增"存量业务项目资产缺失" | ✅ 已落地 |
| 2 | 完成信号合并到 `.runs/events/<task>.jsonl`，删除 `.done.json`/`.processed` | §2.1 §2.2 §2.3 §2.4 §3.1 §6.4 改写；`.pid` 保留仅作 liveness 用 | ✅ 已落地 |
| 3 | §2 头部 ILLUSTRATIVE ONLY banner + G13 shell 硬约束 | §2 banner + §11 G13 | ✅ 已落地 |
| 4 | 新增 §11.5 T1-T9 测试清单（延续 INVARIANTS.md 反例测试模式） | §11.5 新增 9 条 | ✅ 已落地 |
| 5 | 新增 §2.7 "/task-abort 契约" + §11 G14 | §2.7 + §11 G14 | ✅ 已落地 |

附带修订：§9 #1 ScheduleWakeup POC 状态从 ✅ 改为 🟡（60s POC 通过 ≠ 15-min 生产配置验证），交由 §11.5 T9 完成。

### 关键失败模式追补

- **F5 (critical)**：存量业务项目运行 v3 skill 时资产缺失 → 没事件流注入、无告警。需 `task-confirm` 前置检测 `.claude/hooks/task-done-check.sh` 与 `scripts/exec-adapters/codex-bg.sh` 存在性，缺则回退旧 suborch 流程并提示 PM 手动升级。
- **F7 (critical)**：`task-transition.py` I-TT1 只允许 4 种转换，没有 "执行中→待确认"，/task-abort 落地受阻。Issue 5 要求扩展 I-TT1 或借道两步 transition。

### 测试清单 T1-T9（新增到 §11.5）

| # | 目标 | 反例 |
|---|---|---|
| T1 | 事件追加原子性（替代原 atomic mv） | 磁盘满 / SIGKILL 中途 |
| T2 | G5 flock 双入口互斥 | wakeup + hook 同秒触发 |
| T3 | G6 PID + boot epoch 跨 boot | pid 复用 |
| T4 | G7 hook 项目边界 | 非生成器项目 CWD 不触发 |
| T5 | G4 task-id regex 防注入 | 恶意 task 文件名 |
| T6 | G14 /task-abort 原子 | 启动后立即 abort |
| T7 | I-AD2 并发越界检测 | 3 task 并行 + 预埋越界 prompt |
| T8 | G2 max_parallel=1 默认生效 | 未改 config 起 2 个 task 需串行 |
| T9 | G1 ScheduleWakeup 15-min 实延 | 非 60s POC 的真实生产配置 |
